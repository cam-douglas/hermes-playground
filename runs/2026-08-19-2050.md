# 2026-08-19 20:50 Sydney — Skill Clash

## Date

2026-08-19 20:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

People now stack 10–40 agent skills. YAGNI vs “always write tests” vs “never touch auth” vs injection-shaped “helpful” rules silently fight. The missing local product is a collision board: paste or load fixture skills, flag MUST/NEVER contradictions, overlapping domains, and injection-shaped phrases, and show a stack-coherence score that moves when a pack is armed or removed.

## Inspiration

- mattpocock/skills, obra/superpowers, DietrichGebert/ponytail, DeepSeek Harness (inspiration only — not cloned)
- Distinct from Ghost Briefs (human fluency after merge) and Reorder Radar (inventory)
- No Decision Half-life, no agent handoff console, no inventory clone

## Ghost Briefs land / merge status

- Started from `cursor/decision-half-life-f4bc` so Ghost Briefs was already in the tree.
- PR #1 (`Ship Ghost Briefs — 2026-08-19 daily product`) was open and mergeable, but the GitHub merge API returned **403** (PAT cannot merge).
- Fast-forward push of that branch onto `main` **succeeded**: `89efa07..b17ab12`. GitHub then marked PR #1 **merged** (closed 10:52 UTC by cursor[bot]). Production main now includes Ghost Briefs.
- This hour’s Skill Clash work is on `cursor/skill-clash-782d` (PR #2). Direct push to `main` **succeeded** after verification (`b17ab12..660a64e`). Production main now includes Skill Clash plus Ghost Briefs and Reorder Radar.

## What shipped

- `products/skill-clash/` static prototype: fuse-box collision board, five-skill seed with real MUST/NEVER shorts, demo fixture that adds an injection-shaped pack, paste/add, disable/remove, stack-coherence meter, localStorage, JSON export/import, reset
- Catalogue hub now lists **three** products; Skill Clash featured this hour; Ghost Briefs and Reorder Radar remain first-class
- Root README updated for all three live routes
- This hourly run log

## Verification

Headless Chrome (`puppeteer-core` + `/usr/bin/google-chrome-stable`) against `python3 -m http.server`. 24/24 checks passed:

- Catalogue `/` lists Skill Clash, Ghost Briefs, and Reorder Radar and links `/products/skill-clash/`, `/products/ghost-briefs/`, `/products/reorder-radar/`
- Skill Clash seed: 5 skills, 7 red MUST/NEVER line-pairs (YAGNI × Test Always, Sealed Auth × Hotfix Auth), stack-coherence 48
- Load demo fixtures: 6 skills, 8 red MUST/NEVER clashes (adds Helpful Reviewer × Sealed Auth), 5 injection-shaped hits
- Disabling Test Always moves the demo score 0 → 10
- Adding a contradictory skill moves seed 48 → 0; removing it restores 48
- Refresh keeps a disabled skill and the score
- Reset restores the five-skill seed
- Ghost Briefs and Reorder Radar pages still load

## What broke / caveats

- Clash detection is heuristic (shared domain + token overlap), not a full policy prover
- Domain tags are a fixed keyword list (`tests`, `auth`, `yagni`, `skills`, `docs`, `git`)
- Injection flags are phrase-shaped, not a sandbox or model-level defense
- No backend, so “the team stack” is this browser (or an exported JSON file)
- GitHub merge API remains 403; main updates depend on a direct push

## Catalog entry added

- Featured: Skill Clash — `/products/skill-clash/`
- Retained: Ghost Briefs — `/products/ghost-briefs/`
- Retained: Reorder Radar — `/products/reorder-radar/`

## Next hour’s suggested focus

Keep the catalogue at three products. A reserve wedge is still an inbound hidden-glyph inspector — not another skill pack, not Decision Half-life, not a handoff console.
