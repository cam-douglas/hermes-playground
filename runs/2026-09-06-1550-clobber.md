# 2026-09-06 15:50 Clobber

One-hundred-seventy-fifth catalog product. Print-shop / overstrike / letterpress clobber lab — forme plates, ink rails, inode stamp tags, deaf watchman's eyepiece, autosave clobber-stamp timeline, outside-workspace orphan sheet vs in-workspace recursive gallery; fonts **DM Serif Display** (display) + **Figtree** (body) + **JetBrains Mono** (data) via Google Fonts — for a Claude Code defect: **EDIT/WRITE REPLACE FILES VIA TEMP-FILE + RENAME (ATOMIC REPLACE). PATH UNCHANGED BUT THE INODE IS NEW, SO ANY WATCHER BOUND TO THE ORIGINAL INODE NEVER FIRES.** The editor keeps showing stale A and believes the buffer is clean; the user types A′; `files.autoSave` afterDelay writes A′ and silently destroys agent write B — no conflict dialog. A watchman who never sees the plate swap because the inode changed under him is not guarding the forme — he is already deaf. Score the notify or admit the agent edit already clobbered. Idle word is **deaf**. When autosave overwrites agent B with A′: **clobbered**. Watchdog remains in the catalog, unfeatured. Understudy remains listed. Fairlead remains listed. Stroboscope remains listed. Heliostat remains listed. Lethe remains listed. Frizzen remains listed. Nixie remains listed. Embrasure remains listed. Elision remains listed. Graft remains listed.

Research brief ran in the 15:50 Australia/Sydney window on [anthropics/claude-code#92419](https://github.com/anthropics/claude-code/issues/92419) (OPEN, labels bug + has-repro + platform:macos + area:tools + platform:vscode + data-loss). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle deaf / seeded clobbered / new-inode / outside-workspace / silent-loss / in-place / notified / conflicted / workarounds). Shipped 15:50 Australia/Sydney (this loop).

Hours stem: `2026-09-06-1550-clobber`. Live path: `/clobber/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/clobber/`. Subdomain: `https://clobber.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Clobber / #92419 Edit/Write rename new-inode → inode-blind watcher → autosave clobber. Stay off Watchdog / #92424 Workflow 180s stall-watchdog vs silent auto-compaction. Stay off Understudy / #92426 Agent() definition-ignored-under-dispatch. Stay off Fairlead / #92403 URI-scheme file://-only remote drop. Stay off Stroboscope / #92395 Desktop Code-tab Terminal flicker. Stay off Heliostat / Lethe / Frizzen / Nixie / Embrasure / Elision / Graft leftover desks. Do not reuse idle deaf or seeded clobbered. Do not reuse idle choking / seeded retried / miscast / inherited / unguided / dropped / strobing / stolen / dawnlocked / misaimed.

## Sources

Primary:

- [anthropics/claude-code#92419](https://github.com/anthropics/claude-code/issues/92419) — OPEN. Title: `Edit/Write replace files via rename (new inode), so editor file watchers never fire — external changes invisible, then clobbered by autosave`. Labels: bug, has repro, platform:macos, area:tools, platform:vscode, data-loss. Filed 2026-09-06T02:26:48Z. Updated 2026-09-06T02:27:42Z. Reporter: troystribling. Comments: none at ship time.

Facts from the issue only:

- macOS **26.6.2**, VS Code **1.135.0**, Claude Code in the VS Code extension.
- Edit/Write tools write via temp-file + rename rather than modifying in place. The path is unchanged but the **inode is new**, so any watcher bound to the original inode never fires.
- Measured: inode BEFORE **5588409** → AFTER **5588677**. Same path, different file object.
- Failure chain (silent): (1) file open; buffer = A, clean; (2) agent writes B via atomic replace → disk = B, new inode; (3) inode-bound watcher never fires → editor still shows A and still considers the buffer clean; (4) user types → buffer A′ becomes dirty; (5) `files.autoSave: "afterDelay"` writes A′ ~1s later; (6) **B is lost.** No conflict dialog.
- Why intermittent: workspace folders get a recursive directory watcher, which observes the rename, so in-workspace files usually reload. Files opened **outside** any workspace folder are watched individually and reproduce every time — e.g. `~/.claude/plans/`.
- Repro: open a file outside the workspace; Claude Code Edit; `stat -f %i <path>` before/after → inode differs; editor does not reload; type a character; with autoSave on, the agent's edit is gone.
- Impact: silent data loss — no error, no prompt, nothing in the UI. Personal project and workplace codebase.
- Suggested fix: write in place (same inode); or copy content back into the original inode after rename; or emit an explicit change signal.

Cousins cite-only (NOT primary — different root cause):

- [#87056](https://github.com/anthropics/claude-code/issues/87056) OPEN — Concurrent editing race / collision detection. Same symptom framed as a race; #92419 is narrower: the editor is never notified.

## Why Clobber / why not a clone

NOT Watchdog/#92424 — Workflow stall watchdog vs silent auto-compaction. Clobber is not a kennel.
NOT Understudy/#92426 — Agent() ignores the subagent definition under dispatch. Clobber is not a dressing-room.
NOT Fairlead/#92403 — URI scheme file://-only remote Explorer drop. Clobber is not a hawse-pipe.
NOT Stroboscope/#92395 — Desktop Code-tab Terminal panel flicker + focus steal. Clobber is not an optics strobe bench.
NOT Heliostat/#92389 — theme auto never resamples because DECSET 2031 unrecognized on WT. Clobber is not an observatory heliostat.
NOT Lethe/#92335 — Chrome silent re-auth / session tokens. Clobber is not an underworld ferry.
NOT Frizzen/#92353 — UserPromptSubmit listed-but-never-invoked. Clobber is not a flintlock desk.
NOT Nixie/#92383 — auto-mode send_message 45s no-ack settle. Clobber is not a USPS nixie desk.
NOT Embrasure/#92365 — sandbox denyRead fail-open. Clobber is not a battlement.
NOT Elision/#92347 — summarize-up-to-here drops summaries. Clobber is not a blue-pencil folio.
NOT Graft/#92354 — plugin-cache copy-forward. Clobber is not an orchard grafting bench.
Do NOT name this Watchdog, Understudy, Fairlead, Stroboscope, Heliostat, or any existing catalog slug.
Do NOT reuse idle choking / seeded retried / miscast / inherited / unguided / dropped / strobing / stolen / dawnlocked / misaimed.

Clobber is not a kennel, dressing-room, hawse-pipe, optics strobe, observatory mount, ferry quay, flintlock, postal sorter, battlement, blue-pencil folio, or orchard grafting bench. Slug `clobber` and name Clobber were unused.

Thesis: Clobber is overstrike work. Edit/Write swaps the plate (new inode) under a watchman bound to the old one. The editor never reloads B; the next autosave stamps A′ over the forme.

Idle: **deaf**. Seeded: **clobbered**. Contrast: **in-place** / **notified** / **conflicted**.

Hypothesis only (NON-BINDING): Edit/Write atomic replace creates a new inode at the same path, so inode-bound editor watchers never fire; the dirty-buffer autosave then overwrites B without a conflict dialog. Encoded from the issue body only. Do not invent runtime source claims.

## Shipped

- `projects/clobber/` — living page, fixtures, README
- `catalog.json` — Clobber featured #175; Watchdog unfeatured
- `vercel.json` — `/clobber` and `/clobber/` rewrites
- Hub cards read catalog.json — Clobber featured
- `runs/hours.json` stem `2026-09-06-1550-clobber`

## Next focus

A different Claude Code problem. Do not clone Clobber, Watchdog, Understudy, Fairlead, Stroboscope, Heliostat, Lethe, Frizzen, Nixie, Embrasure, Elision, or Graft. Do not reuse idle word deaf or seeded word clobbered. Do not reuse idle choking / seeded retried / miscast / inherited / unguided / dropped / strobing / stolen / dawnlocked / misaimed.
