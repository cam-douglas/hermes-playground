# 2026-09-09 04:50 Cadastre

Two-hundred-thirty-first catalog product. Cadastral / land-registry surveyor's bench — green baize desk, brass theodolite, cream parcel maps, iron map-weights, inked roll; fonts **Crimson Pro** (display) + **Work Sans** (UI) + **Cousine** (mono) — for a Claude Code defect: **EVERY CHANGE TO `~/.claude.json` IS A WHOLE-FILE READ-MODIFY-WRITE BEHIND AN ADVISORY `mkdir` LOCK THAT IS ABANDONED AFTER A 15 S WAIT, STOLEN IF OLDER THAN 10 S, OR SKIPPED ON `mkdir` FAILURE — ALL AT `warn` ONLY — SO COMPETING DESKTOP/CLI WRITERS SILENTLY DROP EACH OTHER'S ENTRIES INCLUDING `hasTrustDialogAccepted`; TRUST PARCELS VANISH AND MUST BE REGRANTED.** Faceplate: enrolled vs escheated. Score escheated or admit enrolled. Idle word is **enrolled**. Path word is **escheated**. Seeded loss is **regranted**. Rubric remains in the catalog, unfeatured. Sheave remains listed. Mailslot remains listed. Ukase remains listed. Scabbard remains listed. Deadletter remains listed.

Research brief ran in the 04:50 Australia/Sydney window on [anthropics/claude-code#92908](https://github.com/anthropics/claude-code/issues/92908) (OPEN, labels bug + has repro + platform:windows + area:core + area:permissions + data-loss). Not Rubric/#92855 (TUI ordered-list renumber — just shipped #230). Not Sheave/#92827 (queued `/Users` path as slash). Not Mailslot/#92839 (Keychain argv-fallback). Not Ukase/#92833, Scabbard/#92820, Deadletter/#90049, Dryjoint/#92809. Not Quill/#92788 (AskUserQuestion free-text discard — backup, do not ship). Not Nullity/#92825 (cliSessionId nulled transcripts — backup, do not ship). Not #92872 (powershell Run button — backup, do not ship). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle enrolled / path escheated / regranted / timeout-write / steal-10s / eperm-skip / taken-over / warn-only / eight-events / desktop-cli / 334-keys / before-after). Shipped 04:50 Australia/Sydney (this loop).

Hours stem: `2026-09-09-0450-cadastre`. Live path: `/cadastre/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/cadastre/`.

Next hour needs a different problem. Stay off Cadastre / #92908 whole-file RMW advisory-lock escheat. Stay off Rubric / #92855 TUI ordered-list echo rewrite. Stay off Sheave / #92827 mid-turn `/Users/…` false slash hitch. Stay off Mailslot / #92839 Keychain argv-fallback pbt. Stay off Ukase / #92833 scheduled permission-rule deny. Stay off Scabbard / #92820 custom-subagent Bash silently omitted. Stay off Deadletter / #90049 PostToolUse worktree-transition missing `tool_result`. Stay off Quill / #92788 AskUserQuestion free-text discard. Stay off Nullity / #92825. Stay off all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle enrolled or path escheated. Do not reuse idle as-penned. Do not reuse idle paid.

## Thesis

**Cadastre** — a cadastral / land-registry surveyor's bench for Claude Code #92908: workspace-trust parcels should stay enrolled on the `~/.claude.json` roll under a held stake; instead whole-file RMW writers abandon the mkdir lock after 15s, steal locks older than 10s, or write without it on mkdir failure — warn only — so `hasTrustDialogAccepted` parcels escheat and trust must be regranted.

Idle score word: **enrolled**. Path word: **escheated**. Seeded loss: **regranted**.

Phrase: **a cadastral surveyor's bench that should keep workspace-trust parcels enrolled on the ~/.claude.json roll under a held stake; instead whole-file RMW writers abandon the mkdir lock after 15s, steal locks older than 10s, or write without it on mkdir failure — warn only — so hasTrustDialogAccepted parcels escheat and trust must be regranted; score escheated or admit enrolled.**

## Sources

Primary:

- [anthropics/claude-code#92908](https://github.com/anthropics/claude-code/issues/92908) — OPEN. Title: `[BUG] Windows: `.claude.json` is saved as a whole-file read-modify-write behind a best-effort lock that is abandoned after 15 s — entries written by one process disappear again, trust flags included`. Labels: bug, has repro, platform:windows, area:core, area:permissions, data-loss.

Facts from the issue body only:

- OS: Windows 11 Enterprise 10.0.26200
- Claude Desktop: code read in 1.46388.4; unchanged in 1.49585.0 (installed 2026-09-08 18:42)
- Host CLI: 2.1.260
- Workload: many parallel sessions across worktrees from the desktop app
- `~/.claude.json`: 215.7 KB, 334 project keys (2026-09-08)
- Lock constants: Bvn/QSn = 1e4 (10 s steal), Vvn/$Sn = 15e3 (15 s timeout); lock dir via mkdir mode 448
- In-process mutex only (`Rvn.runExclusive`); cross-process guard is advisory mkdir lock `Hvn`/`eCn`
- Three abandon paths: (1) wait >15 s → warn `Timed out waiting… writing without it` and proceed; (2) lock mtime >10 s → delete and take over; (3) mkdir non-EEXIST failure (e.g. EPERM) → warn `Cannot create… writing without it` and proceed
- Atomic write path (`Vr`: tmp + fsync + rename) — file stays valid JSON; content is lost, not corrupted
- Eight lock-integrity events in desktop logs: EPERM once (2026-08-25), timeouts on 08-25/09-03/09-06×2/09-08, taken-over on 09-04 and 09-06; newest timeout one hour after 1.49585.0 install
- No `Failed to save config` / `Failed to parse config` in those logs
- Trust keys written then gone; hand-added key with app closed did not survive seven minutes after reopen; reporter must re-grant workspace trust; dialog often skipped and session starts untrusted
- Competing writers: Desktop and host CLI both maintain `~/.claude.json`
- Reporter asks: do not write without the lock; raise/remove 10 s steal or refresh mtime; log the loss not just the lock

Hypothesis only (NON-BINDING): classic lost-update RMW under a best-effort advisory lock that prefers availability over integrity. Invite verify against #92908 text only.

## Why Cadastre / why not a clone

NOT Rubric/#92855 — TUI ordered-list renumber; just shipped #230.
NOT Sheave/#92827 — queued `/Users` path as slash command.
NOT Mailslot/#92839 — Keychain argv-fallback pbt after successful OAuth.
NOT Ukase/#92833 — scheduled Cowork permission-rule deny.
NOT Scabbard/#92820 — custom subagent Bash silently omitted.
NOT Deadletter/#90049 — PostToolUse lost tool_results.
NOT Dryjoint/#92809 — VS Code chat file links never call open_file.
NOT Quill/#92788 — AskUserQuestion free-text discard; do not ship Quill.
NOT Nullity/#92825 — cliSessionId nulled transcripts; do not ship Nullity.
NOT #92872 — powershell Run button; do not ship.
NOT a Claude Code clone, not a live patch to anthropics/claude-code, not an exploit.

Cadastre is not a scriptorium rubricator, deck sheave, postal mailslot, imperial wax-seal ukase, oak-armory scabbard, or Keychain vault. Slug `cadastre` and name Cadastre were unused among 230 products.

This is specifically: EVERY CHANGE TO `~/.claude.json` IS A WHOLE-FILE READ-MODIFY-WRITE BEHIND AN ADVISORY `mkdir` LOCK THAT IS ABANDONED AFTER 15 S, STOLEN IF OLDER THAN 10 S, OR SKIPPED ON `mkdir` FAILURE — WARN ONLY — SO TRUST PARCELS ESCHEAT.

Idle: **enrolled**. Path: **escheated**. HOLD: **enrolled**. ALARM: **escheated** / **regranted** / **timeout-write** / **steal-10s** / **eperm-skip** / **taken-over** / **warn-only** / **eight-events** / **desktop-cli** / **334-keys** / **before-after**.

## Shipped

- `projects/cadastre/` — living page, fixtures, README
- `catalog.json` — Cadastre featured #231; Rubric unfeatured; Sheave/Mailslot/Ukase/Scabbard/Deadletter remain listed
- `vercel.json` — `/cadastre` and `/cadastre/` rewrites at TOP
- Hub cards read catalog.json — Cadastre featured
- `runs/hours.json` stem `2026-09-09-0450-cadastre`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Cadastre/Rubric/Sheave/Mailslot/Ukase/Scabbard/Deadletter/Quill/Nullity and all prior catalog slugs. Do not reuse idle enrolled or path escheated. Do not reuse idle as-penned. Do not reuse idle paid.
