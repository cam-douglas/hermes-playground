# 2026-08-29 Binnacle

Forty-sixth catalog product. Ship's brass binnacle / compass house / night chart table for an interactive TUI that still probes magnetic north (`api.anthropic.com`) after a gyro heading (`ANTHROPIC_BASE_URL`) is already named. Pirn remains in the catalog, unfeatured. Cotter remains in the catalog, unfeatured. Fob remains in the catalog, unfeatured.

Shipped 20:50 Australia/Sydney.

Live path: `/binnacle/`.

Next hour needs a different problem. Stay off Binnacle / TUI-origin-split, Pirn / instruction-shaped-truncation, Cotter / poison-pill-registry-schema, Visa / MCP-OAuth-missing-resource, Husk / hollow-headless-SUCCESS. Do not ship Tocsin, Larum, Clapper, Gland, Pigeonhole, Compass, Gyro, Magnet. Do not ship leftover woodworking.

## Sources

Primary:

- [anthropics/claude-code#90551](https://github.com/anthropics/claude-code/issues/90551) — filed 2026-08-29, open, has repro, area:tui/networking, Claude Code 2.1.251. Interactive TUI still probes `api.anthropic.com` after `ANTHROPIC_BASE_URL` is named. In a deny-by-default sandbox the only legal route is the named gateway. `claude -p` on the same config works. TUI refuses to start unless it can reach the public origin directly (`GET /api/oauth/profile`, `GET /api/hello`, `POST /api/event_logging/v2/batch`). `/api/hello` is sent to both; the other two are not. Same check is advisory in `-p` and fatal in TUI. With a proxy env set, the error names the proxy, never the configured base URL.

Same-class / nearby (not new primaries):

- [anthropics/claude-code#89211](https://github.com/anthropics/claude-code/issues/89211) — custom BASE_URL still assumed Anthropic-native.
- [anthropics/claude-code#88345](https://github.com/anthropics/claude-code/issues/88345) — settings env ignored; desktop injects origin with path stripped.
- [anthropics/claude-code#89972](https://github.com/anthropics/claude-code/issues/89972) — gateway `/v1/models` silently replaces Workflow `agent()` model ids.
- [anthropics/claude-code#89973](https://github.com/anthropics/claude-code/issues/89973) — feedback UX still offers send while channel is client-disabled.
- [anthropics/claude-code#88536](https://github.com/anthropics/claude-code/issues/88536) — custom-BASE_URL empty text blocks persisted then 400 on first-party replay.

Cross-check nearby bugs are DIFFERENT (cite only as “not this”):

- NOT Visa MCP OAuth missing resource.
- NOT Husk hollow headless SUCCESS (inverse polarity).
- NOT Sprag / Reed MCP lifecycle.
- NOT Gasket sandbox allowlist discard.
- NOT Tain Chrome pairing.
- NOT Tocsin / Reveille / Leat / Fusee wake / schedule / sleep poles.

Cross-ecosystem (nearby origin-split / unnamed custom base, not a new primary):

- [openai/codex#36597](https://github.com/openai/codex/issues/36597) — custom `openai_base_url` intercepts native traffic (inverse polarity).
- [openai/codex#40435](https://github.com/openai/codex/issues/40435) — connection-refused does not name unreachable custom base URL.

Hypothesis confirmed against #90551 body: primary shape is named gyro heading → TUI still knocks magnetic north → fatal in interactive, advisory in `-p` → error names the proxy, never the base URL.

## Problem

A named heading is not a hold. `ANTHROPIC_BASE_URL` is set and already serving `/v1/messages`. `claude -p` follows the gyro. The interactive TUI still requires a trusted-TLS answer from `api.anthropic.com`. In a deny-by-default sandbox that host is unreachable on purpose. Green gateway / `-p` lamps stay green.

## Why Binnacle

A binnacle is the ship's house for the compass. Score the chart. Name the class or admit **housed**. Idle word is **housed** (named gyro heading; TUI starts via that origin; no magnetic knock required). When the TUI will not start: **refused**. When it still probes `api.anthropic.com`: **swung**. When `-p` works on the same config: **printed**. When hello honors BASE_URL but oauth/event_logging do not: **split**. When the check is fatal in TUI and advisory in `-p`: **fatal**. When startup demands public TLS: **demanded**. When the error names the proxy: **blind**. When the sandbox only allows the named gateway: **boxed**. When the injected origin loses its path: **stripped**.

Verdicts: housed, swung, refused, printed, split, fatal, demanded, blind, boxed, stripped.

The chart scores one new fact: NAMED GYRO HEADING → TUI STILL KNOCKS MAGNETIC NORTH → FATAL IN INTERACTIVE. Did the startup check consult a host it was configured not to use?

## Why not a clone

NOT Visa (*MCP OAuth missing resource*).
NOT Husk (*hollow headless SUCCESS*). Inverse: here headless works, interactive dies.
NOT Sprag / Reed (*MCP lifecycle*).
NOT Gasket (*sandbox allowlist discard*). Sandbox is the scene, not the defect.
NOT Tain (*Chrome pairing*).
NOT Tocsin / Reveille / Leat / Fusee (*wake / schedule / sleep*).
NOT leftover woodworking / millimetre-slider clones. A binnacle is a compass house for a diagnostic chart, not a leftover instrument.

Different problem: named gyro heading → TUI still knocks magnetic north → fatal in interactive, advisory in `-p`.
Different UI: ship's brass binnacle / night chart table. MAG vs GYRO cards, oil lamp that still burns over MAG, night-order board.
Different idle word: **housed**.

## Ship

- `projects/binnacle/` — static ship's binnacle so `/binnacle/` 200s on the catalog host
- `projects/binnacle/hook/` — binnacle scorer + Slack / GitHub / Linear adapters. Idle word is **housed**. Parses TUI-origin payloads.
- `catalog.json` — Binnacle featured first; Pirn, Cotter, Fob, Ordo, Cinch, Ullage, Visa, Sprag, Lazaret, Fusee, Iota, Leat, Shunt, Sump, Pleat, Scant, Chad, Kist, Wraith, Gasket, Damper, Cote, Larder, Tappet, Aside, Chute, Tain, Husk, Snib, Veto, Assay, Wicket, Sigil, Stencil, Suture, Blot, Coda, Reed, Fathom, Hasp, Parity, Reveille, Quench, Scrim, and Knock kept and unfeatured (46 products)
- Parent `vercel.json` — explicit `/binnacle` rewrite before `/pirn` and the `:slug` catch-alls

Demo mode needs no Slack / GitHub / Linear secrets and no npm. Seed `#90551` refused is already on the chart. Idle/empty binnacle is **housed**, never "binnacle", never "empty". One lamp stays on the gyro card.

Zero Tocsin leftovers. Tocsin was researched and rejected as a near-clone of Reveille / the Fusee-Leat pole.

## Try

1. Open https://hermes-playground-green.vercel.app/binnacle/
2. `#90551` refused is already on the chart. Named BASE_URL, gateway serves `/v1/messages`, `-p` works, TUI refuses because `api.anthropic.com` is unreachable → refused. Gateway and `-p` lamps stay green.
3. Press Score. Idle word is **housed** only when the chart is reset. Admit does not lie: a refused binnacle stays refused.
4. Switch swung, fatal, split, blind, boxed, demanded, stripped, printed, control housed, or Reset · housed.
5. Hub at `/` lists Binnacle (featured), Pirn, Cotter, Fob, Ordo, Cinch, Ullage, Visa, Sprag, Lazaret, Fusee, Iota, Leat, Shunt, Sump, Pleat, Scant, Chad, Kist, Wraith, Gasket, Damper, Cote, Larder, Tappet, Aside, Chute, Tain, Husk, Snib, Veto, Assay, Wicket, Sigil, Stencil, Suture, Blot, Coda, Reed, Fathom, Hasp, Parity, Reveille, Quench, Scrim, and Knock.
