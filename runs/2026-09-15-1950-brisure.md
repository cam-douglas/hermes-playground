# 2026-09-15 19:50 — Brisure

Shipped catalog #378: herald's college / armorial roll / cadency desk / lacquered shield rack booth for anthropics/claude-code#94396.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/94396 (OPEN; labels: bug, has repro, platform:macos, area:core).

Title: `[BUG] Forked sessions never become Remote Control eligible, so they never appear in the mobile Code tab`.

A session created by forking an existing session is never registered with the Remote Control bridge, so it never appears in the mobile Code tab — no error, no UI hint; simply absent. `remoteControlAutoEligible` is only assigned on a session's first turn (`finishInitialEnqueue` with first_turn). A fork adopts an existing transcript so it takes the `cold_resume` path and the flag is never set for the whole life of the session. Policy `remoteControlPolicyCovers` requires `remoteControlAutoEligible` and no `scheduledTaskId`. Nothing references `forkedFromSessionId`, so fork exclusion looks incidental. Neighbouring resume-like paths DO re-arm: `/clear` uses `??= true`; prewarm claim sets true then first_turn. Data across 25 sessions: ordinary 18 eligible+visible; scheduled 2 eligible but no bridge (expected); forked 5 absent eligible + absent bridge + never visible. Workaround: manual Remote Control enable works (`remoteControlUserRequested`) but needs an active query first.

Cousins cite-only (do NOT rebuild / do NOT conflate): #94400 (Cancellans — resume-fork tools-array / deferred_tools_delta), #94397 (Diptych brief-echo), #93458 (Forksink source-fork).

Backups cite-only (next focus only — do not auto-pick): #94393 #94392 #86198 #94417 #94452 #94451 #94430 #94458 #93924 #93770 #93777 #94151.

Hypothesis (NON-BINDING): a fork takes cold_resume so remoteControlAutoEligible is never armed, and the policy therefore never offers the cadet to the bridge. Offered as a scoring narrative, not a source-root-cause claim.

Why this solution: herald's college booth scores enrolled vs brisure after a fork-resume.

Why not a clone: paradigm and UI distinct from Cancellans/Forksink/Diptych/Vizard/Treacle/Somnus/Cresset/Dictabelt/Lemure/Arras. Educational diagnostic product, not a Claude Code fix.

## Ship notes

- Idle **enrolled** / seeded **brisure** / path **fork-resume**
- Phrase: Score brisure or admit enrolled.
- Folder: `projects/brisure/`
- Live: https://hermes-playground-green.vercel.app/brisure/
- Catalog: featured Brisure #378; Diptych unfeatured
- Fonts: Cinzel + Sora + IBM Plex Mono
- Palette: gules `#7B1224`, argent `#F3EBDC`, sable `#0E0B09`, or `#D6B45A`, azure `#1F4F8F`, lacquer `#24160F`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
