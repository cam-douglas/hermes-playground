# 2026-09-13 16:50 — Crasis

Shipped catalog #332: manuscript crasis / fused-ligature booth for anthropics/claude-code#93960.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/93960 (OPEN; labels: bug, has repro, platform:windows, area:core).

Per-project storage under `~/.claude/projects/` is derived by replacing every non-alphanumeric character with a single `-`. That mapping is not injective, so two different project directories can resolve to the same store and then share auto-memory and transcripts with no warning. MEMORY.md is loaded automatically at the start of every session. Claude Code 2.1.238; Windows 11 (10.0.26200), desktop app.

Two collision classes, both live on 2.1.238:

1. Non-alphanumeric collapse — every non-ASCII character becomes one `-`, so only the count survives. `가나다` vs `라마바` (length 3) both → `C--Users-USER-AppData-Local-Temp-clash----`. Control `가나다라` (length 4) → `…-clash-----` and does not leak.
2. Separator ambiguity — `ab-cd` vs `ab/cd` (or `a-b` vs `a\b`) both become `…-ab-cd`. This is the #29471 case, still live though that issue was bot-closed COMPLETED.

Reporter scanned 1,085 stores: 2 stores held more than one real project (8 projects affected); one store shared by six Korean client projects. Workaround cited (not a product): `autoMemoryDirectory` isolates memory only; transcripts still pool.

Cousins cite-only (do not re-booth): #29471, #93743, #7009, #21085, #35162.

Backups cite-only (next focus only — do not auto-pick): #93772 #93770 #93777 #93782 #93889 #93821 #93811 #93809 #93823 #93924 #93925.

Hypothesis (NON-BINDING): the published encoding is not injective, so equal-length non-ASCII names and hyphen-vs-separator paths fuse into one drawer. Offered from published issue text only.

Why this solution: crasis/manuscript booth scores injective vs crased store-slug-collide.

Why not a clone: paradigm and UI distinct from all prior catalog entries especially Tessera/Mojibake/Scissel/Homograph.

## Ship notes

- Idle **injective** / seeded **crased** / path **store-slug-collide**
- Phrase: Score crasis or admit injective
- Folder: `projects/crasis/`
- Live: https://hermes-playground-green.vercel.app/crasis/
- Catalog: featured Crasis #332; Tessera unfeatured
- Fonts: Fraunces + Source Sans 3 + IBM Plex Mono
- Palette: ink `#1A1520`, vellum `#F4ECDF`, cinnabar `#B83A2E`, verdigris `#2F6F5E`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
