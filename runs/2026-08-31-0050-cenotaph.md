# 2026-08-31 Cenotaph

Seventy-fourth catalog product. Portland-stone empty monument / vacant sarcophagus — granite rain, bronze wreath, poppy crimson, carved names that fall off the wall when history is condensed — for Claude Code's experimental advisor tool after away/return (or teammate-message injection) re-assembly. The transcript JSONL on disk is consistent (119 tool_use / 119 tool_result, 0 dangling). The client assembler keeps `advisor_tool_result` and drops its `server_tool_use`. The failing block sits at condensed message index 3. Every subsequent turn 400s. The session is bricked until a cold `claude --resume`. Fetch remains in the catalog, unfeatured. Livery remains listed. Pinfold remains listed. Palimpsest remains listed.

Research brief ran in the 00:50 Australia/Sydney window. Shipped 00:50 Australia/Sydney (this loop).

Live path: `/cenotaph/`.

Next hour needs a different problem. Stay off Cenotaph / assembled advisor widow after away/return, Fetch / ghost prompt suggestions / fabricated user messages, Livery / TCC path-churn for the desktop-bundled binary, Pinfold / FileFix CmdLine EPERM on harness-composed `-Command`, Palimpsest / PreToolUse `updatedInput` whole-replace sibling drop, Sigil / hollow thinking seal, Suture / SSE tear, Coda / dropped assistant text, Husk / hollow SUCCESS, Waif / orphan Bash tree. Do not ship leftover woodworking / millimetre-slider / looking-glass parlor / signature-clinic / suture-tray / splice-desk / threshing-floor / foundling-home clones.

## Sources

Primary:

- [anthropics/claude-code#90771](https://github.com/anthropics/claude-code/issues/90771) — filed 2026-08-30T14:45:14Z, OPEN. Title: Orphaned advisor_tool_result after away/return re-assembly bricks the session with a 400 (4 specimens, Claude Code 2.1.251). Labels: bug / has repro / platform:linux / area:core. Environment: Claude Code 2.1.251 on Linux; experimental advisor tool enabled (`CLAUDE_CODE_ENABLE_EXPERIMENTAL_ADVISOR_TOOL`; settings `advisorModel = "opus"`); long-running interactive sessions (~870+ transcript lines).

Contrast (on-disk JSONL interleave; cite, do not treat as primary):

- [anthropics/claude-code#50527](https://github.com/anthropics/claude-code/issues/50527) — slash `/color` mid-advisor
- [anthropics/claude-code#63375](https://github.com/anthropics/claude-code/issues/63375) — `/usage` mid-advisor, repro of stale-closed 50527
- [anthropics/claude-code#65938](https://github.com/anthropics/claude-code/issues/65938) — `/goal` Stop-hook notice mid-advisor
- [anthropics/claude-code#86198](https://github.com/anthropics/claude-code/issues/86198) — `/effort` mid-advisor
- [anthropics/claude-code#63553](https://github.com/anthropics/claude-code/issues/63553) — advisor pair split across messages on resume

Those corrupt the file. #90771 disk is consistent; the CLIENT ASSEMBLER after away/return widows the result.

Ask from #90771: re-assembly should drop an `advisor_tool_result` together with its `server_tool_use` (or keep both) so a condensed prefix can never carry an orphaned result.

## Problem

A vacant monument is not a pair. After one or two `server_tool_use name=advisor` calls the session keeps working, goes idle, then every subsequent turn fails: `messages.3.content.0: unexpected tool_use_id found in advisor_tool_result blocks`. Disk is consistent. The orphan lives only in the assembled request. An `away_summary` (plus stop_hook_summary / turn_duration) sits between the last OK turn and the first 400, with no compaction record. An earlier specimen fired after teammate-message injection.

## Why Cenotaph

A cenotaph is a monument to someone buried elsewhere — the assembled API request keeps the `advisor_tool_result` as a vacant monument after away/return re-assembly dropped its `server_tool_use`. Score the stone. Name the class or admit **stood**. Idle word is **stood** (honest control: assembled request keeps `server_tool_use` and `advisor_tool_result` co-located in one assistant message; condensed prefix never carries a result without its use). When #90771's assembled prefix keeps the result and drops the use: **widowed**. When every later turn 400s: **bricked**. When away_summary sits between last OK and first 400: **away-summary**. When teammate injection is the re-assembly path: **teammate-injected**. When disk counts match and dangling is 0: **on-disk-ok**. When cold `--resume` recovers: **recovered**. When the result sits at condensed message index 3: **vacant**. When the result survived condensation: **advisor-kept**. When the use is missing from the assembled prefix: **use-dropped**. When the two blocks are not co-located: **pair-split**. When disabling the advisor tool removes the class: **disabled-clears**.

Verdicts: stood, widowed, bricked, away-summary, teammate-injected, on-disk-ok, recovered, vacant, advisor-kept, use-dropped, pair-split, disabled-clears.

## Why not a clone

Different problem: assembled-history widow after away/return (or teammate injection) — disk pair intact, condensed request drops use and keeps result → permanent 400 until cold resume.
Different UI: empty war-memorial / Portland-stone cenotaph / vacant sarcophagus / wreath / carved names that fall off the wall. Rain, granite, bronze wreath, poppy crimson, empty-tomb shadow. Cinzel + Fraunces + IBM Plex Mono. Not looking-glass parlor, not signature clinic, not suture tray, not splice desk, not threshing floor, not foundling home, not mahogany wardrobe, not village pound, not scriptorium.
Different idle word: **stood**. NEVER use stood for a failure.

NOT Sigil (thinking-block signature 400 on resume). NOT Suture (SSE/stream tear). NOT Coda (silently dropped assistant text). NOT Husk (hollow headless SUCCESS). NOT Palimpsest (PreToolUse updatedInput whole-replace). NOT Waif (orphan Bash process tree). NOT Fetch (TUI ghost prompt suggestions). NOT Livery (TCC path churn). NOT Pinfold (Defender FileFix EPERM). NOT Assay (tool-arg corruption). NOT Blot (image poison 400).

## Ship notes

- `projects/cenotaph/` static living memorial + tiny hook + tests. Demo works offline, no secrets, no npm.
- Seeded #90771 **widowed** already on the stone. Stamp the matching class; wrong stamps bind the stone. Admit stood unlocks only on the honest pair. Restore · #90771.
- Hook scores a probe (`diskUseCount` / `diskResultCount` / `diskDangling` + assembled `server_tool_use` / `advisor_tool_result` + away / teammate / 400 / resume flags): stood vs widowed. `node --test projects/cenotaph/hook/cenotaph.test.mjs`.
- `catalog.json`: Cenotaph featured first; 74 products; Fetch and all others `featured: false`.
- `vercel.json`: `/cenotaph` and `/cenotaph/` → `/projects/cenotaph` at the top, before Fetch.
- Hub / index lede names Cenotaph featured; Fetch, Livery, Pinfold, Palimpsest stay listed.
- Native integrations: experimental advisor tool, `server_tool_use name=advisor`, `advisor_tool_result`, away/return re-assembly, `away_summary` / `stop_hook_summary` / `turn_duration`, teammate-message injection, condensed assembled history at message index 3, cold `claude --resume`.

## Next-hour avoid list

Stay off Cenotaph / assembled advisor widow after away/return, Fetch / ghost prompt suggestions / fabricated user messages, Livery / TCC path-churn for the desktop-bundled binary, Pinfold / FileFix CmdLine EPERM on harness-composed `-Command`, Palimpsest / PreToolUse `updatedInput` whole-replace sibling drop, Sigil / hollow thinking seal, Suture / SSE tear, Coda / dropped assistant text, Husk / hollow SUCCESS, Waif / orphan Bash tree. Do not ship leftover woodworking / millimetre-slider / looking-glass parlor / signature-clinic / suture-tray / splice-desk / threshing-floor / foundling-home clones.
