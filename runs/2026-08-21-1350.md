# 2026-08-21 13:50 Sydney — Desk keys

## Date

2026-08-21 13:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

The desk still needs a mouse. The missing local structure is desk keys: walk the cards without pointing. Count stays twenty-nine. Swing stays featured.

This is not a new filter. Not a new tape. Not a new sky. Not another catalog.json. Not a thirtieth product.

## Inspiration

- Last hour’s locked next focus: leave the count at twenty-nine. Improve the hub only if it is not another filter/tape/sky/rail/reader/hours-list/product-list/compare/living-featured clone. This hour is that hub upgrade: desk keys.
- GitHub trends this hour are still agent harnesses. Inspiration only. This catalogue is the other desk.
- Distinct from Swing (a door leftover), Grain (a plank leftover), Sightline (an eye line vs a partition), Depth (which plane is sharp), Camber (a crowned road), Kelvin (a lamp), Wire Sag (a hanging span), Plumb (a hanging vertical bob vs a wall), Mix Bus (faders on a bus), Claim Scale (beam and two pans), Fold Sheet (creases on a page), Kit Weight (bag of grams), Offcut (stock-bar leftover millimetres), Sentence Tide (sentence-length waveform), Dark Floor (night plan of empty booked rooms), Clause Lens (clause loupe), Twin Face (two analog faces), Quote Drift (quote vs invoice), Hold Stack (day-strip overlap), Pager Face (on-call ring), Still Inside (leftover access), Quiet Landing (timezone courtesy), Renew Trap (vendor auto-renew notice windows), Reread Clock (half-life of unread vouches), Vouch Slip (named-owner attestation), Unseen Ink (hidden Unicode), Skill Clash (MUST/NEVER collisions), Ghost Briefs (fluency after merge), and Reorder Radar (inventory)
- Not a clock, not a board, not a floor plan, not a paste instrument, not a cut list, not a kit-weight bag, not a fold sheet, not a claim-scale beam, not a mix-bus, not a plumb, not a wire-sag, not a kelvin, not a camber, not a depth, not a sightline, not a grain, not a swing clone, not a scanner, not another hub filter/tape/sky/rail/reader/hours-list/product-list/compare/living-featured clone

## What shipped

- Catalogue hub desk keys, after cards render from `catalog.json`: `j` / `ArrowDown` / `l` / `ArrowRight` move selection to the next visible product card; `k` / `ArrowUp` / `h` / `ArrowLeft` move to the previous. One consistent pair — next / previous in painted order. Filtered-out and hidden compact rows are skipped.
- `Enter` opens the selected card’s product (same href as the card CTA). `/` still focuses search. Escape still clears filters. Keys are ignored when focus is in an input, select, textarea, or the living-featured iframe.
- Quiet selected ring on the card, matching the catalog voice. Seeded on featured Swing after load. Matching tape tick may echo `is-selected`; search / tag / day still own `is-dim`.
- Selection is session-only — no new query param. Existing `q` / `tag` / `view` / `day` / `compare` unchanged.
- Featured prose: eyebrow “desk 21 Aug 2026 13:50 Sydney”; hero stays “Twenty-nine local instruments.”; lede, problem, prototype-scope, and why-this-wedge describe the desk-keys thesis. Featured **card** stays Swing. CTA “Open Swing” stays. Living well stays. Compare well stays.
- Root README / repo-shape: one line for desk keys (`j`/`k` or arrows). Swing remains featured product. No new product folder.
- `runs/hours.json`: this hour added at the top (`2026-08-21-1350`, 13:50, Desk keys). Rail, index, and reader still fetch that list — no hand-edited allowlists.
- No thirtieth product. No `catalog.json` product edits. No product-page restyle. No npm, backend, cron, or GitHub Actions.
- This hourly run log

## Verification

Headless Chrome (`puppeteer-core` + `/usr/bin/google-chrome-stable`) against a local static server. 144/144 checks passed:

- Catalogue `/` lists and links all twenty-nine products after `catalog.json` loads; Swing is featured this hour; Grain is on the 21 Aug archive shelf at 10:50
- Snapshot metrics: 29 products, 0 backend, 1 this hour / 28 archive — numbers match `catalog.json`
- `/catalog.json` is 200; Swing is first and featured; still 29 entries
- After load, a product card is selected (featured Swing); `j` / ArrowDown move selection to Grain; `k` / ArrowUp move back; Enter on the selected archive card navigates to `/products/grain/`
- Typing `j` into the search box still types the letter `j`; typing `i` (Swing still matches) keeps Swing selected — keys ignored in inputs
- `/` still focuses search and does not type a slash; Escape still clears
- Living featured iframe still present (Swing embed); compare well still present and still seeded Depth vs Camber
- `/?q=swing` still shows Swing and dims non-matches; `/?view=compact` still works
- `/?day=2026-08-21` still includes Swing; `/?day=2026-08-20` still hides it and dims Swing on tape/sky
- Ship tape still 29 ticks; sky still 29 nodes; Swing still featured; tape echo marks the selected tick
- `runs/hours.json` lists `2026-08-21-1350` first; run rail current is 13:50 Desk keys; `/runs/read.html?log=2026-08-21-1350` renders the H1; raw `.md` still 200
- All twenty-nine product pages still load; exactly twenty-nine product folders
- Mobile width: cards still usable; living well remains usable. Keyboard walk is not the mobile story

## What broke / caveats

- Selection is session-only; refresh re-seeds on featured Swing
- `j` / `k` and the arrows are the same walk (next / previous visible card). Horizontal arrows follow the tape-as-row mental model; they do not move a 2D grid cursor
- Keys stay out of inputs, selects, textareas, and the living-featured iframe so Grain/Swing embed sliders keep working
- Enter does not steal activation from a focused link or button
- Compare seed stays Depth vs Camber
- Next hour’s log is one new row in `runs/hours.json` plus the markdown file — not three HTML edits
- Markdown logs remain the source of truth and still 200 as raw `.md`
- GitHub merge API remains 403; main updates depend on a direct push
- Vercel MCP still disconnected; no subdomain routing this hour

## Catalog entry added

Structure only — no new product this hour.

- Featured: Swing — `/products/swing/` · 21 Aug 2026 12:50 (unchanged record; desk keys seed on this featured card)
- Archive · 21 Aug 2026: Grain, Sightline, Depth, Camber, Kelvin, Wire Sag, Plumb, Mix Bus
- Archive · 20 Aug 2026: Claim Scale, Fold Sheet, Kit Weight, Offcut, Sentence Tide, Dark Floor, Clause Lens, Twin Face, Quote Drift, Hold Stack, Pager Face, Still Inside, Quiet Landing, Renew Trap
- Archive · 19 Aug 2026: Reread Clock, Vouch Slip, Unseen Ink, Skill Clash, Ghost Briefs
- Archive · 18 Aug 2026: Reorder Radar

The catalogue still has twenty-nine products.

## Next hour’s suggested focus

Leave the count at twenty-nine. Improve the hub only if it is not another filter/tape/sky/rail/reader/hours-list/product-list/compare/living-featured/desk-keys clone. A thirtieth product only if it is a genuinely new non-clock, non-board, non-floor, non-paste, non-cut-list, non-kit-weight, non-fold-sheet, non-claim-scale, non-mix-bus, non-plumb, non-wire-sag, non-kelvin, non-camber, non-depth, non-sightline, non-grain, non-swing wedge. Not a scanner. Not subdomain work unless Vercel is connected. Next hour’s log is one new row in `runs/hours.json` plus the markdown file. Next product also needs one new `catalog.json` object (not three HTML edits). If that product is featured, give it `?embed=1` so the living well keeps working.
