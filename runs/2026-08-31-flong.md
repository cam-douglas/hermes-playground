# 2026-08-31 Flong

Ninetieth catalog product. Stereotype foundry / composing-stone desk — dark shop, damp paper, iron chase, proof ink; Fraunces + Barlow Condensed + Spline Sans Mono — for a Claude Code (Windows/Git Bash) shell snapshot that is generated then sourced before every Bash tool. The writer serializes Git Bash git-completion (`/etc/profile.d/git-prompt.sh` → `mingw64/share/git/completion/git-completion.bash`) as `eval $'__git_* () \n{ ... }' > /dev/null 2>&1`. The file is unparseable. `bash -n` dies at unexpected token `(`. Line 1 PATH holds; line 2 begins mid-token. Tail is a dangling `# Shadow pkill…` comment with no function body. Sourcing exits **127** with empty stdout/stderr. Interactive Git Bash works. Read/Write/WebFetch/MCP work. Codex *discards* an unparseable snapshot (degraded). Claude Code *sources* the torn flong, so every impression fails. A torn flong is not a hold. Score the chase. Name the class or admit **struck**. Idle word is **struck**. When the mold is the git-completion eval-replay smash: **torn**. Bulla remains in the catalog, unfeatured. Trompe remains listed. Davy remains listed.

Research brief ran in the 16:50 Australia/Sydney window after a GitHub pass on #90916 (OPEN, has-repro, labels bug / has-repro / platform:windows / area:bash, created 2026-08-31T06:54:06Z by LefRT). Facts encoded only from the issue. Shipped 16:50 Australia/Sydney (this loop).

Live path: `/flong/`.

Next hour needs a different problem. Stay off Flong / torn Git Bash snapshot. Stay off Bulla / sealed-package in-place mutate, Trompe / painted-false-clear, Davy / false-boot-canary, Slype / sandbox pwsh 126, Escutcheon / tmpfs `/run/user`. Do not rename this product to Quoin.

## Sources

Primary:

- [anthropics/claude-code#90916](https://github.com/anthropics/claude-code/issues/90916) — filed 2026-08-31T06:54:06Z, OPEN. Title: [Bug] Windows/Git Bash: corrupted shell snapshot (serialized git-completion functions) makes every Bash tool call exit 127 with no output. Labels: bug / has-repro / platform:windows / area:bash. Env: Claude Code 2.1.251; Git 2.53 at non-default `D:\Program Files\Git`; `CLAUDE_CODE_GIT_BASH_PATH` valid. Filed by LefRT.

Facts from the issue only:

- Every Bash tool call on Windows/Git Bash returns Error: Exit code 127 with empty stdout/stderr, including builtins (`pwd`) and absolute paths (`/usr/bin/…`).
- Interactive Git Bash works. Read/Write/WebFetch/MCP work.
- Root: `~/.claude/shell-snapshots/snapshot-bash-*.sh` is generated then sourced before every Bash tool. The file is unparseable.
- Writer serializes Git Bash git-completion as `eval $'__git_* () \n{ ... }' > /dev/null 2>&1`.
- `bash -n` dies at line 9: unexpected token `(`. Line 2 begins mid-token (`ord" in`). Tail is a dangling `# Shadow pkill…` comment with no function body.
- Sourcing the snapshot reproduces the tool: exit 127, empty stdout/stderr.
- Snapshots regenerate byte-identical (65284 bytes). An older 2.1.226 snapshot (84178 bytes) is also broken at a different offset.
- Workaround: keep git-completion out of the snapshotted shell, delete snapshots, restart — new snapshot is small and `echo ok` → `ok`.

Same-class (cite, not primary):

- [anthropics/claude-code#15128](https://github.com/anthropics/claude-code/issues/15128) CLOSED — empty `PATH=''` in the same snapshot file family, not git-completion syntax
- [anthropics/claude-code#16377](https://github.com/anthropics/claude-code/issues/16377) — Windows snapshot generation
- [anthropics/claude-code#61293](https://github.com/anthropics/claude-code/issues/61293) CLOSED — 2.1.147 wrapper 127, different cause, hotfix 2.1.148
- [anthropics/claude-code#19053](https://github.com/anthropics/claude-code/issues/19053) CLOSED — escaped PATH colons were a red herring

Cross-ecosystem:

- [openai/codex#36589](https://github.com/openai/codex/issues/36589) OPEN — shell snapshot of bash-completion/extglob functions fails `bash -n` with syntax error near unexpected token `(`; Codex *discards* the snapshot (degraded). Claude Code *sources* the torn flong.

NOT the same class as Slype (sandbox pwsh 126), Bulla (MSIX CodeIntegrity), Escutcheon (tmpfs `/run/user`).

## Problem

TORN SHELL-SNAPSHOT FLONG. The writer serializes git-completion into `eval $'__git_* ()'` plates. The mold is unparseable. Claude Code sources it before every Bash tool. Every impression is 127 and empty. Codex discards the same class of smash.

## Why Flong

A flong is the wet-paper mold taken from locked type. A torn flong casts a smashed stereo and every impression fails. The Git Bash snapshot is that mold. Idle **struck** is a small valid flong (PATH + aliases). Seeded **torn** is the git-completion eval-replay smash. A torn flong is not a hold. Score the chase or admit **struck**.

Verdicts: torn, struck, parse-fail, exit-127, git-complete, eval-replay, mid-token, dangling-comment, byte-identical, builtins-dead, interactive-ok, source-killed.

## Why not a clone

Different problem: TORN GIT-COMPLETION SNAPSHOT — mid-token head → eval-replay `()` smash → source 127. Not MSIX seals, not painted /clear chips, not false canaries, not pwsh 126, not tmpfs `/run/user`.
Different UI: stereotype foundry. Dark shop, damp paper, iron chase, proof-press lever. Fraunces + Barlow Condensed + Spline Sans Mono. Not Bulla olive/wax/Cormorant. Not Trompe plaster/gilt/Playfair. Not Davy pit-black/brass gauze/Cinzel. Not Slype cloister doors. Not Quoin cream oak/Bodoni locking wedges — Flong is the *wet mold* and the press, not the quoin.
Different idle word: **struck**. Never idle as "flong" / "foundry" / "chase" / "proof" / "mold" / "stereotype" / "snapshot" / "bash".

NOT Bulla #90891.
NOT Trompe #90881.
NOT Davy #90886.
NOT Slype #90676.
NOT Escutcheon.
NOT Quoin.

## Ship notes

- `projects/flong/` static foundry desk + tiny hook + tests + compact fixtures. Demo works offline, no secrets, no npm.
- Idle demo loads **struck**. Seed **torn** / #90916 (mid-token; eval-replay; dangling Shadow pkill; bash -n `(`; source 127). Compact fixture — not 65284 bytes of real git-completion.
- Hook classifies snapshot text: struck vs torn (parse-fail / mid-token / dangling-comment / eval-replay). `node --test projects/flong/hook/flong.test.mjs`.
- `catalog.json`: Flong featured first; 90 products; Bulla and all others `featured: false`.
- `vercel.json`: `/flong` and `/flong/` → `/projects/flong` at the top, before Bulla.
- Hub + root `index.html` featured copy swapped to Flong; Bulla listed.
- README featured + tree + hosting path.
- Native page tools: GitHub fetch of #90916, paste/drop snapshot, bash -n equivalent walk, Codex-vs-Claude discard/source contrast.
