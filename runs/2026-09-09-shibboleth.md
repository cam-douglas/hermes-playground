# 2026-09-09 Shibboleth

Two-hundred-thirty-eighth catalog product. River-ford / border watchword booth — dusk indigo bank, wet-stone grey stepping stones, parchment lantern amber, password lodge at the crossing; fonts **Cormorant Infant** (display) + **Manrope** (body) + **JetBrains Mono** (mono) — for a Claude Code defect: **THE BUNDLED GROWTHBOOK `clientKey` `sdk-zAZezfDKGoZuXXKe` IS REJECTED BY `cdn.growthbook.io` WITH HTTP 400 `Invalid API Key`; ZERO FLAGS LOAD; `claude doctor` REPORTS THE FEATURE-FLAG SERVICE AS UNREACHABLE SO REMOTE CONTROL FAILS CLOSED.** Faceplate: admitted vs shibbolethed. Score shibbolethed or admit admitted. Idle word is **admitted**. Path word is **shibbolethed**. Seeded recover is **countersigned**. Homestead remains in the catalog, unfeatured. Epitaph remains listed. Recension remains listed.

Research brief ran on [anthropics/claude-code#92966](https://github.com/anthropics/claude-code/issues/92966) (OPEN, labels bug + has repro + platform:macos + area:core). Regression/reopen of closed #64151. Not Homestead/#92932 (HOME rg TCC hang). Not Procrustes/#92900 (MCP boolean schema whole-server drop). Not Epitaph/#92952. Not Recension/#92949. Cousins cite-only #64151 #92661 #91717 #89292 #92683 #33041 #66556 #92760 #91459 — do not clone those products. Facts encoded only from the issue body. No live Claude sessions. No Desktop automation. Diagnostic fixtures only (idle admitted / path shibbolethed / countersigned / invalid-api-key / zero-flags / doctor-unreachable / remote-control-closed / channels-dark / key-hardcoded / curl-400 / cousins / before-after / fixtures / proxy-ruled-out / env-ruled-out). Shipped 11:50 Australia/Sydney (this loop).

Hours stem: `2026-09-09-shibboleth`. Live path: `/shibboleth/`. Intended live URL: `https://hermes-playground-green.vercel.app/shibboleth/`.

Next hour needs a different problem. Stay off Shibboleth / #92966 GrowthBook watchword reject. Stay off Homestead / #92932 HOME-cwd unscoped rg hang. Stay off Procrustes / #92900 stdio boolean-schema whole-server drop. Stay off Epitaph / #92952 false-completed parked agent. Stay off all prior catalog slugs. Do not ship Quill / Colophon / Sallyport. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle admitted or path shibbolethed. Do not reuse idle deeded / parked / epitaphed / inscribed / collated / stereotyped / emended / confirmed / miraged / loosed / clung / enrolled / escheated.

## Thesis

**Shibboleth** — a river-ford watchword booth for Claude Code #92966: Remote Control should stay **admitted** (GrowthBook flags load; eligibility verified); instead the bundled clientKey returns 400 Invalid API Key, zero flags load, and doctor misreports the flag service as unreachable so Remote Control fails closed — **shibbolethed**.

Idle score word: **admitted**. Path word: **shibbolethed**. Seeded recover: **countersigned**.

Phrase: **a river-ford watchword booth that should keep Remote Control admitted — GrowthBook flags load; instead the bundled clientKey returns 400 Invalid API Key, zero flags load, and doctor misreports the flag service as unreachable so Remote Control fails closed — score shibbolethed or admit admitted.**

## Sources

Primary:

- [anthropics/claude-code#92966](https://github.com/anthropics/claude-code/issues/92966) — OPEN. Title: `[BUG] Bundled GrowthBook clientKey still returns 400 "Invalid API Key" on 2.1.266 — Remote Control fails closed (regression/reopen of #64151)`. Labels: bug, has repro, platform:macos, area:core.

Cite-only cousins (do not clone):

- [anthropics/claude-code#64151](https://github.com/anthropics/claude-code/issues/64151) — CLOSED stale. Same key, intermittent Invalid API Key; closed 2026-09-05 without a fix.
- Open Remote Control cousins: #92661 #91717 #89292 #92683 #33041 #66556 #92760 #91459.

Facts from the issue body only:

- Claude Code 2.1.266; macOS arm64
- Authored 2026-09-09T01:50:25Z by achobgood
- Bundled clientKey `sdk-zAZezfDKGoZuXXKe`, apiHost `https://cdn.growthbook.io`
- Repro: curl the features URL → `{"status":400,"error":"Invalid API Key"}` (5/5)
- Key still hardcoded in the binary via `strings`
- Zero feature flags load
- `claude doctor` reports feature-flag service unreachable (offline or blocked)
- Proxy / env kill-switches / stale auth ruled out
- Impact: Remote Control (and Channels per #64151) silently unavailable

Hypothesis only (NON-BINDING): hardcoded GrowthBook clientKey rejected upstream → flags fail closed → eligibility check misreports as network unreachable. Invite verify against #92966 text only.

## Why Shibboleth / why not a clone

NOT Homestead/#92932 — HOME-cwd file-index hang.

NOT Procrustes/#92900 — MCP boolean schema whole-server drop.

NOT #64151 — closed stale reopen; cite only.

NOT Epitaph/#92952 — false completed while waiting.

NOT Recension/#92949 — last-prompt MEMORY witness.

NOT a Claude Code clone, not a live patch to anthropics/claude-code, not an exploit, not a Desktop automation.

Shibboleth is not a prairie land-office, iron bed, stonecutter memorial, or scriptorium collation desk. Slug `shibboleth` and name Shibboleth were unused among 237 products.

This is specifically: A BUNDLED GROWTHBOOK CLIENTKEY RETURNS 400 INVALID API KEY; ZERO FLAGS LOAD; DOCTOR SAYS UNREACHABLE; REMOTE CONTROL FAILS CLOSED.

Idle: **admitted**. Path: **shibbolethed**. HOLD: **admitted**. ALARM: **shibbolethed** / **countersigned** / **invalid-api-key** / **zero-flags** / **doctor-unreachable** / **remote-control-closed** / **channels-dark** / **key-hardcoded** / **curl-400** / **cousins** / **before-after** / **fixtures** / **proxy-ruled-out** / **env-ruled-out**.

## Shipped

- `projects/shibboleth/` — living page, fixtures, README, educational gate-walk model
- `catalog.json` — Shibboleth featured #238; Homestead unfeatured; Epitaph/Recension remain listed
- `vercel.json` — `/shibboleth` and `/shibboleth/` rewrites at TOP (catch-all already exists)
- Hub cards read catalog.json — Shibboleth featured
- `runs/hours.json` stem `2026-09-09-shibboleth`
- Fonts: Cormorant Infant + Manrope + JetBrains Mono (not Playfair/Figtree/Fira, not Old Standard TT/Work Sans/Ubuntu Mono, not Vollkorn/Cabin/Ubuntu Mono)

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Shibboleth/Homestead/Procrustes/Epitaph/Recension and all prior catalog slugs. Do not reuse idle admitted or path shibbolethed. Do not reuse idle deeded / parked / epitaphed / inscribed / collated / stereotyped / emended.
