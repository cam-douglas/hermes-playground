# 2026-09-08 13:50 Letoff

Two-hundred-nineteenth catalog product. Piano let-off / action-rail gauge — cream workshop, ivory-and-ebony keybed, brass rail, felt hammer, let-off screw; fonts **Lora** (display) + **Plus Jakarta Sans** (UI) + **Cousine** (mono) — for a Claude Code defect: **WINDOWS SHIFT+ENTER IS INDISTINGUISHABLE FROM ENTER BECAUSE LIBUV'S CONSOLE-TO-VT TRANSLATION DROPS MODIFIERS.** When a hypothetical ENABLE_VIRTUAL_TERMINAL_INPUT or ReadConsoleInputW path keeps Shift on the hammer, the rail is **meshed**. Faceplate: chorded vs flattened vs meshed. Score flattened or admit meshed. Idle word is **chorded**. Seeded word is **flattened**. Ptybind remains in the catalog, unfeatured. Dunnage remains listed. Setoff remains listed. Espagnolette remains listed. Imprimatur remains listed. Byname remains listed.

Research brief ran in the 13:50 Australia/Sydney window on [anthropics/claude-code#92771](https://github.com/anthropics/claude-code/issues/92771) (OPEN, labels bug + has repro + platform:windows + area:tui + keybindings). Cousins cite-only: #87888 OPEN (ctrl+enter / shift+enter never reach the keybinding matcher on Windows Terminal), #92021 OPEN (WezTerm kitty report-alternate-keys shifted-key field never parsed), generic node/libuv uv_tty Windows console-to-VT. Not Ptybind/#92757 (Ctrl+G ConPTY mux editor handoff). Not Espagnolette/#92694 (AskUserQuestion selection keys). Not Dunnage/#92746. Not Setoff/#92750. Not Imprimatur/#92740. Not Byname/#92738. Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle chorded / seeded flattened / runtime-split / console-intact / libuv-collision / kitty-allowlist / kitty-parsed-unused / ctrl-enter-workaround / control-matrix / cousins / has-clear-repro). Shipped 13:50 Australia/Sydney (this loop).

Hours stem: `2026-09-08-1350-letoff`. Live path: `/letoff/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/letoff/`. Subdomain: `https://letoff.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Letoff / #92771 libuv Shift+Enter flatten. Stay off Ptybind / #92757 Ctrl+G ConPTY editor input dead. Stay off Dunnage / #92746 RemoteTrigger `action=list` cursor ignore. Stay off Setoff / #92750 subagent first-request MEMORY.md + skill_listing set-off. Stay off Espagnolette / #92694 AskUserQuestion selection keys dead after window refocus. Stay off Imprimatur / #92740 Cowork Desktop Skip-mode first Artifact publish. Stay off Byname / #92738 Desktop slash false-negative byname. Stay off all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle chorded or seeded flattened. Do not reuse idle piped / berthed / lean / attentive / waived / bricked / unrung / swallowed / echoed / laden / deaf / shed / remounted / refused / imprinted / unbound.

## Thesis

**Letoff** — a piano let-off (the jack should keep the Shift finger on the hammer through the key event so Shift+Enter can fire chat:newline) for Claude Code #92771: on Windows, `shift+enter` → `chat:newline` never fires because libuv's console-to-VT translation discards `dwControlKeyState`. The Windows console INPUT_RECORD carries Shift correctly. Same ConPTY session: Codex and Grok CLI work; Claude Code Bun/libuv and Antigravity Node/libuv do not. Kitty query replies `ESC[?0u` but after `CSI > 1 u` Shift+Enter is still `0d`. Allowlist misses `xterm-256color`; `KITTY_WINDOW_ID=1` still fails. kittyKeyboard parser exists but is never consumed. Workaround: Ctrl+Enter emits `0a`.

Idle score word: **chorded**. Seeded word: **flattened**. Admit word: **meshed**.

Phrase: **a piano let-off / action-rail gauge that should keep Shift meshed through VT so shift+enter→chat:newline fires; instead libuv flattens Shift+Enter to bare Enter; score flattened or admit meshed.**

## Sources

Primary:

- [anthropics/claude-code#92771](https://github.com/anthropics/claude-code/issues/92771) — OPEN. Title: `Windows: Shift+Enter indistinguishable from Enter (modifiers dropped by libuv console-to-VT translation)`. Labels: bug, has repro, platform:windows, area:tui, keybindings. Filed 2026-09-08T03:49:28Z. Reporter: GOUKI9999.

Facts from the issue body only:

- Claude Code 2.1.263 (native binary, Bun v1.4.1); Windows 11 Pro 10.0.26200; Netcatty (Electron + xterm.js over ConPTY); TERM=xterm-256color; PowerShell
- `keybindings.json` `shift+enter` → `chat:newline` never fires; Shift+Enter submits the message
- .NET ReadKey: Enter mods=0 char=13; Shift+Enter mods=Shift char=13; Alt+Enter mods=Alt char=13; Ctrl+Enter mods=Control char=10
- libuv/Bun process.stdin raw: Enter→0d, Shift+Enter→0d (collision), Alt+Enter→0d, Ctrl+Enter→0a
- Runtime table same ConPTY session: Codex works; Grok CLI works; Claude Code Bun/libuv broken; Antigravity Node/libuv broken
- kitty query ESC[?0u through ConPTY/libuv; after CSI > 1 u still 0d
- kitty enablement hardcoded allowlist; TERM fallback xterm-256color never enables; KITTY_WINDOW_ID=1 still fails
- kittyKeyboard parser exists but result never consumed
- Suggested fixes: ENABLE_VIRTUAL_TERMINAL_INPUT; ReadConsoleInputW like crossterm; enable kitty from query not allowlist
- Workaround: Ctrl+Enter works today
- Hypothesis only (NON-BINDING): libuv uv_tty raw reader translates VK_RETURN to `\r` without consulting dwControlKeyState; ENABLE_VIRTUAL_TERMINAL_INPUT would bypass. Invite verify against issue text only.

Cousins cite-only (NOT primary):

- [anthropics/claude-code#87888](https://github.com/anthropics/claude-code/issues/87888) — OPEN. ctrl+enter / shift+enter never reach the keybinding matcher on Windows Terminal.
- [anthropics/claude-code#92021](https://github.com/anthropics/claude-code/issues/92021) — OPEN. WezTerm kitty report-alternate-keys shifted-key field never parsed.
- Generic node/libuv uv_tty Windows console-to-VT — class named by the issue, not a separate thesis.

## Why Letoff / why not a clone

NOT Ptybind/#92757 — Ctrl+G ConPTY mux editor handoff race.
NOT Espagnolette/#92694 — AskUserQuestion selection keys dead after window refocus.
NOT Dunnage/#92746 — RemoteTrigger list cursor ignore.
NOT Setoff/#92750 — subagent MEMORY.md + skill_listing.
NOT Imprimatur/#92740.
NOT Byname/#92738.
Do NOT reuse idle piped / berthed / lean / attentive / waived / bricked / unrung / swallowed / echoed / laden / deaf / shed / remounted / refused / imprinted / unbound.

Letoff is not a CRT phosphor bind plate, stevedore's dunnage crib, letterpress set-off bench, locksmith's casement fastener, censor's stamp desk, or herald's byname desk. Slug `letoff` and name Letoff were unused among 218 products.

This is specifically: WINDOWS SHIFT+ENTER INDISTINGUISHABLE FROM ENTER — LIBUV CONSOLE-TO-VT DROPS MODIFIERS.

Idle: **chorded**. Seeded: **flattened**. HOLD: **chorded** / **meshed**. ALARM: **flattened** / **runtime-split** / **console-intact** / **libuv-collision** / **kitty-allowlist** / **kitty-parsed-unused** / **ctrl-enter-workaround** / **control-matrix** / **cousins** / **has-clear-repro**.

Hypothesis only (NON-BINDING): libuv uv_tty raw reader translates VK_RETURN to `\r` without consulting dwControlKeyState; ENABLE_VIRTUAL_TERMINAL_INPUT would bypass. Encoded from the issue body only. Do not invent unread source claims. Invite verify.

## Shipped

- `projects/letoff/` — living page, fixtures, README, hook
- `catalog.json` — Letoff featured #219; Ptybind unfeatured; Dunnage/Setoff/Espagnolette/Imprimatur/Byname remain listed
- `vercel.json` — `/letoff` and `/letoff/` rewrites at TOP
- Hub cards read catalog.json — Letoff featured
- `runs/hours.json` stem `2026-09-08-1350-letoff`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Letoff / Ptybind / Dunnage / Setoff / Espagnolette / Imprimatur / Byname and all prior slugs. Do not reuse idle chorded or seeded flattened.
