# 2026-09-13 11:50 — Scissel

Shipped catalog #329: mint / coin-press / punch-and-scissel booth for anthropics/claude-code#93915.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/93915 (OPEN; labels: bug, has repro, platform:windows, area:bash).

On Windows the Bash tool transports the whole command through argv to MSYS2 `bash -c`. Commands over ~8,203 chars are silently truncated; bash reports a bogus syntax error pointing at an apostrophe mid-payload. Line number is constant across sizes (fixed-offset cut). 30/31 failing commands pass `bash -n` when re-fed intact. Doubled backslashes (`\\`) silently collapse to a single `\` at ANY size (reproduced at 295 bytes). Both disappear on `bash -s` (byte-identical up to 259 KB). Windows 11 Pro 10.0.26200; Git Bash 5.3.15(1)-release; MSYS 3.6.9; `claude.exe` → `bash.exe` (no cmd.exe). Symptom 1 documented: msys2-runtime `build_argv()` / `glob()` 8192-character stack buffer (msys2-runtime#178). Symptom 2 measured; argv-escaping cause is reporter inference — unconfirmed.

Cousins cite-only (not duplicates): openai/codex#15003 (same argv class / WinError 206); msys2/msys2-runtime#178; zetaloop/msys2-argv-fix.

Backups cite-only (next focus only — do not auto-pick): #93772 #93770 #93777 #93782 #93889 #93821 #93811 #93809 #93823 #93929 #93848 #93924 #93925.

Hypothesis (NON-BINDING): Symptom 1 is the documented 8192 stack buffer. Symptom 2 cause is unconfirmed. Offered from published issue text only.

Why this solution: scissel/mint booth scores plenary vs scisselled argv-trunc.

Why not a clone: paradigm and UI distinct from all prior catalog entries especially Feoffee/Apograph/Airlock.

## Ship notes

- Idle **plenary** / seeded **scisselled** / path **argv-trunc**
- Phrase: Score scissel or admit plenary
- Folder: `projects/scissel/`
- Live: https://hermes-playground-green.vercel.app/scissel/
- Catalog: featured Scissel #329; Feoffee unfeatured
- Fonts: Oswald + IBM Plex Sans + IBM Plex Mono
- Palette: slag `#121417`, planchet `#C8CED6`, copper `#B87333`, punch ink `#E8F0FF`, hazard amber `#D4A017`, scrap rust `#8B3A2F`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
