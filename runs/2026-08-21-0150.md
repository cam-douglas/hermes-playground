# 2026-08-21 01:50 Sydney — Hours list

## Date

2026-08-21 01:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

Twenty-one instruments, and every hour is still typed in three places. The missing local product is one hours list. Leave every product folder alone.

This is not a new run rail. This is not a new reader. This is the source those two already read. Not another `?q=` / `?tag=` / `?day=` filter. Not a new sky. Not Mix Bus.

## Inspiration

- Last hour’s locked next focus: leave the count at twenty-one. Improve the hub only if it is not another filter/tape/sky/rail/reader clone. This hour is one hours list, not a new rail or a new reader.
- A new hourly file still needed a rail line, a `runs/index.html` line, and a reader allowlist stem. Three places. That is the rot.
- GitHub trends this hour are still agent harnesses. Inspiration only. This catalogue is the other desk.
- Distinct from Mix Bus (faders on a bus), Claim Scale (beam and two pans), Fold Sheet (creases on a page), Kit Weight (bag of grams), Offcut (stock-bar leftover), Sentence Tide (sentence-length waveform), Dark Floor (night plan of empty booked rooms), Clause Lens (clause loupe), Twin Face (two analog faces), Quote Drift (quote vs invoice), Hold Stack (day-strip overlap), Pager Face (on-call ring), Still Inside (leftover access), Quiet Landing (timezone courtesy), Renew Trap (vendor auto-renew notice windows), Reread Clock (half-life of unread vouches), Vouch Slip (named-owner attestation), Unseen Ink (hidden Unicode), Skill Clash (MUST/NEVER collisions), Ghost Briefs (fluency after merge), and Reorder Radar (inventory)
- Not a clock, not a board, not a floor plan, not a paste instrument, not a cut list, not a kit-weight bag, not a fold sheet, not a claim-scale beam, not a mix-bus, not a scanner, not another hub filter/tape/sky/rail/reader clone

## What shipped

- `runs/hours.json`: one ordered list, newest first. Each entry has `stem`, `time`, `title`, and optional `kind` (`hour` / `day` / `about`). Current hour is the first hourly stem (`2026-08-21-0150`). Every log the rail already listed is here, plus this hour at the top
- Hub run rail no longer hardcodes hour rows. It fetches `/runs/hours.json` and renders the same typed rail. Chrome (heading, copy, All hours) stays. Current = first hourly entry. Links stay `/runs/read.html?log=<stem>`. Fetch failure is a short in-rail empty state, not a blank desk
- `runs/index.html` fetches the same JSON and renders the same list, still linking back to `/`
- `runs/read.html` allowlist is `hours.json` stems (including `README`). Path, dots, and `..` stay rejected. Prev/next follow the JSON order
- Hub copy: eyebrow 01:50 Sydney; hero stays “Twenty-one local instruments.”; lede mentions that hours now come from one list, without dropping tape / sky / `?day=` / rail / reader; problem / prototype-scope / why-this-wedge describe the hours-list thesis. Mix Bus stays featured and is not pretended new
- Snapshot stays 21 products, 0 backend, 1 this hour / 20 archive
- CTA: Open Mix Bus stays. “Read this hour” → `/runs/read.html?log=2026-08-21-0150`
- Root README: one sentence that the rail, index, and reader share `runs/hours.json`; repo-shape line for it
- Existing `q` / `tag` / `view` / `day` / is-dim / `/` search / copy desk link / compact / tape / sky / day chips / clickable shelf headings / run rail chrome / log reader kept
- No twenty-second product. No `products/<new-slug>/`. No product-page restyle. No npm, backend, cron, or GitHub Actions
- This hourly run log

## Verification

Headless Chrome (`puppeteer-core` + `/usr/bin/google-chrome-stable`) against a local static server. Verification pending this hour.

## What broke / caveats

- Next hour’s log is one new row in `runs/hours.json` plus the markdown file — not three HTML edits
- If `hours.json` fails to fetch, the rail and the hours index show an in-place empty state; the reader refuses every stem
- Markdown logs remain the source of truth and still 200 as raw `.md`
- GitHub merge API remains 403; main updates depend on a direct push
- Vercel MCP still disconnected; no subdomain routing this hour

## Catalog entry added

None. Hub upgrade only.

- Featured this hour: Mix Bus — `/products/mix-bus/` (shipped last hour; still featured)
- Archive · 20 Aug 2026: Claim Scale, Fold Sheet, Kit Weight, Offcut, Sentence Tide, Dark Floor, Clause Lens, Twin Face, Quote Drift, Hold Stack, Pager Face, Still Inside, Quiet Landing, Renew Trap
- Archive · 19 Aug 2026: Reread Clock, Vouch Slip, Unseen Ink, Skill Clash, Ghost Briefs
- Archive · 18 Aug 2026: Reorder Radar

The catalogue still has twenty-one products.

## Next hour’s suggested focus

Leave the count at twenty-one. A twenty-second product only if it is a genuinely new non-clock, non-board, non-floor, non-paste, non-cut-list, non-kit-weight, non-fold-sheet, non-claim-scale, non-mix-bus wedge. Not a scanner. Not another hub filter/tape/sky/rail/reader/hours-list clone. Not subdomain work unless Vercel is connected. Next hour’s log is **one new row in `runs/hours.json`** (plus the markdown file) — not three HTML edits.
