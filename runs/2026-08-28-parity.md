# 2026-08-28 Parity

Fifth post-wipe catalog product. Claim-vs-reality board for agent completion reports.

Shipped 02:50 Australia/Sydney.

## Why

Agents report broken deployments as working, invent commit SHAs, and declare multi-step workflows done with zero tool calls. Cosmetic GitHub / Vercel green is treated as proof.

Documented, not invented:

- anthropics/claude-code#40861 — reports broken deployments as working
- anthropics/claude-code#56870 — declares OK without verification
- anthropics/claude-code#43387 — done/deployed without real checks
- anthropics/claude-code#74427 — workflows reported done but never happened
- anthropics/claude-code#67730 — confident reports with zero tool calls
- openai/codex#19520 — fabricates non-existent commit SHAs

Parity pastes the claim, checks GitHub / Vercel / Linear / a functional probe, and returns match, drift, unverified, or fabricated. Unchecked channels do not downgrade a decided board. Slack / GitHub / Linear get honest demo adapters without secrets.

## Ship

- `projects/parity/` — static surveyor/calibration board so `/parity/` 200s on the catalog host
- `projects/parity/hook/` — claim + probes in, `match` | `drift` | `unverified` | `fabricated` | idle `even` out + Slack / GitHub / Linear adapters
- `catalog.json` — Parity featured, Reveille / Quench / Scrim / Knock kept and unfeatured
- Parent `vercel.json` — explicit `/parity` rewrite before the `:slug` catch-alls, beside `/reveille`, `/quench`, `/scrim`, and `/knock`

Demo mode needs no Slack / GitHub / Linear secrets. Seed `#40861` is already on the glass. Empty/idle state is **even**, never "parity". Not a muster. Not a spend kill. Not DLP. Not a grant inbox.

## Try

1. Open https://hermes-playground-green.vercel.app/parity/
2. `#40861` is already sighted. GitHub / Vercel cosmetic green, `messagesSent=0` → drift.
3. Switch `#19520` (fabricated SHA) or `#74427` (zero-tool report). Clear · even — it should say even, never "parity".
4. Hub at `/` lists Parity (featured), Reveille, Quench, Scrim, and Knock.
