# 2026-09-14 22:50 — Agraphia

Shipped catalog #362: clinical agraphia / neurology writing-desk booth for anthropics/claude-code#94251.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/94251 (OPEN; labels: bug, has repro, platform:macos, area:core, area:hooks, regression).

Since upgrading 2.1.267 → 2.1.270, the session transcript (`projects/<project>/<session>.jsonl`) is missing most assistant `text` blocks that come before a `tool_use` in the same response. The text is shown in the terminal as normal. `thinking` and `tool_use` rows are still written, the latter with full input. End-of-turn text is affected much less. A phrase from a reply shown in the UI appears only inside later tool inputs that quote it.

Measurements (one machine, `claude-opus-5`, `entrypoint: cli`, two `CLAUDE_CONFIG_DIR` profiles): share of assistant rows with a text block dropped from 26.4%→12.8% (A) and 27.6%→2.9% (B). Text rows with `stop_reason: tool_use` as a share of tool calls: 51.7%→13.6% (A) and 53.7%→1.9% (B). One session resumed across the upgrade went from 372 text blocks in 1,262 assistant rows (2.1.263) to 3 in 40 (2.1.270). Haiku sessions that never call tools record text on every response on both versions.

Why it matters: hooks that read `transcript_path` to check what the model said before a tool call can no longer see it; PreToolUse payload carries no assistant text; a PreToolUse hook requiring a written marker before certain commands became impossible on 2.1.270.

Cousins cite-only (not duplicates): #65051 (daemon sessions, 2.1.161), #76668 (desktop app) — same shape on other entrypoints. This booth is specifically interactive CLI 2.1.270.

Backups cite-only (next focus only — do not auto-pick): #93987 #93924 #93770 #93777 #94151 #94064 #94256 #94277 #94275 #94274 #94273 #94267.

Hypothesis (NON-BINDING): interactive CLI persistence of assistant content blocks before tool_use regressed between 2.1.267 and 2.1.270 so JSONL omits those text blocks while UI still renders them; hooks reading transcript_path go blind. Offered as a scoring narrative, not a source-root-cause claim.

Why this solution: neurology writing-desk booth scores penned vs agraphia after a pre-tool-omit.

Why not a clone: paradigm and UI distinct from Gauntlet/Lictor/Lychgate/Ouster/Palilalia/Rasure. Not a parchment-scrape rasura clone of existing Rasure/#93791.

## Ship notes

- Idle **penned** / seeded **agraphia** / path **pre-tool-omit**
- Phrase: Score agraphia or admit penned.
- Folder: `projects/agraphia/`
- Live: https://hermes-playground-green.vercel.app/agraphia/
- Catalog: featured Agraphia #362; Gauntlet unfeatured
- Fonts: Crimson Pro + Outfit + JetBrains Mono
- Palette: chalk `#F4F1EA`, slate `#3A4F66`, ink `#14181F`, amber `#D4A04A`, steel `#6B8499`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
