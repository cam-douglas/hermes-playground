# 2026-09-09 02:50 Rubric

Two-hundred-thirtieth catalog product. Scriptorium / manuscript-rubricator bench — vermilion rubric numbers on cream parchment, oak desk, iron-gall ink, candle; fonts **Cardo** (display) + **Figtree** (UI) + **Source Code Pro** (mono) — for a Claude Code defect: **THE TUI SHOULD ECHO THE USER'S OWN ORDERED-LIST NUMBERS AS PENNED; INSTEAD, WHEN THOSE NUMBERS ARE NOT ALREADY SEQUENTIAL, EVERY ITEM AFTER THE FIRST IS REWRITTEN TO FIRST+1, FIRST+2, … SO TYPED `3. / 2. / 1. / 4. / 5.` DISPLAYS AS `3. / 4. / 5. / 6. / 7.` AND ANSWERS BIND TO THE WRONG QUESTIONS (REGRESSION 2.1.234).** Display-only on current versions: the model still receives the typed digits. Last working 2.1.233; measured on 2.1.263. Faceplate: as-penned vs rewritten. Score rewritten or admit as-penned. Idle word is **as-penned**. Path word is **rewritten**. Seeded misbind is **misbound**. Sheave remains in the catalog, unfeatured. Mailslot remains listed. Ukase remains listed. Scabbard remains listed. Deadletter remains listed.

Research brief ran in the 02:50 Australia/Sydney window on [anthropics/claude-code#92855](https://github.com/anthropics/claude-code/issues/92855) (OPEN, labels bug + has repro + platform:linux + area:tui + regression + area:ui). Not Espagnolette/#92694 (AskUserQuestion keys dead after blur). Not Quill/#92788 (AskUserQuestion free-text discard — do not ship Quill). Not Sheave/#92827 (queued `/Users` path as slash command). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle as-penned / path rewritten / misbound / start-honoured / sequential-ok / paren-renumbered / blank-line-renumbered / display-only / before-after). Shipped 02:50 Australia/Sydney (this loop).

Hours stem: `2026-09-09-0250-rubric`. Live path: `/rubric/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/rubric/`.

Next hour needs a different problem. Stay off Rubric / #92855 TUI ordered-list echo rewrite. Stay off Sheave / #92827 mid-turn `/Users/…` false slash hitch. Stay off Mailslot / #92839 Keychain argv-fallback pbt. Stay off Ukase / #92833 scheduled permission-rule deny. Stay off Scabbard / #92820 custom-subagent Bash silently omitted. Stay off Deadletter / #90049 PostToolUse worktree-transition missing `tool_result`. Stay off Espagnolette / #92694 AskUserQuestion keys dead after blur. Stay off Quill / #92788 AskUserQuestion free-text discard. Stay off all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle as-penned or path rewritten. Do not reuse idle paid.

## Thesis

**Rubric** — a scriptorium / manuscript-rubricator bench for Claude Code #92855: TUI echo should keep the user's own ordered-list numbers as penned; instead when those numbers are not already sequential the TUI rewrites every item after the first sequential from the first number so typed 3/2/1/4/5 displays as 3/4/5/6/7 and answers bind to the wrong questions. Regression in 2.1.234. Display-only: the model still receives the typed digits.

Idle score word: **as-penned**. Path word: **rewritten**. Seeded misbind: **misbound**.

Phrase: **a scriptorium rubricator bench that should keep the user's own ordered-list numbers as penned; instead the TUI renumbers every item after the first sequential from the first number so typed 3/2/1/4/5 displays as 3/4/5/6/7 and answers bind to the wrong questions; score rewritten or admit as-penned.**

## Sources

Primary:

- [anthropics/claude-code#92855](https://github.com/anthropics/claude-code/issues/92855) — OPEN. Title: `[BUG] TUI renumbers the user's own ordered list when echoing it — every item after the first loses its typed number (regression in 2.1.234)`. Labels: bug, has repro, platform:linux, area:tui, regression, area:ui.

Facts from the issue body only:

- Claude Code 2.1.263; last working 2.1.233; regression in 2.1.234; Ubuntu/Debian Linux; Xterm
- Non-sequential ordered lists are echoed with later items rewritten to first+1, first+2, …
- Typed `3. / 2. / 1. / 4. / 5.` displays as `3. / 4. / 5. / 6. / 7.`
- Display-only: `message.content` on v2.1.261 keeps the typed digits
- Lone `2.` and already-sequential `1. 2. 3.` are preserved
- `2. 5.` → `2. 3.`; `3. 2. 1.` → `3. 4. 5.`; same with blank lines; `3) 2) 1)` → `3) 4) 5)`
- 2.1.234 changelog: own prompts now render markdown the same way replies do
- User-message counterpart of open #87790
- Prior closed/locked: #51697, #55753, #61463, #64288, #69768, #75199

Hypothesis only (NON-BINDING): markdown ordered-list renumbering on the echo/render path. Invite verify against #92855 text only.

## Why Rubric / why not a clone

NOT Espagnolette/#92694 — AskUserQuestion keys dead after blur.
NOT Quill/#92788 — AskUserQuestion free-text discard; do not ship Quill.
NOT Sheave/#92827 — queued `/Users` path as slash command.
NOT Mailslot/#92839 — Keychain argv-fallback pbt after successful OAuth.
NOT Ukase/#92833 — scheduled Cowork permission-rule deny.
NOT Scabbard/#92820 — custom subagent Bash silently omitted.
NOT Deadletter/#90049 — PostToolUse lost tool_results.
NOT Dryjoint/#92809 — VS Code chat file links never call open_file.
NOT a Claude Code clone, not a live patch to anthropics/claude-code, not an exploit.

Rubric is not a deck sheave, postal mailslot, imperial wax-seal ukase, oak-armory scabbard, or Keychain vault. Slug `rubric` and name Rubric were unused among 229 products.

This is specifically: TUI ECHO REWRITES THE USER'S OWN ORDERED-LIST NUMBERS SEQUENTIAL FROM THE FIRST ITEM (REGRESSION 2.1.234).

Idle: **as-penned**. Path: **rewritten**. HOLD: **as-penned**. ALARM: **rewritten** / **misbound** / **paren-renumbered** / **blank-line-renumbered** / **display-only** / **before-after**.

## Shipped

- `projects/rubric/` — living page, fixtures, README
- `catalog.json` — Rubric featured #230; Sheave unfeatured; Mailslot/Ukase/Scabbard/Deadletter remain listed
- `vercel.json` — `/rubric` and `/rubric/` rewrites at TOP
- Hub cards read catalog.json — Rubric featured
- `runs/hours.json` stem `2026-09-09-0250-rubric`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Rubric/Sheave/Mailslot/Ukase/Scabbard/Deadletter/Espagnolette/Quill and all prior catalog slugs. Do not reuse idle as-penned or path rewritten. Do not reuse idle paid.
