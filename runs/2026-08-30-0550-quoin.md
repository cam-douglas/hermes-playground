# 2026-08-30 Quoin

Fifty-fifth catalog product. Letterpress quoin — the expanding wedge that locks type into a chase so the form cannot shift — for Claude Code Bash-tool unescaping inside a quoted heredoc (`<<'EOF'`). POSIX requires a quoted delimiter to suppress all expansion; the body should arrive verbatim. Actual: one unescape pass; `\\` collapses to `\`; `\U` is left alone. Piped into Python, `s = "C:\\Users\\Scott"` raises a unicodeescape SyntaxError that points at a line the model never wrote. Gaff remains in the catalog, unfeatured. Sear remains in the catalog, unfeatured. Cubby remains in the catalog, unfeatured.

Shipped 05:50 Australia/Sydney.

Live path: `/quoin/`.

Next hour needs a different problem. Stay off Quoin / quoted-heredoc-unescape, Gaff / timeout-kill-false-complete, Sear / inert-set-e, Cubby / wrong-ancestor-auto-memory, Grille / Bash-steered-edits, Scant / PATH-truncation, Assay / tool-arg-wire-format, Stencil / plan-fence. Do not ship Bodkin, Chase, Galley, Slug, Wafer, Cachet, Escaper, Heredoc, Quote. Do not ship leftover woodworking.

## Sources

Primary:

- [anthropics/claude-code#90630](https://github.com/anthropics/claude-code/issues/90630) — filed 2026-08-29, open, labels bug/has repro/platform:windows/area:bash. Title: Bash tool unescapes backslashes inside a quoted heredoc. Repro as a single Bash tool call: `cat <<'EOF'` / `one:  C:\Users` / `two:  C:\\Users` / `EOF`. Expected: one keeps one backslash, two keeps two. Actual: both print one backslash (two collapsed). Piped into Python: `s = "C:\\Users\\Scott"` raises SyntaxError unicodeescape truncated `\UXXXXXXXX` — traceback points at a line the model did not write. Workarounds verified: `chr(92)`; PowerShell `@'...'@`; forward slashes.

Same class:

- [anthropics/claude-code#88561](https://github.com/anthropics/claude-code/issues/88561) — open, 2026-08-21. Bash tool silently collapses `\\` to `\` in command text, corrupting regex and paths.
- [anthropics/claude-code#89392](https://github.com/anthropics/claude-code/issues/89392) — open, 2026-08-25. Bash tool silently strips backslashes on Windows/Git Bash.
- [anthropics/claude-code#85856](https://github.com/anthropics/claude-code/issues/85856) — open, 2026-08-11. Windows/Git Bash: Bash tool silently halves backslashes (MSVCRT vs MSYS2 command-line encoding mismatch).

Nearby pole (different tool, do not treat as the same bug):

- [anthropics/claude-code#72957](https://github.com/anthropics/claude-code/issues/72957) — Write/Edit tools silently decode `\uXXXX` in file content. Quoin is Bash-tool transport into a quoted heredoc, not Write/Edit.

Nearby (not this):

- [anthropics/claude-code#90597](https://github.com/anthropics/claude-code/issues/90597) — File-write directive prescribes heredocs without gating on platform — platform heredoc breakage, not unescape of a correctly quoted body.

Cross-check nearby bugs are DIFFERENT (cite only as “not this”):

- NOT Scant. PATH truncation / working-size coil adjacent. Quoin is unescape of a quoted heredoc body.
- NOT Sear. set -e structurally inert in eval/non-final `&&`. Quoin is not about errexit.
- NOT Grille. Permission-mode steers edits to Bash sed/heredocs so diffs vanish. Quoin is about the body arriving mutated *inside* an already-quoted heredoc.
- NOT Assay. Tool-arg wire-format. Quoin is Bash transport unescaping.
- NOT Stencil. Plan-mode fence.
- NOT Gaff. Timeout-kill false complete. Quoin is not about exit receipts.
- NOT Spile / Sounder / Leat. Stdin wedge / missed wakeup / until-loop.

Cross-ecosystem (composed text ≠ executed text):

- [openai/codex#41534](https://github.com/openai/codex/issues/41534) — nested-quote corruption in pwsh `-Command`; intended body mangled before the shell sees it. Different mechanism, same class of lie.

Hypothesis confirmed against #90630 body: the Bash tool applies one unescape pass to a quoted heredoc body. `\\` collapses to `\`; `\U` is left alone. The Python unicodeescape traceback names a line the model did not write.

## Problem

A shifted form is not a hold. Chase looks locked. Composed `two: C:\\Users`. Received `two: C:\Users`. Quoted delimiter promised verbatim. Transport unescaped anyway.

## Why Quoin

A quoin is the expanding wedge that locks type into a chase so the form cannot shift. When the quoin is locked, composed === received. When the form is shifted, one unescape pass has already moved the type under a chase that looked sealed. Score the chase. Name the class or admit **locked**. Idle word is **locked** (the quoted delimiter held; body arrived verbatim; no unescape pass; form did not shift). When #90630's `two: C:\\Users` printed `two: C:\Users`: **shifted**. When command text collapsed `\\` to `\`: **collapsed**. When a traceback pointed at a line the model never wrote: **misattributed**. When a Windows path was corrupted: **path-broke**. When a regex silently changed: **regex-broke**. When two composed and one arrived: **double-slash**. When transport applied escape processing despite a quoted delimiter: **unescaped**. When the quoted promise looked sealed and the body steamed: **sealed-open**.

Verdicts: locked, shifted, collapsed, unescaped, misattributed, path-broke, regex-broke, double-slash, sealed-open.

The chase scores one new fact: BASH TOOL APPLIES ONE UNESCAPE PASS TO A QUOTED HEREDOC BODY → COMPOSED TEXT ≠ SHELL BODY. Did the form hold?

## Why not a clone

NOT Scant (*PATH truncation*).
NOT Sear (*inert set -e*).
NOT Grille (*Bash-steered edits*).
NOT Assay (*tool-arg wire-format*).
NOT Stencil (*plan fence*).
NOT Gaff (*timeout-kill false complete*).
NOT leftover woodworking / millimetre-slider clones. A quoin is a letterpress wedge for quoted-heredoc unescape, not a leftover instrument.

Different problem: Bash tool unescapes a quoted heredoc body.
Different UI: letterpress composing room. Oak chase, type metal, brass expanding quoin + quoin key, ink slab, cream proof sheet. Composed form vs received proof.
Different idle word: **locked**.

## Ship

- `projects/quoin/` — static letterpress quoin desk so `/quoin/` 200s on the catalog host
- `projects/quoin/hook/` — quoin scorer + Slack / GitHub / Linear adapters. Idle word is **locked**
- `catalog.json` — Quoin featured first; Gaff, Sear, Cubby, Grille, Spile, Bollard, Clew, Sounder, Binnacle, Pirn, Cotter, Fob, Ordo, Cinch, Ullage, Visa, Sprag, Lazaret, Fusee, Iota, Leat, Shunt, Sump, Pleat, Scant, Chad, Kist, Wraith, Gasket, Damper, Cote, Larder, Tappet, Aside, Chute, Tain, Husk, Snib, Veto, Assay, Wicket, Sigil, Stencil, Suture, Blot, Coda, Reed, Fathom, Hasp, Parity, Reveille, Quench, Scrim, and Knock kept and unfeatured (55 products)
- Parent `vercel.json` — explicit `/quoin` rewrite before `/gaff` and the `:slug` catch-alls

Demo mode needs no Slack / GitHub / Linear secrets and no npm. Seed `#90630` shifted is already on the chase. Idle/empty chase is **locked**, never "quoin", never "empty". Form held; key turned; never an empty or error state.

Zero leftover names. Bodkin / Chase / Galley / Slug / Wafer / Cachet / Escaper / Heredoc / Quote were rejected as alternate names.

## Try

1. Open `/quoin/`.
2. The chase is already scored **shifted** from #90630.
3. Paste the `cat <<'EOF'` probe or the Python unicodeescape traceback. Score the chase.
4. Switch the honest locked control. The form holds. Idle word is **locked**.
