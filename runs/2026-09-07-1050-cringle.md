# 2026-09-07 10:50 Cringle

One-hundred-ninety-fourth catalog product. Sailmaker's loft / cringle-and-grommet splicing bench — weathered canvas ecru, navy rope, brass cringle rings, salt-white chalk, teak bench, indigo sailcloth; fonts **Alegreya** (display) + **Source Sans 3** (body) + **Fira Code** (data) via Google Fonts — for a Claude Code defect: **`permissions.deny` BASH RULES MATCH AGAINST THE PROGRAM NAME OF EACH SUB-COMMAND AFTER UNWRAPPING ONLY A FIXED EIGHT-ITEM LIST. ANY OTHER PATH WRAPPER THAT FORWARDS ARGS SHIFTS THE PROGRAM NAME SO THE DENY RULE NEVER SEES THE REAL COMMAND.** When matching is unwrap-aware and unmatched escalates (**sighted**), that is the hold path. Faceplate: slipped vs sighted. Score slipped or admit sighted. Idle word is **slipped**. Kerf remains in the catalog, unfeatured. Demurrage remains listed. Scarph remains listed. Plimsoll remains listed. Diopter remains listed. Decant remains listed. Catachresis remains listed. Bourdon remains listed. Glowplug remains listed. Hangfire remains listed. Thrash remains listed.

Research brief ran in the 10:50 Australia/Sydney window on [anthropics/claude-code#92542](https://github.com/anthropics/claude-code/issues/92542) (OPEN, labels bug + has repro + platform:linux + area:security + area:bash + area:permissions). Cousins cite-only: #49874 CLOSED (Bash(cmd *) allowlist silently bypassed for commands interpolating user env vars; uninformative denial — recommends wrappers), #31558 CLOSED (bypass deny via env variable prefix — issue is explicit this is NOT that case), #4956 CLOSED (Bash Permission Bypass via Command Chaining — issue is explicit this is NOT that case; compound splitting works). Same permissions/Bash matching neighbourhood; different defects. Do NOT treat Kerf/#92539 Remove-Item spaced-path cousins (#90645 #73524 #73882 #66549 #78513) as this product's cousins — that is a different paradigm (sandbox path guard). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle slipped / seeded sighted / eight-unwrap / wrapper-shift / compound-caught / path-wrapper-bypass / cousins). Shipped 10:50 Australia/Sydney (this loop).

Hours stem: `2026-09-07-1050-cringle`. Live path: `/cringle/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/cringle/`. Subdomain: `https://cringle.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Cringle / #92542 deny unwrap wrapper bypass. Stay off Kerf / #92539 Windows Remove-Item whole-command path rive. Stay off Demurrage / #92548 remote-daemon chat process overstay. Stay off Scarph / #92543 Windows `-c` transport ceiling + backslash halving. Stay off Plimsoll / #92434 stale auto-compact threshold on resume reinjection. Prefer fresh OPEN has-repro not catalogued. Backups: #92563 completed-background-task notification never triggers assistant turn (OPEN, has repro, area:core + area:agents + platform:wsl), or #92533 function-hook worktree isolation (OPEN, has repro, area:bash + area:hooks + area:agents + platform:macos) if still OPEN. Do not reuse idle slipped or seeded sighted. Do not reuse idle riven / argbound / accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated / voided / banked.

## Sources

Primary:

- [anthropics/claude-code#92542](https://github.com/anthropics/claude-code/issues/92542) — OPEN. Title: `[BUG] Bash deny rules bypassed by any wrapper program outside ax()'s 8-item unwrap list`. Labels: bug, has repro, platform:linux, area:security, area:bash, area:permissions. Filed 2026-09-06T19:37:44Z. Updated 2026-09-06T19:38:38Z. Reporter: ppravdin. Claude Code 2.1.258. Linux.

Facts from the issue only:

- `permissions.deny` Bash rules match against the program name of each sub-command in the parsed AST.
- Before matching, Claude Code unwraps a **fixed eight-item list** of wrapper programs (function `ax()` in `~/.local/share/claude/versions/2.1.258` per reporter):
  `timeout`, `time`, `nice`, `stdbuf`, `nohup`, `command`, `builtin`, `noglob`
  plus leading `VAR=value` assignments.
- No fallback; no attempt to resolve what an unknown wrapper actually executes.
- Any other wrapper binary/script on PATH that forwards args shifts the program name → deny/allow rules for the underlying program are silently skipped.
- NOT the env-var prefix case (#31558, closed) and NOT command-chaining (#4956).
- Compound splitting works: `cd <dir>; git revert ...`, `rm -f <path>; git add -A ...`, `git add -A && git status` were denied correctly in the same environment.
- Repro: `permissions.deny` includes `Bash(git add -A*)` → bare `git add -A` denied; same op preceded by any non-listed wrapper on PATH that forwards args → **runs** (deny never consulted).
- Real impact: `git add -A` denied six times over weeks, then ran four times behind a wrapper, committing 103 unintended files / 657,373 insertions. Denials: 2026-08-27T18:15:51Z, 2026-08-28T07:23:26Z, 07:42:37Z, 07:59:53Z, 09:35:12Z, 09:41:26Z. Bypasses: 2026-09-06T16:57:28Z, 17:12:26Z, 18:44:10Z, 19:01:59Z.
- Related: #49874 recommends putting commands behind a wrapper so allowlist matching stops seeing them — same mechanism that defeats deny.
- Suggested directions (cite as suggestion only, not implemented): (1) unknown wrapper → treat unmatched / escalate to prompt rather than fall through; (2) warn at config-load that Bash(...) rules bind literal program name; (3) check every token of a sub-command against deny rules; at minimum document the eight-item list.
- Environment: Claude Code 2.1.258, Linux. Permission prompts suppressed for the session so unmatched ran silently; bypass itself does not depend on that.

Cousins cite-only (NOT primary — same permissions/Bash matching neighbourhood, different defect):

- [#49874](https://github.com/anthropics/claude-code/issues/49874) — CLOSED. Bash(cmd *) allowlist silently bypassed for commands interpolating user env vars; uninformative denial (recommends wrappers).
- [#31558](https://github.com/anthropics/claude-code/issues/31558) — CLOSED. Bypass deny via env variable prefix. Issue is explicit this is NOT that case.
- [#4956](https://github.com/anthropics/claude-code/issues/4956) — CLOSED. Bash Permission Bypass via Command Chaining. Issue is explicit this is NOT that case; compound splitting works.

Do NOT treat Kerf/#92539 Remove-Item spaced-path cousins (#90645 #73524 #73882 #66549 #78513) as this product's cousins — that is a different paradigm (sandbox path guard).

## Why Cringle / why not a clone

NOT Kerf/#92539 — Windows Remove-Item whole-command path rive (sandbox path guard; DIFFERENT paradigm).
NOT Demurrage/#92548 — remote daemon chat process leak.
NOT Scarph/#92543 — Windows Bash -c ~8181 truncate + `\\` halving.
NOT Plimsoll/#92434 — stale auto-compact previous-turn tokens.
NOT Diopter/#92524, Decant/#92515, Catachresis/#92518, Bourdon/#92510, Glowplug/#85050, Hangfire/#92478, Thrash/#88257, or any prior catalog slug.
Do NOT name this after prior paradigm desks.
Do NOT reuse idle riven / argbound / accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated / voided / banked.
Do NOT reuse seeded argbound / cleared / fayed / trimmed / sharp / intact / scoped / lit / executed / responsive / excised / rewritten / vented.

Cringle is not a joiner's kerf-gauge bench, harbour laytime ledger, oak shipwright bench, or dry-dock Plimsoll board. Slug `cringle` and name Cringle were unused among 193 products.

This is specifically: Bash `permissions.deny` unwrap — fixed eight-item wrapper list; unknown PATH wrapper shifts the program name so deny/allow never see the real command.

Thesis: A cringle is the reinforced eye in a sail through which a rope is reeved. Claude Code's Bash `permissions.deny` matching unwraps only a fixed eight-item wrapper list before reading the program name. Any other PATH wrapper that execs its arguments shifts the program name so the deny rule never sees the real command — the rope slips past the eye. When matching is unwrap-aware and unmatched escalates (**sighted**), that is the hold path.

Idle: **slipped**. Seeded: **sighted**. HOLD: **sighted**. ALARM: **slipped** / **eight-unwrap** / **wrapper-shift** / **compound-caught** / **path-wrapper-bypass** / **cousins**.

Hypothesis only (NON-BINDING): `ax()` peels only the eight named wrappers and env-prefix tokens, then binds `Bash(...)` rules to the remaining leading program token; an unknown wrapper is treated as the program itself, so the deny pattern never sees the forwarded command. Encoded from the issue body only. Do not invent runtime source claims.

## Shipped

- `projects/cringle/` — living page, fixtures, README, hook
- `catalog.json` — Cringle featured #194; Kerf unfeatured
- `vercel.json` — `/cringle` and `/cringle/` rewrites
- Hub cards read catalog.json — Cringle featured
- `runs/hours.json` stem `2026-09-07-1050-cringle`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Backups: #92563 completed-background-task notification never triggers assistant turn if still OPEN has-repro; or #92533 function-hook worktree isolation if still OPEN. Stay off Cringle / Kerf / Demurrage / Scarph / Plimsoll and all prior slugs. Do not reuse idle slipped or seeded sighted. Do not reuse idle riven / argbound / accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated / voided / banked or seeded argbound / cleared / fayed / trimmed / sharp / intact / scoped / lit / executed / responsive / excised / rewritten / vented.
