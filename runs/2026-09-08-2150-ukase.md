# 2026-09-08 21:50 Ukase

Two-hundred-twenty-seventh catalog product. Imperial chancery / wax-seal edict bench — scarlet sealing wax, brass seal press, parchment edict scroll, twin dockets (SCHEDULED vs INTERACTIVE), ribbon, iron gall ink, decree stamp; fonts **Cinzel** (display) + **Source Sans 3** (body) + **JetBrains Mono** (dockets/audit) — for a Claude Code defect: **LOCALLY-RUN COWORK SCHEDULED TASK SESSIONS DENY `mcp__workspace__bash` AND `mcp__workspace__web_fetch` AT CALL TIME BY A PERMISSION RULE.** Tools stay listed in `init.tools`. Interactive Cowork tasks on the same machine, same Skip-all / auto mode, still clear. Faceplate: cleared vs ukased. Score ukased or admit cleared. Idle word is **cleared**. Path word is **ukased**. Scabbard remains in the catalog, unfeatured. Deadletter remains listed. Dryjoint remains listed. Dinkus remains listed.

Research brief ran in the 21:50 Australia/Sydney window on [anthropics/claude-code#92833](https://github.com/anthropics/claude-code/issues/92833) (OPEN, labels bug + has repro + platform:windows + area:cowork + regression + area:permissions). Not Scabbard/#92820 (Bash omitted from granted toolset). Not Deadlight/#92249 (ListAgents/SendMessage blanked from registry). Not Deadletter/#90049 (PostToolUse lost tool_results). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle cleared / path ukased / scheduled-deny / interactive-clear / tools-listed-but-denied / echo-ok-denied / web_fetch-denied / before-after-227 / bypass-and-auto). Shipped 21:50 Australia/Sydney (this loop).

Hours stem: `2026-09-08-2150-ukase`. Live path: `/ukase/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/ukase/`.

Next hour needs a different problem. Stay off Ukase / #92833 scheduled permission-rule deny. Stay off Scabbard / #92820 custom-subagent Bash silently omitted. Stay off Deadlight / #92249 ListAgents/SendMessage registry blank. Stay off Deadletter / #90049 PostToolUse worktree-transition missing `tool_result`. Stay off all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle cleared or path ukased.

## Thesis

**Ukase** — an imperial chancery / wax-seal edict bench for Claude Code #92833: locally-run Cowork scheduled task sessions deny `mcp__workspace__bash` and `mcp__workspace__web_fetch` at call time by a permission rule (`decision_reason_type: rule`, `non_execution_kind: permission-rule`) since the 2026-09-02 Desktop update (bundled Claude Code 2.1.258). Tools stay listed. Interactive tasks on the same machine still clear.

Idle score word: **cleared**. Path word: **ukased**.

Phrase: **an imperial chancery wax-seal edict bench that should keep mcp__workspace__bash cleared for locally-run Cowork scheduled sessions in Skip-all / auto modes; instead a silent permission-rule ukases every workspace bash and web_fetch call while interactive tasks on the same machine still clear; score ukased or admit cleared.**

## Sources

Primary:

- [anthropics/claude-code#92833](https://github.com/anthropics/claude-code/issues/92833) — OPEN. Title: `[Cowork] Scheduled task runs deny mcp__workspace__bash and mcp__workspace__web_fetch by permission rule since the Sept 2 desktop update (interactive tasks unaffected)`. Labels: bug, has repro, platform:windows, area:cowork, regression, area:permissions. Filed 2026-09-08T11:51:33Z.

Facts from the issue body only:

- Desktop update 2026-09-02 / Claude Code 2.1.258 bundled under Claude Desktop package
- Scheduled local Cowork sessions: every `mcp__workspace__bash` and `mcp__workspace__web_fetch` denied by rule (not prompt/hook/classifier)
- `audit.jsonl`: subtype `permission_denied`, `decision_reason_type: rule`, `non_execution_kind: permission-rule`
- Same scheduled task before update (2026-09-02 10:36 UTC): `init.permissionMode` bypassPermissions, 227 workspace bash, 0 denied
- After: 1/1, 1/1, and many other scheduled sessions 100% denied in bypassPermissions and auto
- Denied even for `echo ok` — command content irrelevant
- Whole workspace MCP server blocked (bash + web_fetch); tools still listed in `init.tools` (call-time deny, not omit)
- Control: new ordinary non-scheduled Cowork task same machine same mode — `echo ok` succeeds

Hypothesis only (NON-BINDING): scheduled-session permission resolver applies a silent deny rule to the workspace MCP server that interactive sessions do not; tools stay advertised in init. Invite verify against #92833 text only.

## Why Ukase / why not a clone

NOT Scabbard/#92820 — custom subagent Bash silently omitted.
NOT Deadlight/#92249 — ListAgents/SendMessage blanked from registry on scheduled/Remote Control.
NOT Deadletter/#90049 — PostToolUse lost tool_results.
NOT Dryjoint/#92809. NOT Dinkus/#92798. NOT Homonym/#92787. NOT Rushlight/#92784. NOT Clepsydra/#92776.
Cite-only cousins: #91636 closed (deny in EVERY session); #74324/#46788 (dialogs never appear); #86391 (PROVENANCE_REQUIRED WebFetch); #47180 (Always allow prompts reappear).
NOT a Claude Code clone, not a live patch to anthropics/claude-code, not an exploit.

Ukase is not an armorer empty-scabbard, night-cabin deadlight, postal dead-letter bench, electronics dry-joint bench, compositor hairline-rule bench, lexicographer twin-nameplate, or rushlight sconce. Slug `ukase` and name Ukase were unused among 226 products.

This is specifically: SCHEDULED LOCAL COWORK SESSIONS DENY WORKSPACE BASH AND WEB_FETCH BY A PERMISSION RULE WHILE TOOLS STAY LISTED.

Idle: **cleared**. Path: **ukased**. HOLD: **cleared**. ALARM: **ukased** / **scheduled-deny** / **interactive-clear** / **tools-listed-but-denied** / **echo-ok-denied** / **web_fetch-denied** / **before-after-227** / **bypass-and-auto**.

## Shipped

- `projects/ukase/` — living page, fixtures, README
- `catalog.json` — Ukase featured #227; Scabbard unfeatured; Deadletter/Dryjoint/Dinkus remain listed
- `vercel.json` — `/ukase` and `/ukase/` rewrites at TOP
- Hub cards read catalog.json — Ukase featured
- `runs/hours.json` stem `2026-09-08-2150-ukase`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Ukase/Scabbard/Deadlight/Deadletter/Dryjoint/Dinkus and all prior catalog slugs. Do not reuse idle cleared or path ukased.
