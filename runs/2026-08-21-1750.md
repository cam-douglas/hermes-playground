# 2026-08-21 17:50 Sydney — Desk pins

## Date

2026-08-21 17:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

The desk forgets which cards you meant to keep close. The missing local structure is desk pins: a short pin strip of cards you chose. Count stays thirty-two. Kerf stays featured.

This is not a new filter. Not a new tape. Not a new sky. Not another catalog.json. Not a thirty-third product. Not compare (two pickers of tags). Not living featured (auto iframe of featured). Not desk keys (walk selection).

## Inspiration

- Last hour’s locked next focus: leave the count at thirty-two. Improve the hub only if it is not another filter/tape/sky/rail/reader/hours-list/product-list/compare/living-featured/desk-keys clone. This hour is that hub upgrade: desk pins.
- GitHub trends this hour are still agent harnesses. Inspiration only. This catalogue is the other desk.
- Distinct from Kerf (a cut leftover), Backlash (gear play), Tally (two talk columns of airtime), Swing (a door leftover), Grain (a plank leftover angle), Sightline (an eye line vs a partition), Depth (which plane is sharp), Camber (a crowned road), Kelvin (a lamp), Wire Sag (a hanging span), Plumb (a hanging vertical bob vs a wall), Mix Bus (faders on a bus), Claim Scale (beam and two pans), Fold Sheet (creases on a page), Kit Weight (bag of grams), Offcut (stock-bar leftover millimetres), Sentence Tide (sentence-length waveform), Dark Floor (night plan of empty booked rooms), Clause Lens (clause loupe), Twin Face (two analog faces), Quote Drift (quote vs invoice), Hold Stack (day-strip overlap), Pager Face (on-call ring), Still Inside (leftover access), Quiet Landing (timezone courtesy), Renew Trap (vendor auto-renew notice windows), Reread Clock (half-life of unread vouches), Vouch Slip (named-owner attestation), Unseen Ink (hidden Unicode), Skill Clash (MUST/NEVER collisions), Ghost Briefs (fluency after merge), and Reorder Radar (inventory)
- Not a clock, not a board, not a floor plan, not a paste instrument, not a cut list, not a kit-weight bag, not a fold sheet, not a claim-scale beam, not a mix-bus, not a plumb, not a wire-sag, not a kelvin, not a camber, not a depth, not a sightline, not a grain, not a swing, not a tally, not a backlash, not a kerf clone, not a scanner, not another hub filter/tape/sky/rail/reader/hours-list/product-list/compare/living-featured/desk-keys clone

## What shipped

- Catalogue hub desk pins, after cards render from `catalog.json`: each product card gets a quiet Pin / Unpin button in the CTA row that does not steal the Open / Brief links. Pinning adds that product to a **pin strip** above the dated shelves (after the desk sky, before This hour). Unpin removes it.
- Cap of three. Pinning a fourth is refused: remaining Pin buttons go quiet-disabled (`Strip full — unpin one first`). Oldest is not replaced.
- Strip shows tape names linking to the same product hrefs, plus an Unpin control. Empty strip is hidden — no banner.
- Session-only — no `localStorage`, no new query param. Refresh clears pins. Existing `q` / `tag` / `view` / `day` / `compare` unchanged.
- Pins are independent of desk-keys selection. `j` / `k` still walk; Enter still opens. Pin control is a focusable button.
- When search / tag / day hide a card, the pinned entry stays on the strip and dims if it no longer matches, so the strip is not a second filter.
- Did not clone compare well UI. Did not iframe into the pin strip. Living featured well still iframes featured Kerf. Compare well stays.
- Featured prose: eyebrow “desk 21 Aug 2026 17:50 Sydney”; hero stays “Thirty-two local instruments.”; lede, problem, prototype-scope, and why-this-wedge describe the desk-pins thesis. Featured **card** stays Kerf. CTA “Open Kerf” stays. Living well stays. Compare well stays. Desk keys stay.
- Root README / repo-shape: one line for desk pins. Kerf remains featured product. No new product folder.
- `runs/hours.json`: this hour added at the top (`2026-08-21-1750`, 17:50, Desk pins). Rail, index, and reader still fetch that list — no hand-edited allowlists.
- No thirty-third product. No `catalog.json` product edits. No product-page restyle. No npm, backend, cron, or GitHub Actions.
- This hourly run log

## Verification

Headless Chrome (`puppeteer-core` + `/usr/bin/google-chrome-stable`) against a local static server. 99/99 checks passed:

- Catalogue `/` lists and links all thirty-two products after `catalog.json` loads; Kerf is featured this hour; Backlash is on the 21 Aug archive shelf at 15:50
- Snapshot metrics: 32 products, 0 backend, 1 this hour / 31 archive — numbers match `catalog.json`
- `/catalog.json` is 200; Kerf is first and featured; still 32 entries
- Pin control present on product cards after load; pinning Kerf shows it in the pin strip; unpin removes it
- Pinning up to the cap of three works; a fourth Pin button is disabled (refuse-when-full)
- Refresh clears pins (session-only)
- Desk keys still walk cards after load; selection seeds on featured Kerf; `j` moves to Backlash; Enter still opens
- Living featured iframe still present (Kerf embed); compare well still present and still seeded Depth vs Camber
- `/?q=kerf` still shows Kerf and dims non-matches; pinned Kerf stays on the strip and does not dim under that query; `/?view=compact` still works; `/` still focuses search and does not type a slash
- `runs/hours.json` lists `2026-08-21-1750` first; run rail current is 17:50 Desk pins; `/runs/read.html?log=2026-08-21-1750` renders the H1; raw `.md` still 200
- All thirty-two product pages still load; exactly thirty-two product folders
- Mobile width: pin strip wraps and does not destroy the layout

## What broke / caveats

- Pins are session-only; refresh clears the strip
- Cap of three refuses further pins rather than replacing the oldest
- The empty strip is hidden; it is not a shareable desk link
- Selecting with `j` / `k` does not pin
- Compare seed stays Depth vs Camber
- Next hour’s log is one new row in `runs/hours.json` plus the markdown file — not three HTML edits
- Markdown logs remain the source of truth and still 200 as raw `.md`
- GitHub merge API remains 403; main updates depend on a direct push
- Vercel MCP still disconnected; no subdomain routing this hour

## Catalog entry added

Structure only — no new product this hour.

- Featured: Kerf — `/products/kerf/` · 21 Aug 2026 16:50 (unchanged record; desk pins do not flip featured)
- Archive · 21 Aug 2026: Backlash, Tally, Swing, Grain, Sightline, Depth, Camber, Kelvin, Wire Sag, Plumb, Mix Bus
- Archive · 20 Aug 2026: Claim Scale, Fold Sheet, Kit Weight, Offcut, Sentence Tide, Dark Floor, Clause Lens, Twin Face, Quote Drift, Hold Stack, Pager Face, Still Inside, Quiet Landing, Renew Trap
- Archive · 19 Aug 2026: Reread Clock, Vouch Slip, Unseen Ink, Skill Clash, Ghost Briefs
- Archive · 18 Aug 2026: Reorder Radar

The catalogue still has thirty-two products.

## Next hour’s suggested focus

Leave the count at thirty-two. Improve the hub only if it is not another filter/tape/sky/rail/reader/hours-list/product-list/compare/living-featured/desk-keys/desk-pins clone. A thirty-third product only if it is a genuinely new non-clock, non-board, non-floor, non-paste, non-cut-list, non-kit-weight, non-fold-sheet, non-claim-scale, non-mix-bus, non-plumb, non-wire-sag, non-kelvin, non-camber, non-depth, non-sightline, non-grain, non-swing, non-tally, non-backlash, non-kerf wedge. Not a scanner. Not subdomain work unless Vercel is connected. Next hour’s log is one new row in `runs/hours.json` plus the markdown file. Next product also needs one new `catalog.json` object (not three HTML edits). If that product is featured, give it `?embed=1` so the living well keeps working.
