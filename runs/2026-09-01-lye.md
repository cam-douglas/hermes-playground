# 2026-09-01 Lye

Ninety-ninth catalog product. Fuller's / alkali wash house — wet stone, pewter, soft ash-green, soap foam, iron vat, hanging hanks of yarn, steam haze; Libre Baskerville + Source Sans 3 + IBM Plex Mono — for a Claude Code defect: with `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1`, version 2.1.251 removes `CLAUDE_CONFIG_DIR` from the environment of subprocesses Claude Code spawns (verified for hooks and the Bash tool). The parent still writes its own state into the relocated `CLAUDE_CONFIG_DIR`, so the session uses that directory while processes it starts resolve `~/.claude`. 2.1.250 passes the variable through with scrub enabled. Repro uses `/tmp/scrubprobe` with a SessionStart hook dumping env; `grep '^CLAUDE_CONFIG_DIR='` is 1 on 2.1.250 and 0 on 2.1.251; `ls` of the relocated config still shows `.claude.json`, `projects/`, `sessions/` on both. Bash tool same. Nothing printed about the removal; `--debug` log contains no message either. A fresh empty config directory can exit "Not logged in" after the hook has run. A scrub that strips the relocated address from every child while the parent still writes there is not a hold. Score the vat. Name the class or admit **rinsed**. Idle word is **rinsed**. When children lost `CLAUDE_CONFIG_DIR` while the parent still wrote the relocated vat: **scrubbed**. Advowson remains in the catalog, unfeatured. Smutch remains listed. Bitting remains listed. Puncheon remains listed.

Research brief ran in the 01:50 Australia/Sydney window after a GitHub pass on #91020 (OPEN, labels bug / has repro / platform:windows / area:security / area:bash / area:hooks / regression, created 2026-08-31T15:46:51Z). Facts encoded only from the issue. Same-class Pale #90683 / Pawl #90784 / Ambo #90685 / Chatelaine #90647 / Advowson #91005 cited, not primary — nearby hook surface gaps and a Workflow name silent built-in; different problems. Backups researched but not this ship: #91026 virtual-drive folder picker silent reject; #91028 MSIX identity sandbox RPC (too near Bulla); #91017 stale Desktop session index. No invented session IDs. Shipped 01:50 Australia/Sydney (this loop).

Live path: `/lye/`.

Next hour needs a different problem. Stay off Lye / #91020 CLAUDE_CONFIG_DIR scrub. Stay off Advowson / #91005 Workflow name silent built-in. Stay off Smutch / #90993 Icon\r folder stamp. Stay off Bitting / #90970 Slack MCP most-recent-mint exclusivity. Stay off Puncheon / #90962 BOM-less `.ps1`. Stay off Pale / #90683 silent-absent hooks. Stay off Pawl / #90784 sticky stop. Stay off Ambo / #90685 systemMessage non-render. Stay off Chatelaine / #90647 mcpOAuth Keychain. Stay off Bulla / MSIX. Stay off #91026 virtual-drive picker. Stay off #91028 MSIX identity sandbox RPC. Stay off #91017 stale Desktop session index.

## Sources

Primary:

- [anthropics/claude-code#91020](https://github.com/anthropics/claude-code/issues/91020) — filed 2026-08-31T15:46:51Z, OPEN. Title: 2.1.251 regression: CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1 strips CLAUDE_CONFIG_DIR from hooks and Bash subprocesses. Labels: bug / has repro / platform:windows / area:security / area:bash / area:hooks / regression.

Facts from the issue only:

- With `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1`, version 2.1.251 removes `CLAUDE_CONFIG_DIR` from the environment of subprocesses Claude Code spawns (verified for hooks and Bash tool).
- The parent still writes its own state into the relocated `CLAUDE_CONFIG_DIR`, so the session uses that directory while processes it starts resolve `~/.claude`.
- 2.1.250 passes the variable through with scrub enabled.
- Repro uses `/tmp/scrubprobe` with SessionStart hook dumping env; `grep '^CLAUDE_CONFIG_DIR='` is 1 on 2.1.250 and 0 on 2.1.251; `ls` of relocated config still shows `.claude.json`, `projects/`, `sessions/`, etc. on both.
- Bash tool same: `env | grep -c CLAUDE_CONFIG_DIR` prints 1 on 2.1.250 and 0 on 2.1.251 under scrub.
- Nothing printed about the removal; `--debug` log contains no message about it either.
- Fresh empty config directory can exit "Not logged in" after the hook has run (does not change the observation).
- Impact: deployment that relocates the config directory ends up with session and children disagreeing about where config lives (hooks/Bash blind to relocated dir).

Same-class (cite, not primary):

- Pale [#90683](https://github.com/anthropics/claude-code/issues/90683) — silent-absent hooks (different).
- Pawl [#90784](https://github.com/anthropics/claude-code/issues/90784) — sticky stop (different).
- Ambo [#90685](https://github.com/anthropics/claude-code/issues/90685) — systemMessage non-render (different).
- Chatelaine [#90647](https://github.com/anthropics/claude-code/issues/90647) — mcpOAuth nested in Keychain (different).
- Advowson [#91005](https://github.com/anthropics/claude-code/issues/91005) — Workflow name silent built-in (different).

Backups researched but not this ship: #91026 virtual-drive folder picker silent reject; #91028 MSIX identity sandbox RPC (too near Bulla); #91017 stale Desktop session index.

NOT the same class as Advowson (Workflow name silent built-in), Smutch (Icon\r folder stamp), Bitting (Slack MCP most-recent-mint exclusivity), Puncheon (BOM-less `.ps1`), Pale (silent-absent), Pawl (sticky stop), Ambo (systemMessage non-render), Chatelaine (mcpOAuth Keychain).

## Problem

ENV-SCRUB REGRESSION: `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1` ON 2.1.251 STRIPS `CLAUDE_CONFIG_DIR` FROM HOOKS AND BASH WHILE THE PARENT STILL WRITES THE RELOCATED VAT. 2.1.250 PASS-THROUGH. SILENT. UNLOGGED.

Hypothesis only (non-binding): treat this as an env-scrub regression in subprocess spawn paths; verify against issue text and discard if wrong.

## Why Lye

Lye is the alkali wash — fuller's vat, the scrub bath that strips too much from the yarn while the master bolt still holds the dye house address. Idle **rinsed** is scrub on with `CLAUDE_CONFIG_DIR` still reaching children (2.1.250 pass-through). Seeded **scrubbed** is the #91020 strip. A scrub that strips the relocated address from every child while the parent still writes there is not a hold. Score the vat or admit **rinsed**.

Verdicts: scrubbed, stripped, relocated-parent, default-home, hook-blind, bash-blind, silent-drop, regression-251, scrub-flag, config-dir-lie, dual-home, unlogged, pass-through-250, not-logged-in, rinsed.

## Why not a clone

Different problem: ENV-SCRUB REGRESSION + RELOCATED `CLAUDE_CONFIG_DIR` STRIPPED FROM CHILDREN + PARENT STILL WRITES THE VAT. Not Advowson Workflow name. Not Pale silent-absent. Not Pawl sticky stop. Not Ambo systemMessage. Not Chatelaine Keychain. Not Smutch Icon\r. Not Bitting MCP mint. Not Puncheon BOM.
Different UI: fuller's / alkali wash house. Wet stone, pewter, ash-green, soap foam, iron vat, hanging hanks, steam haze. Libre Baskerville + Source Sans 3 + IBM Plex Mono. Not Advowson Cormorant / Karla / Roboto parchment/violet. Not Smutch Fraunces / DM Sans blotter/brass. Not Bitting Libre Bodoni / Figtree / JetBrains felt-green. Not Puncheon Cinzel / Outfit / Spline walnut/gold/oxblood.
Different idle word: **rinsed**. Never idle as "scrubbed" / "stripped" / "lye" / "advowson" / "reserved" / "vacant" / "smutch" / "plain" / "seated" / "bound" / "hallmarked" / "pointed" / "collapsed" / "spoiled" / "banked".

NOT Advowson #91005.
NOT Smutch #90993.
NOT Bitting #90970.
NOT Puncheon #90962.
NOT Pale #90683.
NOT Pawl #90784.
NOT Ambo #90685.
NOT Chatelaine #90647.

## Ship notes

- `projects/lye/` static wash house + tiny hook + tests + ticket fixtures. Demo works offline, no secrets, no npm.
- Idle demo loads **rinsed**. Seed **scrubbed** / #91020 (`CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1` on 2.1.251 strips `CLAUDE_CONFIG_DIR` from hooks and Bash; parent still writes `/tmp/scrubprobe`; hook grep 0; Bash grep 0; nothing printed; `--debug` silent).
- Hook classifies a rinse vs scrub trace: rinsed vs scrubbed vs pass-through-250 (stripped / relocated-parent / default-home / hook-blind / bash-blind / silent-drop / regression-251 / scrub-flag / config-dir-lie / dual-home / unlogged / not-logged-in). `node --test projects/lye/hook/lye.test.mjs`.
- `catalog.json`: Lye featured first; 99 products; Advowson and all others `featured: false`.
- `vercel.json`: `/lye` and `/lye/` → `/projects/lye` at the top, before Advowson.
- Hub + root `index.html` featured copy swapped to Lye; Advowson listed.
- README featured + tree + hosting path.
- Native page tools: GitHub fetch of #91020, pin idle rinsed / pin seeded scrubbed, score the vat, admit rinsed, load fixtures, reset to rinsed, admit pass-through, vat/hank board, foam buoys, vat / hank / scrub-flag / dual-home plaques.
