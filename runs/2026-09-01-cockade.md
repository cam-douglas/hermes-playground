# 2026-09-01 Cockade

One-hundredth catalog product. Milliner / hatter's cockade bench — charcoal felt hat-blocks, crimson and navy silk ribbon reels, antique brass cockade pins, ivory brim tickets, warm walnut counters, pin cushions; Playfair Display + Nunito Sans + IBM Plex Mono — for a Claude Code defect: ultracode arms with no ultracode setting in any scope; the session header shows plain "with xhigh effort" while ultracode is active. Originally filed as a cosmetic badge bug; further checking inverted the diagnosis. Opening `/effort` shows the slider marker sitting ON the ultracode position ("xhigh + workflows") while the session header in the same frame reads "Fable 5 with xhigh effort". The badge was truthful; the header is the element mislabeling the state (ultracode reports its level as `xhigh` internally, per statusline docs). Two defects: (1) Ultracode is armed with no ultracode opt-in anywhere. (2) Header hides ultracode. Fresh Fable 5 session boots with `/effort` slider on ultracode despite no `ultracode` key in user/project/local/managed settings, no Fable 5 effort setting (only Opus 5 `xhigh`), `CLAUDE_CODE_EFFORT_LEVEL` unset, no `--effort`/`--settings` flags, and no effort/ultracode state in `~/.claude.json`. Docs: ultracode default is "unset, so ultracode is off"; Fable 5 default effort is `high`. With effort actually at ultracode, `/effort xhigh` is a silent no-op — no `modelSettings` entry is written. A cockade seated on ultracode with no opt-in, while the brim ticket still reads xhigh, is not a hold. Score the brim. Name the class or admit **unpinned**. Idle word is **unpinned**. When the slider sat on ultracode with no order in the book and the brim still read xhigh: **cocked**. Lye remains in the catalog, unfeatured. Advowson remains listed. Smutch remains listed. Bitting remains listed.

Research brief ran in the 02:50 Australia/Sydney window after a GitHub pass on #91033 (OPEN, labels bug / has repro / platform:windows / area:tui / area:statusline, created 2026-08-31T16:46:22Z by kenflorian). Facts encoded only from the issue and the kenflorian comment. No same-class cite required. No invented session IDs. Shipped 02:50 Australia/Sydney (this loop).

Live path: `/cockade/`.

Next hour needs a different problem. Stay off Cockade / #91033 ultracode silent arm. Stay off Lye / #91020 CLAUDE_CONFIG_DIR scrub. Stay off Advowson / #91005 Workflow name silent built-in. Stay off Smutch / #90993 Icon\r folder stamp. Stay off Bitting / #90970 Slack MCP most-recent-mint exclusivity. Stay off Puncheon / #90962 BOM-less `.ps1`. Stay off Gnomon / #90954 shared-mtime closed transcripts. Stay off Spoil / stale-`GIT_INDEX_FILE`. Stay off Pale / #90683 silent-absent hooks. Stay off Pawl / #90784 sticky stop. Stay off Ambo / #90685 systemMessage non-render. Stay off Chatelaine / #90647 mcpOAuth Keychain. Stay off Bulla / MSIX. Stay off Limpet / process leak. Stay off #91026 virtual-drive picker. Stay off #91028 MSIX RPC. Stay off #91017 stale Desktop session index. Stay off #91034 RC ccd-cli leak. Stay off #91031 connectors-until-/login.

## Sources

Primary:

- [anthropics/claude-code#91033](https://github.com/anthropics/claude-code/issues/91033) — filed 2026-08-31T16:46:22Z, OPEN. Title: Ultracode arms with no ultracode setting in any scope; header shows plain "with xhigh effort" while it is active. Labels: bug / has repro / platform:windows / area:tui / area:statusline. Filed by kenflorian. Claude Code v2.1.251, npm-global, Windows 11 Pro, VS Code terminal (Git Bash), Claude Max.

Facts from the issue only:

- Originally filed as a cosmetic badge bug; further checking inverted the diagnosis.
- Opening `/effort` shows the slider marker sitting ON the ultracode position ("xhigh + workflows") while the session header in the same frame reads "Fable 5 with xhigh effort".
- The badge was truthful; the HEADER is the element mislabeling the state (ultracode reports its level as `xhigh` internally, per statusline docs).
- Two defects: (1) Ultracode is armed with no ultracode opt-in anywhere. (2) Header hides ultracode.
- Fresh Fable 5 session boots with `/effort` slider on ultracode despite:
  - `~/.claude/settings.json`: no `ultracode` key; only `"modelSettings": { "claude-opus-5": { "effortLevel": "xhigh" } }` — no effort setting at all for Fable 5
  - Project `.claude/settings.json` / `.claude/settings.local.json`: no ultracode/effort keys
  - No managed settings file (`C:\ProgramData\ClaudeCode\` absent)
  - `CLAUDE_CODE_EFFORT_LEVEL` unset in process, user, and machine scope
  - No `--effort`/`--settings` launch flags (plain `claude`)
  - `~/.claude.json`: no effort-level or ultracode state (only UI flags like `effortCalloutDismissed`)
- Docs: ultracode default is "unset, so ultracode is off"; Fable 5 default effort is `high`.
- If Fable 5 intentionally defaults to ultracode in this build, that is undocumented; if not, it is arming spontaneously.
- An Opus 5 (1M) session on the same machine also showed the ultracode badge while its header read "with xhigh effort", with the only relevant config being the per-model `xhigh` entry — suggesting a saved plain `xhigh` may also be loaded/seated as ultracode.
- With effort actually at ultracode, `/effort xhigh` is a silent no-op — no `modelSettings` entry is written, presumably because ultracode reports its level as `xhigh` so the CLI sees no change to persist. Impossible to leave ultracode via `/effort xhigh`; only selecting a different level (or the slider) forces a write.
- Repro: v2.1.251, config as above; `claude` in a fresh terminal on a Fable 5 session; Header "Fable 5 with xhigh effort"; Open `/effort`: marker on ultracode ("xhigh + workflows"); Footer badge: "ultracode".
- Expected: ultracode off unless enabled via a documented route (or document it and show it in the header); header reflects ultracode when active; `/effort xhigh` persists per-model xhigh even when current state is ultracode.

Comment (kenflorian):

- Documented Fable 5 default effort is `high`, not ultracode.
- Fable 5 hold holds the *model default* (`high`).
- `ultracode` settings key default: unset, so ultracode is off.
- CHANGELOG has no entry about ultracode becoming a default.
- 2.1.243 fixed `/model` picker silently ignoring Ultracode selection.
- Header "with xhigh effort" while ultracode is active is consistent with statusline doc "Ultracode is not a distinct level and reports as `xhigh`" — the header renders the reported level, which hides the workflows flag.

Hypothesis only (NON-BINDING): treat this as an undocumented / spontaneous ultracode arm (possibly a saved plain `xhigh` seated as ultracode) plus a header that prints ultracode's internal reported level (`xhigh`) so `/effort xhigh` is a silent no-op. Do not claim a root cause in Claude Code source you have not seen.

NOT the same class as Lye (env-scrub), Advowson (Workflow name silent built-in), Smutch (Icon\r folder stamp), Bitting (Slack MCP most-recent-mint exclusivity), Puncheon (BOM-less `.ps1`), Gnomon (shared mtime), Spoil (stale private `GIT_INDEX_FILE`), Pale (silent-absent), Pawl (sticky stop), Ambo (systemMessage non-render), Chatelaine (mcpOAuth Keychain), Bulla (MSIX), Limpet (process leak).

## Problem

UNDOCUMENTED ULTRACODE ARM + HEADER HIDES ULTRACODE AS "xhigh effort" + `/effort xhigh` SILENT NO-OP. BADGE TRUTHFUL. NO OPT-IN IN ANY SETTINGS DRAWER. FABLE 5 DOCUMENTED DEFAULT IS `high`.

## Why Cockade

A cockade is the silk badge on a hat. The real effort badge sits on ultracode while the brim ticket still reads "xhigh effort", with no order in the book. Idle **unpinned** is ultracode off, header honest, slider on documented default `high`. Seeded **cocked** is the #91033 arm. A cocked cockade with no order in the book is not a hold. Score the brim or admit **unpinned**.

Verdicts: cocked, mislabeled, silent-noop, xhigh-mask, no-opt-in, slider-ultracode, header-lie, effort-xhigh, workflows-armed, settings-absent, env-unset, persist-miss, badge-true, fable-default, undocumented, unpinned, documented-default.

## Why not a clone

Different problem: UNDOCUMENTED ULTRACODE ARM + HEADER HIDES ULTRACODE AS "xhigh effort" + `/effort xhigh` SILENT NO-OP. Not Lye env-scrub. Not Advowson Workflow name. Not Smutch Icon\r. Not Bitting MCP mint. Not Puncheon BOM. Not process leaks. Not MSIX. Not virtual-drive picker. Not connectors-until-/login.
Different UI: milliner / hatter's shop. Charcoal felt, crimson silk, navy ribbon, antique brass, ivory card, warm walnut. Playfair Display + Nunito Sans + IBM Plex Mono. Not Lye Libre Baskerville / Source Sans 3 wet-stone/pewter/ash-green. Not Advowson Cormorant / Karla parchment/violet. Not Smutch Fraunces blotter/brass. Not Bitting Libre Bodoni felt-green. Not Puncheon Cinzel walnut/gold/oxblood.
Different idle word: **unpinned**. Never idle as cocked / armed / ultracode / cockade / rinsed / scrubbed / stripped / lye / vacant / reserved / advowson / smutch / plain / seated / bound.

NOT Lye #91020.
NOT Advowson #91005.
NOT Smutch #90993.
NOT Bitting #90970.
NOT Puncheon #90962.
NOT Gnomon #90954.
NOT Spoil #90943.
NOT Pale #90683.
NOT Pawl #90784.
NOT Ambo #90685.
NOT Chatelaine #90647.

## Ship notes

- `projects/cockade/` static milliner bench + tiny hook + tests + ticket fixtures. Demo works offline, no secrets, no npm.
- Idle demo loads **unpinned**. Seed **cocked** / #91033 (slider ultracode; header "Fable 5 with xhigh effort"; no ultracode key; `/effort xhigh` silent no-op; badge truthful).
- Hook classifies a brim/cockade trace: unpinned vs cocked vs documented-default (mislabeled / silent-noop / xhigh-mask / no-opt-in / slider-ultracode / header-lie / effort-xhigh / workflows-armed / settings-absent / env-unset / persist-miss / badge-true / fable-default / undocumented). `node --test projects/cockade/hook/cockade.test.mjs`.
- `catalog.json`: Cockade featured first; 100 products; Lye and all others `featured: false`.
- `vercel.json`: `/cockade` and `/cockade/` → `/projects/cockade` at the top, before Lye.
- Hub + root `index.html` featured copy swapped to Cockade; Lye listed.
- README featured + tree + hosting path.
- Native page tools: GitHub fetch of #91033, pin idle unpinned / pin seeded cocked, score the brim, admit unpinned, load fixtures, reset to unpinned, admit documented-default, hat-block / cockade / brim-ticket board, silk-reel chips, slider / header / settings-absent / silent-noop plaques.
