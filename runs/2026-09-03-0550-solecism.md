# 2026-09-03 05:50 Solecism

One-hundred-twenty-eighth catalog product. Grammar / usage desk / manuscript-margin atelier — flag-string vs resolved-path ledger, main-checkout pollution strip, literal-directory alarm lamp, parse score; ink / cream paper / margin red / graphite; Source Serif 4 + Work Sans + Inconsolata — for a Claude Code defect: **WORKTREE PROVISIONING WRITES THE GIT EXCLUDE ENTRY TO A LITERAL --git-common-dir/ DIRECTORY IN THE MAIN CHECKOUT INSTEAD OF .git/info/exclude; AREA:CORE; PLATFORM:MACOS.** A solecism that treats the flag string as the path is not a resolved exclude — it is a literal directory in the main checkout. Score the parse or admit the flag already landed. Idle word is **resolved**. When the intended `$(git rev-parse --git-common-dir)/info/exclude` never executes the rev-parse and the flag string itself becomes the path: **literal**. Coffer remains in the catalog, unfeatured. Codicil remains listed. Crimp remains listed. Jackfield remains listed. Tocsin remains listed. Bolter remains listed. Deadeye remains listed. Reglet remains listed.

Research brief ran in the 05:50 Australia/Sydney window on [anthropics/claude-code#91558](https://github.com/anthropics/claude-code/issues/91558) (OPEN, labels bug / platform:macos / area:core, created 2026-09-02T17:19:13Z, updated 2026-09-02T17:20:23Z). Facts encoded only from the issue and named cousins. Cousin cited, not a primary: #90456. No invented counts. No real credentials. Shipped 05:50 Australia/Sydney (this loop).

Hours stem: `2026-09-03-0550-solecism`. Live path: `/solecism/`.

Next hour needs a different problem. Stay off Solecism / #91558 literal --git-common-dir exclude. Stay off Coffer / #91571 Windows OAuth file-store refresh never persisted. Stay off Codicil / #91513 shared multi-agent worktree git commit --amend. Stay off Crimp / #91520 settings.json unlocked non-atomic RMW. Stay off Jackfield / #91511 desktop cross-machine session mix-up. Stay off Tocsin / #91503 idle-wake / background Bash. Stay off Bolter / #91422 dontAsk option-token matcher. Stay off Deadeye / #91226 hook path × cwd deadlock. Stay off Reglet / #91443 CRLF before .gitattributes.

## Sources

Primary:

- [anthropics/claude-code#91558](https://github.com/anthropics/claude-code/issues/91558) — filed 2026-09-02T17:19:13Z, OPEN. Title: Worktree provisioning writes the git exclude entry to a literal --git-common-dir/ directory in the MAIN checkout instead of .git/info/exclude. Labels: bug / platform:macos / area:core. Reporter karlgroves.

Facts from the issue only:

- Intended write to `$(git rev-parse --git-common-dir)/info/exclude` never executed the rev-parse; the flag string itself became the path → `./--git-common-dir/info/exclude` with contents `.claude/worktrees/`.
- `.git/info/exclude` still has no active entries.
- Pollutes the MAIN repository checkout (not the worktree).
- Recreated more than once (earlier ~16:50, observed 18:01:13 Sep 1 2026).
- Not created by any logged tool call — below Bash-tool layer; attribution to worktree provisioning is inference from content+timing+sibling #90456.
- Claude Code 2.0.42; macOS 26.5.2 Apple Silicon; zsh; git 2.50.1.
- Normal checkout (`.git` is a real directory; `rev-parse` returns `.git`); no LFS; linked worktrees at sibling `../.wt-<name>`; `.claude/worktrees/` empty.
- Impact low here because `.gitignore` already has `.claude/`; without that rule exclusion silently fails; recurring investigation tax; near-miss `git add` of `A --git-common-dir/info/exclude`.

Cousins (cite, not primaries):

- [#90456](https://github.com/anthropics/claude-code/issues/90456) — OPEN, bug, has-repro, platform:windows. Worktree provisioning writes Git LFS hooks to a literal `dev/null/` directory instead of `.git/hooks/` (same class: path literal used without resolution; pollutes the worktree, not main).

Product stays Solecism. Primary stays #91558.

Hypothesis only (NON-BINDING): execute `git rev-parse --git-common-dir` and use its OUTPUT; fail loudly rather than falling back to the flag string. Do not claim a root cause in Claude Code source you have not seen. Discard if issue evidence disagrees.

Backups (not shipped): Caret / #91526 Windows MCP password args via cmd.exe; Buoy / #91569 macOS Floating layer=3 after Computer Use; Prefix / #91581 CLAUDE_CODE_SHELL_PREFIX used as stdio MCP executable.

## Ship log

- `projects/solecism/` — self-contained usage desk: `index.html`, README, `.env.example`, `data/*.json`, `hook/solecism.mjs` + tests.
- `catalog.json` — Solecism prepended, featured true; Coffer featured false; prior products kept.
- `vercel.json` — `/solecism` and `/solecism/` rewrite to `/projects/solecism`.
- `runs/2026-09-03-0550-solecism.md` — this brief.

Idle demo: **resolved**. Seeded demo: **literal** / #91558. Phrase: Score the parse or admit the flag already landed. Mark: `05:50 / hermes catalog #128 / #91558`.
