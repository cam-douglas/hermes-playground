# 2026-09-07 13:50 Seizing

One-hundred-ninety-seventh catalog product. Sailmaker's / bosun's seizing bench — tarred hemp, spun yarn, wooden fid, copper nails, oak block, dark loft, salt air; fonts **Libre Caslon Text** (display) + **Sora** (body) + **Inconsolata** (code) via Google Fonts — for a Claude Code defect: **AN EDR'S TRANSIENT SECOND HARD LINK (`nlink` 1→2→1, SAME INODE) FALSE-TRIGGERS THE BASH OUTPUT-FILE IDENTITY CHECK, THEN SIGTERM→SIGKILL (~5s) WITH EXIT 137 AND "OUTPUT FILE WAS REPLACED OR COULD NO LONGER BE VERIFIED".** Short commands like `pwd` run and the output file has correct content, but read-back is reported failed. When nlink=1 identity holds and the command survives (**sole**), that is the hold path. Faceplate: culled vs sole. Score culled or admit sole. Idle word is **culled**. Seeded word is **sole**. Gland remains in the catalog, unfeatured. Larum remains listed. Cringle remains listed. Kerf remains listed. Demurrage remains listed. Scarph remains listed.

Research brief ran in the 13:50 Australia/Sydney window on [anthropics/claude-code#92586](https://github.com/anthropics/claude-code/issues/92586) (OPEN, labels bug + has repro + platform:macos + area:bash + area:sandbox). Cousins cite-only: #92590 (sandbox.enabled leaves $TMPDIR read-only when CLAUDE_CODE_TMPDIR is set — Linux bubblewrap; related TMPDIR surface, different failure). Not Scarph/#92543 (Windows Bash -c ~8181 truncate + backslash halving). Not Kerf/#92539 (Remove-Item spaced-path false positive). Not Cringle/#92542 (deny unwrap 8-item wrapper bypass). Not Gland/#92533 (Bash function-hook strips worktree isolation). Not Demurrage/#92548 (daemon chat process leak). Not Larum/#92563 (task-notification with no assistant turn). Not Oubliette/#92095. Not Holdfast/#92112. Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle culled / seeded sole / nlink-spike / sigkill-5s / ramdisk-ok / cousins). Shipped 13:50 Australia/Sydney (this loop).

Hours stem: `2026-09-07-1350-seizing`. Live path: `/seizing/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/seizing/`. Subdomain: `https://seizing.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Seizing / #92586 EDR-transient-hard-link-false-replacement. Stay off Gland / #92533 Bash-hook-strips-worktree-isolation. Stay off Larum / #92563 written-notice-with-no-turn. Stay off Cringle / #92542 deny unwrap wrapper bypass. Stay off Kerf / #92539 Windows Remove-Item whole-command path rive. Stay off Demurrage / #92548 remote-daemon chat process overstay. Stay off Scarph / #92543 Windows `-c` transport ceiling + backslash halving. Stay off Plimsoll / Diopter / Decant and all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Backups: #92573 worktree pool reuse data-loss, #92582 additionalDirectories ignored by blockReads, #92583 Windows orphan background Bash handle leak — if still OPEN. Do not reuse idle culled or seeded sole. Do not reuse idle stripped / packed / unanswered / roused / slipped / sighted / riven / argbound / accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented.

## Thesis

**Seizing** — a sailmaker's / bosun's seizing bench for Claude Code #92586: Every Bash tool call (foreground or background, sandboxed or `dangerouslyDisableSandbox`) dies ~5s after start with exit 137 because an EDR adds a transient second hard link (`nlink` 1→2→1, same inode/dev/size/owner/mode/content) and the output-file identity check treats that as a replaced file.

Idle score word: **culled**. Seeded/admit word: **sole**.

## Sources

Primary:

- [anthropics/claude-code#92586](https://github.com/anthropics/claude-code/issues/92586) — OPEN. Title: `Bash tool kills every command after ~5s ("output file was replaced or could no longer be verified") when an EDR transiently hard-links new files`. Labels: bug, has repro, platform:macos, area:bash, area:sandbox. Filed 2026-09-07T02:57:31Z. Updated 2026-09-07T03:33:08Z. Reporter: jskoo-dp. Claude Code 2.1.263 (also 2.1.260). macOS 26.3 Darwin 25.3.0 Apple Silicon. Genian Insights EDR (system extension).

Facts from the issue body only:

- Every Bash tool call (foreground/background, sandboxed or `dangerouslyDisableSandbox`) dies ~5s after start, exit 137.
- Error: `Command killed: its output file was replaced or could no longer be verified` plus bash output unavailable for `/private/tmp/claude-501/<project>/<session>/tasks/<id>.output` (unknown), blaming another Claude Code process startup cleanup.
- Short commands like `pwd` also fail: command runs and output file has correct content, but read-back reported failed. Read tool reads the same file fine. No other Claude Code process active.
- Root cause: EDR adds a second hard link to every newly created file on the boot volume for ~1–8s then removes it. `stat` shows `nlink=2` then `nlink=1`. Inode, size, owner, mode, content never change.
- Claude Code output-file identity check (since 2.1.251, "sandboxed command cannot redirect or replace them") treats this as replaced → SIGTERM then SIGKILL; trap recorded SIGTERM at exactly +5s from parent `claude`.
- Evidence: polling `stat -f %l` on new files in /tmp, $HOME, ~/.claude, 0700 dir → nlink 1→2→1 within ~8s. On separately mounted APFS RAM disk or sparse image, nlink stays 1.
- Headless repro `claude -p "run: echo hi && sleep 7 && echo alive" --allowedTools 'Bash(echo:*)'`:
  - default tmp: killed 137
  - `CLAUDE_CODE_TMPDIR=~/cctmp` on boot volume 0700: killed 137
  - `CLAUDE_CODE_TMPDIR` on APFS ramdisk or sparseimage volume: works
- Expected: transient extra hard link with unchanged dev/inode is NOT a replaced file; compare device+inode (and maybe owner/mode), or retry before killing. Error message is misleading.
- Workaround: point `CLAUDE_CODE_TMPDIR` at a directory on a separately mounted volume (settings.json `env` works).

Cousins cite-only (NOT primary):

- [#92590](https://github.com/anthropics/claude-code/issues/92590) — sandbox.enabled leaves $TMPDIR read-only when CLAUDE_CODE_TMPDIR is set (Linux bubblewrap; related TMPDIR surface, different failure).

Same-issue follow-up on the thread (not a cousin; not closed-source verification): a second reporter reproduced on Linux with a plain `ln` of the task `.output` file — nlink 1→2 on the same inode, then kill 137. Encoded as confirmation that a second hard link is enough. Do not treat bundle internals as verified facts.

## Why Seizing / why not a clone

NOT #92590 — sandbox.enabled leaves $TMPDIR read-only when CLAUDE_CODE_TMPDIR is set (Linux bubblewrap). Related TMPDIR surface, different failure. Cite only as a cousin.
NOT Scarph/#92543 — Windows Bash -c ~8181 truncate + backslash halving.
NOT Kerf/#92539 — Remove-Item spaced-path false positive.
NOT Cringle/#92542 — deny unwrap 8-item wrapper bypass.
NOT Gland/#92533 — Bash function-hook strips worktree isolation.
NOT Demurrage/#92548 — daemon chat process leak.
NOT Larum/#92563 — task-notification with no assistant turn.
NOT Oubliette/#92095 — cold parent voids child-completion queue.
NOT Holdfast/#92112 — mid-session `--worktree` cwd guard.
Do NOT reuse idle stripped / packed / unanswered / roused / slipped / sighted / riven / argbound / accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented.

Seizing is not a stuffing-box packing gland, sail cringle loft, kerf-gauge joiner, scarph shipwright, demurrage ledger, or watchtower larum. Slug `seizing` and name Seizing were unused among 196 products.

This is specifically: EDR transient hard-link (nlink>1, same inode) false-triggers Bash output-file replacement check → kill at ~5s.

Idle: **culled**. Seeded: **sole**. HOLD: **sole** / **ramdisk-ok**. ALARM: **culled** / **nlink-spike** / **sigkill-5s** / **cousins**.

Hypothesis only (NON-BINDING): the identity check may be using nlink or a fingerprint that breaks when an EDR adds a transient hard link. Encoded from the issue body only. Do not invent runtime source claims.

## Shipped

- `projects/seizing/` — living page, fixtures, README, hook
- `catalog.json` — Seizing featured #197; Gland unfeatured
- `vercel.json` — `/seizing` and `/seizing/` rewrites
- Hub cards read catalog.json — Seizing featured
- `runs/hours.json` stem `2026-09-07-1350-seizing`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Backups: #92573 worktree pool reuse data-loss, #92582 additionalDirectories ignored by blockReads, #92583 Windows orphan background Bash handle leak — if still OPEN. Stay off Seizing / Gland / Larum / Cringle / Kerf / Demurrage / Scarph / Plimsoll / Diopter / Decant and all prior slugs. Do not reuse idle culled or seeded sole. Do not reuse idle stripped / packed / unanswered / roused / slipped / sighted / riven / argbound / accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented.
