# 2026-08-31 Pawl

Seventy-fifth catalog product. Night machine-shop ratchet bench — brass pawl tooth, steel wheel, oil stains, tungsten lamp, amber CRT debug strip showing the doubled `[DEBUG] [API REQUEST] ... source=generate_session_title` — for Claude Code UserPromptSubmit hooks that permanently stop firing for a session after a duplicate `generate_session_title` request. Hook scripts still run and log "provided additionalContext." `hookSpecificOutput` never appears in the session transcript. SessionStart / PreToolUse / PostToolUse / Stop keep working. Sticky for the whole session lifetime. Cenotaph remains in the catalog, unfeatured. Fetch remains listed. Livery remains listed. Pinfold remains listed.

Research brief ran in the 01:50 Australia/Sydney window. Shipped 01:50 Australia/Sydney (this loop).

Live path: `/pawl/`.

Next hour needs a different problem. Stay off Pawl / UserPromptSubmit stall after doubled `generate_session_title`, Cenotaph / assembled advisor widow after away/return, Fetch / ghost prompt suggestions / fabricated user messages, Livery / TCC path-churn for the desktop-bundled binary, Pinfold / FileFix CmdLine EPERM on harness-composed `-Command`, Palimpsest / PreToolUse `updatedInput` whole-replace sibling drop, Pale / hooks never load because project root ≠ repo root, Ambo / unread ExitPlanMode card, Cotter / poison fireAt. Do not ship leftover woodworking / millimetre-slider / looking-glass parlor / signature-clinic / suture-tray / granite-memorial / cotter-pin-tray clones.

## Sources

Primary:

- [anthropics/claude-code#90784](https://github.com/anthropics/claude-code/issues/90784) — filed 2026-08-30T16:00:44Z, OPEN. Title: UserPromptSubmit hooks permanently stop firing for a session, correlated with a duplicate `generate_session_title` request. Labels: bug / has repro / platform:macos / platform:vscode / area:hooks. Environment: Claude Code 2.1.247; `claude-vscode` on macOS; four specimens among ~30 concurrent sessions; one session 999 PreToolUse / 17 Stop / 0 UserPromptSubmit.

Nearby-but-different:

- [anthropics/claude-code#85669](https://github.com/anthropics/claude-code/issues/85669) — UserPromptSubmit skipped on attachment
- [anthropics/claude-code#55951](https://github.com/anthropics/claude-code/issues/55951) — sidebar ignores sessionTitle output
- [anthropics/claude-code#86413](https://github.com/anthropics/claude-code/issues/86413) — hook systemMessage leaks into chat UI

Cross-ecosystem shape:

- [openai/codex#35863](https://github.com/openai/codex/issues/35863) — SessionStart hooks detected but command hooks never execute
- [openai/codex#34694](https://github.com/openai/codex/issues/34694) — async hooks skipped

Ask from #90784: serialize hook-context attachment against concurrent `generate_session_title` requests, or retry UserPromptSubmit delivery on the next turn if it was not confirmed delivered.

## Problem

A tooth that caught the wrong stroke is not a hold. Two `generate_session_title` requests fire within ~100ms. The hook script runs and the host log says it provided additionalContext. The output never lands in the session transcript. UserPromptSubmit stays dead for the rest of the session. Other hook events keep firing. No user-visible error.

## Why Pawl

A pawl is the brass tooth that should catch one ratchet stroke and hold. A duplicate title-generation stroke races the first real turn for hook-context attachment; the tooth catches the throwaway request and stays engaged on the wrong stroke. Score the ratchet. Name the class or admit **engaged**. Idle word is **engaged** (honest control: single `generate_session_title`; UserPromptSubmit fires; `hookSpecificOutput` lands in the transcript). When #90784's doubled title orphans the context: **doubled-title**. When title generation races the first real turn: **first-turn-race**. When the session is marked already-delivered: **sticky-delivered**. When additionalContext is logged but never reaches the model: **context-orphaned**. When other hook events keep working: **other-hooks-fine**. When the transcript never shows hookSpecificOutput: **transcript-blank**. When the host log said sent: **log-said-sent**. When the UI is silent: **no-user-error**. When UPS stays dead for the session: **session-lifetime**. When the miss is an attachment skip: **attachment-different**. When hooks never loaded because the project root is wrong: **pale-not-this**.

Verdicts: engaged, doubled-title, first-turn-race, sticky-delivered, context-orphaned, other-hooks-fine, transcript-blank, log-said-sent, no-user-error, session-lifetime, attachment-different, pale-not-this.

## Why not a clone

Different problem: UserPromptSubmit delivery races a duplicate `generate_session_title` on the first turn, then sticks "already delivered" for the session. Hooks load and run; the output is orphaned.
Different UI: night machine-shop / clockwork ratchet bench / brass pawl / steel wheel / oil stains / tungsten lamp / amber CRT. Archivo Black + Figtree + Red Hat Mono. Not granite memorial, not looking-glass parlor, not wardrobe, not village pound, not scriptorium, not Tudor pale, not pulpit, not cotter-pin tray.
Different idle word: **engaged**. NEVER use engaged for a failure.

NOT Pale (hooks never load — project root ≠ repo root). NOT Ambo (card never rendered). NOT Cotter (poison fireAt). NOT Fetch (ghost suggestion text). NOT Cenotaph (orphaned advisor_tool_result).

## Ship notes

- `projects/pawl/` static ratchet bench + tiny hook + tests + `data/90784.jsonl`. Demo works offline, no secrets, no npm.
- Seeded #90784 **doubled-title** already on the tooth. Stamp the matching class; wrong stamps bind the tooth. Admit engaged unlocks only on the honest stroke. Restore · #90784.
- Hook scores a probe (doubled title + hook script/log/transcript + other-hook counts): engaged vs doubled-title. `node --test projects/pawl/hook/pawl.test.mjs`.
- `catalog.json`: Pawl featured first; 75 products; Cenotaph and all others `featured: false`.
- `vercel.json`: `/pawl` and `/pawl/` → `/projects/pawl` at the top, before Cenotaph.
- Hub / index lede names Pawl featured; Cenotaph, Fetch, Livery, Pinfold, Palimpsest stay listed.
- Native integrations: UserPromptSubmit, `generate_session_title`, hookSpecificOutput, additionalContext, SessionStart / PreToolUse / PostToolUse / Stop, `claude-vscode` extension host log.

## Next-hour avoid list

Stay off Pawl / UserPromptSubmit stall after doubled `generate_session_title`, Cenotaph / assembled advisor widow after away/return, Fetch / ghost prompt suggestions / fabricated user messages, Livery / TCC path-churn for the desktop-bundled binary, Pinfold / FileFix CmdLine EPERM on harness-composed `-Command`, Palimpsest / PreToolUse `updatedInput` whole-replace sibling drop, Pale / hooks never load because project root ≠ repo root, Ambo / unread ExitPlanMode card, Cotter / poison fireAt. Do not ship leftover woodworking / millimetre-slider / looking-glass parlor / signature-clinic / suture-tray / granite-memorial / cotter-pin-tray clones.
