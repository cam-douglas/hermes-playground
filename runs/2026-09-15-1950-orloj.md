# 2026-09-15 19:50 — Orloj

Shipped catalog #379: Prague orloj / astronomical clock / zodiac dial / automaton tower / calendar dial booth for anthropics/claude-code#94393.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/94393 (OPEN; labels: bug, has repro, platform:linux, area:tools).

Title: `[BUG] Monitor tool: doesn't respect persistent flag and timeout_ms schema caps at 3600000 but actual lifetime is ~30 minutes, even during active (non-idle) sessions`.

The Monitor tool hard-rejects persistent and `timeout_ms` above 3600000 (1 hour). Calling with `86400000` (24h) fails immediately with `InputValidationError: timeout_ms must be <= 3600000`. At the allowed max 3600000 the confirmation says "expires in 30m unless the source ends first; you get one notice at expiry — re-arm if you still need the watch". Distinct from #63023 / #65968 (background tasks harvested on session pause/idle-suspend): the reporter's session never went idle — actively processing `task-notification` every ~30m from this monitor's own expiry for hours. Re-arming immediately after each expiry produces the same ~30-minute lifetime again. Docs: https://code.claude.com/docs/en/tools-reference#monitor-tool.

Cousins cite-only (do NOT rebuild / do NOT conflate): #63023 (idle/pause harvest of background tasks), #65968 (closed duplicate; idle/suspend boundaries).

Backups cite-only (next focus only — do not auto-pick): #94392 #86198 #94417 #94452 #94451 #94430 #94458 #93924 #93770 #93777 #94151.

Hypothesis (NON-BINDING): schema max / docs disagree with honored lifetime; silent ~30m cap during active use. Offered as a scoring narrative, not a source-root-cause claim.

Why this solution: Prague clock-tower booth scores lasting vs orloj after a half-life.

Why not a clone: paradigm and UI distinct from Brisure/Diptych/Vizard/Treacle/Somnus/Cresset/Dictabelt/Lemure/Cancellans/Arras and from #63023/#65968 idle/pause harvest. Educational diagnostic product, not a Claude Code fix.

## Ship notes

- Idle **lasting** / seeded **orloj** / path **half-life**
- Phrase: Score orloj or admit lasting.
- Folder: `projects/orloj/`
- Live: https://hermes-playground-green.vercel.app/orloj/
- Catalog: featured Orloj #379; Brisure unfeatured
- Fonts: Bodoni Moda + Karla + Source Code Pro
- Palette: night indigo `#0C1228`, gilt brass `#C9A24A`, clock-face cream `#F6EAD4`, rust automaton red `#A63A28`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
