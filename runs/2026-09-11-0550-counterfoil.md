# 2026-09-11 05:50 Counterfoil

Two-hundred-seventy-eighth catalog product. Cheque-counter / ticket-stub / banker's counterfoil booth — perforated stub, brass grille window, ink dating stamp, ledger blotter; fonts **Source Serif 4** (display) + **Libre Franklin** (body) + **Noto Sans Mono** (mono) — for a Claude Code defect: **ADD-JSON `--client-secret` STORES UNDER A HEADERS-STRIPPED KEYFOR WHILE LOGIN LOOKS UP A HEADERS-INCLUDED KEY.** Faceplate: matched vs skewed. Score counterfoil or admit matched. Idle word is **matched**. Seeded word is **skewed**. Path word is **headers-hash**. Lucida remains in the catalog, unfeatured. Fomite remains listed. Snubber remains listed. Fosse remains listed.

Research brief ran in the 05:50 Australia/Sydney window on [anthropics/claude-code#93446](https://github.com/anthropics/claude-code/issues/93446) (OPEN, labels bug + has repro + platform:linux + area:auth + area:mcp). Cousins cite-only: #67528 (closed stale, same defect), #89969, #84839 — do not rebuild. Not Cartulary/#93331 unbounded mcpOAuth accretion. Not Paraph/#93327 issuer %22. Backups cite in data only: #93458, #92264, #86531, #93403, #93445, #93405, #93402, #93426. Not leftover woodworking / mm-slider. Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only. Shipped 05:50 Australia/Sydney (this loop).

Hours stem: `2026-09-11-0550-counterfoil`. Live path: `/counterfoil/`. Intended live URL: `https://hermes-playground-green.vercel.app/counterfoil/`.

## Thesis

**Counterfoil** — cheque-counterfoil booth for Claude Code #93446: `claude mcp add-json <name> '<json>' --client-secret` for an HTTP MCP server whose JSON has a non-empty `headers` field stores the OAuth client secret under a credential-store key computed without the headers, while `claude mcp login <name>` looks the secret up under a key computed with the headers. Lookup misses → token exchange goes out with no `client_secret` → auth server rejects (GitHub remote MCP). Same defect as closed-stale #67528; still present in 2.1.267. Idle matched / seeded skewed / path headers-hash. Score counterfoil or admit matched.

Idle word: **matched**. Seeded word: **skewed**. Path word: **headers-hash**. Product score word: **counterfoil**.

Phrase: **when add-json --client-secret stores the secret under a headers-stripped keyFor while mcp login looks it up under a headers-included key, score counterfoil or admit matched.**

## Research brief

Sources:

- Primary: https://github.com/anthropics/claude-code/issues/93446
- Cite-only cousins (do not rebuild): #67528 (closed stale, same defect), #89969 (oauth block clientId cached unsubstituted), #84839 (Kaggle MCP token exchange missing client_secret). Nearby MCP OAuth key issues.
- Backups cite-only (do not auto-pick): #93458, #92264, #86531, #93403, #93445, #93405, #93402, #93426

Problem: add-json client-secret store key drops headers while login lookup includes them, so the secret is filed under a different serial than the cheque presented at the grille.
Why this solution: A cheque-counterfoil booth makes the store/lookup serial split tangible (matched vs skewed) without cloning monastic cartulary accretion or wax-seal issuer-paraph UIs.
Why not a clone: different defect, different UX metaphor, different idle/seeded/path vocabulary, different fonts/layout. Especially vs Cartulary #93331 unbounded mcpOAuth accretion and Paraph #93327 issuer %22.

## Published walk

idle matched (store + login use same keyFor with full config incl. headers) → add-json-persist → save-secret-stripped → store-key → mcp-login → lookup-key → lookup-miss → token-exchange → github-reject → skewed → headers-hash → counterfoil.

Hypothesis only (NON-BINDING): add-json persists the full config then calls saveMcpClientSecret with a rebuilt `{type,url}` that drops headers, while login hashes the full stored config, so keyFor diverges. Invite verify against #93446 text only. Do not claim Claude Code source root cause. Do not patch anthropics/claude-code.

## Files

- `projects/counterfoil/` — index.html, counterfoil.mjs, counterfoil.test.mjs, data/matched.json, data/skewed.json, data/headers-hash.json, data/counterfoil.json, data/cousins.json, data/backups.json, README.md
- `catalog.json` — Counterfoil featured #278; Lucida unfeatured
- `hub/catalog.json` — Counterfoil featured; others unfeatured
- `vercel.json` — `/counterfoil` and `/counterfoil/` → `/projects/counterfoil`
- Hub cards read catalog.json — Counterfoil featured
- `runs/hours.json` stem `2026-09-11-0550-counterfoil`
