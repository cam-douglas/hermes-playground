# 2026-09-06 01:50 Priory

One-hundred-sixty-first catalog product. Limestone priory / makepri scriptorium / AppX resource-index desk — cool stone, vellum, iron-gall ink, pale cloister light, package-tree and HRESULT panes; Alegreya + Nunito Sans + Source Code Pro — for a Claude Desktop defect: **STRAY `priconfig.xml` IN THE SHIPPED MSIX ROOT MAKES WINDOWS LOOK FOR RESOURCE PACKAGES THAT WERE NEVER PACKED.** A cloister that lets makepri's priconfig.xml walk into the shipped MSIX root is not an admin lockout — it is already leaked. Score the index or admit the resource packages were never packed. Idle word is **sealed**. When `priconfig.xml` sits in the package root and Windows rings for Language | Scale | DXFeatureLevel packages that do not exist: **leaked**. Latchkey remains in the catalog, unfeatured. Stubble remains listed. Intake remains listed. Pasteboard remains listed. Spillway remains listed.

Research brief ran in the 01:50 Australia/Sydney window on [anthropics/claude-code#92345](https://github.com/anthropics/claude-code/issues/92345) (OPEN, labels bug + has repro + platform:windows + area:cowork + area:installation + area:desktop). Facts encoded only from the issue body. No invented logs or payloads. No live AppX install. Diagnostic fixtures only (published HRESULTs, event ids, package identity). Shipped 01:50 Australia/Sydney (this loop).

Hours stem: `2026-09-06-0150-priory`. Live path: `/priory/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/priory/`. Subdomain: `https://priory.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Priory / #92345 MSIX `priconfig.xml` leak / AppX resource indexing. Stay off Latchkey / #92330 Remote Control OAuth startup-guard false `/login`. Stay off Stubble / #92328 Write `.cmd` UTF-8 LF + CP932 empty del. Stay off Intake / #92305 CLI piped-stdin double-composition. Stay off Pasteboard / #92312 platform-conditional `chat:imagePaste`. Stay off Spillway / #92311 ultracode early-return. Stay off Relict / #92173 versioned WindowsApps Run path.

## Sources

Primary:

- [anthropics/claude-code#92345](https://github.com/anthropics/claude-code/issues/92345) — OPEN. Title: `[BUG] Stray priconfig.xml in Desktop MSIX breaks installation with 0x80073CF9`. Labels: bug, has repro, platform:windows, area:cowork, area:installation, area:desktop. Filed 2026-09-05T15:41:52Z. Updated 2026-09-05T15:42:54Z. Reporter: byalvaroft. Comments: none at ship time.

Facts from the issue only:

- Windows 11 Pro, build 10.0.26100 (24H2), x64; system display language Spanish (es-ES); Claude Setup 1.46388.4.0 and 1.10628.2.0 (both fail identically). Sideloading and Developer Mode enabled, no MDM, no domain join. Fails since ~June 2026 on the reporter's machine.
- Installer UI: `Installation failed: AddPackage failed: AddPackage failed with HRESULT 0x80073CF9` then `Administrator access is required to install Claude with full features. You can try again or install without Cowork.`
- The second message is misleading. The installer's own log shows `Is elevated: true` and failed identically, in under a second.
- `0x80073CF9` is `ERROR_INSTALL_FAILED`, a generic wrapper. `Microsoft-Windows-AppXDeploymentServer/Operational` shows `0x80070490` ERROR_NOT_FOUND during Evaluated (events 605 / 401 / 404 / 613). Indexing 62 ms, Evaluation 47 ms, Hardlinking evaluation 47 ms, Stage required cost 0 ms.
- Process Monitor: no file or registry operation fails at the moment of the error — internal lookup, not a missing file on disk.
- Root cause: `priconfig.xml` (~1,480 bytes) sits in the package root and declares `<autoResourcePackage>` for Language, Scale, and DXFeatureLevel. That tells Windows resources live in separate resource packages. The MSIX is a single package, not a bundle, so they do not exist.
- Unverified sub-hypothesis: Spanish display vs package default `Language=en-US` may make the phantom lookup fatal. Reporter did not test a language switch. Cite as a guess.
- Isolation: automated delta-debug packed, signed, and installed subsets; isolated `priconfig.xml` out of 3,058 candidate files in 11 steps. Official package minus that one file installs. `CoworkVMService` Running, Automatic. `PackageFullName : Claude_1.46388.4.0_x64__pzs8sxrjxfjjc`.
- Ruled out by direct test: leftover `CoworkVMService`, orphaned StateRepository rows, stale PackageRepository extensions, missing VCLibs / WindowsAppRuntime / UI.Xaml, permissions/elevation, Anthropic signing chain, AppX volume health, SisPath, KnownFolder descriptors, catalog database, fresh Windows user (machine-level).
- Suggested fix in the issue (document only): exclude `priconfig.xml` from the packaging step. It is a `makepri` build input, not a runtime asset. A self-signed rebuild loses Anthropic auto-update and will reintroduce the file.

Cousins cite-only (NOT primary; found by brief MSIX/AppX search; not invented):

- [#88727](https://github.com/anthropics/claude-code/issues/88727) — MSIX install `0x80073CF6`
- [#89108](https://github.com/anthropics/claude-code/issues/89108) — leftover CoworkVMService `0x80073CF6` / `0x80073CF9` (ruled out on this machine)
- [#90411](https://github.com/anthropics/claude-code/issues/90411) — same false "Administrator access required (Cowork)" banner

## Why Priory / why not a clone

Latchkey = Remote Control OAuth startup-guard false `/login` while refreshToken is healthy. Priory = installer / MSIX packaging leak / AppX resource indexing. Completely different paradigm.
Stubble = Write UTF-8 LF `.cmd` + CP932 lead-byte → empty del / CWD wipe.
Intake = piped stdin double-composition toward the context limit.
Pasteboard = platform-conditional Alt+V image-paste miss.
Spillway = ultracode concurrency cap bypass.
Relict = leftover versioned WindowsApps Run path after MSIX update — same store, different surface.
Tabula = wax-tablet / blank-slate composer desk — not a limestone cloister HRESULT desk.

Priory is not an oak/brass latchkey board, stubble field, kraft pasteboard, dam spillway, water intake, Relict outcrop, or Tabula wax tablet. Slug `priory` and name Priory were unused.

Thesis: Priory is the stone scriptorium where `priconfig.xml` is the illuminator's recipe that must never leave the cloister into the shipped MSIX. When it leaks, Windows rings the resource-package bell for packages that were never packed — ERROR_NOT_FOUND — then the porter falsely blames the abbot (admin) and Cowork.

Idle: **sealed**. Seeded: **leaked**.

## Shipped

- `projects/priory/` — living page, hook stub (does not claim to ship in Claude Code), fixtures, README
- `catalog.json` — Priory featured #161; Latchkey unfeatured
- `vercel.json` — `/priory` and `/priory/` rewrites
- Hub lede — Featured: Priory. Latchkey stays listed. Stubble stays listed.
- Root README Featured block — Priory; Latchkey moved to Listed
- `runs/hours.json` stem `2026-09-06-0150-priory`

## Next focus

A different Claude Code problem. Do not clone Priory, Latchkey, Stubble, Intake, Pasteboard, Spillway, Relict, or Tabula.
