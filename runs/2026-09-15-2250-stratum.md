# 2026-09-15 22:50 — Stratum

Shipped catalog #382: geology / core-sample / bedding-plane / field-stratigraphy booth for anthropics/claude-code#94417.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/94417 (OPEN; labels: bug, has repro, area:core).

Title: `CLAUDE.md / auto-memory block is never shared across sessions: it sits in messages[0] after the system breakpoint, with no cache_control of its own`.

Claude Code 2.1.270 (also 2.1.266); Windows 11; Opus 5; subscription (1h TTL); both CLI and desktop. Docs describe project context as its own cached layer, shared by parallel sessions in the same directory. In practice the CLAUDE.md / auto-memory block is the first content block of `messages[0]`, the user's prompt is a later block of that same message, and the next `cache_control` marker is on a session-context message with a per-session UUID. No request writes a cache entry ending at the project bed. 703 session starts; 474 warm; zero hits beyond the system prompt layer.

Cousins cite-only (do NOT rebuild / do NOT conflate): #94400 (Cancellans — resume-fork tools-array drop), #93490 (Cachet — resume flatten), #93848 (Mojibake — encoding in CLAUDE.md), #91151 (resume cache collapses to system+tools floor). Docs claim parallel sessions share project context.

Backups cite-only (next focus only — do not auto-pick): #94452 #94451 #94430 #94458 #93924 #93770 #93777 #94151 #94496 #94499.

Hypothesis (NON-BINDING): project context is placed inside messages[0] after the system breakpoint without its own cache_control, so no cacheable prefix ends at that layer and parallel sessions never share it. Offered as a scoring narrative, not a source-root-cause claim.

Why this solution: core-sample / field-stratigraphy booth scores shared vs stratum after a layer-unsealed.

Why not a clone: paradigm and UI distinct from Tmesis/Vedette/Orloj/Brisure/Diptych/Vizard/Treacle/Somnus/Cresset/Dictabelt/Lemure/Cancellans/Arras/Stereotype/Cachet and from prior prompt-cache booths. Educational diagnostic product, not a Claude Code fix.

## Ship notes

- Idle **shared** / seeded **stratum** / path **layer-unsealed**
- Phrase: Score stratum or admit shared.
- Folder: `projects/stratum/`
- Live: https://hermes-playground-green.vercel.app/stratum/
- Catalog: featured Stratum #382; Tmesis unfeatured
- Fonts: Fraunces + Sora + IBM Plex Mono
- Palette: shale `#1E262C`, ochre `#C67B28`, slate `#4A5964`, core `#E4B25A`, clay `#D8C4A0`, rust `#8C3A16`, night `#101418`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
