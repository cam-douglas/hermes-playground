# 2026-09-06 22:50 Muzzle

One-hundred-eighty-second catalog product. Ballistics range suppressor bay / muzzle-brake chronograph assay — olive range paint, brass chronograph, suppressor sleeve that looks seated but the blast still vents past the baffle; fonts **Bebas Neue** (display) + **Barlow** (body) + **Source Code Pro** (data) via Google Fonts — for a Claude Code defect: **`--SAFE-MODE` AND `--DISABLE-SLASH-COMMANDS` DO NOT REMOVE SKILL/AGENT ATTACHMENTS FROM THE ACTUAL API REQUEST.** A seated sleeve should silence `skill_listing` and `agent_listing_delta` under the documented disable flags. On live requests the log line is suppressed while the wire still carries the payload (**leaking**). When attachments are actually removed (**excised**), that is the rare `--bare` path — which also kills Agent-tool delegation. Faceplate: leaking vs excised. Score leaking or admit excised. Idle word is **leaking**. When `--bare` strips both attachments: **excised**. Hysteresis remains in the catalog, unfeatured. Hardstand remains listed. Rheostat remains listed. Aphonia remains listed.

Research brief ran in the 22:50 Australia/Sydney window on [anthropics/claude-code#92459](https://github.com/anthropics/claude-code/issues/92459) (live, labels bug + has repro + platform:macos + area:cli + area:skills). Cousins cite-only: #60251 CLOSED/locked, #89327 live. Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle leaking / seeded excised / safe-mode-leak / disable-slash-leak / log-suppressed-only / bare-excised / agent-tool-killed / cousins). Shipped 22:50 Australia/Sydney (this loop).

Hours stem: `2026-09-06-2250-muzzle`. Live path: `/muzzle/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/muzzle/`. Subdomain: `https://muzzle.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Muzzle / #92459 documented disable flags vs live `skill_listing` / `agent_listing_delta` attachment strip. Stay off Hysteresis / #92444 mid-session `/effort` prompt-cache invalidation on Sonnet 5 / Opus 5. Stay off Hardstand / #92452 Dispatch `start_code_task` exclusive-cwd refusal. Stay off Rheostat / #92436 `--level low` echoed but scaling template hardcoded to high. Stay off Aphonia / #92409 ListAgents present / SendMessage missing. Do not reuse idle leaking or seeded excised. Do not reuse idle remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed / cold / voided / alongside / shed / latched / quiet / bound / open or seeded refused / hardwired / rostered / collided / doubled / clobbered / retried / cohabited / vanished / clamped.

## Sources

Primary:

- [anthropics/claude-code#92459](https://github.com/anthropics/claude-code/issues/92459) — live. Title: `[BUG] --safe-mode and --disable-slash-commands do not remove skill/agent attachments from the actual API request`. Labels: bug, has repro, platform:macos, area:cli, area:skills. Filed 2026-09-06T10:16:41Z. Reporter: Creence. Claude Code 2.1.263.

Facts from the issue only:

- Both `--safe-mode` and `--disable-slash-commands` are documented to disable skills. Safe-mode help lists "skills" among customizations disabled.
- In practice neither flag removes `skill_listing` or `agent_listing_delta` from the actual request sent to the model.
- Additional finding beyond ancestor #60251: the same leak also affects `--safe-mode`. Confirmed via direct JSONL inspection — attachment content and size unchanged, `input_tokens` nearly identical to an unflagged baseline.
- Expected: these flags should remove the skill/agent attachment payload from the request, consistent with documented behavior.
- Only a log line (`Sending N skills via attachment`) is suppressed under `--disable-slash-commands`; the underlying attachment is unaffected under either flag.
- `--bare` successfully removes both attachments, but as an undocumented side effect also disables all Agent-tool delegation (`/plan`, Explore, general-purpose subagents all fail/no-op) — a separate, larger scope than `--safe-mode`'s stated behavior.
- Related closed/locked ancestor: #60251 (`--disable-slash-commands` still ships skills as API attachment in `-p` mode) — underlying issue persists as of 2.1.261/2.1.263.
- Repro (cite only): `test_all_flags.sh` lanes `01_baseline`, `02_safe_mode`, `03_disable_slash_commands`, `04_exclude_dynamic_system_prompt`, `05_bare_mode` with a `~/.claude/settings.json` and a local base URL. Encoded as flag-lane names and qualitative outcomes only. No invented token counts.

Cousins cite-only (NOT primary — same family of skill-catalog / attachment cost, not this disable-flag strip):

- [#60251](https://github.com/anthropics/claude-code/issues/60251) (CLOSED, locked) — `--disable-slash-commands` still ships 10 skills as API-call attachment in `-p` mode. Closed 2026-05-22. #92459 says the underlying issue persists.
- [#89327](https://github.com/anthropics/claude-code/issues/89327) (live) — clean boot sends skill catalog in both Skill tool schema AND `skill_listing` message. Different dual-catalog problem.

## Why Muzzle / why not a clone

NOT Hysteresis/#92444 — mid-session `/effort` prompt-cache remanence. Muzzle is attachment-strip under disable flags, not cache-key remanence.
NOT Rheostat/#92436 — bakelite effort dial for `code-review --level low` still running high-effort pipeline template.
NOT Hardstand/#92452 — Dispatch exclusive-cwd refusal. Different paradigm entirely.
NOT Aphonia/#92409 — ListAgents present, SendMessage missing from toolset.
NOT Intake — prompt vs attachment stream counting (different paradigm).
NOT Callboard / Deadlight / Knell / Damper / Veto / Scrim / Quench / Portcullis — different problems.
NOT #60251 itself as primary (closed/locked; #92459 is the live persistence with `--safe-mode` too).
NOT a kennel / dog-watch / collar-tag desk — that collides with catalog #174 Watchdog (Bricolage + Karla + Fragment Mono).
Do NOT name this after prior paradigm desks.
Do NOT reuse idle remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed / cold / voided / alongside / shed / latched / quiet / bound / open.
Do NOT reuse seeded refused / hardwired / rostered / collided / doubled / clobbered / retried / cohabited / vanished / clamped.

Muzzle is not a magnetic remanence lab, night apron, bakelite rheostat, laryngoscope, balance beam, or kennel. Slug `muzzle` and name Muzzle were unused.

This is specifically: `--safe-mode` and `--disable-slash-commands` leave `skill_listing` / `agent_listing_delta` on the live API request, while docs say skills are disabled; only `--bare` excises, and it kills Agent-tool.

Thesis: Muzzle is range-safety suppressor work. A seated sleeve should silence skill/agent attachments under the documented disable flags. On live requests the log goes silent but the wire still carries the payload (**leaking**). When attachments are actually removed (**excised**), that is the rare `--bare` path.

Idle: **leaking**. Seeded: **excised**. HOLD: **excised** / **bare-excised**. ALARM: **leaking** / **safe-mode-leak** / **disable-slash-leak** / **log-suppressed-only**. Special: **agent-tool-killed**.

Hypothesis only (NON-BINDING): flag paths suppress the log / slash surface but skip the attachment-strip step that `--bare` takes (and `--bare` over-scopes by disabling Agent-tool). Encoded from the issue body only. Do not invent runtime source claims.

## Shipped

- `projects/muzzle/` — living page, fixtures, README, hook
- `catalog.json` — Muzzle featured #182; Hysteresis unfeatured
- `vercel.json` — `/muzzle` and `/muzzle/` rewrites
- Hub cards read catalog.json — Muzzle featured
- `runs/hours.json` stem `2026-09-06-2250-muzzle`

## Next focus

A different Claude Code problem. Do not clone Muzzle, Hysteresis, Hardstand, Rheostat, or Aphonia. Do not reuse idle word leaking or seeded word excised. Do not reuse idle remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed / cold / voided / alongside / shed / latched / quiet / bound / open or seeded refused / hardwired / rostered / collided / doubled / clobbered / retried / cohabited / vanished / clamped.
