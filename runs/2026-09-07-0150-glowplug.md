# 2026-09-07 01:50 Glowplug

One-hundred-eighty-fifth catalog product. Diesel glow-plug preheat bay — deep charcoal / soot, steel bulkheads, amber tip-glow on the plug, subtle heat-shimmer; fonts **Teko** (display) + **Outfit** (body) + **Share Tech Mono** (data) via Google Fonts — for a Claude Code defect: **WINDOWS: ~75S OF SILENT, UNLOGGED STARTUP PHASES ENDING AT `[skills] idle` / `[ScheduledTasks] scheduler start` — PERSISTS WITH SKILLS REMOVED, MCP DISABLED, NONESSENTIAL TRAFFIC OFF.** Before the engine can fire, the glow plug silently soaks heat with no telemetry. Claude Code on Windows sits in two unlogged warm-up voids (**preheating**). When the empty-config path is already 6s (**lit**), that is the hold path. Faceplate: preheating vs lit. Score preheating or admit lit. Idle word is **preheating**. Hangfire remains in the catalog, unfeatured. Thrash remains listed. Muzzle remains listed. Hysteresis remains listed. Hardstand remains listed. Rheostat remains listed.

Research brief ran in the 01:50 Australia/Sydney window on [anthropics/claude-code#85050](https://github.com/anthropics/claude-code/issues/85050) (OPEN, area:core). Cousins cite-only: #84478, #83988. Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle preheating / seeded lit / gap-skills-idle / gap-scheduler / skills-removed-persists / nonessential-traffic-noop / cert-store-bundled-noop / empty-config-fast / wall-times / cousins). Shipped 01:50 Australia/Sydney (this loop).

Hours stem: `2026-09-07-0150-glowplug`. Live path: `/glowplug/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/glowplug/`. Subdomain: `https://glowplug.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Glowplug / #85050 silent unlogged Windows startup gaps ending at `[skills] idle` / ScheduledTasks. Stay off Hangfire / #92478 queued `/compact` demoted to a plain prompt. Stay off Thrash / #88257 first-prompt event-loop stall + RSS balloon. Stay off Muzzle / #92459 documented disable flags vs live `skill_listing` / `agent_listing_delta` attachment strip. Stay off Hysteresis / #92444 mid-session `/effort` prompt-cache invalidation. Stay off Hardstand / #92452 Dispatch exclusive-cwd. Stay off Rheostat / #92436 `--level low` hardwired high. Do not reuse idle preheating or seeded lit. Do not reuse idle hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen.

## Sources

Primary:

- [anthropics/claude-code#85050](https://github.com/anthropics/claude-code/issues/85050) — OPEN. Title: `[BUG] Windows: ~75s of silent, unlogged startup phases (ending at '[skills] idle' / '[ScheduledTasks] scheduler start') - persists with skills removed, MCP disabled, nonessential traffic off`. Labels: area:core. Filed 2026-08-08T14:02:50Z. Reporter: arpitjaiswal-0701. Claude Code 2.1.224 (also 2.1.223).

Facts from the issue only:

- On Windows, every `claude` start (interactive, `-p`, and `--resume`) spends ~75 seconds in two completely silent startup phases — nothing written to `--debug` while they run.
- Trivial `claude -p --model haiku "Reply OK"` cold start is 111–136s with full config; same command with fresh empty `CLAUDE_CONFIG_DIR` in empty directory is **6s**.
- Two large debug gaps: ~59.7s between `Org fast mode: enabled` and `[skills] idle — switching poll interval to 30000ms`; then ~38.4s until `[ScheduledTasks] scheduler start() — enabled=false, hasTasks=false`. Same two-gap structure (~57–60s + ~17–38s) every run.
- Gaps persist with user skills directory renamed away (so first gap is not only skill scanning). After second gap, rest of startup completes in ~1s.
- Measurements (all `claude -p --model haiku --strict-mcp-config "Reply OK"`, PowerShell Stopwatch): full config 128–136s; pruned skills 111s; skills renamed away 85.5s; `--disable-slash-commands` 85.1s; `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1` 127.9s (no effect); `CLAUDE_CODE_CERT_STORE=bundled` 115.3s (no effect); empty config **6s**.
- ~25–45s scales with skill count, but the ~57s + ~17–38s silent phases persist with skills removed and are unexplained.
- Working-directory choice irrelevant. stdio MCP ruled out via `--strict-mcp-config`.
- Knock-on: `claude -p --resume` of 8.7 KB session ~130s; 44 MB session 5.4 minutes. Interactive resume looks hung; users abandon.
- Env: win32 Windows 11 Enterprise 10.0.26200 corporate proxy+VPN; native install; Windows Terminal / PowerShell 5.1.

Cousins cite-only (NOT primary — same-shape silent waits, not this Windows two-gap preheat):

- [#84478](https://github.com/anthropics/claude-code/issues/84478) — macOS: 45s+ silent startup in system CA trust-store lookup (same shape; bundled cert workaround does NOT help on this Windows issue).
- [#83988](https://github.com/anthropics/claude-code/issues/83988) — Desktop: window appears ~1s then blank up to 117s with no progress indication.

## Why Glowplug / why not a clone

NOT Thrash/#88257 — first-prompt TUI freeze / event-loop stall ~40808ms AFTER “Sending 12 skills via attachment”. Glowplug is PRE-prompt silent unlogged Windows startup gaps ending at skills idle / ScheduledTasks.
NOT Hangfire/#92478 — queued `/compact` demoted to plain prompt (`promptSource:queued`).
NOT Muzzle/#92459 — safe-mode skill_listing attachment leak.
NOT Hysteresis/#92444 — `/effort` cache remanence.
NOT Hardstand/#92452 — Dispatch exclusive-cwd.
NOT Rheostat/#92436 — `--level low` hardwired high.
NOT Aphonia/Fulcrum/Wildcat/Clobber/Watchdog/Understudy or any prior catalog slug.
Do NOT name this after prior paradigm desks.
Do NOT reuse idle hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen.
Do NOT reuse seeded executed / responsive / excised / rewritten / refused / hardwired.

Glowplug is not a delayed-primer chronograph, CRT paging-storm console, or olive suppressor bay. Slug `glowplug` and name Glowplug were unused.

This is specifically: Windows ~75s of silent, unlogged startup phases ending at `[skills] idle` / `[ScheduledTasks] scheduler start` — persists with skills removed, MCP disabled, nonessential traffic off.

Thesis: Glowplug is diesel glow-plug preheat work. Before the engine can fire, the plug silently soaks heat with no telemetry. Claude Code on Windows sits in two unlogged warm-up voids (**preheating**). When the empty-config path is already 6s (**lit**), that is the hold path.

Idle: **preheating**. Seeded: **lit**. HOLD: **lit**. ALARM: **preheating** / **gap-skills-idle** / **gap-scheduler** / **skills-removed-persists** / **nonessential-traffic-noop** / **cert-store-bundled-noop**. Control: **empty-config-fast**.

Hypothesis only (NON-BINDING): silent gaps may be network/proxy/VPN or config-hydration work that logs nothing; empty config bypasses them (6s). Encoded from the issue body only. Do not invent runtime source claims.

## Shipped

- `projects/glowplug/` — living page, fixtures, README, hook
- `catalog.json` — Glowplug featured #185; Hangfire unfeatured
- `vercel.json` — `/glowplug` and `/glowplug/` rewrites
- Hub cards read catalog.json — Glowplug featured
- `runs/hours.json` stem `2026-09-07-0150-glowplug`

## Next focus

A different Claude Code problem. Do not clone Glowplug, Hangfire, Thrash, Muzzle, Hysteresis, Hardstand, or Rheostat. Do not reuse idle word preheating or seeded word lit. Do not reuse idle hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen or seeded executed / responsive / excised / rewritten / refused / hardwired.
