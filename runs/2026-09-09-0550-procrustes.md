# 2026-09-09 05:50 Procrustes

Two-hundred-thirty-second catalog product. Blacksmith / innkeeper's iron bed bench — dark forge iron, linen sheets, rust-blood accents, brass measuring marks; fonts **Instrument Serif** (display) + **Source Sans 3** (UI) + **IBM Plex Mono** (mono) — for a Claude Code defect: **ONE LEGAL BOOLEAN PROPERTY SCHEMA ANYWHERE IN ONE TOOL'S `inputSchema` (E.G. `"payload": true`, JSON SCHEMA ANY-VALUE) CAUSES THE AGENT SDK CLI (0.1.77) CONVERTER TO SILENTLY CULL EVERY TOOL FROM A CONNECTED STDIO MCP SERVER — BISECT 0/53 VS 53/53 — WITH ZERO DIAGNOSTICS.** Faceplate: intact vs culled. Score culled or admit intact. Idle word is **intact**. Path word is **culled**. Seeded recovery is **relisted**. Cadastre remains in the catalog, unfeatured. Rubric remains listed. Sheave remains listed. Crenel remains listed. Clepsydra remains listed.

Research brief ran in the 05:50 Australia/Sydney window on [anthropics/claude-code#92900](https://github.com/anthropics/claude-code/issues/92900) (OPEN, labels bug + has repro + platform:linux + area:mcp + area:agent-sdk). Not Cadastre/#92908 (trust RMW lock — just shipped #231). Not Rubric/#92855 (TUI ordered-list renumber). Not Sheave/#92827 (queued path-as-slash). Not Crenel (MCP resources `{}`). Not Clepsydra/#92776 (OTel stall). Cousins cite-only: #88049 (HTTP non-object schema, OPEN) and #82949 (boolean named-property, CLOSED not_planned by bot — this re-files). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle intact / path culled / relisted / bisect / connected-silent / boolean-trigger / object-form-workaround / cousins / sdk-0-1-77 / before-after). Shipped 05:50 Australia/Sydney (this loop).

Hours stem: `2026-09-09-0550-procrustes`. Live path: `/procrustes/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/procrustes/`.

Next hour needs a different problem. Stay off Procrustes / #92900 stdio boolean-schema whole-server cull. Stay off Cadastre / #92908 whole-file RMW advisory-lock escheat. Stay off Rubric / #92855 TUI ordered-list echo rewrite. Stay off Sheave / #92827 mid-turn `/Users/…` false slash hitch. Stay off Crenel MCP resources `{}`. Stay off Clepsydra / #92776 OTel stall. Stay off all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle intact or path culled. Do not reuse idle enrolled. Do not reuse idle as-penned.

## Thesis

**Procrustes** — a blacksmith / innkeeper's iron bed bench for Claude Code #92900: an MCP server's full tool palette should stay intact while the server reports connected; instead one legal boolean property schema anywhere in one tool's `inputSchema` (e.g. `payload: true` meaning JSON Schema any-value) causes the Agent SDK CLI (0.1.77) converter to silently cull EVERY tool from that server (bisect 0/53 vs 53/53) with zero diagnostics — Procrustean fit: one bad measure, whole guest mutilated.

Idle score word: **intact**. Path word: **culled**. Seeded recovery: **relisted**.

Phrase: **a blacksmith / innkeeper's iron bed bench that should keep an MCP server's full tool palette intact while the server reports connected; instead one legal boolean property schema anywhere in one tool's inputSchema (e.g. payload: true meaning JSON Schema any-value) causes the Agent SDK CLI (0.1.77) converter to silently cull EVERY tool from that server (bisect 0/53 vs 53/53) with zero diagnostics — Procrustean fit: one bad measure, whole guest mutilated; score culled or admit intact.**

## Sources

Primary:

- [anthropics/claude-code#92900](https://github.com/anthropics/claude-code/issues/92900) — OPEN. Title: `MCP (stdio): one boolean property schema silently drops ALL tools of the server (SDK 0.1.77; stdio twin of #88049, re-filing of #82949)`. Labels: bug, has repro, platform:linux, area:mcp, area:agent-sdk.

Cite-only cousins:

- [anthropics/claude-code#88049](https://github.com/anthropics/claude-code/issues/88049) — OPEN. HTTP non-object schema, same blast radius, completely silent on both sides.
- [anthropics/claude-code#82949](https://github.com/anthropics/claude-code/issues/82949) — CLOSED `not_planned` by inactivity bot on 2026-09-08 with no fix landed. Boolean named-property throwing in `jsonSchemaToZodShape`. This issue re-files it.

Facts from the issue body only:

- SDK: `@anthropic-ai/claude-agent-sdk` 0.1.77 (declared `^0.1.0`, lockfile-resolved `0.1.77`) and bundled `cli.js`
- Runtime: Node.js 22.x via `tsx`; Linux x86_64
- Transport: stdio MCP server
- Trigger: one tool (`propose`) advertises `inputSchema.properties.payload = true` — legal JSON Schema 2020-12 §4.3.2 any-value
- Observed: 0 of the server's tools reach the API request; server status "connected"; no warning or error anywhere
- Expected: accept the boolean schema, or drop only the offending tool with a diagnostic
- Bisect: path without `payload: true` → 53/53; path with it → 0/53; object-form any-value → 53/53
- Two operations in an OpenAPI-enriched 53-tool bridge declared `"payload": true`
- Capture method: `ANTHROPIC_BASE_URL` pointed at a credential-free Anthropic-format stub; count `tools` in the recorded request body
- Impact: capability floor collapsed 11/11 → 1/6; looked like a model-behavior regression
- Workaround: `true` → `{ "description": "Any JSON value" }` (no `type`); lint properties-map values as objects
- Suggested fix: handle `typeof node === 'boolean'` before `'properties' in node`; per-tool `try/catch` in `createSdkServer`

Hypothesis only (NON-BINDING): `jsonSchemaToZodShape` throws on a boolean node and `createSdkServer` maps the whole tool list without per-tool isolation. Invite verify against #92900 text only.

## Why Procrustes / why not a clone

NOT Cadastre/#92908 — trust RMW lock; just shipped #231.
NOT Rubric/#92855 — TUI ordered-list renumber.
NOT Sheave/#92827 — queued `/Users` path as slash command.
NOT Crenel — MCP resources `{}`.
NOT Clepsydra/#92776 — OTel stall.
NOT Mailslot/#92839, Ukase/#92833, Scabbard/#92820, Deadletter/#90049, Dryjoint/#92809.
NOT a Claude Code clone, not a live patch to anthropics/claude-code, not an exploit.

Procrustes is not a cadastral survey desk, scriptorium rubricator, deck sheave, merlon, or water-clock. Slug `procrustes` and name Procrustes were unused among 231 products.

This is specifically: ONE LEGAL BOOLEAN PROPERTY SCHEMA ANYWHERE IN ONE TOOL'S `inputSchema` CAUSES THE AGENT SDK CLI (0.1.77) CONVERTER TO SILENTLY CULL EVERY TOOL FROM A CONNECTED STDIO MCP SERVER (BISECT 0/53 VS 53/53) WITH ZERO DIAGNOSTICS.

Idle: **intact**. Path: **culled**. HOLD: **intact**. ALARM: **culled** / **relisted** / **bisect** / **connected-silent** / **boolean-trigger** / **object-form-workaround** / **cousins** / **sdk-0-1-77** / **before-after**.

## Shipped

- `projects/procrustes/` — living page, fixtures, README
- `catalog.json` — Procrustes featured #232; Cadastre unfeatured; Rubric/Sheave remain listed
- `vercel.json` — `/procrustes` and `/procrustes/` rewrites at TOP
- Hub cards read catalog.json — Procrustes featured
- `runs/hours.json` stem `2026-09-09-0550-procrustes`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Procrustes/Cadastre/Rubric/Sheave/Crenel/Clepsydra and all prior catalog slugs. Do not reuse idle intact or path culled. Do not reuse idle enrolled. Do not reuse idle as-penned.
