# 2026-09-08 17:50 Dinkus

Two-hundred-twenty-third catalog product. Compositor's dinkus / hairline-rule bench — cool slate desk, zinc chase, vermilion dinkus mark, cream galley slip; fonts **Zilla Slab** (display) + **Atkinson Hyperlegible** (body) + **JetBrains Mono** (mono) — for a Claude Code defect: **PLUGIN SETTINGS FRONTMATTER PARSER TREATS EVERY `---` AS A SED RANGE DELIMITER SO BODY HORIZONTAL RULES LEAK INTO FRONTMATTER AND SILENTLY DISABLE PLUGINS.** When a hypothetical closed folio requires opening `---` on line 1 and quits at the first close, the chase is **closed**. Faceplate: bound vs leaked vs closed. Score leaked or admit closed. Idle word is **bound**. Path word is **leaked**. Homonym remains in the catalog, unfeatured. Rushlight remains listed. Clepsydra remains listed. Letoff remains listed.

Research brief ran in the 17:50 Australia/Sydney window on [anthropics/claude-code#92798](https://github.com/anthropics/claude-code/issues/92798) (OPEN, labels bug + has repro + area:plugins). Cousins cite-only: #52755 CLOSED (TUI `---` divider), #44901 CLOSED (plugin frontmatter docs), #19377 CLOSED (rules `paths:` frontmatter docs). Not Homonym/#92787 (Desktop UUID connector mounts). Not Clepsydra/#92776 (OTel mid-session meter). Not Rushlight/#92784 (session-scoped TCC). Not Letoff/#92771 (libuv Shift+Enter flatten). Not Espagnolette/#92694. Not Cribble/#92684. Not Springe/#92675. Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle bound / path leaked / sed-range-reopen / body-hr-bleed / enabled-true-false-concat / silent-disabled-path / false-as-missing / validate-hr-count / cousins / has-clear-repro). Shipped 17:50 Australia/Sydney (this loop).

Hours stem: `2026-09-08-1750-dinkus`. Live path: `/dinkus/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/dinkus/`.

Next hour needs a different problem. Stay off Dinkus / #92798 plugin-settings sed frontmatter range. Stay off Homonym / #92787 Desktop UUID mounts. Stay off Clepsydra / #92776 OTel token.usage mid-session arrest. Stay off Rushlight / #92784 session-scoped TCC AppData grant. Stay off Letoff / #92771 libuv Shift+Enter flatten. Stay off all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle bound or path leaked. Do not reuse idle matched / orphaned / keyed / dripping / arrested / credited / chorded / flattened / meshed / piped / swallowed / unbound / berthed / lean / attentive / waived / bricked / unrung / echoed / laden / deaf / shed / remounted / refused / imprinted / scored / voided / banked / rewritten / cold / seeded / drained / lit / snuffed.

## Thesis

**Dinkus** — a compositor's hairline-rule bench for Claude Code #92798: plugin-settings scripts extract frontmatter with `sed -n '/^---$/,/^---$/{ /^---$/d; p; }'`. A sed range reopens every time the start pattern matches again, so a body horizontal rule (the dinkus that should stay in the *body*) starts a second chase. The published repro returns `true\nfalse` for `enabled`; the help-style compare against `"true"` fails; the plugin takes the disabled path with nothing printed.

Idle score word: **bound**. Path word: **leaked**. Admit word: **closed**.

Phrase: **a compositor's hairline-rule bench that should keep plugin-settings frontmatter closed at the first ---; instead sed ranges reopen on body horizontal rules so enabled:false leaks and plugins take the silent disabled path; score leaked or admit closed.**

## Sources

Primary:

- [anthropics/claude-code#92798](https://github.com/anthropics/claude-code/issues/92798) — OPEN. Title: `[BUG] parse-frontmatter.sh returns body content as frontmatter when the markdown body contains ---`. Labels: bug, has repro, area:plugins. Filed 2026-09-08T07:25:28Z. Reporter: Sagexd08. Repo at `main` (ab9b2cf).

Facts from the issue body only:

- Scripts in `plugins/plugin-dev/skills/plugin-settings/scripts/` extract with `sed -n '/^---$/,/^---$/{ /^---$/d; p; }'`
- A sed range reopens every time the start pattern matches again
- Repro `.claude/my-plugin.local.md`: `enabled: true`, body `---`, docs `enabled: false` → `true\nfalse`
- Help-style `ENABLED=$(... enabled)` compare against `"true"` fails; silent disabled path
- `enabled: false` errors as field not found (`[ -z "$VALUE" ]`); dotted keys as regex; missing close runs to EOF
- validate-settings.sh Check 3 counts any `---` (2+ passes with no frontmatter); Check 7 warns boolean on leaked multi-line; Check 8 drops body rules from body line count
- Cited fix (do not implement in this catalog product): Sagexd08 `fix/parse-frontmatter-range` requires opening `---` on line 1 and quits at first close
- Hypothesis only (NON-BINDING): sed address ranges restart on every `/^---$/` match so body horizontal rules reopen frontmatter extraction. Invite verify against issue text only.

Cousins cite-only (NOT primary):

- [anthropics/claude-code#52755](https://github.com/anthropics/claude-code/issues/52755) — CLOSED. Markdown horizontal rule (`---`) renders like a user interruption divider.
- [anthropics/claude-code#44901](https://github.com/anthropics/claude-code/issues/44901) — CLOSED. Plugin output styles docs omit `keep-coding-instructions` frontmatter support.
- [anthropics/claude-code#19377](https://github.com/anthropics/claude-code/issues/19377) — CLOSED. `paths:` frontmatter syntax in rules files is incorrect.

Backups do not auto-pick: #92788 AskUserQuestion free-text discard, #92761 worktree plugin row order, #92801 workspace trust not persisted.

## Why Dinkus / why not a clone

NOT Homonym/#92787 — Desktop UUID connector mounts.
NOT Clepsydra/#92776 — OTel main-loop meter arrest.
NOT Rushlight/#92784 — session-scoped TCC AppData grant.
NOT Letoff/#92771 — libuv Shift+Enter flatten.
NOT Espagnolette/#92694. NOT Cribble/#92684. NOT Springe/#92675.
NOT a generic markdown linter, not a live Claude session, not an exploit.
Do NOT reuse idle matched / orphaned / keyed / dripping / arrested / credited / chorded / flattened / meshed / piped / swallowed / unbound / berthed / lean / attentive / waived / bricked / unrung / echoed / laden / deaf / shed / remounted / refused / imprinted / scored / voided / banked / rewritten / cold / seeded / drained.

Dinkus is not a lexicographer twin-nameplate, marble water-clock, iron sconce, piano let-off gauge, or locksmith casement. Slug `dinkus` and name Dinkus were unused among 222 products.

This is specifically: PLUGIN SETTINGS FRONTMATTER PARSER TREATS EVERY `---` AS A SED RANGE DELIMITER SO BODY HORIZONTAL RULES LEAK INTO FRONTMATTER AND SILENTLY DISABLE PLUGINS.

Idle: **bound**. Path: **leaked**. HOLD: **bound** / **closed**. ALARM: **leaked** / **sed-range-reopen** / **body-hr-bleed** / **enabled-true-false-concat** / **silent-disabled-path** / **false-as-missing** / **validate-hr-count** / **cousins** / **has-clear-repro**.

Hypothesis only (NON-BINDING): sed address ranges restart on every `/^---$/` match so body horizontal rules reopen frontmatter extraction. Encoded from the issue body only. Do not invent unread source claims. Invite verify.

## Shipped

- `projects/dinkus/` — living page, fixtures, README, hook
- `catalog.json` — Dinkus featured #223; Homonym unfeatured; Rushlight/Clepsydra/Letoff remain listed
- `vercel.json` — `/dinkus` and `/dinkus/` rewrites at TOP
- Hub cards read catalog.json — Dinkus featured
- `runs/hours.json` stem `2026-09-08-1750-dinkus`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Backups: #92788 AskUserQuestion free-text discard, #92761 worktree plugin row order, #92801 workspace trust not persisted. Stay off Dinkus/Homonym/Rushlight/Clepsydra/Letoff and all prior catalog slugs. Do not reuse idle bound or path leaked.
