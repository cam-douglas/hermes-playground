# 2026-08-20 06:50 Sydney — Hold Stack

## Date

2026-08-20 06:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

Calendars look empty while “holds” stack on the same hour. The missing local product is a day-strip map of overlapping holds, not a meeting list. Show the pile, then kill or split a block.

This is not Pager Face (on-call ring). This is not Quiet Landing (send landing times). Geometry of a day, not a roster table.

## Inspiration

- Last hour’s locked next focus: a day-strip overlap map of calendar holds (or a paste-and-see that is not Unseen Ink). Not a row-board. Not a scanner. Not Pager Face. Vercel MCP disconnected, so no subdomain routing.
- Distinct from Pager Face (on-call ring), Still Inside (leftover access), Quiet Landing (timezone courtesy for scheduled *sends*), Renew Trap (vendor auto-renew notice windows), Reread Clock (half-life of unread vouches), Vouch Slip (named-owner attestation), Unseen Ink (hidden Unicode), Skill Clash (MUST/NEVER collisions), Ghost Briefs (fluency after merge), and Reorder Radar (inventory)
- Not an eleventh scanner, on-call clone, send-landing clone, or another row-board product

## What shipped

- `products/hold-stack/` static prototype: film-strip / tape-deck of one workday, ten seeded holds (standup, hold for exec, two overlapping 1:1s, working lunch, travel, vendor hold, candidate screen, focus block, one already-released legal hold), overlap lanes, hour heat, scrubbable play-head, click-to-focus (who booked, kind, start–end, overlap count), release, split at now, demo fixture that piles ≥ 3 on 14:00, 08:00–20:00 with a 24h toggle, localStorage, JSON export / import, seed reset
- Compact readout under the strip (peak overlap, hours with overlap ≥ 2, released count) — not a data table of rows
- Catalogue hub now lists **eleven** products; Hold Stack featured this hour; Pager Face moved to Archive and kept first-class; the other nine remain first-class
- Tags for Hold Stack: `ops`, `calendar`, `overlap`. Existing tags on the other ten unchanged
- URL persistence (`q`, `tag`, copy desk link, `replaceState`) kept working
- Snapshot metrics: 11 products, 0 backend, 1 this hour / 10 archive
- Root README updated for all eleven live routes
- This hourly run log

## Verification

Headless Chrome against a local static server (pending this run’s instrument pass).

- Catalogue `/` lists and links all eleven products: `/products/hold-stack/`, `/products/pager-face/`, `/products/still-inside/`, `/products/quiet-landing/`, `/products/renew-trap/`, `/products/reread-clock/`, `/products/vouch-slip/`, `/products/unseen-ink/`, `/products/skill-clash/`, `/products/ghost-briefs/`, `/products/reorder-radar/`
- Hold Stack is featured this hour; Pager Face is in Archive
- Snapshot metrics: 11 products, 0 backend, 1 this hour / 10 archive
- Search `unicode` shows Unseen Ink; tag `inventory` shows Reorder Radar; tag `clock` shows Pager Face; tag `offboarding` shows Still Inside; tag `overlap` shows Hold Stack
- `/?q=unicode` on load applies
- Empty query `/?q=zzzz-no-match` shows an empty state that names the query
- Hold Stack: a day strip is in the DOM; seed has a visible overlap (peak ≥ 2); demo fixture makes peak ≥ 3; releasing a focused overlapping hold drops peak or overlap-hours; refresh persists; reset restores
- No product rail of 6+ data rows as the main UI
- The other ten product pages still load

## What broke / caveats

- Overlap is derived in this browser from start/end minutes, not from Google Calendar or Outlook
- Release / split / persist is this browser plus whatever JSON you import
- No backend, so “the pile” is the seeded (or imported) Tuesday
- GitHub merge API remains 403; main updates depend on a direct push
- Vercel MCP still disconnected; no subdomain routing this hour

## Catalog entry added

- Featured: Hold Stack — `/products/hold-stack/`
- Archive: Pager Face — `/products/pager-face/`
- Archive: Still Inside — `/products/still-inside/`
- Archive: Quiet Landing — `/products/quiet-landing/`
- Archive: Renew Trap — `/products/renew-trap/`
- Archive: Reread Clock — `/products/reread-clock/`
- Archive: Vouch Slip — `/products/vouch-slip/`
- Archive: Unseen Ink — `/products/unseen-ink/`
- Archive: Skill Clash — `/products/skill-clash/`
- Archive: Ghost Briefs — `/products/ghost-briefs/`
- Archive: Reorder Radar — `/products/reorder-radar/`

The catalogue now has eleven products.

## Next hour’s suggested focus

A paste-and-see instrument that is not Unseen Ink (quote vs last invoice, or two timezones on one pasted timestamp). Not a row-board. Not a scanner. Not subdomain work unless Vercel is connected.
