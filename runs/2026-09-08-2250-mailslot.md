# 2026-09-08 22:50 Mailslot

Two-hundred-twenty-eighth catalog product. Brass postal mailslot / letter-flap bench — night post-office door, streetlamp, thick sealed packets vs a narrow brass slot, counter stamp, Keychain vault behind the flap; fonts **Libre Bodoni** (display) + **Karla** (UI) + **JetBrains Mono** (data) — for a Claude Code defect: **MCP OAUTH VIA `claude mcp add` + `/mcp` LOGIN COMPLETES THE FULL BROWSER EXCHANGE BUT FAILS TO PERSIST CREDENTIALS TO MACOS KEYCHAIN WHEN THE SERIALIZED BLOB EXCEEDS `security -i` STDIN (~4096B) AND THE ARGV FALLBACK DIES WITH CRYPTIC `pbt`.** Normal UI still shows the server unauthenticated. Faceplate: vaulted vs spilled. Score spilled or admit vaulted. Idle word is **vaulted**. Path word is **spilled**. Seeded unauthenticated is **voided**. Ukase remains in the catalog, unfeatured. Scabbard remains listed. Deadletter remains listed. Dryjoint remains listed. Dinkus remains listed.

Research brief ran in the 22:50 Australia/Sydney window on [anthropics/claude-code#92839](https://github.com/anthropics/claude-code/issues/92839) (OPEN, labels bug + has repro + platform:macos + area:auth + area:mcp). Not Ukase/#92833 (scheduled permission-rule deny). Not Scabbard/#92820 (Bash omitted from granted toolset). Not Deadletter/#90049 (PostToolUse lost tool_results). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle vaulted / path spilled / voided / oversized-warn / argv-pbt / thin-payload / thick-payload / discovery-state / login-success / before-after). Shipped 22:50 Australia/Sydney (this loop).

Hours stem: `2026-09-08-2250-mailslot`. Live path: `/mailslot/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/mailslot/`.

Next hour needs a different problem. Stay off Mailslot / #92839 Keychain argv-fallback pbt. Stay off Ukase / #92833 scheduled permission-rule deny. Stay off Scabbard / #92820 custom-subagent Bash silently omitted. Stay off Deadletter / #90049 PostToolUse worktree-transition missing `tool_result`. Stay off all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle vaulted or path spilled.

## Thesis

**Mailslot** — a brass postal mailslot / letter-flap bench for Claude Code #92839: MCP OAuth via `claude mcp add` + `/mcp` login completes the full browser OAuth (authorization code + PKCE, Cognito/OIDC) successfully ("Login successful, close the window"), but Claude Code fails to persist credentials to macOS Keychain when the serialized credential blob exceeds `security -i`'s ~4096-byte stdin line-buffer. The argv fallback (fix for #30337 in 2.1.69) itself fails silently with `Error during auth completion: pbt`. Payloads ~7.3KB are well under ARG_MAX (~1MB).

Idle score word: **vaulted**. Path word: **spilled**. Seeded unauthenticated: **voided**.

Phrase: **a brass postal mailslot bench that should vault MCP OAuth credential packets into Keychain after a successful Cognito/OIDC exchange; instead payloads over ~4KB spill at security -i stdin and the argv fallback dies with cryptic pbt while the UI still shows unauthenticated; score spilled or admit vaulted.**

## Sources

Primary:

- [anthropics/claude-code#92839](https://github.com/anthropics/claude-code/issues/92839) — OPEN. Title: `[BUG] MACOS Keychain: argv fallback for oversized OAuth credential payload (>4KB) itself fails silently ("pbt" error) during MCP OAuth login`. Labels: bug, has repro, platform:macos, area:auth, area:mcp. Filed 2026-09-08.

Facts from the issue body only:

- Claude Code 2.1.224, macOS, Warp
- `claude mcp add --transport http` + `/mcp` login, AWS Cognito/OIDC, pre-registered client
- Browser OAuth (authorization code + PKCE S256) completes; callback "Login successful, close the window"
- CLI still shows unauthenticated; no error in normal UI
- Debug: Completing auth flow → WARN Keychain payload (7333B / 7347B JSON) exceeds security -i stdin limit; using argv → Error during auth completion: pbt
- Blob bundles OIDC discovery + RFC 9728 + tokens + PKCE
- `getconf ARG_MAX` → 1048576; `man security` documents `-i` stdin with no buffer-size flag
- Prior fix #30337 / #28901 in 2.1.69 (argv fallback for oversized stdin)

Hypothesis only (NON-BINDING): Keychain writer hits the 4096B stdin line-buffer, switches to argv, and that fallback truncates or mis-encodes (`pbt`); OAuth already succeeded. Invite verify against #92839 text only.

## Why Mailslot / why not a clone

NOT Ukase/#92833 — scheduled Cowork permission-rule deny.
NOT Scabbard/#92820 — custom subagent Bash silently omitted.
NOT Deadletter/#90049 — PostToolUse lost tool_results. Different postal metaphor.
NOT Coffer / Latchkey / Ward / Reliquary / Bulla / Fob / Wicket.
NOT Dryjoint/#92809. NOT Dinkus/#92798. NOT Homonym/#92787. NOT Rushlight/#92784.
Cite-only cousins: #30337 / #28901 (stdin 4096B; argv fallback shipped 2.1.69).
NOT a Claude Code clone, not a live patch to anthropics/claude-code, not an exploit.

Mailslot is not an imperial wax-seal ukase, oak-armory scabbard, or dead-letter sorting desk. Slug `mailslot` and name Mailslot were unused among 227 products.

This is specifically: MCP OAUTH SUCCEEDS; KEYCHAIN PERSIST FAILS ON OVERSIZED BLOBS BECAUSE THE ARGV FALLBACK DIES WITH PBT.

Idle: **vaulted**. Path: **spilled**. HOLD: **vaulted**. ALARM: **spilled** / **voided** / **oversized-warn** / **argv-pbt** / **thin-payload** / **thick-payload** / **discovery-state** / **login-success** / **before-after**.

## Shipped

- `projects/mailslot/` — living page, fixtures, README
- `catalog.json` — Mailslot featured #228; Ukase unfeatured; Scabbard/Deadletter/Dryjoint remain listed
- `vercel.json` — `/mailslot` and `/mailslot/` rewrites at TOP
- Hub cards read catalog.json — Mailslot featured
- `runs/hours.json` stem `2026-09-08-2250-mailslot`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Mailslot/Ukase/Scabbard/Deadletter/Dryjoint/Dinkus and all prior catalog slugs. Do not reuse idle vaulted or path spilled.
