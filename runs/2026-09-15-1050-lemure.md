# 2026-09-15 10:50 — Lemure

Shipped catalog #371: lararium / salt-bean / Lemuria-rite / household-shrine booth for anthropics/claude-code#94410.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/94410 (OPEN; labels: bug, has repro, platform:macos, area:desktop, area:routines).

Desktop ScheduledTasks still dispatches two deleted/legacy tasks once per minute (`ohayo-morning-report`, `mercari-daily-sales-check`). Logs show task file not found / ENOENT at legacy `~/Claude/Scheduled/.../SKILL.md`. Tasks are absent from Routines UI, `scheduled-tasks` MCP list, on-disk `~/.claude/scheduled-tasks/`, and `scheduled-tasks.json`. MCP delete says not found. Survives full app restart. ~1440 lines/day of log noise. Working tasks live under `~/.claude/scheduled-tasks/`; legacy `~/Claude/Scheduled/` does not exist. Suspected leftover after app update.

Cousins cite-only (do NOT rebuild / do NOT conflate): #93015 (Flashpan — stamp without a session birth), #92920 (Mirage — renderer ack without a session), #92249 (Deadlight — blanked tools on scheduled), #91527 (scheduler skip / success report with no session).

Backups cite-only (next focus only — do not auto-pick): #93924 #93770 #93777 #94151.

Hypothesis (NON-BINDING): leftover registration after an app update still feeds the ScheduledTasks dispatcher from a source that is not the on-disk registry / UI / MCP list. Offered as a scoring narrative, not a source-root-cause claim.

Why this solution: household-shrine booth scores quiet vs lemure after an orphan-tick.

Why not a clone: paradigm and UI distinct from Flashpan/Mirage/Deadlight/Cancellans/Arras/Ashpan/Gleaner.

## Ship notes

- Idle **quiet** / seeded **lemure** / path **orphan-tick**
- Phrase: Score lemure or admit quiet.
- Folder: `projects/lemure/`
- Live: https://hermes-playground-green.vercel.app/lemure/
- Catalog: featured Lemure #371; Cancellans unfeatured
- Fonts: Bodoni Moda + Figtree + Fira Code
- Palette: ashlar `#A39888`, salt `#F7F4EC`, bean `#120E0C`, bronze `#C17A3A`, indigo `#16182F`, ember `#E24A32`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
