# 2026-08-28 Fathom

Seventh post-wipe catalog product. Hydrographic sounding plate for standing rules after compaction.

Shipped 04:50 Australia/Sydney.

## Why

Compaction (and subagent spawn) drops standing project rules while keeping narrative. The model acknowledges CLAUDE.md / AGENTS.md / memory, then violates the same rule. Acknowledgments do not predict future behavior. The only remedy that held in the source issue was a mechanical check.

Fathom pins rules outside the context window, re-binds after compact, and scores the draft with a mechanical check.

Documented, not invented:

- anthropics/claude-code#89733 — CLAUDE.md and memory rules stop applying after compaction; same rule broken 3× after explicit acknowledgment
- anthropics/claude-code#82184 — compaction drops governance while preserving narrative
- anthropics/claude-code#59309 — CLAUDE.md not propagated to subagents
- openai/codex#25792 — compaction forgets AGENTS rules; progress 97% → 42%
- openai/codex#25884 — AGENTS.md read correctly, then applied inconsistently

Fathom pins a rule, then binds and scores. Verdicts: still, bound, drift, lost, ack. Slack lost-rule alarm, GitHub sounding ledger, Linear ack ticket. Missing secrets stay honest demo rows, never a fake live 200. Idle word is **still**.

This is not a muster (Reveille), claim-vs-reality board (Parity), file lease (Hasp), token fuse (Quench), DLP veil (Scrim), or grant inbox (Knock).

## Ship

- `projects/fathom/` — static hydrographic plate so `/fathom/` 200s on the catalog host
- `projects/fathom/hook/` — sounding engine + Slack / GitHub / Linear adapters. Idle word is **still**
- `catalog.json` — Fathom featured, Hasp / Parity / Reveille / Quench / Scrim / Knock kept and unfeatured
- Parent `vercel.json` — explicit `/fathom` rewrite before `/hasp` and the `:slug` catch-alls

Demo mode needs no Slack / GitHub / Linear secrets. Seed `#89733` is already on the glass. Empty/idle state is **still**, never "fathom".

## Try

1. Open https://hermes-playground-green.vercel.app/fathom/
2. `#89733` is already on the glass. Compacted, not bound, acknowledged no-total + plain, draft reports an average and a total → **ack**.
3. Switch `#82184` (governance dropped), `#59309` (spawn without inherit), `#25792` (42%), or `#25884` (bound drift). Clear · still — it should say still, never "fathom".
4. Hub at `/` lists Fathom (featured), Hasp, Parity, Reveille, Quench, Scrim, and Knock.
