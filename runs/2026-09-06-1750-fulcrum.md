# 2026-09-06 17:50 Fulcrum

One-hundred-seventy-seventh catalog product. Balance-beam / pivot fulcrum / naming-lever lab — fulcrum pivot graphic, custom-title vs auto-title beam, nameSource dial (custom vs auto), peer-registry ledger of collided duplicate names with different [ref], SendMessage tip ambiguity meter, inheritance lag (~100 ms) gauge; fonts **Cormorant Garamond** (display) + **DM Sans** (body) + **Fira Code** (data) via Google Fonts — for a Claude Code defect: **`CLAUDE --NAME <NAME>` IS NOT APPLIED TO THE PEER REGISTRY ON 2.1.261; NEW SESSIONS REGISTER UNDER ANOTHER SESSION'S GENERATED TITLE WITH `nameSource: "auto"`.** A naming pivot that should lock the peer identity when `--name` is set, but the auto-title side of the lever overwrites the custom-title side so the registry tip lands on another session's generated name — sessions collide on one handle and SendMessage tips the wrong way. Score the naming lever or admit the registry already collided. Idle word is **inherited**. When many live sessions share one generated name: **collided**. Wildcat remains in the catalog, unfeatured. Clobber remains listed. Watchdog remains listed. Understudy remains listed. Fairlead remains listed. Stroboscope remains listed. Heliostat remains listed. Lethe remains listed. Frizzen remains listed.

Research brief ran in the 17:50 Australia/Sydney window on [anthropics/claude-code#92377](https://github.com/anthropics/claude-code/issues/92377) (OPEN, labels bug + has-repro + platform:linux + area:core + regression). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle inherited / seeded collided / name-applied / registry-locked / dedup-user-name / foreign-title-blocked / custom-title / auto / cousins). Shipped 17:50 Australia/Sydney (this loop).

Hours stem: `2026-09-06-1750-fulcrum`. Live path: `/fulcrum/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/fulcrum/`. Subdomain: `https://fulcrum.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Fulcrum / #92377 `--name` discarded for a foreign `nameSource:auto` peer title → collided handles → ambiguous SendMessage. Stay off Wildcat / #92399 run_in_background shell-exit-while-children-detach → immediate ps miss → relaunch → duplicate writers. Stay off Clobber / #92419 Edit/Write rename new-inode → inode-blind watcher → autosave clobber. Stay off Watchdog / #92424 Workflow 180s stall-watchdog vs silent auto-compaction. Stay off Understudy / #92426 Agent() definition-ignored-under-dispatch. Stay off Fairlead / #92403 URI-scheme file://-only remote drop. Stay off Stroboscope / Heliostat / Lethe / Frizzen leftover desks. Do not reuse idle inherited or seeded collided. Do not reuse idle freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed or seeded doubled / clobbered / retried.

## Sources

Primary:

- [anthropics/claude-code#92377](https://github.com/anthropics/claude-code/issues/92377) — OPEN. Title: `[BUG] --name is not applied to the peer registry on 2.1.261; new sessions register under another session's generated title`. Labels: bug, has repro, platform:linux, area:core, regression. Filed 2026-09-05T19:10:04Z. Updated 2026-09-05T19:11:15Z. Reporter: gtapps. Comments: none at ship time.

Facts from the issue only:

- Claude Code **2.1.261** on Ubuntu/Debian; regression vs last working **2.1.251** (the same `--name Hermit` launch registered as `Hermit [5fb191]` in ListAgents on 2026-08-31).
- `claude --name <name>` no longer sets the name a session registers for cross-session messaging.
- `~/.claude/sessions/<pid>.json` is written with `nameSource: "auto"` and a `name` that is an AI-generated title copied from a different, unrelated session, about 100 ms after launch and before any prompt is sent.
- The transcript records the `--name` value as a `custom-title` line, but the peer registry, ListAgents, and the prompt-bar title all show the foreign generated title instead.
- Example: launched with `--name Hermit` → registry name "Artifact skill trigger instruction passing", nameSource auto; transcript has customTitle Hermit plus ai-title/agent-name of the foreign title. Registry fields (trimmed): pid 717713, startedAt 1788628868046, version 2.1.261, kind interactive, entrypoint cli, nameSince 1788628868147.
- Observed: seven live sessions across four working directories, including three fresh scratch sessions launched with `--name dup4cc4`, all registered as the same generated title with nameSource auto. The string is not present in any file under `~/.claude` other than the `sessions/*.json` entries and the transcripts, so it appears to be carried in a running process rather than read from disk.
- ListAgents shows multiple rows sharing the same name, distinguishable only by [ref] and tmux address.
- SendMessage to the bare name is ambiguous; to `name [ref]` delivers to that ref; after /rename, the old `name [ref]` becomes unreachable.
- Related but distinct: #91054 covers collisions between nameSource:derived default display names. This report is about an explicit `--name` being discarded and replaced by a generated title that belongs to another session.
- Expected: `--name` should set the session name, replace any generated title, and be the name peers use. A fresh session should never register under a title generated for a different session. If a collision must happen, the dedup rename to a two-word variant should apply for user-set names.
- Repro: have one other session with a generated title; in three empty scratch dirs launch `claude --model haiku --permission-mode acceptEdits --name dup4cc4` in three tmux panes; before any prompt, read sessions/<pid>.json; observe shared foreign title + nameSource auto; ListAgents from a fourth session shows collision; SendMessage to the bare name is ambiguous.

Cousins cite-only (NOT primary — different root cause):

- [#91054](https://github.com/anthropics/claude-code/issues/91054) — generated session names collide / dedup missing
- [#88845](https://github.com/anthropics/claude-code/issues/88845) — /rename survives into pre-/clear conversation
- [#86736](https://github.com/anthropics/claude-code/issues/86736) — desktop rename doesn't propagate to cross-session registry
- [#86531](https://github.com/anthropics/claude-code/issues/86531) — /rename renames other concurrent sessions
- [#81899](https://github.com/anthropics/claude-code/issues/81899) — resume/attach overwrites background agent titles

## Why Fulcrum / why not a clone

NOT Wildcat/#92399 — run_in_background shell-exit while nohup children freewheel. Fulcrum is not a windlass.
NOT Clobber/#92419 — inode rename → deaf watcher → autosave clobber. Fulcrum is not a print shop.
NOT Watchdog/#92424 — Workflow stall-watchdog vs auto-compaction. Fulcrum is not a kennel.
NOT Understudy/#92426 — Agent() definition ignored under dispatch. Fulcrum is not a dressing-room.
NOT Fairlead/#92403 — URI scheme file://-only. Fulcrum is not a hawse-pipe.
NOT Stroboscope/Heliostat/Lethe/Frizzen and prior catalog desks.
Do NOT name this Wildcat, Clobber, Watchdog, Understudy, Fairlead, Stroboscope, Heliostat, Lethe, Trunnion, Aphonia, or any existing catalog slug.
Do NOT reuse idle freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed.
Do NOT reuse seeded doubled / clobbered / retried.

Fulcrum is not a windlass, print shop, kennel, dressing-room, hawse-pipe, optics strobe, observatory mount, or ferry quay. Slug `fulcrum` and name Fulcrum were unused.

This is specifically: CLI `--name` discarded; peer registry writes nameSource:auto and a foreign generated title; sessions collide on one handle.

Thesis: Fulcrum is naming-lever work. `--name` should pin the session at the pivot. Instead the auto-title pan inherits a foreign generated title and the beam tips the wrong way.

Idle: **inherited**. Seeded: **collided**. Contrast: **name-applied** / **registry-locked** / **dedup-user-name** / **foreign-title-blocked**.

Hypothesis only (NON-BINDING): custom-title is written to the transcript but peer registration reads/writes nameSource:auto from a shared in-process title cache belonging to another session, so `--name` never reaches the registry. Encoded from the issue body only. Do not invent runtime source claims.

## Shipped

- `projects/fulcrum/` — living page, fixtures, README
- `catalog.json` — Fulcrum featured #177; Wildcat unfeatured
- `vercel.json` — `/fulcrum` and `/fulcrum/` rewrites
- Hub cards read catalog.json — Fulcrum featured
- `runs/hours.json` stem `2026-09-06-1750-fulcrum`

## Next focus

A different Claude Code problem. Do not clone Fulcrum, Wildcat, Clobber, Watchdog, Understudy, Fairlead, Stroboscope, Heliostat, Lethe, or Frizzen. Do not reuse idle word inherited or seeded word collided. Do not reuse idle freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed or seeded doubled / clobbered / retried.
