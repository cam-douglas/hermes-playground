# 2026-09-13 06:50 — Aneroid

Shipped catalog #324: aneroid-barometer / meteorological instrument-panel booth for anthropics/claude-code#93901.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/93901 (OPEN; labels: bug, has repro, area:ide, platform:vscode).

VS Code extension 2.1.269 (win32-x64); CLI 2.1.158; Windows 11 Enterprise 10.0.26200; Opus 5, 1M context; `~/.claude/settings.json` contains `"autoCompactWindow": 500000`. The context ring and hover are computed against the **model's** context window, not the configured one. `autoCompactWindow` does not appear in `webview/index.js` (0 hits) but appears 18 times in `bin/claude.exe`. Webview is handed `contextWindow: usageData.contextWindow - usageData.maxOutputTokens - 13000` then suppresses the ring while `U >= 50` against that wrong window. Ring first appears ~500k tokens used; auto-compaction fires almost immediately. Hover: `50% of context remaining until auto-compact` when none remains. Bottom-right: `50% context used` measured against 1M. Lowering `autoCompactWindow` to compact sooner can remove the warning entirely. Suggested fix in the issue (not implemented here): thread the CLI-resolved window into the webview.

Cousins cite-only (not duplicates): #90756 (Desktop UI control to set the value), #91385 (ring vs hard per-prompt window mid-turn).

Backups cite-only (next focus only — do not auto-pick): #93744 #93772 #93770 #93777 #93782 #93862 #93859 #93863 #93889 #93821 #93811 #93809 #93823.

Hypothesis (NON-BINDING): the webview never receives the CLI-resolved `autoCompactWindow`, so percentages and the hard 50% suppress gate run against the model window. The issue grepped 0 hits in `webview/index.js` and 18 in `bin/claude.exe` — offered as the issue's own evidence, not a source-root-cause claim beyond that grep.

## Ship notes

- Idle **calibrated** / seeded **aneroided** / path **wrong-window-ring**
- Phrase: Score aneroid or admit calibrated
- Folder: `projects/aneroid/`
- Live: https://hermes-playground-green.vercel.app/aneroid/
- Catalog: featured Aneroid #324; Simulacrum unfeatured
- Fonts: Orbitron + Exo 2 + IBM Plex Mono
- Palette: panel charcoal `#1A1C1F`, dial brass `#C9A227`, storm amber `#F0A202`, phosphor cyan `#3EE8E0`, sealed-glass teal `#1B6B6B`
- hours.json skipped: recent 09-13 ships are not maintained there

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
