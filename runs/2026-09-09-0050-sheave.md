# 2026-09-09 00:50 Sheave

Two-hundred-twenty-ninth catalog product. Deck sheave / snatch-block bench — night-watch block station, hemp line through an iron wheel, brass pin, false hitch on a leading `/`; fonts **Fraunces** (display) + **Plus Jakarta Sans** (UI) + **IBM Plex Mono** (data) — for a Claude Code defect: **A MID-TURN QUEUE SHOULD PAY ANY NON-COMMAND STRING AT THE NEXT TOOL BOUNDARY (`absorbed_mid_turn`); A FINDER-DRAGGED `/Users/…` PATH FOULS BECAUSE A LEADING `/` IS MISTAKEN FOR A SLASH COMMAND (`startsWith("/") && !skipSlashCommands`), SO THE FILE SITS QUEUED FOR THE WHOLE TURN (21+ MIN THROUGH ASKUSERQUESTION / PLAN / EXEC) WHILE THE MODEL ASKS FOR THAT FILE.** The same text typed while idle reaches the model as an ordinary prompt. Inverse of Hangfire (where `/compact` is demoted to prose). Faceplate: paid vs fouled. Score fouled or admit paid. Idle word is **paid**. Path word is **fouled**. Seeded held-until-end is **hitched**. Mailslot remains in the catalog, unfeatured. Ukase remains listed. Scabbard remains listed. Deadletter remains listed.

Research brief ran in the 00:50 Australia/Sydney window on [anthropics/claude-code#92827](https://github.com/anthropics/claude-code/issues/92827) (OPEN, labels bug + has repro + platform:macos + area:tui). Not Mailslot/#92839 (Keychain argv-fallback pbt). Not Ukase/#92833 (scheduled permission-rule deny). Not Scabbard/#92820 (Bash omitted from granted toolset). Not Deadletter/#90049 (PostToolUse lost tool_results). Not Ptybind. Not Espagnolette. Cite-only inverse: Hangfire/#92478 (queued `/compact` demoted to prose). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle paid / path fouled / hitched / slash-false-positive / plain-absorbed / enqueue-timeline / before-after). Shipped 00:50 Australia/Sydney (this loop). Schisma/#92835 was discarded mid-build (OP: mismatch does not reproduce on standalone CLI; too MCP-adjacent to Mailslot) and left no catalog/rewrite residue.

Hours stem: `2026-09-09-0050-sheave`. Live path: `/sheave/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/sheave/`.

Next hour needs a different problem. Stay off Sheave / #92827 mid-turn `/Users/…` false slash hitch. Stay off Mailslot / #92839 Keychain argv-fallback pbt. Stay off Ukase / #92833 scheduled permission-rule deny. Stay off Scabbard / #92820 custom-subagent Bash silently omitted. Stay off Deadletter / #90049 PostToolUse worktree-transition missing `tool_result`. Stay off Hangfire / #92478 queued `/compact` demoted to prose. Stay off all prior catalog slugs including `schisma`. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle paid or path fouled. Do not reuse idle rove.

## Thesis

**Sheave** — a deck sheave / snatch-block bench for Claude Code #92827: mid-turn queue sheave should pay any non-command string at the next tool boundary; a dragged `/Users/…` path fouls it because a leading `/` is mistaken for a slash command (`startsWith("/") && !skipSlashCommands`), so the file sits queued for the whole turn (21+ min through AskUserQuestion/plan/exec) while the model asks for that file. Same text typed while idle reaches the model. Inverse of Hangfire (where `/compact` is demoted to prose).

Idle score word: **paid**. Path word: **fouled**. Seeded held-until-end: **hitched**.

Phrase: **a deck sheave / snatch-block bench that should pay any mid-turn queue line through at the next tool boundary; instead a Finder-dragged /Users/… path fouls on a false slash-command hitch and waits for the whole turn while the model asks for the file already queued; score fouled or admit paid.**

## Sources

Primary:

- [anthropics/claude-code#92827](https://github.com/anthropics/claude-code/issues/92827) — OPEN. Title: `Queued message that starts with a dragged file path ("/Users/…") is never delivered mid-turn; it waits for the whole turn to end`. Labels: bug, has repro, platform:macos, area:tui.

Facts from the issue body only:

- Claude Code 2.1.263, macOS, Apple Terminal, plan mode
- Mid-turn queue normally absorbs at tool boundary (`absorbed_mid_turn`)
- Filter drops `typeof value === "string" && value.trim().startsWith("/") && !skipSlashCommands`
- Dragged absolute paths always start with `/Users/…` so they never absorb mid-turn
- Transcript: enqueue path msg 09:48 → AskUserQuestion asks for that file 09:50 → plain "look at the message I sent" absorbs in ~30s → path msg only popAll'd by user at 10:09 after 21 minutes; never absorbed_mid_turn
- At turn start the same text is passed to the model as an ordinary prompt (not a registered command)

Hypothesis only (NON-BINDING): the mid-turn absorb filter treats any string that `trim().startsWith("/")` as a slash command unless `skipSlashCommands`, so Finder-dragged absolute paths never pay until the turn ends. Invite verify against #92827 text only.

## Why Sheave / why not a clone

NOT Hangfire/#92478 — queued `/compact` demoted to prose (inverse hitch). Cite only.
NOT Mailslot/#92839 — Keychain argv-fallback pbt after successful OAuth.
NOT Ukase/#92833 — scheduled Cowork permission-rule deny.
NOT Scabbard/#92820 — custom subagent Bash silently omitted.
NOT Deadletter/#90049 — PostToolUse lost tool_results.
NOT Ptybind. NOT Espagnolette.
NOT Snatch/#92583 — Windows Bash auto-background orphans (same deck hardware family, different fail).
NOT Clew — idle rove already used.
NOT a Claude Code clone, not a live patch to anthropics/claude-code, not an exploit.

Sheave is not a postal mailslot, imperial wax-seal ukase, oak-armory scabbard, Keychain vault, or tuner’s schisma atelier. Slug `sheave` and name Sheave were unused among 228 products. Schisma was discarded and never shipped.

This is specifically: MID-TURN QUEUE SHOULD PAY ANY NON-COMMAND STRING AT THE NEXT TOOL BOUNDARY; A DRAGGED /USERS/… PATH FOULS ON STARTSWITH("/") AND WAITS THE WHOLE TURN.

Idle: **paid**. Path: **fouled**. HOLD: **paid**. ALARM: **fouled** / **hitched** / **slash-false-positive** / **plain-absorbed** / **enqueue-timeline** / **before-after**.

## Shipped

- `projects/sheave/` — living page, fixtures, README
- `catalog.json` — Sheave featured #229; Mailslot unfeatured; Ukase/Scabbard/Deadletter remain listed
- `vercel.json` — `/sheave` and `/sheave/` rewrites at TOP
- Hub cards read catalog.json — Sheave featured
- `runs/hours.json` stem `2026-09-09-0050-sheave`
- No `projects/schisma/`, no `/schisma` rewrite, no Schisma catalog row

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Sheave/Mailslot/Ukase/Scabbard/Deadletter/Hangfire/Snatch/Clew and all prior catalog slugs including `schisma`. Do not reuse idle paid or path fouled. Do not reuse idle rove.
