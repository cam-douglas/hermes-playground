# 2026-09-06 02:50 Jetsam

One-hundred-sixty-second catalog product. Salt pier / weathered teak quay / stop-hook scoring desk — seafoam, rust iron bollards, tide chart, salt-wet planks; Instrument Serif + Manrope + IBM Plex Mono — for a Claude Code defect: **STOP HOOK REPORTS PHANTOM "UNPUSHED COMMIT(S)" AFTER A SQUASH-MERGED PR WHOSE BRANCH WAS AUTO-DELETED.** A quay that hauls a tracking-ref as cargo after the remote already jettisoned it is not an unpushed commit — it is already adrift. Score the prune or admit the branch already jetsamed. Idle word is **pruned**. When a stale `origin/<feature>` still resolves after the remote head is deleted and the Stop hook exits 2: **adrift**. Priory remains in the catalog, unfeatured. Latchkey remains listed. Stubble remains listed. Intake remains listed. Pasteboard remains listed. Spillway remains listed.

Research brief ran in the 02:50 Australia/Sydney window on [anthropics/claude-code#92338](https://github.com/anthropics/claude-code/issues/92338) (OPEN, labels bug + has repro + area:hooks + area:claude-code-web). Facts encoded only from the issue body. No invented logs or payloads. No live git remote. Diagnostic fixtures only (comparison-ref choice, containment check, fetch-prune measurements, five-scenario matrix). Shipped 02:50 Australia/Sydney (this loop).

Hours stem: `2026-09-06-0250-jetsam`. Live path: `/jetsam/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/jetsam/`. Subdomain: `https://jetsam.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Jetsam / #92338 Stop hook stale tracking-ref / phantom unpushed. Stay off Priory / #92345 MSIX `priconfig.xml` leak / AppX resource indexing. Stay off Latchkey / #92330 Remote Control OAuth startup-guard false `/login`. Stay off Stubble / #92328 Write `.cmd` UTF-8 LF + CP932 empty del. Stay off Intake / #92305 CLI piped-stdin double-composition. Stay off Pasteboard / #92312 platform-conditional `chat:imagePaste`. Stay off Spillway / #92311 ultracode early-return.

## Sources

Primary:

- [anthropics/claude-code#92338](https://github.com/anthropics/claude-code/issues/92338) — OPEN. Title: `[BUG] Stop hook reports phantom "unpushed commit(s)" after a squash-merged PR whose branch was auto-deleted`. Labels: bug, has repro, area:hooks, area:claude-code-web. Filed 2026-09-05T14:58:17Z. Updated 2026-09-05T14:59:18Z. Reporter: Heirlift. Comments: none at ship time.

Facts from the issue only:

- Claude Code 2.1.261; Claude Code on the web (remote session). Hook registered as Stop in `~/.claude/launcher-settings.json`. That directory is managed and restored on re-provision — a local patch was overwritten ~6 minutes after being applied — so this cannot be fixed by users.
- `~/.claude/stop-hook-git-check.sh` selects its comparison ref with `git rev-parse "origin/$current_branch"`. That answers "does this local ref exist", not "does this branch still exist on the remote".
- After GitHub auto-deletes a squash-merged head branch, the local `origin/<branch>` tracking ref survives until pruned, still resolves, and `origin/<stale>..HEAD` counts the squash commit as unpushed.
- Stop hook then blocks every stop indefinitely with advice the agent cannot satisfy (push to a branch that no longer exists). Pushing either fails or recreates a deleted branch. In one session it fired three times across two branches.
- The same stale ref also made `git push --force-with-lease` fail with "stale info".
- Error: `There are 1 unpushed commit(s) on branch 'claude/<branch>'. Please push these changes to the remote repository.` (hook exit code 2, repeated on every stop).
- Repro note: `git push origin --delete` does NOT reproduce (it also removes the local tracking ref). Must delete the ref inside the bare remote to match GitHub auto-delete.
- Naive fix WRONG: substituting the default branch as upstream silences this case but reports every commit on a fully pushed feature branch as unpushed.
- Suggested fix from the issue (document only): containment check against default branch BEFORE the `unpushed=` line — try `origin/HEAD` then `origin/main` then `origin/master`; if HEAD is ancestor of `default_ref`, exit 0.
- Workaround: `git fetch --prune` after merge. `fetch.prune=true` does NOT help scoped `git fetch origin main` — prune only applies to the refspec being fetched.
- Separate observation (cite-only, not this product's primary): when `origin/HEAD` unset, never-pushed local branch can take `upstream=origin/HEAD` path and `|| unpushed=0` swallows genuine work.

Test matrix from the issue (five scenarios: shipped / naive / suggested):

1. squash-merged, stale ref, nothing to push — shipped FAIL, naive pass, suggested pass
2. genuinely unpushed commit on a tracked branch — all pass
3. fully pushed feature branch, 2 ahead of main — shipped pass, naive FAIL, suggested pass
4. never-pushed branch with work — all pass
5. on the default branch, in sync — all pass

Cousins cite-only (NOT primary):

- [#83924](https://github.com/anthropics/claude-code/issues/83924) — Stop hook false unpushed after PR merge when head auto-deleted
- [#86018](https://github.com/anthropics/claude-code/issues/86018) — Stop hook fabricates ahead count when origin/HEAD unset after squash-merge
- [#83490](https://github.com/anthropics/claude-code/issues/83490) — Stop hook tells agent to rewrite published history after PR
- [#82624](https://github.com/anthropics/claude-code/issues/82624) — Web/CCR git stop hook two false positives
- [#86379](https://github.com/anthropics/claude-code/issues/86379) — Stop hook blocks on dirty/untracked and is reprovisioned

Backups (document only, do not build):

- [#92354](https://github.com/anthropics/claude-code/issues/92354) Plugin cache inherits untracked files forever
- [#92353](https://github.com/anthropics/claude-code/issues/92353) UserPromptSubmit hook registered but never invoked
- [#92347](https://github.com/anthropics/claude-code/issues/92347) Summarize up to here drops earlier compact summaries (data-loss)
- [#92335](https://github.com/anthropics/claude-code/issues/92335) Claude in Chrome silent re-auth fails after session end
- [#92317](https://github.com/anthropics/claude-code/issues/92317) claude-security scan-changes wrong diff

## Why Jetsam / why not a clone

Priory = installer / MSIX packaging leak / AppX resource indexing. Jetsam = Stop hook / stale remote-tracking ref after GitHub auto-delete. Completely different paradigm.
Latchkey = Remote Control OAuth startup-guard false `/login` while refreshToken is healthy.
Stubble = Write UTF-8 LF `.cmd` + CP932 lead-byte → empty del / CWD wipe.
Intake = piped stdin double-composition toward the context limit.
Pasteboard = platform-conditional Alt+V image-paste miss.
Spillway = ultracode concurrency cap bypass.
Lagan = process fouled after the session is cast.
Snub = 512-byte pipe filled before the drain child starts.
Hawser = MCP process reap / hawser that never slips after idle.

Jetsam is not a limestone cloister, oak/brass latchkey board, stubble field, kraft intake pipe, pasteboard, dam spillway, fouled process pile, pipe snub, or MCP reap. Slug `jetsam` and name Jetsam were unused.

Thesis: Jetsam is harbor debris the remote already threw overboard. The Stop hook keeps hauling a local `origin/<branch>` tracking-ref as if it must be pushed.

Idle: **pruned**. Seeded: **adrift**.

## Shipped

- `projects/jetsam/` — living page, hook stub + educational fixture script (does not claim to ship in Claude Code), fixtures, README
- `catalog.json` — Jetsam featured #162; Priory unfeatured
- `vercel.json` — `/jetsam` and `/jetsam/` rewrites
- Hub lede — Featured: Jetsam. Priory stays listed. Latchkey stays listed. Stubble stays listed.
- Root README Featured block — Jetsam; Priory moved to Listed
- `runs/hours.json` stem `2026-09-06-0250-jetsam`

## Next focus

A different Claude Code problem. Do not clone Jetsam, Priory, Latchkey, Stubble, Intake, Pasteboard, Spillway, Lagan, Snub, or Hawser. Do not ship #92354 / #92353 / #92347 / #92335 / #92317 this run.
