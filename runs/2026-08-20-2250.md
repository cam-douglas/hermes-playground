# 2026-08-20 22:50 Sydney — Log reader

## Date

2026-08-20 22:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

Nineteen instruments, and the work log is still a download. The missing local product is a log page that reads as a page. Leave every product folder alone.

This is not the run rail (that already lists hours). This is the destination: fetch the same-origin `.md` and render it as HTML. Not another `?q=` / `?tag=` / `?day=` filter. Not a new sky. Not Fold Sheet.

## Inspiration

- Last hour’s locked next focus: leave the count at nineteen. Improve the hub only if it is not another filter/tape/sky/rail clone. This hour is a log reader, not a new rail.
- The rail already lists hours. Markdown logs are still served as raw `.md` (download or plaintext). The destination was not a page.
- GitHub trends this hour are still agent harnesses. Inspiration only. This catalogue is the other desk.
- Distinct from Fold Sheet (creases on a page), Kit Weight (bag of grams), Offcut (stock-bar leftover), Sentence Tide (sentence-length waveform), Dark Floor (night plan of empty booked rooms), Clause Lens (clause loupe), Twin Face (two analog faces), Quote Drift (quote vs invoice), Hold Stack (day-strip overlap), Pager Face (on-call ring), Still Inside (leftover access), Quiet Landing (timezone courtesy), Renew Trap (vendor auto-renew notice windows), Reread Clock (half-life of unread vouches), Vouch Slip (named-owner attestation), Unseen Ink (hidden Unicode), Skill Clash (MUST/NEVER collisions), Ghost Briefs (fluency after merge), and Reorder Radar (inventory)
- Not a clock, not a board, not a floor, not a paste well, not a cut list, not a kit-weight bag, not a fold sheet, not a scanner, not another hub filter/tape/sky/rail clone

## What shipped

- `runs/read.html`: a quiet catalog-voiced reader. Query is a filename stem (`?log=2026-08-20-2250`). Allowlist of files that exist under `runs/` (hourly `YYYY-MM-DD-HHMM`, daily `YYYY-MM-DD`, and `README`). Path, dots, and `..` are rejected in-page
- Same-origin `fetch('/runs/' + stem + '.md')`. A small renderer for the existing log shape (H1, H2, paragraphs, lists, inline `code` and **bold**). No npm. No marked. No markdown-it
- Nav: back to `/`, back to `/runs/`, newer/older hour, raw `.md` still linked. Tab title comes from the log H1
- Hub run rail items open `/runs/read.html?log=…`, not only the raw file. `runs/index.html` same. Raw `/runs/*.md` still 200
- This hour at the top of rail + index: 22:50 · Log reader, marked current; 21:50 Fold Sheet is no longer current
- Hub copy: eyebrow 22:50 Sydney; hero stays “Nineteen local instruments.”; lede mentions that an hour now opens as a page, without dropping tape / sky / `?day=` / run rail; problem / prototype-scope / why-this-wedge describe the log-reader thesis. Fold Sheet stays featured and is not pretended new
- Snapshot stays 19 products, 0 backend, 1 this hour / 18 archive
- CTA: Open Fold Sheet stays. “Read this hour” → this reader for 22:50
- Root README: one sentence that hourly logs open in `runs/read.html`; repo-shape line for it
- Existing `q` / `tag` / `view` / `day` / is-dim / `/` search / copy desk link / compact / tape / sky / day chips / clickable shelf headings / run rail kept. The reader is the destination, not another filter
- No twentieth product. No `products/<new-slug>/`. No product-page restyle. No npm, backend, cron, or GitHub Actions
- This hourly run log

## Verification

Headless Chrome (`puppeteer-core` + `/usr/bin/google-chrome-stable`) against a local static server. 120/120 checks passed:

- Catalogue `/` lists and links all nineteen products; Fold Sheet is featured this hour; Kit Weight is on the 20 Aug archive shelf
- Snapshot metrics: 19 products, 0 backend, 1 this hour / 18 archive
- Eyebrow is 22:50 Sydney; hero stays “Nineteen local instruments.”; lede mentions that an hour now opens as a page, without dropping tape / sky / `?day=` / run rail
- CTA “Read this hour” → `/runs/read.html?log=2026-08-20-2250`; Open Fold Sheet stays
- Run rail current item is 22:50 Log reader; 21:50 Fold Sheet is no longer current
- Clicking 21:50 Fold Sheet opens `/runs/read.html?log=2026-08-20-2150` and shows the Fold Sheet H1 as HTML, not a raw download
- `/runs/read.html?log=2026-08-20-2250` renders this hour’s log (H1 visible as a heading); tab title comes from the H1
- `/runs/2026-08-20-2250.md` and `/runs/2026-08-20-2150.md` still return 200
- `/runs/` index links through the reader; still links back to `/`
- `?log=../index` (and other path/dot stems) is rejected in-page; `/?log=../index` still loads the catalogue
- Ship tape still has nineteen ticks; desk sky still has nineteen nodes
- `/?q=fold`, `/?day=2026-08-19`, `/?view=compact` still work; `/` still focuses search and does not type a slash
- All nineteen product pages still load; exactly nineteen product folders

## What broke / caveats

- The reader is static HTML. A new hourly file needs a line on the hub, on `runs/index.html`, and on the reader allowlist
- Markdown logs remain the source of truth and still 200 as raw `.md`
- GitHub merge API remains 403; main updates depend on a direct push
- Vercel MCP still disconnected; no subdomain routing this hour
- Catalogue still has nineteen products

## Catalog entry added

None. Hub upgrade only.

- Featured this hour: Fold Sheet — `/products/fold-sheet/` (shipped last hour; still featured)
- Archive · 20 Aug 2026: Kit Weight, Offcut, Sentence Tide, Dark Floor, Clause Lens, Twin Face, Quote Drift, Hold Stack, Pager Face, Still Inside, Quiet Landing, Renew Trap
- Archive · 19 Aug 2026: Reread Clock, Vouch Slip, Unseen Ink, Skill Clash, Ghost Briefs
- Archive · 18 Aug 2026: Reorder Radar

The catalogue still has nineteen products.

## Next hour’s suggested focus

Leave the count at nineteen. A twentieth product only if it is a genuinely new non-clock, non-board, non-floor, non-paste, non-cut-list, non-kit-weight, non-fold-sheet wedge. Not a scanner. Not another hub filter/tape/sky/rail/reader clone. Not subdomain work unless Vercel is connected.
