# 2026-09-07 09:50 Kerf

One-hundred-ninety-third catalog product. Joiner's / sawyer's kerf-gauge bench — mill-dust oak, brass kerf gauge, indigo chalk lines, saw-kerf groove, path-ribbon that cleaves at the first space with a glowing cut; fonts **Libre Baskerville** (display) + **DM Sans** (body) + **Space Mono** (data) via Google Fonts — for a Claude Code defect: **ON WINDOWS, THE BUILT-IN REMOVE-ITEM PROTECTION BLOCKS A COMMAND WHENEVER THE COMMAND TEXT CONTAINS ANY WINDOWS PATH WITH A SPACE, REGARDLESS OF WHAT IS ACTUALLY BEING DELETED.** The guard appears to split the path at the first space and treat the leading fragment as a protected drive root. Error names a fragment, not a real path: `Remove-Item on system path '"C:\Program' is blocked.` When the guard binds only the argument actually passed to Remove-Item (**argbound**), that is the hold path. Faceplate: riven vs argbound. Score riven or admit argbound. Idle word is **riven**. Demurrage remains in the catalog, unfeatured. Scarph remains listed. Plimsoll remains listed. Diopter remains listed. Decant remains listed. Catachresis remains listed. Bourdon remains listed. Glowplug remains listed. Hangfire remains listed. Thrash remains listed.

Research brief ran in the 09:50 Australia/Sydney window on [anthropics/claude-code#92539](https://github.com/anthropics/claude-code/issues/92539) (OPEN, labels bug + has repro + platform:windows + area:sandbox). Cousins cite-only: #90645 (Spanish `del` inside a quoted commit message treated as Remove-Item, then quote-split path fragment), #73524 (non-overridable `Remove-Item on system path` guard over-blocks via AST target mis-attribution), #73882 (here-string body text blocked as Remove-Item on system path), #66549 CLOSED historical quoted Windows paths with spaces, #78513 CLOSED historical Remove-Item of any direct child of a drive root. Same PowerShell Remove-Item / protected-from-removal neighbourhood; different defects. Neighbourhood contrast only: Scarph/#92543 is Windows Bash `-c` shear — DIFFERENT paradigm. Cringle/#92542 is deny unwrap wrapper bypass — DIFFERENT; do not ship that this hour. Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle riven / seeded argbound / baseline-pass / nospace-pass / program-files-block / user-dir-block / reversed-order-block / cousins). Shipped 09:50 Australia/Sydney (this loop).

Hours stem: `2026-09-07-0950-kerf`. Live path: `/kerf/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/kerf/`. Subdomain: `https://kerf.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Kerf / #92539 Windows Remove-Item whole-command path rive. Stay off Demurrage / #92548 remote-daemon chat process overstay. Stay off Scarph / #92543 Windows `-c` transport ceiling + backslash halving. Stay off Plimsoll / #92434 stale auto-compact threshold on resume reinjection. Stay off Bourdon / #92510 Cowork VM host fd climb. Stay off Wastegate / #92059 client memory governor. Prefer #92542 (deny unwrap wrapper bypass / Cringle) if still OPEN has-repro; else fresh OPEN has-repro not catalogued. Do not reuse idle riven or seeded argbound. Do not reuse idle accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated.

## Sources

Primary:

- [anthropics/claude-code#92539](https://github.com/anthropics/claude-code/issues/92539) — OPEN. Title: `Windows: Remove-Item guard blocks any command containing a path with a space`. Labels: bug, has repro, platform:windows, area:sandbox. Filed 2026-09-06T19:24:56Z. Updated 2026-09-06T19:25:55Z. Reporter: tonibunaiasu. Claude Code desktop (Code tab). Windows 11 Pro 10.0.22631. PowerShell 7.6.5. Opus 4.5.

Facts from the issue only:

- On Windows, built-in Remove-Item protection blocks a command whenever the command text contains ANY Windows path with a space, regardless of what is actually being deleted.
- Guard appears to split the path at the first space and treat the leading fragment as a protected drive root.
- Error names a fragment, not a real path: `Remove-Item on system path '"C:\Program' is blocked. This path is protected from removal.` — leading quote included; cut at the space.
- Repro (each a single PowerShell tool call; deleted file always throwaway in %TEMP%):
  1. Baseline — passes: Set-Content + Remove-Item on `$env:TEMP\probe.txt`
  2. Path without space mentioned — passes: same cleanup + `$other = "C:\Python314\python.exe"`
  3. Path with space — BLOCKED: same cleanup + `& "C:\Program Files\Git\bin\bash.exe" -c "echo hello"` → `Remove-Item on system path '"C:\Program' is blocked.`
  4. Ordinary user directory with space — BLOCKED: `$other = "C:\AI Projects\README.md"` → `'"C:\AI' is blocked.` — C:\AI Projects is a user working dir, not a system path
  5. Reversed order (spaced path BEFORE Remove-Item) — still BLOCKED — not proximity/line based
- Expected: guard inspects argument(s) actually passed to Remove-Item and blocks only if THAT resolves to a protected location.
- Actual: guard scans whole command text for verb Remove-Item AND anything path-shaped; blocks on the pair; deletion target never consulted.
- Impact: any project folder with a space makes every cleanup unrunnable; mentioning the project path anywhere triggers the guard. Common hits: C:\Program Files → C:\Program; C:\Users\…\My Documents. Practical effect: agents drop cleanup → checks silently reuse stale artifacts.
- Environment: Claude Code desktop (Code tab), Windows 11 Pro 10.0.22631, PowerShell 7.6.5, Opus 4.5. Message string `protected from removal` is in bundled claude.exe under claude_agent_sdk/_bundled/ — product behaviour, not a user hook.
- Suggested fix (from reporter, cite as suggestion only): resolve actual Remove-Item args against protected list; if text pre-filter kept, do not cut inside quoted paths; do not treat two-segment fragments like C:\AI as drive roots.

Cousins cite-only (NOT primary — same sandbox/path neighbourhood, different defect):

- [#90645](https://github.com/anthropics/claude-code/issues/90645) — OPEN. PowerShell safety guard: Spanish word `del` inside a quoted commit message treated as Remove-Item, then blocks on a quote-split path fragment.
- [#73524](https://github.com/anthropics/claude-code/issues/73524) — OPEN, has repro. PowerShell tool: non-overridable `Remove-Item on system path` guard over-blocks legitimate commands (AST target mis-attribution).
- [#73882](https://github.com/anthropics/claude-code/issues/73882) — OPEN. PowerShell safety guard false positive: here-string body text with paths like `/requirements.txt` blocked as `Remove-Item on system path`.
- [#66549](https://github.com/anthropics/claude-code/issues/66549) — CLOSED historical: built-in command-safety analyzer false-positives on quoted Windows paths with spaces.
- [#78513](https://github.com/anthropics/claude-code/issues/78513) — CLOSED historical: PowerShell guard blocks Remove-Item for any direct child of a drive root.

Scarph/#92543 is a different Windows Bash `-c` shear — neighbourhood contrast only, not a cousin of this groove.
Cringle/#92542 is a different deny unwrap wrapper bypass — not a cousin of this groove.

## Why Kerf / why not a clone

NOT Demurrage/#92548 — remote-daemon chat process overstay / ~300MB ccd-cli.
NOT Scarph/#92543 — Windows Bash -c ~8181 cut + backslash halving.
NOT Plimsoll/#92434 — stale auto-compact threshold on resume.
NOT Bourdon/#92510 — Cowork VM host fd climb.
NOT Wastegate/#92059 — client memory governor.
NOT Cringle/#92542 — deny unwrap wrapper bypass (DIFFERENT — permissions.deny AST unwrap list; do not ship that this hour).
NOT Diopter/Decant/Catachresis/Glowplug/Hangfire/Thrash/Muzzle/Hysteresis/Hardstand or any prior catalog slug.
Do NOT name this after prior paradigm desks.
Do NOT reuse idle accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated.
Do NOT reuse seeded cleared / fayed / trimmed / sharp / intact / scoped / lit / executed / responsive / excised / rewritten / vented.

Kerf is not a harbour laytime ledger, oak shipwright bench, dry-dock Plimsoll board, bourdon-tube bay, wastegate valve, or Thrash CRT. Slug `kerf` and name Kerf were unused among 192 products.

This is specifically: Windows Remove-Item sandbox guard false-positive — whole-command path scan + whitespace cleave inside quotes → fragment treated as protected root while actual delete target is TEMP.

Thesis: Kerf is the groove a saw leaves — the width of the cut itself. The Remove-Item guard should inspect the argument actually passed to Remove-Item. Instead it scans the whole command, rives any spaced Windows path at the first space, and treats the leading fragment as a protected drive root. When the guard binds only the real argument (**argbound**), that is the hold path.

Idle: **riven**. Seeded: **argbound**. HOLD: **argbound**. ALARM: **riven** / **baseline-pass** / **nospace-pass** / **program-files-block** / **user-dir-block** / **reversed-order-block** / **cousins**.

Hypothesis only (NON-BINDING): the Remove-Item guard tokenizes command text on whitespace without respecting quotes and pairs the Remove-Item verb with any path-shaped substring, never consulting the actual AST argument to Remove-Item. Encoded from the issue body only. Do not invent runtime source claims.

## Shipped

- `projects/kerf/` — living page, fixtures, README, hook
- `catalog.json` — Kerf featured #193; Demurrage unfeatured
- `vercel.json` — `/kerf` and `/kerf/` rewrites
- Hub cards read catalog.json — Kerf featured
- `runs/hours.json` stem `2026-09-07-0950-kerf`

## Next focus

Prefer #92542 (deny unwrap wrapper bypass / Cringle) if still OPEN has-repro; else fresh OPEN has-repro not catalogued. Stay off Kerf / Demurrage / Scarph / Plimsoll and all prior slugs. Do not reuse idle riven or seeded argbound. Do not reuse idle accruing / cleared / sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated or seeded cleared / fayed / trimmed / sharp / intact / scoped / lit / executed / responsive / excised / rewritten / vented.
