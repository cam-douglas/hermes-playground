# 2026-09-11 03:50 Fomite

Two-hundred-seventy-sixth catalog product. Sterile-lab / epidemiology booth — glass slide, culture dish, agar plate, pathogen droplet hitchhiking on a directory-marketplace install copy; fonts **Alegreya** (display) + **Nunito Sans** (body) + **Fira Code** (mono) — for a Claude Code defect: **DIRECTORY-MARKETPLACE PLUGIN INSTALL COPIES GITIGNORED FILES INCLUDING A ROOT `.env` INTO VERSIONED PLUGIN CACHE WITH NO WARNING.** Faceplate: scrubbed vs contaminated. Score fomite or admit scrubbed. Idle word is **scrubbed**. Seeded word is **contaminated**. Path word is **gitignore**. Fosse remains in the catalog, unfeatured. Hibernacle remains listed. Scapegoat remains listed. Graft remains listed as a cite-only cousin, not this ship.

Research brief ran in the 03:50 Australia/Sydney window on [anthropics/claude-code#93423](https://github.com/anthropics/claude-code/issues/93423) (OPEN, labels bug + has repro + platform:macos + area:security + area:plugins). Cousins cite-only: #93426, #92354 — do not rebuild. Backups cite in data only: #93429, #93403, #93405, #93402, #93426. Not Fosse/#93358. Not Hibernacle/#93372. Not Graft/#92354. Not Springe/#92675. Not leftover woodworking / mm-slider. Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only. Shipped 03:50 Australia/Sydney (this loop).

Hours stem: `2026-09-11-0350-fomite`. Live path: `/fomite/`. Intended live URL: `https://hermes-playground-green.vercel.app/fomite/`.

## Thesis

**Fomite** — a sterile-lab / epidemiology booth for Claude Code #93423: a directory marketplace install should stay scrubbed (gitignore honored; no `.env` / `node_modules` in the versioned cache; install matches the tracked tree). Instead the copy excludes `.git` but never reads `.gitignore`, so a root `.env` and 51M of `node_modules` hitchhike into a 128M cache against 4.5M tracked. Idle scrubbed / seeded contaminated / path gitignore. Score fomite or admit scrubbed.

Idle score word: **scrubbed**. Seeded word: **contaminated**. Path word: **gitignore**.

Phrase: **when a directory marketplace install copies gitignored files including a root .env into the versioned plugin cache with no warning, score fomite or admit scrubbed.**

## Research brief

Sources:

- Primary: anthropics/claude-code#93423
- Cite-only cousins: #93426, #92354 — do not rebuild
- Backups cite-only (do not auto-pick): #93429, #93403, #93405, #93402, #93426

Why not a clone: NOT Fosse/#93358, NOT Hibernacle/#93372, NOT Graft/#92354, NOT Springe/#92675, NOT leftover woodworking/mm-slider. Different paradigm: epidemiology / fomite — an object that carries infection on a marketplace directory copy — not Plan9 host/guest, not idle working-set trim, not plugin-cache copy-forward.

## Published walk

idle scrubbed (gitignore honored; no .env / node_modules; install matches tracked 4.5M) → marketplace-add → plugin-install → copy-excludes-git → skip-gitignore → env-hitch → node-modules-51M → cache-128M → scratch-hitch → no-warning → persists-until-replaced → contaminated → gitignore → fomite.

Hypothesis only (NON-BINDING): directory-marketplace install walks the marketplace root with a naive copy that excludes `.git` but never consults `.gitignore`, global excludes, or `core.excludesFile`, so gitignored secrets and scratch hitchhike into the versioned cache. Invite verify against #93423 text only. Do not claim Claude Code source root cause. Do not patch anthropics/claude-code.

## Files

- `projects/fomite/` — index.html, fomite.mjs, fomite.test.mjs, data/scrubbed.json, data/contaminated.json, data/gitignore.json, data/fomite.json, data/env-hitch.json, data/node-modules.json, README.md
- `catalog.json` — Fomite featured #276; Snubber unfeatured
- `hub/catalog.json` — Fomite featured; others unfeatured
- `vercel.json` — `/fomite` and `/fomite/` → `/projects/fomite`
- Hub cards read catalog.json — Fomite featured
- `runs/hours.json` stem `2026-09-11-0350-fomite`
