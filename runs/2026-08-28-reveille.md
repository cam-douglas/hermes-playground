# 2026-08-28 Reveille

Fourth post-wipe catalog product. Living muster board for in-flight agent handles.

Shipped 01:50 Australia/Sydney.

## Why

Compaction loses in-flight agent handles. Workflow run records appear on disk only at completion. The session concludes nothing is live and re-dispatches onto the same artifact.

Documented, not invented:

- anthropics/claude-code#90036 — compaction loses track of in-flight agents; duplicate work
- anthropics/claude-code#90034 — running workflow leaves no on-disk record; dispatch collides
- anthropics/claude-code#29193 — background task handles orphaned after compaction
- anthropics/claude-code#77730 — agent/task IDs stop resolving across a session-identity boundary

Reveille keeps heartbeats and artifact claims across that boundary. Duplicate dispatch is held. A missed beat is orphan / missing. Slack / GitHub / Linear get honest demo adapters without secrets.

## Ship

- `projects/reveille/` — static dawn/indigo muster board so `/reveille/` 200s on the catalog host
- `projects/reveille/hook/` — muster snapshot in, `clear` | `hold` | `orphan` out + Slack / GitHub / Linear adapters
- `catalog.json` — Reveille featured, Quench / Scrim / Knock kept and unfeatured
- Parent `vercel.json` — explicit `/reveille` rewrite beside `/quench`, `/scrim`, and `/knock` (no host wildcard)

Demo mode needs no Slack / GitHub / Linear secrets. Seed collision `compact-90036` is already on the roll. Empty/idle state is **quiet**, never "reveille". Not a spend kill. Not DLP. Not a grant inbox.

## Try

1. Open https://hermes-playground-green.vercel.app/reveille/
2. Three agents are already claimed. Docs is past the 90s TTL.
3. Try duplicate dispatch — HOLD. Fire compaction — count++, roster stays. Stand down — it should say quiet, never "reveille".
4. Hub at `/` lists Reveille (featured), Quench, Scrim, and Knock.
