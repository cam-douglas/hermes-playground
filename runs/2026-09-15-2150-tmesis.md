# 2026-09-15 21:50 — Tmesis

Shipped catalog #381: manuscript / rhetoric / spliced parchment / editorial-desk booth for anthropics/claude-code#86198.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/86198 (OPEN; labels: bug, has repro, reproduced, platform:macos, area:core).

Title: `Running a slash command (/effort) while advisor is in flight injects local_command records mid-message and permanently 400s the session`.

Claude Code 2.1.226; macOS 15.5 / darwin 25.5.0. While a server-side `advisor` tool call is in flight, typing an ordinary slash command (e.g. `/effort`) appends that command's `system` / `local_command` records **inside** the still-open assistant message, between `server_tool_use` and `advisor_tool_result`. The `advisor_tool_result`'s `parentUuid` then chains to the command's stdout record instead of the `server_tool_use`. Every subsequent request replays the corrupted history and fails with a non-retryable 400. Session permanently dead. Session `f11035d0-7407-4b01-8b8a-b9aaf785457d` made 63 advisor calls; 62 well-formed; the 63rd is broken. Lines 79113 and 79116 share one `message.id` (`msg_011CdyMt`). Error: `API Error: 400 messages.83.content.0: unexpected tool_use_id found in advisor_tool_result blocks: srvtoolu_01MF7bundmTNou6yC7iNqAs6`.

Cousins cite-only (do NOT rebuild / do NOT conflate): #81397 (Stop-hook injector, same contiguity break), #92509 (server tool result separated by interleaved system messages), #81233 / #60523 (compaction — different).

Backups cite-only (next focus only — do not auto-pick): #94417 #94452 #94451 #94430 #94458 #93924 #93770 #93777 #94151.

Hypothesis (NON-BINDING): local_command records are appended to the open assistant message while server_tool_use awaits advisor_tool_result; parentUuid chain breaks; API 400 forever. Offered as a scoring narrative, not a source-root-cause claim.

Why this solution: spliced-parchment / editorial-desk booth scores contiguous vs tmesis after a mid-inject.

Why not a clone: paradigm and UI distinct from Vedette/Orloj/Brisure/Diptych/Vizard/Treacle/Somnus/Cresset/Dictabelt/Lemure/Cancellans/Arras and from #81397 Stop-hook / #81233 #60523 compaction. Educational diagnostic product, not a Claude Code fix.

## Ship notes

- Idle **contiguous** / seeded **tmesis** / path **mid-inject**
- Phrase: Score tmesis or admit contiguous.
- Folder: `projects/tmesis/`
- Live: https://hermes-playground-green.vercel.app/tmesis/
- Catalog: featured Tmesis #381; Vedette unfeatured
- Fonts: Cormorant Garamond + Source Sans 3 + IBM Plex Mono
- Palette: parchment `#F4E8D0`, walnut `#2C1B12`, vermillion `#C43C2C`, seal `#8B1E1E`, ink blue `#243B55`, gold `#C4A46A`, night `#14100C`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
