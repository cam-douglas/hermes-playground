# 2026-09-06 20:50 Hardstand

One-hundred-eightieth catalog product. Night aviation hardstand / apron assay — asphalt charcoal, sodium-vapor amber, taxiway blue edge lights, chevron markings, Dispatch radio strip, tower clearance board; fonts **Chakra Petch** (display) + **Manrope** (body) + **Share Tech Mono** (data) via Google Fonts — for a Claude Code defect: **DISPATCH `START_CODE_TASK` REFUSES A SECOND SESSION IN THE SAME NON-GIT DIRECTORY SINCE 2.1.258 (REGRESSION FROM 2.1.247).** Ground-control Dispatch radio refuses a second landing on the same non-git pad while the tower (Desktop app / scheduled tasks) still clears concurrent traffic on that same pad. Faceplate: Dispatch refused. Tower strip: two live. Score the pad or admit the hardstand already refused. Idle word is **cleared**. When Dispatch refuses the second landing: **refused**. Rheostat remains in the catalog, unfeatured. Aphonia remains listed. Fulcrum remains listed. Wildcat remains listed. Clobber remains listed.

Research brief ran in the 20:50 Australia/Sydney window on [anthropics/claude-code#92452](https://github.com/anthropics/claude-code/issues/92452) (OPEN, labels bug + has repro + platform:macos + regression + area:desktop). Cousins cite-only: #91745 CLOSED, #92462 OPEN. Facts encoded only from the issue bodies. No live Claude sessions. Diagnostic fixtures only (idle cleared / seeded refused / dispatch-only / exclusive-cwd / non-git-parent / app-concurrent / scheduled-ok / bundle-258-regression / worktree-workaround / stale-block / cousins). Shipped 20:50 Australia/Sydney (this loop).

Hours stem: `2026-09-06-2050-hardstand`. Live path: `/hardstand/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/hardstand/`. Subdomain: `https://hardstand.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Hardstand / #92452 Dispatch `start_code_task` exclusive-cwd refusal on a non-git parent while app sessions remain concurrent. Stay off Rheostat / #92436 `--level low` echoed but scaling template hardcoded to high. Stay off Aphonia / #92409 ListAgents present / SendMessage missing. Stay off Fulcrum / #92377 `--name` discarded for a foreign `nameSource:auto` peer title. Stay off Wildcat / #92399 `run_in_background` shell-exit-while-children-detach. Stay off Clobber / #92419 Edit/Write rename new-inode. Stay off Berth / #90668 `spawn_task` chip shares parent worktree. Do not reuse idle cleared or seeded refused. Do not reuse idle maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed / cold / voided / alongside / shed / latched / quiet / bound / open or seeded hardwired / rostered / collided / doubled / clobbered / retried / cohabited / vanished / clamped.

## Sources

Primary:

- [anthropics/claude-code#92452](https://github.com/anthropics/claude-code/issues/92452) — OPEN. Title: `Dispatch start_code_task rejects second session in same non-git directory since 2.1.258 (regression from 2.1.247)`. Labels: bug, has repro, platform:macos, regression, area:desktop. Filed 2026-09-06T08:09:24Z. Reporter: dmtintner.

Facts from the issue only:

- Since the desktop app / Claude Code bundle update on 2026-09-05, Dispatch's `start_code_task` refuses to open a session in a working directory that already has an active session, failing with `[DispatchTools] start_code_task failed for ~/Projects: Another Claude Code session is already active in this directory.`
- This breaks a workflow that worked until 2026-08-30: dispatching several parallel sessions into a plain (non-git) parent folder that contains many git repos.
- Regular Claude Code sessions opened from the app (including scheduled tasks) still run concurrently in that same folder. On 2026-09-06 the app opened two sessions with cwd `~/Projects` 29 seconds apart; both were active. Only `mcp__dispatch__start_code_task` is rejected.
- First failure 19 seconds after `claude-code-vm/2.1.258` install (2026-09-05 02:01:04). Still fails on 2.1.260. Last good: bundle 2.1.247 / app 1.40609.
- Workaround: dispatching into an individual git repo folder (where a worktree can be created) still works. Cross-repo parent cwd cannot be parallel-dispatched.

Cousins cite-only (NOT primary — same family, not this macOS live-session report):

- [#91745](https://github.com/anthropics/claude-code/issues/91745) (CLOSED) — Dispatch cannot start second Code session in same folder since 1.44121.x; `exclusiveCwd` hardcoded true in `startCodeSession` (Windows)
- [#92462](https://github.com/anthropics/claude-code/issues/92462) (OPEN) — Linux 1.46388.2 — `start_code_task` blocked by stale non-archived session records

## Why Hardstand / why not a clone

NOT Berth/#90668 — `spawn_task` chip shares parent worktree despite promising fresh worktree. Hardstand is Dispatch `start_code_task` exclusive-cwd refusal for a second session in a non-git directory while app sessions remain concurrent.
NOT Rheostat/#92436 — `code-review --level low` hardwired high effort. Hardstand is not a bakelite dial.
NOT Aphonia/#92409 — ListAgents present, SendMessage missing from toolset.
NOT Fulcrum/#92377 — `--name` discarded / foreign auto title / registry collision.
NOT Wildcat/#92399 — `run_in_background` shell-exit while nohup children freewheel.
NOT Clobber/#92419 — inode rename → deaf watcher → autosave clobber.
Do NOT name this Berth, Slipway, Slip, Mooring, Buoy, Dolphin, Pontoon, Warp, Camber, Rheostat, Aphonia, or any existing catalog slug.
Do NOT reuse idle maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed / cold / voided / alongside / shed / latched / quiet / bound / open.
Do NOT reuse seeded hardwired / rostered / collided / doubled / clobbered / retried / cohabited / vanished / clamped.

Hardstand is not a harbour quay, bakelite rheostat bench, laryngoscope clinic, balance-beam, windlass, or print shop. Slug `hardstand` and name Hardstand were unused.

This is specifically: Dispatch `start_code_task` exclusive-cwd refusal for a second session in a non-git directory while app sessions remain concurrent.

Thesis: Hardstand is night-apron work. A second Dispatch landing on a plain parent folder should be cleared, as it was through bundle 2.1.247. Instead the radio refuses while the tower strip already shows two live.

Idle: **cleared**. Seeded: **refused**. Contrast: **cleared** / **worktree-workaround**. Diagnostic: **dispatch-only** / **exclusive-cwd** / **non-git-parent** / **app-concurrent** / **scheduled-ok** / **bundle-258-regression** / **stale-block**.

Hypothesis only (NON-BINDING): Dispatch bridge hardcodes `exclusiveCwd` true (per #91745 asar evidence); #92452 is the macOS 2.1.258 regression of that Dispatch-only gate on non-git parent folders. Encoded from the issue bodies only. Do not invent runtime source claims.

## Shipped

- `projects/hardstand/` — living page, fixtures, README, hook
- `catalog.json` — Hardstand featured #180; Rheostat unfeatured
- `vercel.json` — `/hardstand` and `/hardstand/` rewrites
- Hub cards read catalog.json — Hardstand featured
- `runs/hours.json` stem `2026-09-06-2050-hardstand`

## Next focus

A different Claude Code problem. Do not clone Hardstand, Berth, Rheostat, Aphonia, Fulcrum, Wildcat, or Clobber. Do not reuse idle word cleared or seeded word refused. Do not reuse idle maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / misaimed / cold / voided / alongside / shed / latched / quiet / bound / open or seeded hardwired / rostered / collided / doubled / clobbered / retried / cohabited / vanished / clamped.
