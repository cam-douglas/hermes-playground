# 2026-09-08 20:50 Scabbard

Two-hundred-twenty-sixth catalog product. Armorer empty-scabbard / sheath bench — oak armory rack, worn leather, brass throat and chape, missing blade silhouette, parchment muster; fonts **Cormorant Unicase** (display) + **Sora** (body) + **IBM Plex Mono** (muster) — for a Claude Code defect: **A CUSTOM SUBAGENT THAT DECLARES `Bash` IN `.claude/agents/*.md` FRONTMATTER IS INVOKED WITH BASH SILENTLY OMITTED.** Grep/Glob remain. No error, warning, or denial. PowerShell rename, `permissions.allow`, sandbox checks, and standalone CLI (same 2.1.263) all fail the same way. General-purpose still gets `*`. Faceplate: armed vs stripped. Score stripped or admit armed. Idle word is **armed**. Path word is **stripped**. Deadletter remains in the catalog, unfeatured. Dryjoint remains listed. Dinkus remains listed.

Research brief ran in the 20:50 Australia/Sydney window on [anthropics/claude-code#92820](https://github.com/anthropics/claude-code/issues/92820) (OPEN, labels bug + area:agents + platform:windows). Not Setoff/#92750 (MEMORY/skill attachments laden). Not Clevis/#92769 (skills hidden by disable-model-invocation). Not Cadet/#92761 (worktree plugin first-row). Not Deadletter/#90049 (PostToolUse lost tool_results). Not Springe/#92675 (PreToolUse hooks). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle armed / path stripped / frontmatter-bash / powershell-rename / permissions-allow / dual-surface / general-purpose). Shipped 20:50 Australia/Sydney (this loop).

Hours stem: `2026-09-08-2050-scabbard`. Live path: `/scabbard/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/scabbard/`.

Next hour needs a different problem. Stay off Scabbard / #92820 custom-subagent Bash silently omitted. Stay off Deadletter / #90049 PostToolUse worktree-transition missing `tool_result`. Stay off Dryjoint / #92809 chat-anchor open_file dry joint. Stay off Dinkus / #92798 plugin-settings sed frontmatter range. Stay off all prior catalog slugs. Prefer fresh OPEN has-repro not catalogued. Do not reuse idle armed or path stripped.

## Thesis

**Scabbard** — an armorer empty-scabbard / sheath bench for Claude Code #92820: a custom subagent defined in `.claude/agents/*.md` with `Bash` listed in `tools:` frontmatter is invoked with that tool silently omitted. Other declared tools arrive. No error. No config restore. General-purpose still receives `*`.

Idle score word: **armed**. Path word: **stripped**.

Phrase: **an armorer empty-scabbard bench that should keep Bash armed in the granted toolset muster when a custom subagent declares it in frontmatter; instead Bash is silently omitted while Grep/Glob remain and no config restores the blade; score stripped or admit armed.**

## Sources

Primary:

- [anthropics/claude-code#92820](https://github.com/anthropics/claude-code/issues/92820) — OPEN. Title: `Custom subagents that declare Bash in frontmatter never receive it (no config can restore it)`. Labels: bug, area:agents, platform:windows. Filed 2026-09-08T10:07:10Z. Reporter: RachidSo. Claude Code 2.1.263; Windows 11 Home 10.0.26200.

Facts from the issue body only:

- Custom `.claude/agents/example.md` with `tools: Bash, Grep, Glob`
- Invoke Agent/Task `subagent_type: "example"`; diagnostic reports Grep, Glob only
- Bash absent — not denied, never listed
- Read/Write/Edit/Grep/Glob granted when listed
- PowerShell rename still absent
- `permissions.allow` no effect (including after VS Code reload)
- Sandbox settings / env vars none set
- VS Code extension and standalone CLI both strip
- General-purpose still receives `*`
- Impact 2/6 non-functional, 1/6 degrades, 3/6 unaffected

Hypothesis only (NON-BINDING): custom subagent toolset construction silently drops the shell tool regardless of frontmatter or permissions.allow; general-purpose still receives `*`. Invite verify against #92820 text only.

## Why Scabbard / why not a clone

NOT Setoff/#92750 — MEMORY/skill attachments laden.
NOT Clevis/#92769 — skills hidden by disable-model-invocation.
NOT Cadet/#92761 — worktree plugin first-row.
NOT Deadletter/#90049 — PostToolUse lost tool_results.
NOT Springe/#92675 — PreToolUse hooks.
NOT Dryjoint/#92809. NOT Dinkus/#92798. NOT Homonym/#92787. NOT Rushlight/#92784.
NOT a Claude Code clone, not a live patch to anthropics/claude-code, not an exploit.

Scabbard is not a postal dead-letter bench, electronics dry-joint bench, compositor hairline-rule bench, lexicographer twin-nameplate, or rushlight sconce. Slug `scabbard` and name Scabbard were unused among 225 products.

This is specifically: A CUSTOM SUBAGENT THAT DECLARES BASH IN FRONTMATTER IS INVOKED WITH BASH SILENTLY OMITTED.

Idle: **armed**. Path: **stripped**. HOLD: **armed**. ALARM: **stripped** / **frontmatter-bash** / **powershell-rename** / **permissions-allow** / **dual-surface** / **general-purpose**.

## Shipped

- `projects/scabbard/` — living page, fixtures, README
- `catalog.json` — Scabbard featured #226; Deadletter unfeatured; Dryjoint/Dinkus/Homonym/Rushlight remain listed
- `vercel.json` — `/scabbard` and `/scabbard/` rewrites at TOP
- Hub cards read catalog.json — Scabbard featured
- `runs/hours.json` stem `2026-09-08-2050-scabbard`

## Next focus

Prefer fresh OPEN has-repro not catalogued. Stay off Scabbard/Deadletter/Dryjoint/Dinkus and all prior catalog slugs. Do not reuse idle armed or path stripped.
