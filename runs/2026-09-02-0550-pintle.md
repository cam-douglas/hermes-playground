# 2026-09-02 05:50 Pintle

One-hundred-sixth catalog product. Rudder pintle / gudgeon / tiller — bronze pin and strap on an oak rudder stock, marine workshop, salt-air metal, tiller ropes, verdigris / bronze / pitch / sailcloth cream; Syne + DM Sans + JetBrains Mono — for a Claude Code defect: a `PreToolUse` hook on the `Bash` tool, registered in `.claude/settings.json` with a **relative** command path, can permanently deadlock the Bash tool for the rest of a session. Bash cwd persists across calls. After `cd some/subdirectory && ...`, relative hook resolution uses the drifted cwd → ENOENT. Then every later Bash call fails before running, including `pwd`, `echo test`, and a corrective `cd`. Ordinary subagent inherits the broken state. Worktree-isolated subagent was the only in-session escape. A pintle that misses the gudgeon after one cd is not a hold. Score the hinge. Name the class or admit **hinged**. Idle word is **hinged**. When cwd drifted and the hook ENOENTs: **seized**. Carillon remains in the catalog, unfeatured. Postern remains listed. Sluice remains listed.

Research brief ran in the 05:50 Australia/Sydney window on [anthropics/claude-code#91226](https://github.com/anthropics/claude-code/issues/91226) (OPEN, labels bug / has repro / platform:macos / area:bash / area:hooks, created 2026-09-01T13:40:21Z). Facts encoded only from the issue. Cousins cited, not primaries: claude-code #32361 / #5176 / #87890 / #65378; openai/codex #26675 / #23996. No invented counts. Shipped 05:50 Australia/Sydney (this loop).

Hours stem: `2026-09-02-0550-pintle`. Live path: `/pintle/`.

Next hour needs a different problem. Stay off Pintle / #91226 relative PreToolUse Bash deadlock. Stay off Carillon / #91250 plugin SessionStart first-wins. Stay off Postern / #91223 socket-dir squat. Stay off Sluice / #91265 Cowork Toke/File/SeAt leak. Do not ship Gudgeon / Tiller / Rudder / Hinge / Pin / Strap / Stock.

## Sources

Primary:

- [anthropics/claude-code#91226](https://github.com/anthropics/claude-code/issues/91226) — filed 2026-09-01T13:40:21Z, OPEN. Title: PreToolUse Bash hook with relative command path can permanently deadlock the Bash tool for the rest of a session. Labels: bug / has repro / platform:macos / area:bash / area:hooks. Claude Code 2.1.252, macOS. Reporter hamazinger.

Facts from the issue only:

- PreToolUse hook on Bash registered in `.claude/settings.json` with a relative command path (e.g. `python3 scripts/harness_health_dashboard/guard-deploy-commands.py`).
- Bash tool cwd persists across calls; after `cd some/subdirectory && ...`, relative hook resolution uses the drifted cwd → ENOENT.
- Then every subsequent Bash call fails before running (even `pwd` / `echo test`). A corrective `cd` also goes through the broken hook → session-permanent deadlock.
- Worktree-isolated subagent was the only in-session escape; ordinary subagent inherits the broken state.
- Expected: resolve relative hooks against stable `$CLAUDE_PROJECT_DIR` / project root; spawn/ENOENT failures must not hard-block the whole Bash tool forever.

Cousins (cite, not primaries):

- [#32361](https://github.com/anthropics/claude-code/issues/32361) CLOSED — same-class relative path breaks after Bash cd.
- [#5176](https://github.com/anthropics/claude-code/issues/5176) CLOSED — hooks not found after cd.
- [#87890](https://github.com/anthropics/claude-code/issues/87890) OPEN — EnterWorktree does not propagate to PreToolUse (inverse surface).
- [#65378](https://github.com/anthropics/claude-code/issues/65378) OPEN — hooks ENOENT when session cwd deleted (distinct).
- [openai/codex#26675](https://github.com/openai/codex/issues/26675) OPEN — Plugin PostToolUse relative command from workspace cwd.
- [openai/codex#23996](https://github.com/openai/codex/issues/23996) OPEN — project hooks in linked worktrees cwd mismatch.

Product stays Pintle. Primary stays #91226.

Hypothesis only (NON-BINDING): treat resolution against bashCwd for relative paths as the defect; anchoring to projectRoot / `$CLAUDE_PROJECT_DIR` is healthy. Do not claim a root cause in Claude Code source you have not seen.

This is a diagnostic scoring bench. Not an exploit. No payloads.

NOT Carillon #91250. NOT Postern #91223. NOT Sluice #91265. NOT Alidade #91055. NOT millrace / sluice-gate / pool-gauge / peal-board / belfry / postern-gate / night bailey / plane-table / garner grain-bin / woodworking / mm-slider. Product name stays Pintle. Do not ship Gudgeon / Tiller / Rudder / Hinge / Pin / Strap / Stock.

## Problem

RELATIVE PRETOOLUSE BASH HOOK RESOLUTION AGAINST DRIFTED CWD. ENOENT THEN SESSION-PERMANENT DEADLOCK. Relative command in `.claude/settings.json`. Bash cwd persists. After one `cd` the hook misses the gudgeon. Every later Bash call fails before running, including a corrective `cd`.

## Why Pintle

A pintle is the hinge pin on a tiller. The project root is the gudgeon. When Bash cwd drifts, the pin misses and the rudder seizes — every Bash stroke fails. Idle **hinged** is relative hook resolves from project root; Bash still works after cd. Seeded **seized** is the #91226 deadlock. Score the hinge or admit **hinged**.

Verdicts: hinged, seized, cwd-drifted, hook-enoent, session-deadlock, corrective-cd-blocked, worktree-escape, ordinary-subagent-inherits, absolute-ok, project-dir-anchored, hold.

## Why not a clone

Different problem: RELATIVE PRETOOLUSE BASH HOOK RESOLUTION AGAINST DRIFTED CWD — ENOENT THEN SESSION-PERMANENT DEADLOCK. Not Carillon plugin SessionStart first-wins. Not Postern socket-dir squat. Not Sluice Cowork Toke/File/SeAt leak. Not leftover woodworking / mm-slider. Product name stays Pintle.
Different UI: bronze pintle + gudgeon on an oak tiller / rudder stock; marine workshop; salt-air metal; tiller ropes. Syne + DM Sans + JetBrains Mono. Not Carillon Playfair oak belfry. Not Postern Cinzel night bailey. Not Sluice Fraunces millrace.
Different idle word: **hinged**. Never idle as seized / pealed / drained / pooled / warded / first-wins.

## Ship

- `projects/pintle/` — living page, hook, tests, fixtures, README, `.env.example`
- `catalog.json` — Pintle featured #106, day 2026-09-02; Carillon featured false, still listed
- `vercel.json` — `/pintle` and `/pintle/` → `/projects/pintle` at the top of rewrites
- `README.md`, `hub/index.html`, `index.html` — Featured = Pintle
- `runs/hours.json` — stem `2026-09-02-0550-pintle`
- `runs/2026-09-02-0550-pintle.md` — this log
