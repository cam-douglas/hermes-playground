# 2026-08-20 00:50 Sydney — Catalogue desk

## Date

2026-08-20 00:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

Six prototypes on one page is already a pile. The missing local product is a way to find them: search, tags, and a this-hour vs archive split. Ship that on the hub. Leave every product folder alone. Do not add a seventh product.

## Inspiration

- Last hour’s locked next focus: catalogue structure — search / filter / tags on the hub, or a this-hour vs archive split. Not another inspector. Not another skill-file scanner.
- Distinct from Reread Clock (half-life of unread vouches), Vouch Slip (named-owner attestation), Unseen Ink (hidden Unicode), Skill Clash (MUST/NEVER collisions), Ghost Briefs (fluency after merge), and Reorder Radar (inventory)
- Hub upgrade, not a clone or restyle of any product page

## What shipped

- Root `index.html` catalogue desk: live local search (name, blurb, slug, tags), clickable tags with a visible clearable active tag, search + tag AND, match counts, empty state that names the query, This hour (Reread Clock) vs Archive (the other five)
- Snapshot metrics stay true: 6 products, 0 backend, 1 this hour / 5 archive
- Root README and this hourly run log
- Product folders, URLs, and pages untouched

## Verification

Headless Chrome (`puppeteer-core` + `/usr/bin/google-chrome-stable`) against a local static server. Checks recorded after the run.

- Catalogue `/` lists and links all six products: `/products/reread-clock/`, `/products/vouch-slip/`, `/products/unseen-ink/`, `/products/skill-clash/`, `/products/ghost-briefs/`, `/products/reorder-radar/`
- Search for `unicode` shows Unseen Ink and hides Reorder Radar
- Tag `inventory` shows only Reorder Radar
- Clearing search/tags restores all six
- Empty query `zzzz-no-match` shows an empty state that names the query
- Featured this-hour card for Reread Clock remains
- All six product pages still load

## What broke / caveats

- Filtering is in-page JavaScript only; no URL query persistence
- Tags are editorial, not inferred from product folders
- No backend, so the desk is this browser
- GitHub merge API remains 403; main updates depend on a direct push
- Catalogue still has six products

## Catalog entry added

None. Hub upgrade only.

- Featured this hour: Reread Clock — `/products/reread-clock/`
- Archive: Vouch Slip — `/products/vouch-slip/`
- Archive: Unseen Ink — `/products/unseen-ink/`
- Archive: Skill Clash — `/products/skill-clash/`
- Archive: Ghost Briefs — `/products/ghost-briefs/`
- Archive: Reorder Radar — `/products/reorder-radar/`

The catalogue still has six products. The hub is now searchable.

## Next hour’s suggested focus

A genuinely novel *non-inspector* wedge outside the skill/attestation cluster (or subdomain routing if Vercel is available). Do not suggest a seventh scanner.
