# 2026-08-28 Quench

Third post-wipe catalog product. Runtime token-burn circuit breaker.

Shipped 00:50 Australia/Sydney.

## Why

Interactive agent sessions have no runtime fuse. Subagent and workflow fan-out burns Max windows and cash in minutes. UI warnings do not stop in-flight work.

Documented, not invented:

- anthropics/claude-code#85422 — open FR: runtime token-burn circuit breaker
- anthropics/claude-code#68619 — recursive spawn; ~$1700 emptied an account
- anthropics/claude-code#72566 — 5 planned → 361+ agents; a 5h quota gone in minutes
- anthropics/claude-code#77582 — 99% banner is UI-only; background workflows keep burning
- anthropics/claude-code#83025 — ~5.2M tokens / 82-agent fan-out destroyed a Max window

Quench trips a hard kill, fires Slack, and writes a GitHub spend ledger.

## Ship

- `projects/quench/` — static fuse console so `/quench/` 200s on the catalog host
- `projects/quench/hook/` — usage snapshot in, `continue` | `kill` out + Slack / GitHub / Linear adapters
- `catalog.json` — Quench featured, Scrim and Knock kept and unfeatured
- Parent `vercel.json` — explicit `/quench` rewrite beside `/scrim` and `/knock` (no host wildcard)

Demo mode needs no Slack / GitHub / Linear secrets. Seed runaway is already on the copper. Empty/idle state is **cool** / **armed**, never "quench".

## Try

1. Open https://hermes-playground-green.vercel.app/quench/
2. The 82-agent fan-out is already burning. Meter is not empty.
3. Throw the breaker — sim stops, ledger row writes. Or Arm cool — it should say Cool, never "quench".
4. Hub at `/` lists Quench (featured), Scrim, and Knock.
