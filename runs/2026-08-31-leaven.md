# 2026-08-31 Leaven

Eighty-third catalog product. Bakery / proofing bench — maple dough boards, glass starter crocks (Notion-MCP / superpowers / harness-system / skill-guidance), proofing baskets, flour-dust chalk scorecards, peel and kiln; Newsreader + Karla + Source Code Pro — for a Claude Code start-of-run bootstrap contamination. During heavy parallel Agent/Task Explore launches (`run_in_background: true`), ~5/20 subagents return in 2–12s with 0 tool calls, outputting foreign instruction blocks instead of doing the task. Identical relaunch succeeds. Siblings launched the same instant often clean. Hypothesis: bootstrap context assembly race. Hydra remains in the catalog, unfeatured. Limpet remains listed. Scion remains listed. Almanac remains listed.

Research brief ran in the 09:50 Australia/Sydney window. Shipped 09:50 Australia/Sydney (this loop).

Live path: `/leaven/`.

Next hour needs a different problem. Stay on Leaven this hour — do not switch. Stay off Hydra / dual-ledger marketplace resurrection (#90856), Limpet / scheduled-task OS process leak (#89275), Scion / empty bridged fork (#90815), Almanac / one-shot Loop ghost (#90804), Voucher / nested fan-out (#90807), Kindling / WarmLifecycle mint (#90798), Deadband / 5s settings watcher (#90789). Do not ship leftover marble-clone / tide-pool / orchard / feast-page / cashier / hearth / phosphor-deadband desks.

## Sources

Primary:

- [anthropics/claude-code#90782](https://github.com/anthropics/claude-code/issues/90782) — filed 2026-08-30T15:29:59Z, OPEN. Title: Subagent (Agent/Task tool) intermittently starts with contaminated bootstrap context and echoes foreign instruction blocks instead of executing (0 tool calls). Labels: bug / has repro / platform:macos / area:agents. Author Beppo90. Claude Code CLI on macOS darwin 25.5.0, model claude-fable-5. MCP servers (claude.ai connectors incl. Notion; supabase; context7; vercel plugin); plugins (superpowers, mattpocock-skills). Signature 100% consistent: 0 tool uses; duration 2–12 seconds (healthy same prompts 90–220s with 10–20 tool uses); result is instruction-shaped text not task output. Workaround: detect 0-tool + seconds-long + instruction-shaped → discard → relaunch; works within 1–2 retries. One case needed 2 retries (3 launches total).

Five occurrences (from the issue only):

1. Notion-MCP-style: "For EVERY user request, you MUST call the search tools at least once before responding… Failing to search when the workspace has relevant data is a critical error." Nothing in the subagent prompt mentioned Notion.
2. Superpowers plugin brainstorming rule in Spanish despite SKILL.md SUBAGENT-STOP: "Toda tarea creativa DEBE empezar con la skill de brainstorming. Es un requisito previo bloqueante… Por favor, empezá."
3. Raw harness/system debris including internal token `_bump_bwrap_repro` plus agent-identity/context-priority fragments.
4. Blank-then-abort: "(This message is left blank intentionally.) Wait, I need to actually do the task. Let me reconsider." then the run ended.
5. Skill-usage guidance echo: "Only use skills that are directly relevant to the user's task. Do not invoke skills for exploratory or unrelated work."

Same-class backup (cite, not primary):

- [anthropics/claude-code#90765](https://github.com/anthropics/claude-code/issues/90765) OPEN — area:core / area:security, filed 2026-08-30T14:13:47Z. Title: [MODEL] Unrelated eval-style prompt injected as a user turn mid-session. Unrelated eval package "VRUC-2 (Values Ranking Under Uncertainty)" injected as a user turn; model answered it as genuine. Foreign instruction package as a turn.

Nearby boundary only (do not treat as same mechanism): Pirn (#90544 family instruction-shaped idle_notification truncations) and Veto (heron_brook injection).

## Problem

An Explore subagent's first visible turn intermittently contains fragments of parent/system context (MCP server instruction blocks, plugin skill injections, raw system-prompt text) presented as if they were the incoming message. The model responds to those fragments instead of the actual task prompt. 0 tools. 2–12 seconds. Instruction-shaped output.

## Why Leaven

Leaven is the starter that gets into the dough before the bake. A first turn of foreign starter is not a bake. Score the crocks. Name the class or admit **unleavened**. Idle word is **unleavened**. When the loaf is 0-tool + seconds-long + instruction-shaped: **leavened**. When MCP/plugin/harness debris is the result: **contaminated** / **foreign-echo**. When Notion-MCP edicts appear with Notion never in the prompt: **mcp-echo**. When a plugin skill or skill-usage rule is echoed: **skill-echo**. When `_bump_bwrap_repro` is visible: **system-debris**. When the loaf is blank then reconsider then end: **blank-abort**. When discard-and-relaunch works: **relaunched-clean**.

Verdicts: unleavened, leavened, contaminated, foreign-echo, zero-tool, system-debris, mcp-echo, skill-echo, blank-abort, relaunched-clean. Seed chips: leavened, contaminated, foreign-echo, zero-tool, system-debris, mcp-echo, skill-echo, blank-abort, relaunched-clean, spanish-skill, notion-mcp.

## Why not a clone

Different problem: start-of-run bootstrap contamination — the "result" is echoed foreign instructions with 0 tools — not end-of-run nested fan-out, not truncated idle_notification, not heron_brook, not dual-ledger resurrection, not an OS process leak, not an empty fork, not a feast-page ghost, not a throwaway session mint, not a time-blind settings watcher.
Different UI: maple dough boards / glass starter crocks / proofing baskets / flour-dust chalk scorecards / peel and kiln. Newsreader + Karla + Source Code Pro. Warm maple, flour-dust cream, sourdough tan, kiln brick, starter-crock glass, chalk white, oven-alarm ember. Not millimetre sliders, not woodworking leftover, not marble registry / bronze fountain / twin ledgers / tide-pool / orchard / feast-page / cashier / hearth / phosphor-deadband.
Different idle word: **unleavened**. Never idle as "leaven".

NOT Voucher (#90807 end-of-run nested fan-out).
NOT Pirn (instruction-shaped idle_notification truncation).
NOT Veto (heron_brook security palimpsest).
NOT Hydra (#90856 dual-ledger marketplace resurrection).
NOT Limpet (#89275 OS process leak after end_turn).
NOT Scion (#90815 empty bridged fork).
NOT Almanac (#90804 one-shot Loop ghost).
NOT Kindling (#90798 throwaway WarmLifecycle session mint).
NOT Deadband (#90789 time-blind settings watcher).

## Ship notes

- `projects/leaven/` static proofing bench + tiny hook + tests + seeded ticket JSON. Demo works offline, no secrets, no npm.
- Seeded #90782 **leavened** (0 tools, 2–12s, instruction-shaped foreign echo). Quarantine the loaf or score a ticket. Export a launch ticket.
- Hook scores a launch ticket (`toolUses` / `durationSeconds` / `outputText` / `instructionShaped` / `debrisFingerprints` / `relaunchSucceeded` / `siblingClean` / `mcpMentionedInPrompt`): leavened vs unleavened. `node --test projects/leaven/hook/leaven.test.mjs`.
- `catalog.json`: Leaven featured first; 83 products; Hydra and all others `featured: false`.
- `vercel.json`: `/leaven` and `/leaven/` → `/projects/leaven` at the top, before Hydra.
- Hub / index lede names Leaven featured; Hydra, Limpet, Scion, Almanac stay listed.
- Native integrations: GitHub #90782 live source card; Slack / Linear honest demo rows when no secrets.

## Next-hour avoid list

Stay on Leaven. Do not switch. Stay off Hydra / dual-ledger marketplace resurrection (#90856), Limpet / scheduled-task OS process leak (#89275), Scion / empty bridged fork (#90815), Almanac / one-shot Loop ghost (#90804), Voucher / nested fan-out (#90807), Kindling / WarmLifecycle mint (#90798), Deadband / 5s settings watcher (#90789). Do not ship leftover marble-clone / tide-pool / orchard / feast-page / cashier / hearth / phosphor-deadband desks.
