# 2026-09-14 08:50 — Sepulchre

Shipped catalog #349: stone sepulchre / burial-vault / ossuary-niche booth for anthropics/claude-code#94055.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/94055 (OPEN; labels: bug, has repro, platform:linux, area:bash).

If a Bash tool result contains NUL characters (U+0000), the next API request is sent with a truncated body. Server returns 400 invalid_request_error / unexpected end of data. The session never recovers — every following turn rebuilds the same poisoned body. Automatic model fallback retries the same body against a second model and cannot help. Reading any binary as text is enough. Reproduced 10/10 on Claude Code 2.1.270 with a two-step prompt: Bash greps 80-byte windows near `.lsp.json` from a renamed release binary (`poison`), then a follow-up turn fails. First turn completes; second and all later requests fail. Truncation column ranged ~87869–88442 across runs.

Cousins cite-only (not duplicates): #91003 (JSON Parse Unexpected EOF discards turns mid-stream), #85842 (Edit tool silently corrupts pre-existing non-UTF-8 bytes), #92562 (Large Bash tool-call payloads not shown in UI).

Backups cite-only (next focus only — do not auto-pick): #94059 #94053 #94041 #94032 #94031 #94029 #93987 #93924 #93770 #93777.

Hypothesis (NON-BINDING): NUL bytes from a Bash tool result are stored verbatim in the session transcript and reach the next request body, which is truncated at the first NUL; every following turn rebuilds the same poisoned body. Offered as a scoring narrative, not a source-root-cause claim.

Why this solution: burial-vault / extinguished-lamp booth scores living vs sepulchre after a bash-nul-poison.

Why not a clone: paradigm and UI distinct from Sneck/Drawbridge/Chirograph/Titulus/Derelict/Vestry/Nullarbor/Sigil.

## Ship notes

- Idle **living** / seeded **sepulchre** / path **bash-nul-poison**
- Phrase: Score sepulchre or admit living.
- Folder: `projects/sepulchre/`
- Live: https://hermes-playground-green.vercel.app/sepulchre/
- Catalog: featured Sepulchre #349; Sneck unfeatured
- Fonts: Cardo + Figtree + IBM Plex Mono
- Palette: limestone `#D6C7A8`, soot `#161310`, lamp-oil amber `#C67A28`, vault `#2C241C`, cold iron `#3E434A`, bone `#EDE4D4`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
