# 2026-09-12 01:50 Cathead

Two-hundred-ninety-sixth catalog product. Ship’s bow cathead / anchor-timber / PTY-slot booth — oak bow timber, iron ring and cat-block, half-catted anchor, slot-vector gauge in multiples of 16, placeholder `cat` line, respawn lever (−k), ENXIO flare, kernel log strip; fonts **Literata** (display) + **Sora** (body) + **Red Hat Mono** (mono) — for a Claude Code defect: **MACOS TEAMMATE SPAWN "FORK FAILED: DEVICE NOT CONFIGURED" — XNU PTMX SLOT-VECTOR RACE TRIGGERED BY CAT PLACEHOLDER + RESPAWN-PANE -K (ROOT CAUSE FOR #77211).** Faceplate: seated vs raced. Score cathead or admit seated. Idle word is **seated**. Seeded word is **raced**. Path word is **ptmx-race**. Anachronism remains in the catalog, unfeatured.

Research brief ran in the 01:50 window on [anthropics/claude-code#93624](https://github.com/anthropics/claude-code/issues/93624) (OPEN). Cousin cite-only: #77211 (closed stale, same symptom) — do not rebuild. Not Anachronism cloud pre-warm checkout. Not Nullarbor plugin HTTP MCP header expand. Not Petard Linux Bash-tool wrapper-argv hoist. Not Aposiopesis statusLine git-cwd mute. Not Disseisin Cowork VM-home evaporation. Backups cite in data only: #93615, #93570, #93589, #93618, #93622, #93652. Facts encoded only from the issue text. No live Claude sessions. Diagnostic fixtures only. Shipped 01:50 (this loop).

Hours stem: `2026-09-12-0150-cathead`. Live path: `/cathead/`. Intended live URL: `https://hermes-playground-green.vercel.app/cathead/`.

## Thesis

**Cathead** — a ship’s bow cathead / anchor-timber / PTY-slot booth for Claude Code #93624: macOS teammate spawn fails with `fork failed: Device not configured` (ENXIO) because TmuxBackend does `split-window … -- cat` (placeholder occupies one PTY) then `respawn-pane -k` which closes the placeholder master and immediately forkpty’s the replacement. On xnu, opening `/dev/ptmx` is two non-atomic steps (`ptmx_clone` picks minor; `ptmx_get_ioctl` grows by `PTMX_GROW_VECTOR=16` only if `pis_free==0`). When system-wide PTY usage is exactly vector_size−1, the placeholder fills the last slot, clone picks `pis_total`, the freed placeholder slot makes `pis_free==1` so grow is skipped → minor out of range → ENXIO. Vector never shrinks; every retry fails identically. Root cause writeup for recurring #77211. Idle seated / seeded raced / path ptmx-race. Score cathead or admit seated.

Idle word: **seated**. Seeded word: **raced**. Path word: **ptmx-race**. Product score word: **cathead**.

Phrase: **when the cat placeholder frees mid-open at a 16-slot boundary, the cathead drops the new pane with ENXIO.**

## Research brief

Sources:

- Primary: https://github.com/anthropics/claude-code/issues/93624
- Cite-only cousins (do not rebuild): #77211
- Backups cite-only (do not auto-pick): #93615, #93570, #93589, #93618, #93622, #93652

Problem: macOS teammate spawn ENXIO; TmuxBackend cat placeholder + respawn-pane -k races xnu ptmx_clone / ptmx_get_ioctl at a 16-slot boundary; grow skipped; vector never shrinks; 6/6 failures; minor 96 out of range.
Why this solution: A bow-cathead booth makes the seated vs raced (pane held vs ENXIO drop) mismatch tangible without cloning continuity-slate, saltbush, siege, speech-break, manor-court, flashback, sanctuary, cloister, or vault UIs.
Why not a clone: different defect, different UX metaphor, different idle/seeded/path vocabulary, different fonts/layout. Especially vs Anachronism #93585 prewarm-latch, Nullarbor #93595 empty-expand, Petard #93607 wrapper-argv, Aposiopesis #93588 git-cwd-mute, Disseisin #93574 home-evaporated.

## Published walk

idle seated (cathead holds the new pane; no cat+respawn race) → split-window -- cat → remain-on-exit failed → respawn-pane -k → ptmx_clone picks pis_total at vector_size−1 → placeholder free skips grow → ptmx_get_ioctl minor 96 out of range → ENXIO → vector never shrinks 6/6 → forkpty-race (wait-exit succeeds) → wait-exit expected → ptmx-race → cathead.

Hypothesis only (NON-BINDING): if TmuxBackend avoided cat+respawn-pane -k (direct split-window command, or wait-for-placeholder-exit before respawn, or ENXIO-triggered forced vector grow on a fresh pane), the race would not fire. Invite verify against #93624 text only. Do not claim Claude Code source root cause. Do not patch anthropics/claude-code.

## Files

- `projects/cathead/` — index.html, cathead.mjs, cathead.test.mjs, data/*.json, README.md
- `catalog.json` — Cathead featured #296; Anachronism unfeatured
- `hub/catalog.json` — Cathead featured; others unfeatured
- `vercel.json` — `/cathead`, `/cathead/`, and `/cathead/:path*` → `/projects/cathead`
