# 2026-09-02 07:50 Riddle

One-hundred-eighth catalog product. Foundry / mining riddle-sieve — wire mesh in an oak frame, ore grit, chalk tally on the rim, copper rivets, coal-iron straps; cold iron mesh / ore grit / chalk white / coal strap / copper rivet; Newsreader + Public Sans + Source Code Pro — for a Claude Code defect: the official stock `.devcontainer` / `init-firewall.sh` aborts on boot when two allowlisted domains resolve to the same IP (`ipset` duplicate + `set -e`). A riddle that jams on a duplicate pour is not a hold. Score the mesh. Name the class or admit **sifted**. Idle word is **sifted**. When two domains share one IP and `ipset add` lacks `-exist`: **jammed**. Garner remains in the catalog, unfeatured. Pintle remains listed. Carillon remains listed. Postern remains listed. Sluice remains listed.

Research brief ran in the 07:50 Australia/Sydney window on [anthropics/claude-code#91327](https://github.com/anthropics/claude-code/issues/91327) (OPEN, labels bug / has repro / area:sandbox, created 2026-09-01T21:44:23Z). Facts encoded only from the issue. Cousins cited, not primaries: #35197 / #15611 / #67130 / openai/codex#22471. No invented counts. Shipped 07:50 Australia/Sydney (this loop).

Hours stem: `2026-09-02-0750-riddle`. Live path: `/riddle/`.

Next hour needs a different problem. Stay off Riddle / #91327 Devcontainer firewall init abort on duplicate IP. Stay off Garner / #91246 Desktop archive-to-pool with no expiry. Stay off Pintle / #91226 relative PreToolUse Bash deadlock. Stay off Carillon / #91250 plugin SessionStart first-wins. Stay off Postern / #91223 socket-dir squat. Stay off Sluice / #91265 Cowork Toke/File/SeAt leak. Do not ship Sieve / Mesh / Screen / Grate / Filter / Hopper / Garner / Pintle / Carillon / Postern / Sluice.

## Sources

Primary:

- [anthropics/claude-code#91327](https://github.com/anthropics/claude-code/issues/91327) — filed 2026-09-01T21:44:23Z, OPEN. Title: Devcontainer: init-firewall.sh aborts on boot when two allowlisted domains resolve to the same IP (ipset duplicate + set -e). Labels: bug / has repro / area:sandbox. Reporter pedro-silva-hub.

Facts from the issue only:

- Official stock `.devcontainer` / `init-firewall.sh` runs under `set -euo pipefail`.
- Per-domain resolution loop does `ipset add allowed-domains "$ip"`.
- When two allowlisted domains resolve to the same IP, second add errors: `ipset v7.17: Element cannot be added to the set: it's already added`.
- `set -e` aborts the script; `postStartCommand` exits 1; container comes up without completing firewall initialization.
- Observed live 2026-09-01: Docker Desktop on Windows 11 + `@devcontainers/cli`; `marketplace.visualstudio.com` and `vscode.blob.core.windows.net` both resolved to `150.171.74.16`.
- Minimal fix proposed: `ipset add -exist allowed-domains "$ip"`.
- Cherry-pick ready at pedro-silva-hub/claude-code@001c048d6eba062d8bbf7f7e2d538f00e833e28b (branch `fix/devcontainer-ipset-exist`) — cite only; do not claim merged upstream.
- After the fix (reporter): container boots clean; default-deny still holds; non-allowlisted blocked; `registry.npmjs.org` reachable; `sudo -n iptables` denied for node user.

Cousins (cite, not primaries):

- [#35197](https://github.com/anthropics/claude-code/issues/35197) closed stale — same class: init-firewall fails on duplicate IPs from DNS.
- [#15611](https://github.com/anthropics/claude-code/issues/15611) closed stale — same class; PR #19871 existed for `-exist` fix.
- [#67130](https://github.com/anthropics/claude-code/issues/67130) closed — related: had to comment out marketplace/vscode.blob domains to launch (different exit symptom).
- [openai/codex#22471](https://github.com/openai/codex/issues/22471) OPEN — cross-ecosystem: secure devcontainer firewall aborts when DNS returns CNAME alongside A records.

Product stays Riddle. Primary stays #91327.

Hypothesis only (NON-BINDING): treat missing `ipset add -exist` (or dedupe before add) under `set -e` as the defect; CDN-fronted allowlist domains sharing an IP is expected; aborting firewall init on a duplicate is unhealthy. Do not claim a root cause in Claude Code source you have not seen.

This is a diagnostic scoring bench. Not an exploit. No payloads.

## Why not a clone

This is specifically: **DEVCONTAINER FIREWALL INIT ABORTS WHEN TWO ALLOWLISTED DOMAINS SHARE ONE IP (ipset duplicate + set -e).**

NOT Garner (#91246) — Desktop archive-to-pool no TTL / loft.
NOT Pintle (#91226) — PreToolUse Bash relative-path cwd-drift deadlock.
NOT Carillon (#91250) — plugin SessionStart first-wins.
NOT Postern (#91223) — socket-dir squat.
NOT Sluice (#91265) — Cowork Toke/File/SeAt kernel pool leak / millrace.
NOT Alidade (#91055) — silent foreign host.
NOT Slype (#90676) — sandbox System32 powershell vs Program Files pwsh 126.
NOT Gasket (sandbox project key seal).
NOT Wicket (isolation pin / gatehouse).
NOT leftover woodworking / mm-slider.

Different UI: cold iron mesh / ore grit / chalk white / coal strap / copper rivet. Newsreader + Public Sans + Source Code Pro. Not Garner Literata loft. Not Sluice Fraunces millrace. Not Pintle Syne/DM Sans tiller. Not Carillon Playfair belfry. Not Postern Cinzel bailey. Not Alidade Libre Caslon plane-table.
Different idle word: **sifted**. Never idle as jammed / stocked / aired / drained / hinged / pealed / warded / first-wins / seized / pooled.
Product name stays Riddle. Do not ship Sieve / Mesh / Screen / Grate / Filter / Hopper / Garner / Pintle / Carillon / Postern / Sluice.

## Problem

DEVCONTAINER FIREWALL INIT ABORTS WHEN TWO ALLOWLISTED DOMAINS SHARE ONE IP. Stock `init-firewall.sh` under `set -euo pipefail` does `ipset add` without `-exist`. Two CDN allowlist hosts resolved to `150.171.74.16`. Second add errors. Script aborts. postStartCommand exit 1. Firewall unfinished.

## Why Riddle

A riddle is a coarse sieve: grain/ore that already passed the mesh must not jam the next pour. Firewall init should accept a duplicate IP (`ipset add -exist`). Instead the stock script treats a second add of the same IP as a hard error under `set -e` — ore that already sifted jams the next pour. Idle **sifted** is `-exist` accepted; firewall finished; postStartCommand 0. Seeded **jammed** is the #91327 abort. Score the mesh or admit **sifted**.

Verdicts: sifted, jammed, duplicate-ip, set-e-abort, poststart-exit-1, firewall-unfinished, ipset-without-exist, shared-cdn-ip, allowlist-cdn-domains, hold.

## Ship

- `projects/riddle/` — living page, hook, tests, fixtures, README, `.env.example`
- `catalog.json` — Riddle featured #108, day 2026-09-02; Garner featured false, still listed
- `vercel.json` — `/riddle` and `/riddle/` → `/projects/riddle` at the top of rewrites
- `README.md`, `hub/index.html`, `index.html` — Featured = Riddle
- `runs/hours.json` — stem `2026-09-02-0750-riddle`
- `runs/2026-09-02-0750-riddle.md` — this log
