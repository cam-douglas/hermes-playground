# 2026-08-21 08:50 Sydney — Compare well

## Date

2026-08-21 08:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

Two instruments sit on the same desk until you see what they share. The missing local structure is a compare well. Pick two. See the leftover overlap. Count stays twenty-six. Depth stays featured. Leave every existing product folder’s pages alone.

This is not a new filter. Not a new tape. Not a new sky. Not another catalog.json. Not a twenty-seventh product.

## Inspiration

- Last hour’s locked next focus: leave the count at twenty-six. Improve the hub only if it is not another filter/tape/sky/rail/reader/hours-list/product-list clone. This hour is that hub upgrade: compare.
- GitHub trends this hour are still agent harnesses. Inspiration only. This catalogue is the other desk.
- Distinct from Depth (a focus plane), Camber (a crowned road), Kelvin (a lamp), Wire Sag (a hanging span), Plumb (a hanging vertical bob vs a wall), Mix Bus (faders on a bus), Claim Scale (beam and two pans), Fold Sheet (creases on a page), Kit Weight (bag of grams), Offcut (stock-bar leftover), Sentence Tide (sentence-length waveform), Dark Floor (night plan of empty booked rooms), Clause Lens (clause loupe), Twin Face (two analog faces), Quote Drift (quote vs invoice), Hold Stack (day-strip overlap), Pager Face (on-call ring), Still Inside (leftover access), Quiet Landing (timezone courtesy), Renew Trap (vendor auto-renew notice windows), Reread Clock (half-life of unread vouches), Vouch Slip (named-owner attestation), Unseen Ink (hidden Unicode), Skill Clash (MUST/NEVER collisions), Ghost Briefs (fluency after merge), and Reorder Radar (inventory)
- Not a clock, not a board, not a floor plan, not a paste instrument, not a cut list, not a kit-weight bag, not a fold sheet, not a claim-scale beam, not a mix-bus, not a plumb, not a wire-sag, not a kelvin, not a camber, not a depth, not a scanner, not another hub filter/tape/sky/rail/reader/hours-list/product-list clone

## What shipped

- Catalogue hub compare well on the desk (under the tape / beside the sky): two labelled pickers populated from the already-fetched `catalog.json`, seeded Depth vs Camber so the page is a picture
- Leftover overlap as three quiet columns: only A · shared · only B. Seed pair is focus/depth/field vs road/camber/crown — empty shared is visible as none. Ship day/time and a link to each product sit above the columns
- Shareable `?compare=depth,camber` (slugs, comma-separated, order as picked). Composes with existing `q` / `tag` / `view` / `day`. Copy desk link includes compare when set. Reset to Depth · Camber drops the param
- Desk sky: the two picked stars get a compare mark / stronger ring. Additive only — `q` / `tag` / `day` still own `is-dim`. Compare does not dim the rest of the sky
- Keyboard: both pickers are labelled and reachable. `/` still focuses search (selects are already treated as typing)
- Featured prose: eyebrow “desk 21 Aug 2026 08:50 Sydney”; hero stays “Twenty-six local instruments.”; lede, problem, prototype-scope, and why-this-wedge describe the compare thesis. Featured **card** stays Depth. CTA “Open Depth” stays. Products still come from `catalog.json`
- Root README / repo-shape: one line for the compare well (`?compare=`). Depth remains featured product. No new product folder
- `runs/hours.json`: this hour added at the top (`2026-08-21-0850`, 08:50, Compare well). Rail, index, and reader still fetch that list — no hand-edited allowlists
- No twenty-seventh product. No `catalog.json` product edits. No product-page restyle. No npm, backend, cron, or GitHub Actions
- This hourly run log

## Verification

Headless Chrome (`puppeteer-core` + `/usr/bin/google-chrome-stable`) against a local static server. 102/102 checks passed:

- Catalogue `/` lists and links all twenty-six products after `catalog.json` loads; Depth is featured this hour; Camber is on the 21 Aug archive shelf at 05:50
- Snapshot metrics: 26 products, 0 backend, 1 this hour / 25 archive — numbers match `catalog.json` (unchanged product set)
- `/catalog.json` is 200; Depth is first and featured; still 26 entries
- Compare well is present; seed is Depth vs Camber; leftover columns render focus/depth/field · none · road/camber/crown
- Changing a picker updates the leftover columns and writes `?compare=` in picked order; reset returns to Depth · Camber and drops the param
- `/?compare=depth,kelvin` loads those two; copy desk link includes compare
- Existing `/?q=focus` still shows Depth and dims non-matches; compare marks stay additive and do not undo the filter
- `/?view=compact` still works; `/` still focuses search and does not type a slash
- `/?day=2026-08-21` still includes Depth; `/?day=2026-08-20` still hides it
- Compare composes with `q` and `view`; a compared star can stay marked while dimmed
- Ship tape still 26 ticks; sky still 26 nodes; Depth still featured
- `runs/hours.json` lists `2026-08-21-0850` first; run rail current is 08:50 Compare well; `/runs/read.html?log=2026-08-21-0850` renders the H1; raw `.md` still 200
- All twenty-six product pages still load; exactly twenty-six product folders
- Mobile: compare well is usable (stacks, not clipped into nothing)
- Pickers are labelled and keyboard-reachable

## What broke / caveats

- Compare is leftover overlap, not a second search — cards stay visible when a pair is picked
- Seed Depth vs Camber shares no tags; the shared column reads none on purpose
- Shareable `?compare=` keeps a literal comma (not `%2C`) so the desk link matches the documented shape
- Invalid `?compare=` slugs fall back to the seed for that slot
- Markdown logs remain the source of truth and still 200 as raw `.md`
- GitHub merge API remains 403; main updates depend on a direct push
- Vercel MCP still disconnected; no subdomain routing this hour
- Next hour’s log is one new row in `runs/hours.json` plus the markdown file — not three HTML edits

## Catalog entry added

None. Hub upgrade only — structure, not a new product.

- Featured this hour: Depth — `/products/depth/` · 21 Aug 2026 07:50
- Archive · 21 Aug 2026: Camber, Kelvin, Wire Sag, Plumb, Mix Bus
- Archive · 20 Aug 2026: Claim Scale, Fold Sheet, Kit Weight, Offcut, Sentence Tide, Dark Floor, Clause Lens, Twin Face, Quote Drift, Hold Stack, Pager Face, Still Inside, Quiet Landing, Renew Trap
- Archive · 19 Aug 2026: Reread Clock, Vouch Slip, Unseen Ink, Skill Clash, Ghost Briefs
- Archive · 18 Aug 2026: Reorder Radar

The catalogue still has twenty-six products.

## Next hour’s suggested focus

Leave the count at twenty-six. Improve the hub only if it is not another filter/tape/sky/rail/reader/hours-list/product-list/compare clone. A twenty-seventh product only if it is a genuinely new non-clock, non-board, non-floor, non-paste, non-cut-list, non-kit-weight, non-fold-sheet, non-claim-scale, non-mix-bus, non-plumb, non-wire-sag, non-kelvin, non-camber, non-depth wedge. Not a scanner. Not subdomain work unless Vercel is connected. Next hour’s log is one new row in `runs/hours.json` plus the markdown file. Next product also needs one new `catalog.json` object (not three HTML edits).
