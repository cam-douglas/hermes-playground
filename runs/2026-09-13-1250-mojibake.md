# 2026-09-13 12:50 — Mojibake

Shipped catalog #330: compositor / foul-proof / geta-tofu booth for anthropics/claude-code#93848.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/93848 (OPEN; labels: bug, has repro, platform:windows, area:core).

In headless sessions (`claude -p`) on Windows, the text of the user's global `CLAUDE.md` that Claude Code embeds in the first user message (`messages[0].content[0].text`) intermittently arrives at the API with one multibyte character replaced by three U+FFFD replacement characters. The file on disk is valid UTF-8 and never changes. Claude Code 2.1.258 native Windows; Windows 11 22H2 (10.0.22621); PowerShell 7; `%USERPROFILE%\.claude\CLAUDE.md` 19,597 bytes UTF-8 without BOM, Korean prose; `bytes.decode('utf-8')` strict OK. Clean (14 of 17): `'바람이 불어 창문이 흔들리는 탓에 …'`. Corrupted (3 of 17): `'바람이 ���어 창문이 흔들리는 탓에 …'`. `불` (UTF-8 `EB B6 88`) → three U+FFFD at char index 3615; UTF-8 offsets 7,982 / 7,665 — not a 4/8/16 KiB boundary. Same position across four sessions; only *whether* a request is corrupted varies. Prompt prefix changes → full cache miss (sid `8fdae59a` vs `600ce64a`).

Cousins cite-only (not duplicates): #40396 (closed; Korean U+FFFD in responses on macOS); #88836 (AskUserQuestion newlines → U+FFFD since 2.1.235).

Backups cite-only (next focus only — do not auto-pick): #93772 #93770 #93777 #93782 #93889 #93821 #93811 #93809 #93823 #93929 #93924 #93925.

Hypothesis (NON-BINDING): an encoding-boundary or Windows wchar round-trip on the embed path may substitute three U+FFFD for one Hangul syllable. The reporter could not find the pattern that decides which request is corrupted. Offered from published issue text only.

Why this solution: mojibake/compositor booth scores verbatim vs mojibaked fffd-spall.

Why not a clone: paradigm and UI distinct from all prior catalog entries especially Scissel/Feoffee/Apograph/Airlock.

## Ship notes

- Idle **verbatim** / seeded **mojibaked** / path **fffd-spall**
- Phrase: Score mojibake or admit verbatim
- Folder: `projects/mojibake/`
- Live: https://hermes-playground-green.vercel.app/mojibake/
- Catalog: featured Mojibake #330; Scissel unfeatured
- Fonts: Syne + Source Sans 3 + JetBrains Mono
- Palette: rice-paper `#F4EFE6`, ink `#1A1612`, geta magenta `#C41E6A`, foul-proof amber `#C9892E`, cache-hit teal `#1F6F6A`, slate rule `#3D3832`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
