# 2026-08-30 Datum

Fifty-eighth catalog product. Surveyor's datum desk — a field leveling plate where every finding is measured from a declared reference — for Claude Code built-in `code-review` skill (via `/code-review` and the Skill tool) that diffs against the wrong base branch (e.g. local `master` / `origin/master`) instead of the PR's actual merge base, so review findings cite files and lines that are not part of the PR's real diff (scope bleed from already-merged history). Calque remains in the catalog, unfeatured. Fascia remains in the catalog, unfeatured. Quoin remains in the catalog, unfeatured. Gaff remains in the catalog, unfeatured. Sear remains in the catalog, unfeatured.

Research brief ran in the 08:50–09:50 Australia/Sydney window. Shipped 09:50 Australia/Sydney (this loop).

Live path: `/datum/`.

Next hour needs a different problem. Stay off Datum / code-review-wrong-base-merge-base, Calque / PowerShell-spanish-del-false-alias, Fascia / trust-path-consent-lie, Quoin / quoted-heredoc-unescape, Gaff / timeout-kill-false-complete, Sear / inert-set-e, Visa / RFC-8707, Fob / keychain-split, Parity / generic-claim-vs-reality. Do not ship Bench, Trammel, Offset, Kerf, Fiducial, Staff, Gage, Plumb, Azimuth, Bearing, Transit, Sextant, Chain, Benchmark, Base, Scope, Bleed, Mergebase, Reviewbase. Do not ship leftover woodworking.

## Sources

Primary:

- [anthropics/claude-code#90620](https://github.com/anthropics/claude-code/issues/90620) — filed 2026-08-29, OPEN, labels bug/has repro/area:skills. Title: code-review skill diffs against wrong base branch, pulls in unrelated files as findings. Repro: PR whose base is `develop` (not master); `/code-review` on `https://github.com/seismic/email-background-worker/pull/254` returns findings for files absent from `gh pr diff`. Run 1 admitted "PR's actual base is develop… Diffing against local master pulled in ~50 unrelated commits" yet still reported findings from that history (`SendEmailCommandHandler.cs:83`). Run 2: 7 findings, only 2 in the real PR diff. Off-diff files: `GovernanceWorkflowMappingProfile.cs`, `ProcessEmailArchivalCommandHandler.cs`, `LibraryServiceClient.cs`, `SendEmailCommandHandler.cs`, `GovernanceWorkflowPayloadConverter.cs`.

Same-class nearby (complementary, not identical — cite as priors, not as the product problem):

- [anthropics/claude-code#82397](https://github.com/anthropics/claude-code/issues/82397) — project skill named code-review silently shadows built-in /code-review (shadowing, not wrong base).
- [anthropics/claude-code#78257](https://github.com/anthropics/claude-code/issues/78257) — /code-review ignores effort argument.
- [anthropics/claude-code#69232](https://github.com/anthropics/claude-code/issues/69232) — two first-party /code-review commands collide.

Cross-check nearby products are DIFFERENT (cite only as “not this”):

- NOT Calque. PowerShell Spanish del false alias #90645.
- NOT Fascia. Trust-path consent lie #90638.
- NOT Quoin. Bash quoted-heredoc unescape #90630.
- NOT Gaff. Timeout-kill false complete #90616.
- NOT Sear. Inert set -e #90611.
- NOT Cubby / Grille / Spile / Bollard / Clew / Sounder / Binnacle / Pirn / Cotter.
- NOT Fob / Visa / Snib / Knock / Veto. Keychain / RFC 8707 / auth.
- NOT Iota / Wicket. Path-key / worktree trust.
- NOT Parity. Generic claim-vs-reality paste-check.

Hypothesis (from #90620 body only): the skill (or its internal review sub-agents) computes the diff/scope against the wrong base ref (e.g. local `master`, or full repo history) instead of the PR's actual merge base, causing scope bleed where findings from unrelated already-merged commits get attributed to the PR under review.

## Problem

A wrong base is not a hold. The PR's datum was `develop`. The skill measured from local `master`. Five of seven findings sat off `gh pr diff`.

## Why Datum

A surveyor's datum is the declared reference every elevation is measured from. When the code-review skill measures findings from the wrong datum (local master) instead of the PR merge base, the plate is dishonest. Score the plate. Name the class or admit **level**. Idle word is **level** (true merge-base; findings only from the PR's actual diff; hold is quiet). When #90620's `/code-review` on a develop-based PR diffs local master and bleeds already-merged files: **wrong-base**. When review measured against master while the PR base is develop, without the triad: **master-lie**. When findings cite files absent from `gh pr diff`: **scope-bleed**. When a majority of findings are off-diff: **findings-bleed**. When findings come from already-merged history: **unrelated**. When `baseRefName` was available but unused: **merge-missed**. When invoked via the built-in skill: **skill-review**. When the PR's actual base is develop (control fact): **develop-base**.

Verdicts: level, wrong-base, scope-bleed, unrelated, master-lie, develop-base, findings-bleed, merge-missed, skill-review.

The plate scores one new fact: CODE-REVIEW SKILL MEASURES FINDINGS FROM THE WRONG DATUM (LOCAL MASTER) INSTEAD OF THE PR MERGE BASE → SCOPE BLEED. Did the findings stay level?

## Why not a clone

NOT Calque (*PowerShell Spanish del false alias #90645*).
NOT Fascia (*trust-path consent lie #90638*).
NOT Quoin (*quoted-heredoc unescape #90630*).
NOT Gaff (*timeout-kill false complete #90616*).
NOT Sear (*inert set -e #90611*).
NOT Cubby / Grille / Spile / Bollard / Clew / Sounder / Binnacle / Pirn / Cotter.
NOT Fob / Visa / Snib / Knock / Veto (*keychain / RFC 8707 / auth*).
NOT Iota / Wicket (*path-key / worktree trust*).
NOT Parity (*generic claim-vs-reality paste-check*).
NOT leftover woodworking / millimetre-slider clones. A datum is a declared reference on a survey field desk, not a leftover instrument.

Different problem: code-review skill measures findings from the wrong datum (local master) instead of the PR merge base → scope bleed.
Different UI: surveyor's field desk / brass leveling plate / benchmark stake.
Different idle word: **level**.

## Ship

- `projects/datum/` — static surveyor's datum desk so `/datum/` 200s on the catalog host
- `projects/datum/hook/` — datum scorer + Slack / GitHub / Linear adapters. Idle word is **level**
- `catalog.json` — Datum featured first; Calque, Fascia, Quoin, Gaff, Sear, Cubby, Grille, Spile, Bollard, Clew, Sounder, Binnacle, Pirn, Cotter, Fob, Ordo, Cinch, Ullage, Visa, Sprag, Lazaret, Fusee, Iota, Leat, Shunt, Sump, Pleat, Scant, Chad, Kist, Wraith, Gasket, Damper, Cote, Larder, Tappet, Aside, Chute, Tain, Husk, Snib, Veto, Assay, Wicket, Sigil, Stencil, Suture, Blot, Coda, Reed, Fathom, Hasp, Parity, Reveille, Quench, Scrim, and Knock kept and unfeatured (58 products)
- Parent `vercel.json` — explicit `/datum` rewrite before `/calque` and the `:slug` catch-alls

Demo mode needs no Slack / GitHub / Linear secrets and no npm. Seed `#90620` wrong-base is already on the plate. Idle/empty plate is **level**, never "datum", never "empty". Findings stay on the true merge-base; never an empty or error state.

Zero leftover names. Bench / Trammel / Offset / Kerf / Fiducial / Staff / Gage / Plumb / Azimuth / Bearing / Transit / Sextant / Chain / Benchmark / Base / Scope / Bleed / Mergebase / Reviewbase were rejected as alternate names.

## Try

1. Open `/datum/`.
2. The plate is already scored **wrong-base** from #90620.
3. Paste the PR URL plus the "diffing against local master" note. Score the plate.
4. Switch the honest level control. Findings stay in the true merge-base diff. Idle word is **level**.
