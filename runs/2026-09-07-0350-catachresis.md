# 2026-09-07 03:50 Catachresis

One-hundred-eighty-seventh catalog product. Lexicographer stamp desk / misnomer atelier — ink-pad stamps, wrong-word overlay, OAuth challenge strip, scope ledger, JSON-RPC / WWW-Authenticate panels; dark indigo ink on cream folio; fonts **Fraunces** (display) + **Manrope** (body) + **JetBrains Mono** (data) via Google Fonts — for a Claude Code defect: **MCP CLIENT SHOWS "TOKEN EXPIRED" FOR A 403 `insufficient_scope` RESPONSE.** A rubber stamp should name the challenge on the slip. Claude Code instead stamps **EXPIRED** over a live token (**mislabeled**). When the UI names the missing scope from the challenge (**scoped**), that is the hold path. Faceplate: mislabeled vs scoped. Score mislabeled or admit scoped. Idle word is **mislabeled**. Bourdon remains in the catalog, unfeatured. Glowplug remains listed. Hangfire remains listed. Thrash remains listed. Muzzle remains listed.

Research brief ran in the 03:50 Australia/Sydney window on [anthropics/claude-code#92518](https://github.com/anthropics/claude-code/issues/92518) (OPEN, labels bug + has repro + area:mcp). Cousins cite-only: #19066, #28258, #44652 (all locked; still repro on 2.1.258). Facts encoded only from the issue body. No live Claude sessions. No real tokens. Diagnostic fixtures only (idle mislabeled / seeded scoped / no-401 / no-refresh / token-still-valid / insufficient-scope-challenge / events-write-missing / reauth-widened-scopes / cousins). Shipped 03:50 Australia/Sydney (this loop).

Hours stem: `2026-09-07-0350-catachresis`. Live path: `/catachresis/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/catachresis/`. Subdomain: `https://catachresis.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Catachresis / #92518 MCP 403 `insufficient_scope` stamped as token expired. Stay off Bourdon / #92510 host-side Cowork/Code Apple Virtualization VM fd accumulation toward kern.maxfiles. Stay off Solecism / #91558 literal `--git-common-dir/` exclude path. Stay off Glowplug / #85050 silent unlogged Windows startup gaps. Stay off Hangfire / #92478 queued `/compact` demoted to a plain prompt. Stay off Latchkey / #92330 false re-login on a renewable refresh. Stay off Coffer / #91571 OAuth file-store refresh void. Do not reuse idle mislabeled or seeded scoped. Do not reuse idle saturating / vented / preheating / hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen / resolved / literal.

## Sources

Primary:

- [anthropics/claude-code#92518](https://github.com/anthropics/claude-code/issues/92518) — OPEN. Title: `[BUG] MCP client shows "token expired" for a 403 insufficient_scope response`. Labels: bug, has repro, area:mcp. Filed 2026-09-06T16:32:31Z. Reporter: TadejPolajnar. Claude Code 2.1.258; MCP spec 2026-07-28 OAuth 2.1; TypeScript SDK v2.

Facts from the issue only:

- An MCP server returns `403` with `WWW-Authenticate: Bearer error="insufficient_scope"` when a token lacks the scope for a tool. Claude Code shows `MCP server "..." requires re-authorization (token expired)`.
- The token was not expired. It had ~55 minutes of its 1-hour lifetime remaining. The real problem was a missing scope on the grant.
- The message sends you to debug the wrong thing. The reporter spent a full session investigating token lifetimes and refresh handling before reading server logs and finding a 403, not a 401.
- Read tools need `events:read`, write tools need `events:write`. The grant only carried `events:read`.
- `POST /api/mcp` `events_list` → 200; `events_get` → 200; `events_delete` → 403.
- The 403 carries `WWW-Authenticate: Bearer error="insufficient_scope", scope="events:write", resource_metadata="https://example.com/.well-known/oauth-protected-resource/api/mcp"` and JSON-RPC `Insufficient scope: events:write required` (code -32600).
- No 401 was ever returned. Only two 403s.
- No refresh was attempted. Zero `POST /oauth/token` with `grant_type=refresh_token`. The only token call was the `authorization_code` exchange after manual re-auth.
- Access-token TTL 3600s; token issued ~5 minutes before the failure.
- Re-auth "fixed" it by widening scopes, not by refreshing. The second `/authorize` requested `scope=events:read events:write`; the first had not. The same delete then succeeded.
- Expected: distinguish `error="invalid_token"` (401) → "token expired", re-authorize vs `error="insufficient_scope"` (403) → say the scope is missing, and name it. Ideally step-up using the `scope` from the challenge. At minimum, don't call it an expiry.

Cousins cite-only (NOT primary — same family of MCP OAuth scope / step-up, not this 2.1.258 EXPIRED overlay on a live `events:write` challenge):

- [#19066](https://github.com/anthropics/claude-code/issues/19066) — locked; closed without a lasting fix.
- [#28258](https://github.com/anthropics/claude-code/issues/28258) — locked; "resolved in the next release" (2026-03).
- [#44652](https://github.com/anthropics/claude-code/issues/44652) — locked; same behavior six weeks later on 2.1.92; a commenter confirmed it on 2.1.121.

Still reproducing on **2.1.258**.

## Why Catachresis / why not a clone

NOT Bourdon/#92510 — macOS Cowork VM host fd climb. Catachresis is an MCP OAuth challenge-mapping misnomer, not a pressure-gauge fd leak.
NOT Solecism/#91558 — worktree provisioning writes a literal `--git-common-dir/` path. Different bug.
NOT Glowplug/#85050 — Windows silent startup preheat gaps.
NOT Hangfire/#92478 — queued `/compact` demoted to plain prompt (`promptSource:queued`).
NOT Thrash/#88257 — first-prompt TUI freeze / event-loop stall.
NOT Muzzle/#92459 — safe-mode skill_listing attachment leak.
NOT Latchkey/#92330 — Remote Control auto-start demands `/login` while refresh is still renewable.
NOT Coffer/#91571 — Windows OAuth file-store refresh rotation never persisted.
NOT Lethe — session tokens washed downstream after sign-out.
NOT Hysteresis/Hardstand/Rheostat/Aphonia/Fulcrum/Wildcat/Clobber/Watchdog/Understudy/Fairlead/Stroboscope/Heliostat/Frizzen or any prior catalog slug.
Do NOT name this after prior paradigm desks.
Do NOT reuse idle saturating / vented / preheating / hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen / resolved / literal.
Do NOT reuse seeded lit / executed / responsive / excised / rewritten / refused / hardwired / vented.

Catachresis is not a brass Bourdon-tube gauge, manuscript grammar desk, diesel glow-plug bay, or latchkey board. Slug `catachresis` and name Catachresis were unused.

This is specifically: MCP client maps a 403 `insufficient_scope` challenge (named `events:write`) to "token expired" on a live grant.

Thesis: Catachresis is wrong-word work. A lexicographer stamp should name the challenge on the slip. Here the desk stamps **EXPIRED** over `insufficient_scope` (**mislabeled**). When the UI names the missing scope (**scoped**), that is the hold path.

Idle: **mislabeled**. Seeded: **scoped**. HOLD: **scoped**. ALARM: **mislabeled** / **no-401** / **no-refresh** / **token-still-valid** / **insufficient-scope-challenge** / **events-write-missing** / **reauth-widened-scopes** / **cousins**.

Encoded from the issue body only. Do not invent source-code claims.

## Shipped

- `projects/catachresis/` — living page, fixtures, README, hook
- `catalog.json` — Catachresis featured #187; Bourdon unfeatured
- `vercel.json` — `/catachresis` and `/catachresis/` rewrites
- Hub cards read catalog.json — Catachresis featured
- `runs/hours.json` stem `2026-09-07-0350-catachresis`

## Next focus

A different Claude Code problem. Do not clone Catachresis, Bourdon, Solecism, Glowplug, Hangfire, Thrash, Muzzle, Latchkey, or Coffer. Do not reuse idle word mislabeled or seeded word scoped. Do not reuse idle saturating / vented / preheating / hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen / resolved / literal or seeded lit / executed / responsive / excised / rewritten / refused / hardwired / vented.
