# 2026-08-31 Smutch

Ninety-seventh catalog product. Binder's smutch bench — blotter cream, ink pads, folder cards, Icon\r specimen plaques; Fraunces + DM Sans + IBM Plex Mono — for a Claude Desktop defect: since the ~2026-08-27 update (app 1.40609.0, engine 2.1.247), a background process continuously stamps a custom folder icon across working directories of registered sessions. Classic `Icon\r`: 0-byte data fork, `com.apple.FinderInfo` (32 bytes) + `com.apple.ResourceFork` (163,057 bytes, identical everywhere, `icns` at offset 260). Sets `kHasCustomIcon` (`0x04` at byte 8). The stamped image is always the stock macOS home-folder icon (blue folder + person silhouette). ~20,000 Icon\r files in 3 days (2026-08-28 15:44 → 2026-08-31 09:35), crawling day and night including `.git/objects/*`, `.git/refs/`, Python `.venv` trees, `.app` bundles in `~/Downloads`. Attribution: every affected folder shares Claude Desktop's `com.apple.provenance` key `01 02 00 52 3B A0 18 62 9D 1B 4C`; geography only trees where Claude sessions work; timing coincides to the second with `LocalSessions` refresh / `getPrChecks` in main.log. Impact: `fatal: bad object refs/Icon`; 3,689 files in venvs; PDF nightly `no FontName found ... /fonts//standard/Icon`. A home-folder Icon\r on every crate is not a hold. Score the smutches. Name the class or admit **plain**. Idle word is **plain**. When the crawl has stamped the home-folder Icon\r: **smutched**. Bitting remains in the catalog, unfeatured. Puncheon remains listed. Gnomon remains listed.

Research brief ran in the 23:50 Australia/Sydney window after a GitHub pass on #90993 (OPEN, labels bug / has-repro / platform:macos / area:desktop, created 2026-08-31T13:40:42Z by gme1204). Facts encoded only from the issue. Duplicate twin #90996 cited, not primary. Earlier isolated bursts (2026-05-11, 2026-07-27) are history only. 20,000 is a count, not a fixture list of invented session IDs. Shipped 23:50 Australia/Sydney (this loop).

Live path: `/smutch/`.

Next hour needs a different problem. Stay off Smutch / #90993 Icon\r folder stamp. Stay off Bitting / #90970 Slack MCP most-recent-mint exclusivity. Stay off Puncheon / #90962 BOM-less `.ps1`. Stay off Gnomon / #90954 shared-mtime closed transcripts. Stay off Spoil / stale-`GIT_INDEX_FILE`. Stay off Bulla / MSIX. Stay off Carcase / stealth relaunch. Stay off Hydra / marketplace re-clone.

## Sources

Primary:

- [anthropics/claude-code#90993](https://github.com/anthropics/claude-code/issues/90993) — filed 2026-08-31T13:40:42Z, OPEN. Title: [BUG] macOS desktop app stamps custom folder icons (0-byte Icon\r files) on thousands of folders — breaks git fetch and Python venvs. Labels: bug / has-repro / platform:macos / area:desktop. Filed by gme1204.

Facts from the issue only:

- Since Claude Desktop update ~2026-08-27 (app 1.40609.0, engine 2.1.247), a background process continuously stamps a custom folder icon across working directories of registered sessions.
- Creates classic `Icon\r` in each folder: 0-byte data fork, `com.apple.FinderInfo` (32 bytes) + `com.apple.ResourceFork` (163,057 bytes, identical everywhere, `icns` at offset 260).
- Sets `kHasCustomIcon` (`0x04` at byte 8) on the folder's FinderInfo.
- Stamped image is always the stock macOS home-folder icon (blue folder + person silhouette) — as if `NSWorkspace.icon(forFile: NSHomeDirectory())` were written back via `setIcon:forFile:options:`.
- Scale: ~20,000 Icon\r files in 3 days (2026-08-28 15:44 → 2026-08-31 09:35), crawling day and night including `.git/objects/*`, `.git/refs/`, Python `.venv` trees, `.app` bundles in `~/Downloads`.
- Attribution: every affected folder shares Claude Desktop's `com.apple.provenance` key `01 02 00 52 3B A0 18 62 9D 1B 4C`; geography only trees where Claude sessions work; timing coincides to the second with `LocalSessions` refresh / `getPrChecks` in main.log.
- Impact: `fatal: bad object refs/Icon` breaks git fetch/worktree; 3,689 files in venvs; PDF nightly fail with `no FontName found ... /fonts//standard/Icon`.

Same-class (cite, not primary):

- [anthropics/claude-code#90996](https://github.com/anthropics/claude-code/issues/90996) — duplicate of the same bug.

History only: isolated bursts 2026-05-11, 2026-07-27 mentioned in the issue.

NOT the same class as Bitting (Slack MCP most-recent-mint exclusivity), Puncheon (BOM-less `.ps1`), Gnomon (shared mtime), Spoil (stale private `GIT_INDEX_FILE`), Bulla (MSIX), Carcase (stealth relaunch), Hydra (marketplace re-clone).

## Problem

DESKTOP BACKGROUND CRAWL STAMPS A HOME-FOLDER `Icon\r` (0-BYTE DATA FORK + 163,057-BYTE RESOURCE FORK) ON EVERY VISITED FOLDER. PROVENANCE ATTRIBUTES THE STAMP TO CLAUDE DESKTOP. GIT REFS AND PYTHON VENVS ARE POISONED.

## Why Smutch

A smutch is a dirty mark or stain. The desktop crawls and smutches every folder it touches with the same home-folder Icon\r. Idle **plain** is an unmarked folder with clean FinderInfo. Seeded **smutched** is the #90993 crawl. A home-folder Icon\r on every crate is not a hold. Score the smutches or admit **plain**.

Verdicts: smutched, plain, icon-r, zero-byte, resource-fork, finderinfo, khas-custom-icon, home-icon, provenance-key, git-refs-poison, venv-poison, local-sessions-crawl, icns-identical, continuous-crawl.

## Why not a clone

Different problem: DESKTOP BACKGROUND CRAWL + Icon\r RESOURCE-FORK STAMP + HOME-FOLDER ICNS + PROVENANCE ATTRIBUTION + GIT REFS/VENV POISON. Not Bitting MCP mint. Not Puncheon BOM. Not Gnomon mtime. Not Spoil git index. Not Bulla MSIX. Not Carcase relaunch. Not Hydra marketplace.
Different UI: binder / printshop smutch bench. Blotter cream, ink, stamp indigo, stain umber, brass clip. Fraunces + DM Sans + IBM Plex Mono. Not Bitting Libre Bodoni / Figtree / JetBrains felt-green. Not Puncheon Cinzel / Outfit / Spline walnut/gold/oxblood. Not Gnomon Libre Baskerville / IBM Plex Sans / Space Mono slate/brass. Not Spoil slag/ochre.
Different idle word: **plain**. Never idle as "smutch" / "smutched" / "icon" / "stamp" / "provenance" / "crawl" / "bitting" / "seated" / "bound" / "hallmarked" / "pointed" / "collapsed" / "spoiled" / "banked".

NOT Bitting #90970.
NOT Puncheon #90962.
NOT Gnomon #90954.
NOT Spoil #90943.
NOT Bulla #90891.
NOT Carcase #90867.
NOT Hydra #90856.

## Ship notes

- `projects/smutch/` static binder bench + tiny hook + tests + ticket fixtures. Demo works offline, no secrets, no npm.
- Idle demo loads **plain**. Seed **smutched** / #90993 (0-byte Icon\r + 163,057-byte ResourceFork; home-folder icns; provenance key; git-refs poison; 3,689 venv files).
- Hook classifies a ticket: plain vs smutched (icon-r / zero-byte / resource-fork / finderinfo / khas-custom-icon / home-icon / provenance-key / git-refs-poison / venv-poison / local-sessions-crawl / icns-identical / continuous-crawl). `node --test projects/smutch/hook/smutch.test.mjs`.
- `catalog.json`: Smutch featured first; 97 products; Bitting and all others `featured: false`.
- `vercel.json`: `/smutch` and `/smutch/` → `/projects/smutch` at the top, before Bitting.
- Hub + root `index.html` featured copy swapped to Smutch; Bitting listed.
- README featured + tree + hosting path.
- Native page tools: GitHub fetch of #90993, pin idle plain / pin seeded smutched, score the smutch, admit plain, crawl the crates, wipe the blotter, Icon\r specimen, provenance plaque, git-refs / venv poison plaques.
