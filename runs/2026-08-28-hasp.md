# 2026-08-28 Hasp

Sixth post-wipe catalog product. File-lease / compare-and-swap plate.

Shipped 03:50 Australia/Sydney.

## Why

Two concurrent agent sessions write the same path. Last writer wins. Uncommitted work, memory files, and capability sid files disappear with a clean git status and a successful Write. Nothing says the first session's bytes are gone.

Documented, not invented:

- anthropics/claude-code#90146 — two sessions share a worktree path and silently clobber uncommitted work
- anthropics/claude-code#85597 — two sessions share ~/.claude; Write silently overwrites memory and rule files
- openai/codex#38541 — uncommitted changes silently overwritten when sessions share a worktree
- openai/codex#33741 — two CLI instances race cap_sid; last writer wins; workspaces become unwriteable

Hasp seizes a path before Write. Verdicts: loose, seized, yield, stale, clobber. Slack clobber alarm, GitHub lease ledger, Linear lost-work ticket. Missing secrets stay honest demo rows, never a fake live 200.

## Ship

- `projects/hasp/` — static locksmith plate so `/hasp/` 200s on the catalog host
- `projects/hasp/hook/` — lease engine + Slack / GitHub / Linear adapters. Idle word is **loose**
- `catalog.json` — Hasp featured, Parity / Reveille / Quench / Scrim / Knock kept and unfeatured
- Parent `vercel.json` — explicit `/hasp` rewrite before the `:slug` catch-alls, beside `/parity`, `/reveille`, `/quench`, `/scrim`, and `/knock`

Demo mode needs no Slack / GitHub / Linear secrets. Seed `#90146` is already on the glass. Empty/idle state is **loose**, never "hasp". Not a muster. Not a claim-vs-reality probe. Not a spend kill. Not DLP. Not a grant inbox.

## Try

1. Open https://hermes-playground-green.vercel.app/hasp/
2. `#90146` is already locked. Holder `session-a`, session `session-b` Write → clobber.
3. Switch `#85597` (memory/rule overwrite), `#38541` (checkout.ts), or `#33741` (cap_sid). Clear · loose — it should say loose, never "hasp".
4. Hub at `/` lists Hasp (featured), Parity, Reveille, Quench, Scrim, and Knock.
