# 2026-09-07 04:50 Decant

One-hundred-eighty-eighth catalog product. Gravity-pour cellar rack / carboy assay — amber glass, cork, burgundy lees sediment, slate shelf, process-tree ledger, PATH strip vs full-env pour, probe-variable assay panels, Desktop vs Terminal vs VS Code vs Rider vs Zed comparison rails; deep slate + cork brown + amber glass + burgundy lees; fonts **Spectral** (display) + **Karla** (body) + **Roboto Mono** (data) via Google Fonts — for a Claude Desktop defect: **MACOS DESKTOP PASSES ONLY PATH FROM THE LOGIN SHELL; EVERY OTHER VARIABLE IS DROPPED.** The disclaimer already ran the login shell (full bottle). Desktop **skimmed** only PATH into the session glass and left credentials/paths as lees. When the whole login-shell environment is poured (**intact**), that is the hold path. Faceplate: skimmed vs intact. Score skimmed or admit intact. Idle word is **skimmed**. Catachresis remains in the catalog, unfeatured. Bourdon remains listed. Glowplug remains listed. Hangfire remains listed. Thrash remains listed. Muzzle remains listed.

Research brief ran in the 04:50 Australia/Sydney window on [anthropics/claude-code#92515](https://github.com/anthropics/claude-code/issues/92515) (OPEN, labels bug + has repro + platform:macos + area:desktop). Cousins cite-only: #90074, #82890 (same class — desktop env handed to children — across platforms; this product is the macOS PATH-only decant). Facts encoded only from the issue body. No live Claude sessions. No real tokens. Diagnostic fixtures only (idle skimmed / seeded intact / path-only / marker-unset / probed-0-of-8 / disclaimer-path-merge / spawn-inherits / terminal-full / vscode-full / cousins). Shipped 04:50 Australia/Sydney (this loop).

Hours stem: `2026-09-07-0450-decant`. Live path: `/decant/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/decant/`. Subdomain: `https://decant.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Decant / #92515 macOS Desktop PATH-only login-shell skim. Stay off Catachresis / #92518 MCP 403 `insufficient_scope` stamped as token expired. Stay off Bourdon / #92510 host-side Cowork/Code Apple Virtualization VM fd accumulation toward kern.maxfiles. Stay off Glowplug / #85050 silent unlogged Windows startup gaps. Stay off Hangfire / #92478 queued `/compact` demoted to a plain prompt. Stay off Latchkey / #92330 false re-login on a renewable refresh. Stay off Coffer / #91571 OAuth file-store refresh void. Stay off Ullage. Do not reuse idle skimmed or seeded intact. Do not reuse idle mislabeled / saturating / vented / preheating / hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen / resolved / literal.

## Sources

Primary:

- [anthropics/claude-code#92515](https://github.com/anthropics/claude-code/issues/92515) — OPEN. Title: `Claude Desktop passes only PATH from the login shell to Claude Code sessions; every other variable is dropped`. Labels: bug, has repro, platform:macos, area:desktop. Filed 2026-09-06T16:14:02Z. Reporter: Baune8D. macOS 26.6.2 arm64; Claude Desktop bundled CLI 2.1.258; compared vs Claude Code 2.1.260 terminal, VS Code ext 2.1.260, Rider ACP, Zed external.

Facts from the issue only:

- A Claude Code session started from the Claude Desktop app has a fully repaired `PATH` but none of the other environment variables the login shell exported. Tools therefore resolve while the credentials and paths they need are unset, so failures surface far from the cause — a package restore that 401s, a script that raises `KeyError`, a query that silently returns the wrong scope.
- The app already does the expensive part: its `disclaimer` helper runs a login shell. Only `PATH` survives from it.
- Repro: `export MARKER_FROM_ZPROFILE=1` in `~/.zprofile` (not `~/.zshrc`). Desktop session: echo shows `UNSET`; `PATH` still contains directories that only `~/.zprofile` adds. Terminal-launched `claude`: marker set.
- Process tree of the affected session: `claude <- disclaimer <- Claude <- launchd`.
- Variable counts (names only) + 8 probed credential/path vars from `~/.zprofile`:
  - `Claude.app` main: 14 vars, 4-directory default PATH, 0/8 probed
  - `disclaimer` helper: 45 vars, full login PATH, 0/8 probed
  - `claude` session (Desktop): 45 vars, full PATH, **0/8** probed
  - Terminal-launched `claude`: 67 vars, full PATH, **8/8** probed
- Jump 14→45 at disclaimer is PATH merge only — nothing else from the login shell is kept.
- Anything the session spawns inherits the stripped env (hooks + stdio MCP included).
- Other IDE routes on the same machine do NOT do this: VS Code extension (`useTerminal` false) 72 vars all probed set; Rider ACP 94; Zed external 58. VS Code starts from the same bare 14 on the main process but passes the full env to `claude`.
- Do NOT launch the app from a terminal to test (`open -a` contaminates). Control: main process must show ~14 vars + 4-dir PATH.
- Suggested fix (from the issue): in `disclaimer`, pass on the entire environment returned by the login shell it already runs, not PATH alone.

Cousins cite-only (NOT primary — same class of desktop env handed to children, not this macOS PATH-only decant):

- [#90074](https://github.com/anthropics/claude-code/issues/90074) — OPEN. Windows desktop: stdio MCP gets a ~16-var sanitized allowlist; missing `ProgramData` breaks ssh; missing `COMPUTERNAME` breaks Get-VM.
- [#82890](https://github.com/anthropics/claude-code/issues/82890) — OPEN. Linux Desktop: MCP subprocesses spawned without `DISPLAY`/`XAUTHORITY`, breaking OAuth browser login.

## Why Decant / why not a clone

NOT Catachresis/#92518 — MCP 403 `insufficient_scope` stamped as token expired (lexicographer stamp desk).
NOT Bourdon/#92510 — macOS Cowork VM host fd climb. Decant is a Desktop login-shell env skim, not a pressure-gauge fd leak.
NOT Glowplug/#85050 — Windows silent startup preheat gaps.
NOT Hangfire/#92478 — queued `/compact` demoted to plain prompt (`promptSource:queued`).
NOT Thrash/#88257 — first-prompt TUI freeze / event-loop stall.
NOT Muzzle/#92459 — safe-mode skill_listing attachment leak.
NOT Latchkey/#92330 — Remote Control auto-start demands `/login` while refresh is still renewable.
NOT Coffer/#91571 — Windows OAuth file-store refresh rotation never persisted.
NOT Ullage — catalog slug already used; not a leftover wine/ullage copy.
NOT Hysteresis/Hardstand/Rheostat/Aphonia/Solecism/Oubliette/Ephemera/Commutator/Heddle or any prior catalog slug.
Do NOT name this after prior paradigm desks.
Do NOT reuse idle mislabeled / saturating / vented / preheating / hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen / resolved / literal.
Do NOT reuse seeded scoped / lit / executed / responsive / excised / rewritten / refused / hardwired / vented.

Decant is not a lexicographer stamp desk, brass Bourdon-tube gauge, diesel glow-plug bay, or ullage leftover. Slug `decant` and name Decant were unused.

This is specifically: macOS Desktop login-shell environment decanted to PATH-only.

Thesis: Decant is pour-clear-liquid-off-lees work. A carboy should receive the whole login-shell bottle. Here Desktop **skimmed** only PATH. When the whole environment is poured (**intact**), that is the hold path.

Idle: **skimmed**. Seeded: **intact**. HOLD: **intact**. ALARM: **skimmed** / **path-only** / **marker-unset** / **probed-0-of-8** / **disclaimer-path-merge** / **spawn-inherits** / **cousins**.

Encoded from the issue body only. Do not invent source-code claims.

## Shipped

- `projects/decant/` — living page, fixtures, README, hook
- `catalog.json` — Decant featured #188; Catachresis unfeatured
- `vercel.json` — `/decant` and `/decant/` rewrites
- Hub cards read catalog.json — Decant featured
- `runs/hours.json` stem `2026-09-07-0450-decant`

## Next focus

A different Claude Code problem. Do not clone Decant, Catachresis, Bourdon, Glowplug, Hangfire, Thrash, Muzzle, Latchkey, Coffer, or Ullage. Do not reuse idle word skimmed or seeded word intact. Do not reuse idle mislabeled / saturating / vented / preheating / hangfired / thrashing / leaking / remanent / rewritten / cleared / maxed / mute / inherited / freewheeling / deaf / choking / miscast / unguided / dropped / strobing / stolen / dawnlocked / cold / voided / alongside / shed / latched / quiet / bound / open / sostenutoed / frozen / resolved / literal or seeded scoped / lit / executed / responsive / excised / rewritten / refused / hardwired / vented.
