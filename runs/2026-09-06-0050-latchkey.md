# 2026-09-06 00:50 Latchkey

One-hundred-sixtieth catalog product. Night-porter / brass latchkey board / morning latch desk — dark oak board, brass hooks, hanging latchkeys, cool dawn window light, amber brass, slate, linen paper; Cormorant Garamond + Outfit + IBM Plex Mono — for a Claude Code defect: **REMOTE CONTROL AUTO-START REPORTS "RE-LOGIN REQUIRED" WHEN THE OAUTH REFRESH SIMPLY HAS NOT LANDED YET (CREDENTIAL STILL RENEWABLE).** A morning latch that hands the expensive master key while the cheap latch still opens is not an expired login — it is already miskeyed. Score the board or admit the refresh path was still waiting. Idle word is **waiting**. When the guard demands `/login` while refreshToken is still renewable: **miskeyed**. Stubble remains in the catalog, unfeatured. Intake remains listed. Pasteboard remains listed. Spillway remains listed. Blurt remains listed. Macule remains listed. Deadlight remains listed.

Research brief ran in the 00:50 Australia/Sydney window on [anthropics/claude-code#92330](https://github.com/anthropics/claude-code/issues/92330) (OPEN, labels bug + has repro + platform:windows + area:auth). Facts encoded only from the issue body. No invented tokens, logs, or payloads. No live OAuth. Diagnostic fixtures only (published `expiresAt` clock faces). Shipped 00:50 Australia/Sydney (this loop).

Hours stem: `2026-09-06-0050-latchkey`. Live path: `/latchkey/`.

Next hour needs a different problem. Stay off Latchkey / #92330 Remote Control OAuth startup-guard false `/login`. Stay off Deadlight / #92249 Desktop host tool-registry blank. Stay off Stubble / #92328 Write `.cmd` UTF-8 LF + CP932 empty del. Stay off Intake / #92305 CLI piped-stdin double-composition. Stay off Pasteboard / #92312 platform-conditional `chat:imagePaste`. Stay off Spillway / #92311 ultracode early-return. Stay off Blurt / #92275, Macule / #92294 as primaries.

## Sources

Primary:

- [anthropics/claude-code#92330](https://github.com/anthropics/claude-code/issues/92330) — OPEN. Title: `Remote Control auto-start reports "re-login required" when the OAuth refresh simply hasn't landed yet (credential still renewable)`. Labels: bug, has repro, platform:windows, area:auth. Filed 2026-09-05T13:59:38Z. Reporter: Musco46.

Facts from the issue only:

- claude-code 2.1.260 (global npm install, `bin/claude.exe`); Windows 11 Home 10.0.26200; VS Code extension (also reproduces from the terminal); Pro; scopes include `user:sessions:claude_code`.
- With `remoteControlAtStartup: true`, the first session after overnight access-token expiry reliably prints: "Remote Control disconnected — Claude.ai login expired — run /login to restore Remote Control".
- The login is not expired. The refresh token is valid for weeks. `/remote-control` in the same session, seconds later, connects with the same credentials — no `/login` needed.
- Startup guard (2.1.260 minified) after `ys({credentials, storageV5})` only re-reads `expiresAt` via `E3t()`. If still `<= Date.now()` it emits `oauth_expired_unrefreshable` / `[bridge:repl] Skipping: OAuth token expired and refresh failed (re-login required)`, surfaces the `/login` banner via `G?.("failed", Hve, "auth")`, and records `bridgeOauthDeadExpiresAt` / `bridgeOauthDeadFailCount`.
- It never consults `FAn()` (`refreshToken != null`).
- `ys()` can return without refreshing for transient reasons: `OAuthRefreshLockContendedError` on `~/.claude/.oauth_refresh.lock` (stale window 60s) when multiple sessions start, or a slow/unavailable token endpoint at boot.
- Access token ~8h TTL; always goes stale overnight when Claude Code is not running. Every morning the bridge races its own refresh, loses, and declares login dead while refreshToken is nowhere near expiry.
- Concrete machine data from the issue:
  - `bridgeOauthDeadExpiresAt` = 1788573991415 → 2026-09-05 04:06:31 (recorded dead)
  - accessToken `expiresAt` = 1788642627089 → 2026-09-05 23:10:27
  - refreshToken `expiresAt` = 1791042620089 → 2026-10-03 17:50:20 (valid ~4 more weeks)
- Fail-count backoff (`bridgeOauthDeadFailCount >= 3`) resets each morning because a new stale `expiresAt` arrives, so the banner repeats indefinitely.
- Suggested fix in the issue (document only): before reporting auth failure, check refresh path exists; if refreshToken present/unexpired → transient (retry / same path as `/remote-control`); only demand `/login` when no refresh path.

Cousins cite-only (NOT primary):

- [#90688](https://github.com/anthropics/claude-code/issues/90688) — real refresh death HTTP 400
- [#91708](https://github.com/anthropics/claude-code/issues/91708) — concurrent refresh race kills the token family
- [#88951](https://github.com/anthropics/claude-code/issues/88951) — shadowing keychain / silent no-op

Backups (document only, do not build):

- [#92335](https://github.com/anthropics/claude-code/issues/92335) Chrome silent re-auth
- [#92317](https://github.com/anthropics/claude-code/issues/92317) claude-security wrong diff
- [#92318](https://github.com/anthropics/claude-code/issues/92318) dual-device double-register

## Why not Deadlight / Stubble / Intake / Pasteboard / Spillway / Blurt / Macule

Deadlight = Desktop host blanks ListAgents/SendMessage from the tool registry in scheduled-task / Remote Control sessions. Latchkey = OAuth startup guard false-positive `/login` while refreshToken is healthy. Different surface and symptom.
Stubble = Write UTF-8 LF `.cmd` + CP932 lead-byte → empty del / CWD wipe.
Intake = piped stdin double-composition toward the context limit.
Pasteboard = platform-conditional Alt+V image-paste miss.
Spillway = ultracode concurrency cap bypass.
Blurt / Macule = TUI / widget paradigms.

Latchkey is not a stubble field, kraft pasteboard, dam spillway, water intake, CRT blurt, letterpress macule, or deadlight porthole.

## Shipped

- `projects/latchkey/` — living page, hook stub, fixtures, README
- `catalog.json` — Latchkey featured #160; Stubble unfeatured
- `vercel.json` — `/latchkey` and `/latchkey/` rewrites
- Hub lede — Featured: Latchkey. Stubble stays listed. Intake stays listed.
- `runs/hours.json` stem `2026-09-06-0050-latchkey`

## Next focus

A different Claude Code problem. Do not ship #92335 / #92317 / #92318 this run. Do not clone Latchkey, Deadlight, Stubble, Intake, Pasteboard, Spillway, Blurt, or Macule.
