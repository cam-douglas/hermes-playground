# 2026-09-14 07:50 — Sneck

Shipped catalog #348: Northern cottage / workshop-door / brass-sneck-latch booth for anthropics/claude-code#94052.

## Research brief

Primary: https://github.com/anthropics/claude-code/issues/94052 (OPEN; labels: bug, has repro, platform:windows, area:ide, platform:vscode).

In 2.1.268 the VS Code extension replaced the current-file chip's Hide toggle with an X. The changelog presents this as an improvement. It removed the only working opt-out from IDE context auto-attach. There is now no supported way — setting, command, keybinding, or environment variable — to stop the active editor file and selection from being injected into every prompt. From shipped webview/index.js in 2.1.270: X calls dismissSelection(), stores dismissedSelection; applySelectionUpdate() suppresses re-attachment only while that same file remains active. Dismissal is scoped per file, not per session/conversation. Switch tabs → chip returns. Switch back → returns again. Hide toggle held its state. Repro: a.md + b.ts; X on a.md; switch to b.ts; switch back to a.md → chip returns. 17 settings and 28 commands in 2.1.270; none governs context attachment.

Cousins cite-only (not duplicates): #82492 (no visible chip / confirmation), #93667 (keep IDE selection in footer), #40869 / #24726 (opt-in auto-attach settings proposals), #92516 (diff selection dismiss), #20886 / #26577 (discoverability of removal).

Backups cite-only (next focus only — do not auto-pick): #94041 #94040 #94032 #94031 #94029 #93987 #93924 #93770 #93777.

Hypothesis (NON-BINDING): X/dismissSelection stores dismissedSelection and applySelectionUpdate only suppresses re-attachment while the same file remains active; dismissal is per-file not per-session. Offered as a scoring narrative, not a source-root-cause claim.

Why this solution: cottage-door / brass-sneck booth scores cleared vs sneck after a chip-dismiss-ephemeral.

Why not a clone: paradigm and UI distinct from Drawbridge/Chirograph/Titulus/Derelict/Vestry/Snib/Hasp/Fibula/Cockade/Mondegreen/Diplopia/Fulcrum/Followspot.

## Ship notes

- Idle **cleared** / seeded **sneck** / path **chip-dismiss-ephemeral**
- Phrase: Score sneck or admit cleared.
- Folder: `projects/sneck/`
- Live: https://hermes-playground-green.vercel.app/sneck/
- Catalog: featured Sneck #348; Drawbridge unfeatured
- Fonts: Fraunces + Nunito Sans + Source Code Pro
- Palette: slate `#2F343B`, oak `#8B6914` / `#C4A35A`, brass `#B08D57`, wool `#E8E0D5`, iron `#1C1F24`, rain `#5B7C99`, ink `#12141A`

Do NOT implement a fix in anthropics/claude-code. Diagnostic booth only.
