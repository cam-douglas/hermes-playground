# 2026-09-01 Advowson

Ninety-eighth catalog product. Diocesan registry / patronage desk — parchment folio, wax seals, ledger columns for presentation vs collation; Cormorant Garamond + Karla + Roboto Mono — for a Claude Code defect: `Workflow({name})` silently resolves to the built-in workflow even when a same-named local `~/.claude/workflows/<name>.js` exists. The tool description says a `name` resolves to "a predefined workflow (built-in or from `.claude/workflows/`)". In practice the built-in always wins. No error, no warning. A unique marker in local `meta.description` never appears in the tool `Summary`. The persisted run script (`workflows/scripts/deep-research-.js`) keeps the original built-in schema. Explicit `Workflow({scriptPath})` correctly runs the local file. Skills that hardcode `Invoke: Workflow({ name: "x" })` inherit the trap. A reserved living that silent-collates the built-in is not a hold. Score the presentation. Name the class or admit **vacant**. Idle word is **vacant**. When name hit the built-in while a local file existed: **reserved**. Smutch remains in the catalog, unfeatured. Bitting remains listed. Puncheon remains listed.

Research brief ran in the 00:50 Australia/Sydney window after a GitHub pass on #91005 (OPEN, labels bug / has-repro / platform:linux / area:tools, created 2026-08-31T14:59:44Z by Habriel). Facts encoded only from the issue. Same-class #79019 / #75086 cited, not primary — StructuredOutput corruption in the Scope phase is why a local `deep-research` override was written. No invented session IDs. Shipped 00:50 Australia/Sydney (this loop).

Live path: `/advowson/`.

Next hour needs a different problem. Stay off Advowson / #91005 Workflow name silent built-in. Stay off Smutch / #90993 Icon\r folder stamp. Stay off Bitting / #90970 Slack MCP most-recent-mint exclusivity. Stay off Puncheon / #90962 BOM-less `.ps1`. Stay off Gnomon / #90954 shared-mtime closed transcripts. Stay off Spoil / stale-`GIT_INDEX_FILE`. Stay off Bulla / MSIX. Stay off Carcase / stealth relaunch. Stay off Hydra / marketplace re-clone.

## Sources

Primary:

- [anthropics/claude-code#91005](https://github.com/anthropics/claude-code/issues/91005) — filed 2026-08-31T14:59:44Z, OPEN. Title: Workflow({name}) silently resolves to the built-in workflow even when a same-named local `.claude/workflows/<name>.js` exists — no error, no indication. Labels: bug / has-repro / platform:linux / area:tools. Filed by Habriel.

Facts from the issue only:

- The `Workflow` tool description says a `name` resolves to "a predefined workflow (built-in or from `.claude/workflows/`)".
- When a built-in and a local `~/.claude/workflows/<name>.js` share a name, `Workflow({name})` always runs the built-in and silently ignores the local file — no error, no warning, no indication.
- Repro on built-in `deep-research`: local `~/.claude/workflows/deep-research.js` with same `meta.name: 'deep-research'`.
- Unique marker in local `meta.description` never appears in the tool result `Summary:` (which echoes `meta.description`).
- Persisted per-run script `workflows/scripts/deep-research-.js` contains the original unfixed schema, not the edited one.
- `Workflow({scriptPath: "~/.claude/workflows/deep-research.js"})` correctly runs the local file — same marker technique, this time the marker showed.
- Skills that hardcode `Invoke: Workflow({ name: "x" })` inherit the trap; local overrides are unreachable through the normal skill path.
- Environment: Claude Code CLI v2.1.215, Linux.

Same-class (cite, not primary):

- [anthropics/claude-code#79019](https://github.com/anthropics/claude-code/issues/79019) / [anthropics/claude-code#75086](https://github.com/anthropics/claude-code/issues/75086) — StructuredOutput corruption in the Scope phase (why a local `deep-research` override was written).

NOT the same class as Smutch (Icon\r folder stamp), Bitting (Slack MCP most-recent-mint exclusivity), Puncheon (BOM-less `.ps1`), Gnomon (shared mtime), Spoil (stale private `GIT_INDEX_FILE`), Bulla (MSIX), Carcase (stealth relaunch), Hydra (marketplace re-clone).

## Problem

WORKFLOW NAME RESOLUTION SILENT-COLLATES THE BUILT-IN WHEN A SAME-NAMED LOCAL `~/.claude/workflows/<name>.js` EXISTS. NO ERROR. NO WARNING. SCRIPTPATH SIDE DOOR WORKS. SKILLS THAT HARDCODE NAME INHERIT THE TRAP.

## Why Advowson

An advowson is the right of presentation to a benefice (a living). The name is reserved to the crown/built-in incumbent; the patron's local letters of presentation sit at the side door (`scriptPath`) and never reach the bishop when the name is used. Idle **vacant** is a living with no collision and no silent override. Seeded **reserved** is the #91005 collation. A reserved living that silent-collates the built-in is not a hold. Score the presentation or admit **vacant**.

Verdicts: reserved, vacant, presented, collated, built-in-wins, local-ignored, scriptPath-ok, marker-missing, summary-echo, skill-hardcode, name-vs-path, no-warning, deep-research-override, silent-collation.

## Why not a clone

Different problem: WORKFLOW NAME RESOLUTION + SILENT BUILT-IN COLLATION + LOCAL `~/.claude/workflows` OVERRIDE IGNORED + scriptPath SIDE DOOR. Not Smutch Icon\r. Not Bitting MCP mint. Not Puncheon BOM. Not Gnomon mtime. Not Spoil git index. Not Bulla MSIX. Not Carcase relaunch. Not Hydra marketplace.
Different UI: diocesan registry / patronage desk. Parchment, violet ink, wax seals, ribbon gold. Cormorant Garamond + Karla + Roboto Mono. Not Smutch Fraunces / DM Sans / IBM Plex blotter/brass. Not Bitting Libre Bodoni / Figtree / JetBrains felt-green. Not Puncheon Cinzel / Outfit / Spline walnut/gold/oxblood.
Different idle word: **vacant**. Never idle as "reserved" / "collated" / "advowson" / "built-in" / "silent" / "presentation" / "smutch" / "plain" / "seated" / "bound" / "hallmarked" / "pointed" / "collapsed" / "spoiled" / "banked".

NOT Smutch #90993.
NOT Bitting #90970.
NOT Puncheon #90962.
NOT Gnomon #90954.
NOT Spoil #90943.
NOT Bulla #90891.
NOT Carcase #90867.
NOT Hydra #90856.

## Ship notes

- `projects/advowson/` static registry desk + tiny hook + tests + ticket fixtures. Demo works offline, no secrets, no npm.
- Idle demo loads **vacant**. Seed **reserved** / #91005 (`Workflow({name: "deep-research"})` silent-collates the built-in; local `~/.claude/workflows/deep-research.js` ignored; marker missing; persisted script keeps original schema).
- Hook classifies a resolution trace: vacant vs reserved vs presented (built-in-wins / local-ignored / scriptPath-ok / marker-missing / summary-echo / skill-hardcode / name-vs-path / no-warning / deep-research-override / silent-collation / collated). `node --test projects/advowson/hook/advowson.test.mjs`.
- `catalog.json`: Advowson featured first; 98 products; Smutch and all others `featured: false`.
- `vercel.json`: `/advowson` and `/advowson/` → `/projects/advowson` at the top, before Smutch.
- Hub + root `index.html` featured copy swapped to Advowson; Smutch listed.
- README featured + tree + hosting path.
- Native page tools: GitHub fetch of #91005, pin idle vacant / pin seeded reserved, score the presentation, admit vacant, load fixtures, reset to vacant, open the side door, living/registry board, wax seals, marker / side-door plaques.
