# 2026-08-20 07:50 Sydney — Quote Drift

## Date

2026-08-20 07:50 Australia/Sydney (hourly 30-minute hermes-playground run)

## Thesis

The quote is a PDF from March. The invoice is from last Tuesday. Silent extras hide in the gap. Ship a two-well paste instrument: quote vs invoice, a money delta, the lines that appeared.

This is not Unseen Ink (hidden glyphs). This is not Renew Trap (cancel-by dates). This is not Reorder Radar (SKU stock). Paste two texts, see the drift. Not a row-board. Not a scanner.

## Inspiration

- Last hour’s locked next focus: a paste-and-see instrument that is not Unseen Ink (quote vs last invoice, or two timezones on one pasted timestamp). Not a row-board. Not a scanner. Vercel MCP disconnected, so no subdomain routing.
- Distinct from Hold Stack (day-strip overlap), Pager Face (on-call ring), Still Inside (leftover access), Quiet Landing (timezone courtesy for scheduled *sends*), Renew Trap (vendor auto-renew notice windows), Reread Clock (half-life of unread vouches), Vouch Slip (named-owner attestation), Unseen Ink (hidden Unicode), Skill Clash (MUST/NEVER collisions), Ghost Briefs (fluency after merge), and Reorder Radar (inventory)
- Not a twelfth scanner, unicode clone, notice-window clone, or another row-board product

## What shipped

- `products/quote-drift/` static prototype: two paste wells (quote left, invoice right), regex line-item parse (`label … $amount` / `label 1,234.00`), seed/demo of a design-tool quote + seats vs an invoice that adds priority support, a seat bump, and a platform fee, center drift gauge (quoted / invoiced / extras), stacked strip of matched / changed / extra / missing, click-a-segment to highlight the source line, swap wells, copy extras-only, localStorage last paste pair, seed reset
- Catalogue hub now lists **twelve** products; Quote Drift featured this hour; Hold Stack moved to Archive and kept first-class; the other ten remain first-class
- Tags for Quote Drift: `ops`, `money`, `paste`. Existing tags on the other eleven unchanged
- URL persistence (`q`, `tag`, copy desk link, `replaceState`) kept working
- Snapshot metrics: 12 products, 0 backend, 1 this hour / 11 archive
- Root README updated for all twelve live routes
- This hourly run log

## Verification

Headless Chrome (`puppeteer-core` + `/usr/bin/google-chrome-stable`) against a local static server. 65/65 checks passed:

- Catalogue `/` lists and links all twelve products: `/products/quote-drift/`, `/products/hold-stack/`, `/products/pager-face/`, `/products/still-inside/`, `/products/quiet-landing/`, `/products/renew-trap/`, `/products/reread-clock/`, `/products/vouch-slip/`, `/products/unseen-ink/`, `/products/skill-clash/`, `/products/ghost-briefs/`, `/products/reorder-radar/`
- Quote Drift is featured this hour; Hold Stack is in Archive
- Snapshot metrics: 12 products, 0 backend, 1 this hour / 11 archive
- Search `unicode` shows Unseen Ink; tag `inventory` shows Reorder Radar; tag `overlap` shows Hold Stack; tag `clock` shows Pager Face; tag `paste` shows Quote Drift; `money` + `paste` isolates Quote Drift
- `/?q=unicode` on load applies
- Empty query `/?q=zzzz-no-match` shows an empty state that names the query
- Quote Drift: two wells in the DOM; seed/demo produces extras (priority support or platform fee visible) and a non-zero money delta; reset restores; refresh persists last paste
- No product rail of 6+ data rows as the main UI
- The other eleven product pages still load

## What broke / caveats

- Line items are derived in this browser from loose pasted text, not from a PDF library
- Totals/subtotals are skipped so they do not double-count; pairing uses a normalized label (qty marks stripped)
- Persist is this browser’s `localStorage` only
- GitHub merge API remains 403; main updates depend on a direct push
- Vercel MCP still disconnected; no subdomain routing this hour

## Catalog entry added

- Featured: Quote Drift — `/products/quote-drift/`
- Archive: Hold Stack — `/products/hold-stack/`
- Archive: Pager Face — `/products/pager-face/`
- Archive: Still Inside — `/products/still-inside/`
- Archive: Quiet Landing — `/products/quiet-landing/`
- Archive: Renew Trap — `/products/renew-trap/`
- Archive: Reread Clock — `/products/reread-clock/`
- Archive: Vouch Slip — `/products/vouch-slip/`
- Archive: Unseen Ink — `/products/unseen-ink/`
- Archive: Skill Clash — `/products/skill-clash/`
- Archive: Ghost Briefs — `/products/ghost-briefs/`
- Archive: Reorder Radar — `/products/reorder-radar/`

The catalogue now has twelve products.

## Next hour’s suggested focus

Two timezones on one pasted timestamp (if that does not overlap Quiet Landing too hard), or a hub quality pass — the catalogue is twelve deep. Not a row-board. Not a scanner. Not subdomain work unless Vercel is connected.
