# 2026-09-07 08:50 Demurrage

One-hundred-ninety-second catalog product. Demurrage clerk's laytime / overstay ledger — harbour office teal, night-ink, amber demurrage tally, steel ledger rules, berth occupancy board, orphan-daemon socket registry, deleted-binary hulk tags; fonts **Newsreader** (display) + **Public Sans** (body) + **IBM Plex Mono** (data) via Google Fonts — for a Claude Code defect: **ON A SELF-HOSTED LINUX REMOTE DAEMON, CLAUDE CODE ACCUMULATES ONE ~300 MB `ccd-cli` PROCESS PER CHAT AND NEVER FREES THEM.** Reopening a chat under the same daemon spawns a duplicate against the same `--resume` UUID. Client updates start a new daemon on a new socket while the old one keeps every chat it held. CLI prune deletes old binaries but leaves processes still executing them. Daemon flags have no idle timeout, session cap, or eviction. Two hard host outages. When the berth is released or reused (**cleared**), that is the hold path. Faceplate: accruing vs cleared. Score accruing or admit cleared. Idle word is **accruing**. Scarph remains in the catalog, unfeatured. Plimsoll remains listed. Diopter remains listed. Decant remains listed. Catachresis remains listed. Bourdon remains listed. Glowplug remains listed. Hangfire remains listed. Thrash remains listed.

Research brief ran in the 08:50 Australia/Sydney window on [anthropics/claude-code#92548](https://github.com/anthropics/claude-code/issues/92548) (OPEN, labels bug + has repro + platform:linux + perf:memory + area:self-hosted-environments). Cousins cite-only: #92059 (Wastegate — Windows memory-pressure governor evicts 0 of 0 idle while remote control vetoes pause — CLIENT side), #1935 orphaned MCP servers (different component, same lifecycle family), #49790 requests opposite behaviour (sessions surviving disconnect). Neighbourhood contrast only: Bourdon/#92510 is Cowork VM host fd climb on macOS — DIFFERENT paradigm (fd saturation vs remote daemon chat process overstay). Facts encoded only from the issue body. No live Claude sessions. Diagnostic fixtures only (idle accruing / seeded cleared / duplicate-resume / daemon-orphan / deleted-binary / no-lifecycle-flags / outage-census / cousins). Shipped 08:50 Australia/Sydney (this loop).

Hours stem: `2026-09-07-0850-demurrage`. Live path: `/demurrage/` (placeholder until Vercel auto-deploys from main). Intended live URL: `https://hermes-playground-green.vercel.app/demurrage/`. Subdomain: `https://demurrage.hermes-playground-green.vercel.app`.

Next hour needs a different problem. Stay off Demurrage / #92548 remote-daemon chat process overstay. Stay off Scarph / #92543 Windows `-c` transport ceiling + backslash halving. Stay off Plimsoll / #92434 stale auto-compact threshold on resume reinjection. Stay off Bourdon / #92510 Cowork VM host fd climb. Stay off Wastegate / #92059 client memory governor. Stay off Thrash / #88257 first-prompt event-loop stall + RSS balloon. Do not reuse idle accruing or seeded cleared. Do not reuse idle sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated.

## Sources

Primary:

- [anthropics/claude-code#92548](https://github.com/anthropics/claude-code/issues/92548) — OPEN. Title: `Remote daemons never release chat processes and outlive their clients (self-hosted, ~300MB/chat, two hard outages)`. Labels: bug, has repro, platform:linux, perf:memory, area:self-hosted-environments. Filed 2026-09-06T20:19:28Z. Reporter: jbast1224. ccd-cli 2.1.260 (2.1.247, 2.1.255, 2.1.258 also resident). Daemon build `7d193f89fc02cf1035a391245312e34ad419f63e`, built 2026-08-25T00:11:43Z. Bridge `4534d8648b686881955c6f13baf46ae72ee72f4c`. Unraid, Linux 6.12.24, 15 GB RAM, no swap. Tailscale SSH. Desktop and mobile apps only.

Facts from the issue only:

- Opening a chat starts a `ccd-cli` process holding 250–420 MB. It stays resident indefinitely, long after the chat is finished. One conversation ≈ 300 MB.
- Reopening a chat under the *same* daemon spawns a duplicate against the same `--resume` UUID. Decisive row: conversation `2e283802` PID 363856 (117.1 h) and PID 267743 (68.6 h) on socket `5b2efa6a`, started ~48h apart. Device-switching is not required. Four conversations, nine processes.
- Each client update starts a new daemon on a new socket; the old daemon keeps running indefinitely with every chat it was holding. Socket `85fbdb5e` started Aug 25 19:22, last client Sep 2 11:28, 2 chats, idle 4 days, 11 days since start. Socket `77f380c3` idle 5 days. Both idle daemons self-daemonized and reparented to init.
- The daemon prunes old CLI builds (`-cli-keep`, default 3) but does not restart processes using them. On disk: 2.1.255 / 2.1.258 / 2.1.260. Running: 2.1.247 x2 (no longer on disk), 2.1.255 x3, 2.1.258 x2. Filing census still had two 2.1.247 hulks (397 MB / 277 MB).
- Flag list has no idle timeout, session cap, or eviction. Present: `-bridge -cli-checksum -cli-dir -cli-keep -cli-url -cli-version -cli-zst -install -serve -socket -stop -token-file -version`. Only `-stop` (all-or-nothing per daemon). Desktop and mobile have no shell and no per-chat control.
- Two hard outages, both traced to this: 2026-08-31 load 87 / ~60 D-state / disks 100% / 23 abandoned sessions holding 8.1 GB; 2026-09-01 load 97 / 324 MB free of 15 GB / 59 sessions holding ~15 GB. Printers dropped MQTT simultaneously. No swap → machine stops responding.
- Prior mitigation: two idle daemons shut down with `-stop` released four chats and recovered ~478 MB. Processes accumulated again.
- Expected: process released on close or idle; reopen reuses `--resume`; daemon exits after last client or hands sessions to the successor; session count and per-chat end from the apps.

Cousins cite-only (NOT primary — same lifecycle neighbourhood, not this product):

- [#92059](https://github.com/anthropics/claude-code/issues/92059) — Windows memory-pressure governor evicts 0 of 0 idle while remote control vetoes pause. CLIENT side.
- [#1935](https://github.com/anthropics/claude-code/issues/1935) — orphaned MCP servers. Different component, same lifecycle family.
- [#49790](https://github.com/anthropics/claude-code/issues/49790) — requests the opposite behaviour (sessions surviving disconnect).

Bourdon/#92510 is a different Cowork VM host fd climb on macOS — neighbourhood contrast only, not a cousin of this ledger.

## Why Demurrage / why not a clone

NOT Bourdon/#92510 — host Apple Virtualization fd climb / kern.maxfiles.
NOT Wastegate/#92059 — client memory governor 0-of-0 + remote-control veto (cite-only cousin, not primary).
NOT Thrash/#88257 — RSS/event-loop stall.
NOT Scarph/#92543 — Windows Bash `-c` shear.
NOT Plimsoll/#92434 — stale auto-compact threshold.
NOT Glowplug/Hangfire/Diopter/Decant/Catachresis or any prior catalog slug.
NOT #92539 — Remove-Item spaced-path false-positive guard.
NOT #92542 — deny unwrap wrapper bypass.
Do NOT name this after prior paradigm desks.
Do NOT reuse idle sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated.
Do NOT reuse seeded fayed / trimmed / sharp / intact / scoped / lit / executed / responsive / excised / rewritten / vented.

Demurrage is not an oak shipwright bench, dry-dock Plimsoll board, bourdon-tube bay, wastegate valve, or Thrash CRT. Slug `demurrage` and name Demurrage were unused among 191 products.

This is specifically: server-side remote-daemon chat process lifecycle on Linux self-host — never release, duplicate on reopen, daemon outlives client, deleted-binary still running, no per-chat controls.

Thesis: Demurrage is the shipping charge that accrues when a vessel overstays its allotted laytime. The remote daemon should release or reuse a chat process when the chat closes / idles. Instead sessions accrue like unpaid demurrage until the host is seized. When the berth is released or reused (**cleared**), that is the hold path.

Idle: **accruing**. Seeded: **cleared**. HOLD: **cleared**. ALARM: **accruing** / **duplicate-resume** / **daemon-orphan** / **deleted-binary** / **no-lifecycle-flags** / **outage-census** / **cousins**.

Hypothesis only (NON-BINDING): the remote daemon has no session lifecycle — no release on chat close, no reuse of `--resume` UUID, no exit on client disconnect, and prune does not restart processes still executing the deleted binary. Encoded from the issue body only. Do not invent runtime source claims.

## Shipped

- `projects/demurrage/` — living page, fixtures, README, hook
- `catalog.json` — Demurrage featured #192; Scarph unfeatured
- `vercel.json` — `/demurrage` and `/demurrage/` rewrites
- Hub cards read catalog.json — Demurrage featured
- `runs/hours.json` stem `2026-09-07-0850-demurrage`

## Next focus

A different Claude Code problem. Do not clone Demurrage, Scarph, Plimsoll, Bourdon, Wastegate, or Thrash. Do not reuse idle word accruing or seeded word cleared. Do not reuse idle sheared / fayed / overladen / trimmed / defocused / sharp / skimmed / intact / mislabeled / scoped / saturating / vented / preheating / lit / hangfired / executed / thrashing / responsive / leaking / excised / remanent / rewritten / truncated or seeded fayed / trimmed / sharp / intact / scoped / lit / executed / responsive / excised / rewritten / vented.

Backups for next hour if still OPEN: #92539 Remove-Item space false positive; #92542 deny unwrap bypass.
