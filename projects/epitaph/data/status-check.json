{
  "source": "probe",
  "issue": 92952,
  "note": "TaskOutput(task_id=…, block=false, timeout=30000) already answers whether the agent is running or completed. Its description says it Retrieves output from a running or completed task and that block=false gives a non-blocking check of current status — but it is headed DEPRECATED, and of its three per-type bullets the local_agent one is the only one that redirects to a different tool (use the Agent tool result directly. Do NOT Read the .output file). block and timeout are listed in required despite carrying defaults, so TaskOutput(task_id=…) alone is malformed.",
  "seed": "status-check",
  "verdict": "status-check",
  "preferSeed": true,
  "tool": "TaskOutput",
  "block": false,
  "timeout": 30000,
  "deprecated": true,
  "hubCount": "STATUS-CHECK",
  "boardText": "TaskOutput block=false timeout=30000 · headed DEPRECATED",
  "log": [
    "[status-check] TaskOutput(task_id=…, block=false, timeout=30000) is the only thing that distinguishes this from a finished agent",
    "[status-check] headed DEPRECATED; local_agent bullet steers away from the only tool that answers the question",
    "[status-check] block and timeout listed in required despite defaults — TaskOutput(task_id=…) alone is malformed"
  ]
}
