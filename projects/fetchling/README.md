# Fetchling

A **fae twilight / shadow-double / coin-ledger desk / mirror-swap booth** — twilight, coin-gold, mist, ink, swap-rose, ledger-slate, intact-teal. Fonts **Lora** (display) + **Plus Jakarta Sans** (body) + **Roboto Mono** (mono). Palette: twilight `#12101A`, gold `#D4A84B`, mist `#C8C2D4`, ink `#EDE8F5`, rose `#B84A6A`, slate `#5A5568`, teal `#2A9B8F`. Fresh trio. NOT Souffleur/#94031. NOT Epitome/#94032. NOT Diabolica/#94040. NOT Sallyport/#94082. NOT Palilalia/#94041. NOT Sepulchre/#94055. NOT Sneck/#94052. NOT Drawbridge/#94049. NOT Chirograph/#94045. NOT Titulus. NOT Derelict. NOT Vestry. NOT Mondegreen. NOT Afterimage/#92596. NOT Phosphene. NOT Scotoma. NOT Scrim. NOT Aphonia/#92409 (missing SendMessage). NOT Sourdine/#93531 (mid-narration mute). NOT Anarthria/#93782 (dictation paste drop). NOT Trismus. NOT Quietus. NOT Followspot. NOT Greenroom. NOT Aside. NOT Ambo. Completely different UI/UX/metaphor. This is specifically: **`$N` TOKENS SILENTLY SWAPPED FOR CONVERSATION FRAGMENTS ON THE SKILL PATH WHILE CLAUDE.md KEEPS FACE VALUE.**

The desk should stay **literal** (HOLD: as-written / face-value / mint-true / dollar-intact / skill-verbatim / ledger-true). Instead the booth was **fetchling** after a **skill-dollar-swap**.

Primary:

- [anthropics/claude-code#94065](https://github.com/anthropics/claude-code/issues/94065) (OPEN). Title: `Skill loading: $1-$19 in a skill file are replaced with unrelated conversation text (CLAUDE.md is unaffected)`. Labels: bug, has repro, platform:windows, area:skills. Skill tool load: literal `$` + 1–2 digit sequences (`$5`, `$19`) in skill Markdown do not reach the model as written; replaced with unrelated conversation fragments. Disk file unmodified; silent corruption. Roughly `$1`–`$19` substituted; `$20+` intact. Observed sub: `$3 $5 $10 $13 $15 $19`. Intact: `$20 $25 $49.99 $125 $175`. Same line can mix (`$19` gone, `$25` survives). Replacement text from surrounding conversation (sometimes exact user phrase). Localisation: CLAUDE.md `$10` intact; Skill-tool `$10` becomes `the` — Skill path only. Minimal repro Rules A `$5` B `$19.99` C `$25` D `$13,961` — A/B/D corrupt, C intact. Environment: Windows 11; project skill `.claude/skills/.../SKILL.md` via Skill tool. Cousins cite-only (do NOT rebuild / do NOT conflate — args/Bash surfaces, not the #94065 Skill-load path): #79859 (Skill tool args bash-style $N against SKILL.md prose), #82175 (Skill tool args corrupts $1/$2 in code examples), #89978 (Bash tool strips dollar amounts from message content), #91957 (Skill args silently rewrites literal dollar figures in SKILL.md), #92457 (Skill/command argument substitution off-by-one / limited to $0/$1). Backups cite-only (next focus only — do not auto-pick): #94029, #93987, #93924, #93770, #93777, #94059, #94053, #94174, #94151, #94064. Stay off Souffleur/Epitome/Diabolica/Sallyport/Palilalia/Sepulchre/Sneck/Drawbridge/Chirograph/Titulus/Derelict/Vestry/Mondegreen/Afterimage/Phosphene/Scotoma/Scrim/Aphonia/Sourdine/Anarthria/Trismus/Quietus/Followspot/Greenroom/Aside/Ambo paradigms.

14:50 fetchling: a fae-twilight / shadow-double / coin-ledger / mirror-swap booth for #94065. Skill-tool load replaces literal $1–$19 in skill Markdown with unrelated conversation fragments while the disk file stays unmodified and CLAUDE.md $10 stays intact; $20+ survive; same line can mix ($19 gone, $25 lives). Idle **literal** / seeded **fetchling** / path **skill-dollar-swap**. Score fetchling or admit literal.

Score fetchling or admit literal.

Idle word: **literal** (HOLD: `$N` tokens reach the model as written). HOLD aliases: literal, as-written, face-value, mint-true, dollar-intact, skill-verbatim, ledger-true. Seeded word: **fetchling** / #94065 (the skill-dollar-swap path). Path word: **skill-dollar-swap**. Product score: **fetchling**. Never idle echoing / unabridged / innocent / sealed / silenced / living / cleared / spanned / matched / inscribed / berthed / pegged / latent / flushed / articulate / limber / primed / lit / voiced / mute / rostered / quieted / unrung / demesned / diagrammed or seeded souffleur / epitome / diabolica / sallyport / palilalia / sepulchre / sneck / drawbridge / chirograph / titulus / derelict / vestry / mondegreen / afterimage / phosphene / scotoma / scrim / aphonia / sourdine / anarthria / app-switch-echo-loss / summarized-thinking-force / cannot-show-not-git / reminder-secret-bypass / goal-stop-refire / bash-nul-poison / dictation-paste-drop / mid-narration.

Phrase: **Score fetchling or admit literal.**

- **literal** = IDLE: HOLD; mint tray face-value; ledger true; glass same
- **fetchling** = #94065 seeded path and product score: the shadow-double swaps `$N` for conversation text
- **skill-dollar-swap** = path word: Skill-tool load replaces `$1`–`$19`
- **hold** = HOLD alias for idle literal
- **as-written** = HOLD alias: `$N` tokens reach the model as written
- **face-value** = HOLD alias: coin faces stay mint-true
- **mint-true** = HOLD alias: the mint tray still holds the true coins
- **dollar-intact** = HOLD alias: `$1`–`$19` and `$20+` both stay intact
- **skill-verbatim** = HOLD alias: Skill-tool load matches the disk file
- **ledger-true** = HOLD alias: every ledger row stays as-written
- **claude-md-intact** = CLAUDE.md `$10` stays face-value
- **skill-path-corrupt** = Skill-tool `$10` becomes `the`
- **sub-1-19** = `$3 $5 $10 $13 $15 $19` become conversation fragments
- **intact-20-plus** = `$20 $25 $49.99 $125 $175` stay mint
- **same-line-mix** = `$19` gone, `$25` survives on one line
- **conversation-fragment** = replacement text from surrounding conversation
- **silent-corruption** = disk file unmodified; load is swapped
- **landing** = twilight / mint tray / ledger / fae glass
- **has-repro** = published shape: Windows 11 · Skill tool · Rules A `$5` B `$19.99` C `$25` D `$13,961`
- **cousins** = cite-only #79859 #82175 #89978 #91957 #92457 — args/Bash surfaces, not Skill-load path; do not rebuild; do not conflate
- **backups** = cite-only #94029 #93987 #93924 #93770 #93777 #94059 #94053 #94174 #94151 #94064 — do not auto-pick
- **fixtures** = twilight / mint tray / ledger / fae glass
- **walk** = published idle literal → skill-dollar-swap → fetchling

Verdicts: literal, fetchling, skill-dollar-swap, hold, as-written, face-value, mint-true, dollar-intact, skill-verbatim, ledger-true, claude-md-intact, skill-path-corrupt, sub-1-19, intact-20-plus, same-line-mix, conversation-fragment, silent-corruption, landing, has-repro, cousins, backups, fixtures, walk.

This is a diagnostic scoring booth. Not an exploit. No Claude sessions. No network to Anthropic required for scoring. Score whether the booth is **fetchling** or already **literal**. Fixtures use the issue's published incident only. Do NOT implement a fix in anthropics/claude-code.

Hypothesis only (NON-BINDING): skill content used as regex replacement string so `$nn` expands as capture backrefs (JS String.replace); escape `$$` or use a replacer function. Invite verify against #94065 text only. Do not claim a root cause in Claude Code source you have not seen.

## Research brief

Sources:

- Primary: [anthropics/claude-code#94065](https://github.com/anthropics/claude-code/issues/94065)
- Cousins (cite-only — do NOT rebuild / do NOT conflate; args/Bash surfaces, not the #94065 Skill-load content path): #79859 Skill tool args bash-style $N against SKILL.md prose. #82175 Skill tool args corrupts $1/$2 in code examples. #89978 Bash tool strips dollar amounts from message content. #91957 Skill args silently rewrites literal dollar figures in SKILL.md. #92457 Skill/command argument substitution off-by-one / limited to $0/$1. #94065 is specifically the Skill-tool content load path.
- Backups (data only; next focus only — do not auto-pick): #94029, #93987, #93924, #93770, #93777, #94059, #94053, #94174, #94151, #94064

What happened (from the issue text — do not invent):

- OPEN
- Labels: bug, has repro, platform:windows, area:skills
- Skill tool load of project skill `.claude/skills/.../SKILL.md`
- Windows 11
- Literal `$` + 1–2 digit sequences (`$5`, `$19`) in skill Markdown do not reach the model as written
- Replaced with unrelated conversation fragments
- Disk file unmodified; silent corruption
- Roughly `$1`–`$19` substituted; `$20+` intact
- Observed sub: `$3 $5 $10 $13 $15 $19`
- Intact: `$20 $25 $49.99 $125 $175`
- Same line can mix (`$19` gone, `$25` survives)
- Replacement text from surrounding conversation (sometimes exact user phrase)
- CLAUDE.md `$10` intact; Skill-tool `$10` becomes `the` — Skill path only
- Minimal repro: Rules A `$5` B `$19.99` C `$25` D `$13,961` — A/B/D corrupt, C intact

Problem found: SKILL-DOLLAR-SWAP — Skill-tool load swaps `$1`–`$19` for conversation fragments; `$20+` intact; CLAUDE.md `$10` intact; disk unmodified.

Why Fetchling: A *fetchling* is a shadow / doppelganger-adjacent double. `$N` tokens are silently swapped for conversation fragments while CLAUDE.md keeps face value. Souffleur/#94031 was typing-echo loss after app switch (theatre wings). Epitome/#94032 was scriptorium abridgement. This booth is specifically Skill-path dollar-token swap — different problem, UI, UX, metaphor. NOT a Claude Code patch — educational diagnostic booth only.

Why this solution: literal catalog page + node diagnostic encoding idle **literal** / seeded **fetchling** / path **skill-dollar-swap** so operators can score whether the booth is **fetchling** or already **literal**. Not a Claude Code patch.

Desired (from the issue, scoring narrative only — DO NOT implement a Claude Code fix):

1. Literal `$` + 1–2 digit sequences in skill Markdown should reach the model as written
2. `$5`, `$19`, and `$13,961` should not be replaced with conversation fragments
3. Disk file and model-visible text should match — no silent corruption
4. CLAUDE.md `$10` already stays intact; Skill-tool `$10` should keep the same face value
5. A mixed line should not drop `$19` while `$25` survives

## Why not a clone

This is specifically: **`$N` TOKENS SILENTLY SWAPPED FOR CONVERSATION FRAGMENTS ON THE SKILL PATH WHILE CLAUDE.md KEEPS FACE VALUE.**

Novel paradigm: fae twilight / shadow-double / coin-ledger desk / mirror-swap — twilight, coin-gold, mist, intact-teal, swap-rose. New issue, new paradigm (skill-dollar-swap), new UI/UX/fonts/colors, new scoring vocabulary. An opened ledger with a fae glass and mint coins, not a theatre prompt-corner, scriptorium desk, ENT roster, concert mute, or laryngology paste.

**NOT Souffleur/#94031** (app-switch-echo-loss). Different defect. NOT theatre wings / prompt-corner. Do not reuse echoing / souffleur / app-switch-echo-loss.

**NOT Epitome/#94032** (summarized-thinking-force). Different defect. NOT classical scriptorium. Do not reuse unabridged / epitome / summarized-thinking-force.

**NOT Diabolica/#94040** (cannot-show-not-git). Different defect. NOT inquisitorial parchment-court. Do not reuse innocent / diabolica / cannot-show-not-git.

**NOT Sallyport/#94082** (reminder-secret-bypass). Different defect. NOT fortress / gatehouse. Do not reuse sealed / sallyport / reminder-secret-bypass.

**NOT Palilalia/#94041** (goal-stop-refire). Different defect. NOT speech-pathology / phonograph-groove. Do not reuse silenced / palilalia / goal-stop-refire.

**NOT Sepulchre/#94055** (bash-nul-poison). Different defect. NOT stone burial vault / ossuary. Do not reuse living / sepulchre / bash-nul-poison.

**NOT Sneck/#94052** (chip dismiss ephemeral). Different defect. NOT Northern cottage / brass sneck-latch. Do not reuse cleared / sneck / chip-dismiss-ephemeral.

**NOT Drawbridge/#94049** (RC bridge auto-update drop). Different defect. NOT raised span / bailey. Do not reuse spanned / drawbridge / rc-bridge-update-drop.

**NOT Chirograph/#94045** (recorded branch never refreshed after `git branch -m`). Different defect. NOT medieval lectern / indenture. Do not reuse matched / chirograph / worktree-rename-stale.

**NOT Titulus** (iOS rename vs desktop sidebar / resume-stale-title). Different defect. NOT Roman marble plaque. Do not reuse inscribed / titulus.

**NOT Derelict** (orphaned Bash after session stop). Different defect. NOT maritime abandoned-hulk. Do not reuse berthed / derelict.

**NOT Vestry** (Linux bwrap placeholder mount cleanup is per-process). Different defect. NOT liturgical sacristy. Do not reuse pegged / vestry.

**NOT Mondegreen** (substring "git" false-positive). Different defect. NOT lyric-ballad / mishearing.

**NOT Afterimage/#92596** (Windows text paint latency). Different defect. NOT CRT phosphor.

**NOT Phosphene** (WindowServer CA layer-tree thrash). Different defect. NOT vision flash.

**NOT Scotoma.** Different defect. NOT vision gap.

**NOT Scrim** (runtime DLP redaction). Different product. Do not reuse flushed / scrim.

**NOT Aphonia/#92409** (missing SendMessage). Different defect. NOT ENT roster.

**NOT Sourdine/#93531** (mid-narration mute). Different defect. NOT concert mute.

**NOT Anarthria/#93782** (dictation paste drop). Different defect. NOT laryngology.

**NOT Trismus. NOT Quietus. NOT Followspot. NOT Greenroom. NOT Aside. NOT Ambo.** Different defects. Do not reuse those houses.

Live: https://hermes-playground-green.vercel.app/fetchling/

```
node --test projects/fetchling/fetchling.test.mjs
node projects/fetchling/fetchling.mjs projects/fetchling/data/fetchling.json
```
