{
  "source": "one-behind-path",
  "issue": 93482,
  "note": "one-behind path from #93482. Only overwrites lag exactly one commit. Create is correct. Committing the identical payload a second time lands it.",
  "seed": "one-behind",
  "verdict": "one-behind",
  "preferSeed": true,
  "lagged": true,
  "overwrite": true,
  "staleBytes": true,
  "freshMtime": true,
  "written": true,
  "oneBehind": true,
  "paths": [
    {
      "id": "create",
      "rule": "device_commit_files (new file) VERSION-A… (43 bytes) → written, no rejections",
      "result": "43 bytes, VERSION-A — create is correct every time"
    },
    {
      "id": "overwrite",
      "rule": "device_commit_files (overwrite) VERSION-B… (82 bytes) → written, no rejections",
      "result": "43 bytes, VERSION-A — mtime advanced; disk lags one commit"
    },
    {
      "id": "second",
      "rule": "commit the identical payload a second time, then overwrite VERSION-C",
      "result": "one-behind is overwrite-only; second B lands; C overwrite keeps B"
    }
  ],
  "cue": "lagged",
  "hubCount": "ONE-BEHIND",
  "boardText": "#93482 · one-behind path — exactly one commit behind on overwrite",
  "log": [
    "[one-behind] overwrite reports written with a fresh mtime while on-disk content lags exactly one commit",
    "[one-behind] create is unaffected; second identical commit lands the payload",
    "[one-behind] workaround: commit the same payload twice, then verify content (not byte count / mtime)"
  ]
}
