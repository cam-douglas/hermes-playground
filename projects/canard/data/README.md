# Canard fixtures

Diagnostic JSON only. No Claude sessions. Encoded from #93766 issue facts: VS Code extension spawn ENOENT under OneDrive cwd is mislabeled as a musl/glibc linker mismatch on Windows. Score canard or admit candid.

Idle word: **candid**. Path word: **onedrive-cwd-mislabel**. Seeded loss: **canarded**. Product: **canard**. HOLD: **candid**. ALARM: **canarded** / **canard** / **onedrive-cwd-mislabel** / **spawn-enoent** / **musl-mislabel** / **onedrive-cwd**. Primary: [anthropics/claude-code#93766](https://github.com/anthropics/claude-code/issues/93766).

Fixtures record the published incident only. No session. No secrets. No exploit payloads. No network to Anthropic. No Claude. No patch to anthropics/claude-code.

| File | Verdict | What it scores |
|---|---|---|
| `candid.json` | candid | Idle desk. HOLD: surface ENOENT / cwd honestly; no Linux linker tale. |
| `hold.json` | hold | HOLD alias for idle candid. |
| `canarded.json` | canarded | Seeded #93766 path. ALARM: musl/glibc after spawn ENOENT under OneDrive cwd. |
| `canard.json` | canard | Product score for the newspaper-canard booth. |
| `onedrive-cwd-mislabel.json` | onedrive-cwd-mislabel | Path: OneDrive cwd + ENOENT then a fabricated linker story. |
| `spawn-enoent.json` | spawn-enoent | Real OS error while the exe exists and runs standalone. |
| `musl-mislabel.json` | musl-mislabel | Linux-only linker tale printed on Windows. |
| `onedrive-cwd.json` | onedrive-cwd | Workspace cwd under OneDrive-synced path. |
| `honest-spawn.json` | honest-spawn | HOLD alias: spawn result named as it is. |
| `plain-enoent.json` | plain-enoent | HOLD alias: real OS error stays the headline. |
| `windows-honest.json` | windows-honest | HOLD alias: no Linux linker tale on Windows. |
| `has-repro.json` | has-repro | Published shape. |
| `cousins.json` | cousins | No numbered cousins cited in #93766. |
| `backups.json` | backups | Cite-only backups. Do not auto-pick. |
| `fixtures.json` | fixtures | Aged newsprint / wire ticker / ENOENT stamp / crossed-out musl headline. |
| `walk.json` | walk | Published idle candid → onedrive-cwd-mislabel → canarded → canard. |

## Backups (cite only — do NOT auto-pick or build)

#93764 #93754 #93744 #93782 #93821 #93811 #93809 #93751 #93772 #93770 #93777

Drop any file onto `projects/canard/index.html`. Buttons load the seeded path. The living page admits **candid** / idle wire / #93766.

Probes are diagnostic reconstructions of the issue's published facts. This booth does not run Claude.
