{
  "source": "booth",
  "issue": 93589,
  "note": "Path fixture. egress-allowlist names the ignored user-level flags after the 2.1.266 VM cut.",
  "seed": "egress-allowlist",
  "verdict": "egress-allowlist",
  "preferSeed": true,
  "dammed": true,
  "egressAllowlist": true,
  "put403": true,
  "hostNeverReached": true,
  "event": "egress-allowlist",
  "cue": "dammed",
  "hubCount": "EGRESS-ALLOWLIST",
  "paths": [
    {
      "rule": "Settings \u2192 Capabilities \u2192 Allow network egress ON; host under Additional allowed domains; Domain allowlist = All domains",
      "result": "user-level flags should admit the sandbox PUT through the millrace"
    },
    {
      "rule": "sandbox PUT --data-binary to the custom-domain URL returned by request_upload_url",
      "result": "403 at the egress proxy (host_not_allowed / blocked-by-allowlist); host never reached"
    },
    {
      "rule": "identical settings on VM 2.1.260 the day before",
      "result": "14,641-byte upload arrived; MCP proxy path still works after the 2.1.266 cut"
    }
  ],
  "log": [
    "[egress-allowlist] user flags no longer admit the sandbox PUT \u2014 not a global MCP outage"
  ]
}
