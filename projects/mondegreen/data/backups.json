{
  "source": "probe",
  "issue": 93193,
  "note": "Backups cite in data only — do NOT auto-pick as primary. #93173 sandbox denies writes under repo .claude/ and silently corrupts git checkout. #93182 server-side tools unblockable — deny casing mismatch + PreToolUse never fires. #93218 Desktop --disallowedTools SendMessage but ListAgents still lists peers and documents SendMessage.",
  "seed": "backups",
  "verdict": "backups",
  "preferSeed": true,
  "backups": true,
  "backupsCiteOnly": [
    {
      "issue": 93173,
      "state": "OPEN",
      "title": "Bash sandbox denies writes to /.claude/ paths, silently corrupting the git working tree in repos that track files there",
      "why": "sandbox denies writes under repo .claude/ and silently corrupts git checkout — backup, not primary"
    },
    {
      "issue": 93182,
      "state": "OPEN",
      "title": "Server-side tools are unblockable: deny rejects the registered casing, and PreToolUse hooks never fire for them",
      "why": "deny casing mismatch + PreToolUse never fires — backup, not primary"
    },
    {
      "issue": 93218,
      "state": "OPEN",
      "title": "Desktop app disables SendMessage via --disallowedTools, but ListAgents in the same session still lists peers and documents it as the address",
      "why": "ListAgents still lists peers and documents SendMessage — backup, not primary"
    }
  ],
  "hubCount": "BACKUPS",
  "log": [
    "[backups] cite-only #93173 #93182 #93218 — do not auto-pick as primary"
  ]
}
