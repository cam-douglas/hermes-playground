# Tessera fixtures

Diagnostic JSON only. No Claude sessions. Encoded from #93929 issue facts: native macOS installer runs each Claude Code release from a version-named path, so TCC accumulates one permission row per release that users cannot pry off. Score tessera or admit unitary.

Idle word: **unitary**. Path word: **version-path-tcc**. Seeded loss: **tessellated**. Product: **tessera**. HOLD: **unitary**. ALARM: **tessellated** / **tessera** / **version-path-tcc** / **stale-tcc-row**. Primary: [anthropics/claude-code#93929](https://github.com/anthropics/claude-code/issues/93929).

Fixtures record the published incident only. No session. No secrets. No exploit payloads. No network to Anthropic. No Claude. No patch to anthropics/claude-code.

| File | Verdict | What it scores |
|---|---|---|
| `unitary.json` | unitary | Idle pane. HOLD: one TCC identity on a stable path. |
| `hold.json` | hold | HOLD alias for idle unitary. |
| `tessellated.json` | tessellated | Seeded #93929 path. ALARM: version-named path → new TCC row. |
| `tessera.json` | tessera | Product score for the mosaic / privacy-pane booth. |
| `version-path-tcc.json` | version-path-tcc | Path: version-named binary vs stable-path bundle. |
| `bundled.json` | bundled | HOLD alias: live binary is the ClaudeCode.app bundle. |
| `stable-path.json` | stable-path | HOLD alias: version-independent path. |
| `one-row.json` | one-row | HOLD alias: one TCC row per permission service. |
| `identity-kept.json` | identity-kept | HOLD alias: signing identity and path stay one client. |
| `stale-tcc-row.json` | stale-tcc-row | Updater deletes binaries; TCC rows remain. |
| `bare-version-label.json` | bare-version-label | Settings row labeled with a bare version number. |
| `bundle-frozen.json` | bundle-frozen | ClaudeCode.app Jul 14 inode 319832836. |
| `live-versioned.json` | live-versioned | Running binary at `versions/2.1.263`. |
| `has-repro.json` | has-repro | Published shape. |
| `cousins.json` | cousins | Cite-only #76615 / #38722 / #76080 / #93747. |
| `backups.json` | backups | Cite-only backups. Do not auto-pick. |
| `fixtures.json` | fixtures | Limestone / basalt / mica gold. |
| `walk.json` | walk | Published idle unitary → version-path-tcc → tessellated → tessera. |

## Cousins (cite only)

#76615, #38722 (stale-closed priors). #76080 (permission prompt shows version string as app name). #93747 (Desktop Documents EPERM / fragmented duplicate claude identities). Do not rebuild as separate booths.

## Backups (cite only — do NOT auto-pick or build)

#93772 #93770 #93777 #93782 #93889 #93821 #93811 #93809 #93823 #93924 #93925

Drop any file onto `projects/tessera/index.html`. Buttons load the seeded path. The living page admits **unitary** / idle pane / #93929.

Probes are diagnostic reconstructions of the issue's published facts. This booth does not run Claude.
