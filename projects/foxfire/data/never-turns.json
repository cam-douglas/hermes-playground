{
  "source": "never-turns-path",
  "issue": 93502,
  "note": "never-turns path from #93502. Remote Control message to idle CLI paints dim composer text. No transcript user row. No queue-operation. No turn. Session stays idle.",
  "seed": "never-turns",
  "verdict": "never-turns",
  "preferSeed": true,
  "painted": true,
  "neverTurns": true,
  "sessionIdle": true,
  "turnStarted": false,
  "noTranscript": true,
  "noQueue": true,
  "composerPaint": true,
  "paths": [
    {
      "id": "stop",
      "rule": "Stop hooks complete normally (continue:true then {}); preventedContinuation:false",
      "result": "hooks themselves do not prevent Remote Control delivery"
    },
    {
      "id": "paint",
      "rule": "1.44s later PTY output paints `build the F-831 control` as dim composer text",
      "result": "no matching PTY input; text visible only in the local composer"
    },
    {
      "id": "idle",
      "rule": "no transcript user row, no queue-operation, no turn",
      "result": "never-turns — session remains idle until local Escape / retype"
    }
  ],
  "cue": "painted",
  "hubCount": "NEVER-TURNS",
  "boardText": "#93502 · never-turns path — no turn began; session remains idle",
  "log": [
    "[never-turns] Remote Control message to idle CLI paints dim composer text without starting a turn",
    "[never-turns] no transcript user row; no queue-operation; remote users get no delivery failure",
    "[never-turns] workaround: return to local terminal, Escape, retype — defeats unattended Remote Control"
  ]
}
