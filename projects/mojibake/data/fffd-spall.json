{
  "source": "booth",
  "issue": 93848,
  "note": "Path fixture. fffd-spall names tofu-tile substitution vs hangul-kept sort.",
  "seed": "fffd-spall",
  "verdict": "fffd-spall",
  "preferSeed": true,
  "mojibaked": true,
  "fffdSpall": true,
  "cacheMiss": true,
  "event": "fffd-spall",
  "cue": "mojibaked",
  "hubCount": "FFFD-SPALL",
  "paths": [
    {
      "rule": "embedded CLAUDE.md UTF-8 should reach the API as the same prefix every request",
      "result": "one Hangul codepoint 불 becomes three U+FFFD at char index 3615"
    },
    {
      "rule": "the substitution should not sit on a 4/8/16 KiB boundary if it is a simple buffer cut",
      "result": "UTF-8 offsets 7,982 in the block and 7,665 in the file — not a 4/8/16 KiB boundary; cache miss sid 8fdae59a"
    },
    {
      "rule": "the file on disk should be the source of truth",
      "result": "file never modified; bytes.decode('utf-8') strict OK; only whether a request is corrupted varies"
    }
  ],
  "log": [
    "[fffd-spall] tofu tiles in the chase; hangul-kept sorts stay on the verbatim forme — cousins cite-only"
  ]
}
