{
  "source": "probe",
  "issue": 93081,
  "note": "Manual-cleanup fallback refuses for the same missing .git link: git status failed (.git link missing (partial remove)); not safe to rm. Remove manually if no longer needed.",
  "seed": "fallback-refuses",
  "verdict": "fallback-refuses",
  "preferSeed": true,
  "fallbackRefuses": true,
  "fallbackRefusal": ".git link missing (partial remove); not safe to rm",
  "cue": "mounded",
  "hubCount": "FALLBACK-REFUSES",
  "log": [
    "[fallback] Keeping worktree dir: git status failed (.git link missing (partial remove)); not safe to rm"
  ]
}
