{
  "source": "booth",
  "issue": 93615,
  "note": "Path fixture. cron-websearch names the scheduled WebSearch hang (no result, no error, no timeout).",
  "seed": "cron-websearch",
  "verdict": "cron-websearch",
  "preferSeed": true,
  "becalmed": true,
  "cronWebsearch": true,
  "websearchHang": true,
  "noTimeout": true,
  "lastActivityFreeze": true,
  "event": "cron-websearch",
  "cue": "becalmed",
  "hubCount": "CRON-WEBSEARCH",
  "paths": [
    {
      "rule": "scheduled/cron/background session calls WebSearch",
      "result": "tool call never returns \u2014 no result, no error, no timeout"
    },
    {
      "rule": "lastActivityAt should keep advancing while the session is running",
      "result": "chronometer freezes; session status stays running with zero progress"
    },
    {
      "rule": "identical query in an interactive session on the same machine/account",
      "result": "returns in a few seconds; cap of 2 still hangs on the first cron call"
    }
  ],
  "log": [
    "[cron-websearch] scheduled path never fills the kite \u2014 not a global WebSearch outage"
  ]
}
