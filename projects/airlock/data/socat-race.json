{
  "source": "booth",
  "issue": 93862,
  "note": "Path fixture. socat-race names the hatch opening before the chamber equalizes.",
  "seed": "socat-race",
  "verdict": "socat-race",
  "preferSeed": true,
  "blown": true,
  "socatRace": true,
  "noWaitBind": true,
  "firstCallRefused": true,
  "event": "socat-race",
  "cue": "blown",
  "hubCount": "SOCAT-RACE",
  "paths": [
    {
      "rule": "the sandbox should wait for HTTP:3128 and SOCKS:1080 to bind before the user command",
      "result": "both socat bridges start with &; no listen-socket poll — first call races the bind"
    },
    {
      "rule": "a first-action git ls-remote or curl should succeed against the local proxy",
      "result": "first probe is connection-refused to localhost:3128 after 0 ms"
    },
    {
      "rule": "ss -ltn as the first action should already show 0.0.0.0:3128 and 0.0.0.0:1080",
      "result": "ss as first action shows no listener; the same command 100 ms later shows both ports"
    }
  ],
  "log": [
    "[socat-race] inner shell ~3 ms vs listen 15–30 ms — cousin #62743 cite-only"
  ]
}
