"# Airlock fixtures\n\nDiagnostic JSON only. No Claude sessions. Encoded from #93862 issue facts: the Linux/WSL2 bwrap sandbox starts two socat bridges in the background (`TCP-LISTEN:3128` HTTP + `TCP-LISTEN:1080` SOCKS) and runs the user command with no wait for listen sockets. Inner shell ready ~3 ms; port 3128 accepts ~15–30 ms later. First git/curl is connection-refused; later calls succeed. Score airlock or admit equalized.\n\nIdle word: **equalized**. Path word: **socat-race**. Seeded loss: **blown**. Product: **airlock**. HOLD: **equalized**. ALARM: **blown** / **airlock** / **socat-race** / **no-wait-bind** / **first-call-refused**. Primary: [anthropics/claude-code#93862](https://github.com/anthropics/claude-code/issues/93862).\n\nFixtures record the published incident only. No session. No secrets. No exploit payloads. No network to Anthropic. No Claude. No patch to anthropics/claude-code.\n\n| File | Verdict | What it scores |\n|---|---|---|\n| `equalized.json` | equalized | Idle lock. HOLD: bridges listening before command; hatch sealed. |\n| `hold.json` | hold | HOLD alias for idle equalized. |\n| `blown.json` | blown | Seeded #93862 path. ALARM: first call races the bind. |\n| `airlock.json` | airlock | Product score for the pressure-lock booth. |\n| `socat-race.json` | socat-race | Path: hatch opens before the chamber equalizes. |\n| `no-wait-bind.json` | no-wait-bind | Script never polls listen sockets. |\n| `first-call-refused.json` | first-call-refused | First git/curl connection-refused to localhost:3128. |\n| `later-call-ok.json` | later-call-ok | Later calls in the same command succeed. |\n| `listen-latency.json` | listen-latency | 3 ms shell vs 15–30 ms listen. |\n| `bridge-http.json` | bridge-http | HTTP:3128 background socat. |\n| `bridge-socks.json` | bridge-socks | SOCKS:1080 background socat. |\n| `bridges-ready.json` | bridges-ready | HOLD alias: both listeners bound first. |\n| `listeners-bound.json` | listeners-bound | HOLD alias: accept before first probe. |\n| `hatch-sealed.json` | hatch-sealed | HOLD alias: outer hatch stays sealed. |\n| `has-repro.json` | has-repro | Published shape. |\n| `cousins.json` | cousins | Cite-only #62743. |\n| `backups.json` | backups | Cite-only backups. Do not auto-pick. |\n| `fixtures.json` | fixtures | Navy hull / cyan glow / amber caution / crimson blow-out. |\n| `walk.json` | walk | Published idle equalized → socat-race → blown → airlock. |\n\n## Cousins (cite only)\n\n#62743 (same first-call connection-refused symptom, closed as stale). Different ticket — this booth is the 2.1.269 mechanism + measurements, not a re-ship.\n\n## Backups (cite only — do NOT auto-pick or build)\n\n#93744 #93772 #93770 #93777 #93782 #93859 #93863 #93889 #93821 #93811 #93809 #93823\n\nDrop any file onto `projects/airlock/index.html`. Buttons load the seeded path. The living page admits **equalized** / idle lock / #93862.\n\nProbes are diagnostic reconstructions of the issue's published facts. This booth does not run Claude.\n"
