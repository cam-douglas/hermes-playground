{
  "source": "gitignore-path",
  "issue": 93423,
  "note": "Gitignore path from #93423. Copy already excludes .git, so some filtering happens, but it does not read .gitignore, global excludes, or core.excludesFile. Path word is gitignore.",
  "seed": "gitignore",
  "verdict": "gitignore",
  "preferSeed": true,
  "contaminated": true,
  "gitignore": true,
  "skipGitignore": true,
  "gitignoreUnread": true,
  "excludesGit": true,
  "paths": [
    {
      "id": "exclude-git",
      "rule": ".git",
      "result": "already excluded"
    },
    {
      "id": "skip-gitignore",
      "rule": ".gitignore",
      "result": "never read"
    },
    {
      "id": "skip-global",
      "rule": "global excludes / core.excludesFile",
      "result": "never honored"
    }
  ],
  "cue": "contaminated",
  "hubCount": "GITIGNORE",
  "boardText": "#93423 · gitignore path — ignore rules never consulted",
  "log": [
    "[gitignore] copy already excludes .git — some filtering happens",
    "[gitignore] does not read .gitignore / global excludes / core.excludesFile",
    "[gitignore] directory marketplace install is the path that carries the hitch"
  ]
}
