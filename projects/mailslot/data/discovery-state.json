{
  "source": "probe",
  "issue": 92839,
  "note": "The blob is large because Claude Code bundles the full OIDC discovery document + RFC 9728 protected-resource metadata + tokens + PKCE bookkeeping into one Keychain entry. Log already shows Returning cached discovery state / Saving discovery state.",
  "seed": "discovery-state",
  "vaulted": false,
  "spilled": true,
  "oauthCompleted": true,
  "bundled": [
    "oidc-discovery",
    "rfc-9728-protected-resource",
    "access-token",
    "id-token",
    "refresh-token",
    "pkce-verifier"
  ],
  "authServer": "https://<idp-issuer>",
  "transport": "http",
  "clientKind": "pre-registered (non-DCR)",
  "hubCount": "DISCOVERY-STATE",
  "log": [
    "[discovery-state] Returning cached discovery state (authServer: https://<idp-issuer>)",
    "[discovery-state] Saving discovery state — bundled into the same Keychain entry as tokens",
    "[discovery-state] suggested split: cache discovery by issuer so tokens stay under 4096B"
  ]
}
