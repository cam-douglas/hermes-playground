{
  "source": "probe",
  "issue": 92908,
  "note": "Nothing above warn; JSON stays valid; no user-visible error. Vr writes atomically (tmp + fsync + rename) so the file was never corrupt — content is lost, not corrupted. No Failed to save config and no Failed to parse config appears anywhere in these logs. Entries simply vanish.",
  "seed": "warn-only",
  "enrolled": false,
  "escheated": true,
  "regranted": true,
  "maxLogLevel": "warn",
  "userVisibleError": false,
  "validJson": true,
  "contentLostNotCorrupted": true,
  "atomicWrite": {
    "fn": "Vr",
    "steps": ["tmp", "fsync", "rename"],
    "note": "atomic replacement rules out torn writes; performs no comparison against the state that was read"
  },
  "absentInLogs": [
    "Failed to save config",
    "Failed to parse config"
  ],
  "hubCount": "WARN-ONLY",
  "log": [
    "[warn-only] nothing above warn; JSON stays valid; no user-visible error",
    "[warn-only] no Failed to save config / Failed to parse config — content lost, not corrupted"
  ],
  "outputText": "warn-only; nothing above warn; JSON stays valid; no user-visible error"
}
