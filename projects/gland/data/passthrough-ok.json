{
  "source": "probe",
  "issue": 92533,
  "note": "Hold bisection. Function hooks env off, or a fresh repo plugin with only a session.start hook: pwd prints the worktree path. Isolation collar stays seated.",
  "seed": "passthrough-ok",
  "scenario": "passthrough-ok",
  "stripped": false,
  "packed": true,
  "passthroughOk": true,
  "sessionStartOk": true,
  "sessionStartOnly": true,
  "pwdPrintsWorktree": true,
  "isolation": "worktree",
  "platform": "macos",
  "version": "2.1.263",
  "shippedMark": "pass",
  "scoredMark": "pass",
  "isolateMark": "pass",
  "hubCount": "PASSTHROUGH-OK",
  "boardText": "passthrough-ok · session.start only · or function hooks env off · pwd prints worktree",
  "bisection": [
    { "config": "Same project plugin, CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=0", "result": "OK" },
    { "config": "Fresh git init repo, plugin with only a session.start hook", "result": "OK, prints the worktree path" }
  ],
  "hooks": [
    { "event": "session.start", "passthrough": true, "body": "next(e)" }
  ],
  "bashCalls": [
    {
      "command": "pwd",
      "refused": false,
      "output": "<repo>/.claude/worktrees/agent-xxx"
    }
  ],
  "log": [
    "[ok] function hooks env off → pwd OK",
    "[ok] session.start-only plugin on a fresh repo → pwd prints the worktree path",
    "[contrast] the same plugin with only a Bash tool.call passthrough → refused"
  ],
  "outputText": "passthrough-ok; session.start (or hooks off) leaves the packing seated"
}
