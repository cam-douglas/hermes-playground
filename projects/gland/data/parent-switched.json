{
  "source": "probe",
  "issue": 92533,
  "note": "Side effect: if the blocked subagent calls EnterWorktree(path: <its worktree>) to recover, the parent session gets switched into that worktree. Parent git commands are then refused outside that directory until ExitWorktree.",
  "seed": "parent-switched",
  "scenario": "parent-switched",
  "stripped": true,
  "packed": false,
  "parentSwitched": true,
  "contextLost": true,
  "bashToolCallHook": true,
  "functionHooks": true,
  "isolation": "worktree",
  "platform": "macos",
  "version": "2.1.263",
  "shippedMark": "FAIL",
  "scoredMark": "FAIL",
  "isolateMark": "pass",
  "hubCount": "PARENT-SWITCHED",
  "boardText": "parent-switched · EnterWorktree recovery · parent session follows the child worktree",
  "recovery": "EnterWorktree(path: <its worktree>)",
  "parentEffect": "parent session switched into the child worktree; parent git commands refused outside that directory until ExitWorktree",
  "hooks": [
    { "event": "tool.call", "tool": "Bash", "passthrough": true, "body": "next(e)" }
  ],
  "bashCalls": [
    {
      "command": "pwd",
      "refused": true,
      "error": "The working-directory isolation context for this agent was lost … Refusing to run it."
    }
  ],
  "log": [
    "[side] blocked subagent calls EnterWorktree(path: <its worktree>) to recover",
    "[parent] parent session switched into that worktree",
    "[latch] parent git commands refused outside that directory until ExitWorktree"
  ],
  "outputText": "parent-switched; recovery on the child yanks the parent into the worktree"
}
