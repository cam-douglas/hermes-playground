{
  "source": "booth",
  "issue": 93915,
  "note": "Path fixture. argv-trunc names argv -c punch vs bash -s hopper.",
  "seed": "argv-trunc",
  "verdict": "argv-trunc",
  "preferSeed": true,
  "scisselled": true,
  "argvTrunc": true,
  "trunc8203": true,
  "slashCollapse": true,
  "event": "argv-trunc",
  "cue": "scisselled",
  "hubCount": "ARGV-TRUNC",
  "paths": [
    {
      "rule": "bash.exe -c should deliver the full command past 8,203 chars",
      "result": "silently truncated at 8,203; unexpected EOF at a mid-payload apostrophe"
    },
    {
      "rule": "msys2-runtime glob() should not clip the pattern during Unicode conversion",
      "result": "fixed 8192-character stack buffer (msys2-runtime#178)"
    },
    {
      "rule": "doubled backslashes should survive argv rebuild at any size",
      "result": "\\\\ collapses at 295 B; bash -s stays identical to 259 KB"
    }
  ],
  "log": [
    "[argv-trunc] argv -c punch clips; bash -s hopper stays plenary — cousins cite-only"
  ]
}
