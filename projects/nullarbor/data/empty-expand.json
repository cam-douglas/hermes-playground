{
  "source": "booth",
  "issue": 93595,
  "note": "Path fixture. empty-expand names ${VAR} resolving to empty (HTTP 401), not a literal unexpanded string (HTTP 403).",
  "seed": "empty-expand",
  "verdict": "empty-expand",
  "preferSeed": true,
  "emptied": true,
  "emptyExpand": true,
  "bearerEmpty": true,
  "event": "empty-expand",
  "cue": "emptied",
  "hubCount": "EMPTY-EXPAND",
  "paths": [
    {
      "rule": "${VAR} in plugin HTTP MCP Authorization header should expand from process env",
      "result": "2.1.260 expands to empty; header sent as Bearer  "
    },
    {
      "rule": "direct POST expanded Bearer $TOKEN \u2192 200; literal Bearer ${VAR} \u2192 403; Bearer  empty \u2192 401",
      "result": "2.1.260 matches the empty 401 case, not literal 403"
    },
    {
      "rule": "desktop bundles 2.1.260; npm-global PATH may be older; same failure from a plain shell",
      "result": "not a desktop-vs-terminal host-app split"
    }
  ],
  "log": [
    "[empty-expand] ${VAR} resolves to empty \u2014 not a literal unexpanded string"
  ]
}
