{
  "source": "retire-parked-path",
  "issue": 93529,
  "note": "retire-parked path from #93529. Session-reset md() retires a parked permission via Lo() with default reason interrupt; Uu() stamps user-rejected.",
  "seed": "retire-parked",
  "verdict": "retire-parked",
  "preferSeed": true,
  "attainted": true,
  "retireParked": true,
  "sessionReset": true,
  "parkedPermission": true,
  "hardcodedDenial": true,
  "paths": [
    {
      "id": "roll",
      "rule": "tool already in settings.json permissions.allow; no permission prompt appeared",
      "result": "bill unrolled — allow-listed with no prompt"
    },
    {
      "id": "desk",
      "rule": "internal parked-permission state machine tracks a tool awaiting control_response",
      "result": "clerk desk holds a parked entry with no persisted control_response"
    },
    {
      "id": "stamp",
      "rule": "md() aborts abortController and, unless retireParkedPermission:false, calls Lo() with default reason interrupt",
      "result": "iron stamp lowered on an internal interrupt, not a user keypress"
    }
  ],
  "cue": "attainted",
  "hubCount": "RETIRE-PARKED",
  "boardText": "#93529 · retire-parked path — parked-permission retirement after internal session reset",
  "log": [
    "[retire-parked] md() → Lo(interrupt) → Uu() stamps toolDenialKind user-rejected",
    "[retire-parked] no prompt; no Esc/Ctrl+C; tool already allow-listed",
    "[retire-parked] recurs after /mcp reconnect followed by calls to a different unrelated MCP server"
  ]
}
