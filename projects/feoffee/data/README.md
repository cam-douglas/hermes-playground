# Feoffee fixtures

Diagnostic JSON only. No Claude sessions. Encoded from #93863 issue facts: a `.claude/launch.json` named dev-server (`preview_start({name: "..."})`) fails under `~/Documents/...` with getcwd EPERM / System Policy deny against bash/python3 children, even though `com.anthropic.claudefordesktop` holds Full Disk Access. Rival bundle `com.anthropic.claude-code` walks the same path. Score feoffee or admit vested.

Idle word: **vested**. Path word: **preview-eperm**. Seeded loss: **unseised**. Product: **feoffee**. HOLD: **vested**. ALARM: **unseised** / **feoffee** / **preview-eperm** / **getcwd-eperm**. Primary: [anthropics/claude-code#93863](https://github.com/anthropics/claude-code/issues/93863).

Fixtures record the published incident only. No session. No secrets. No exploit payloads. No network to Anthropic. No Claude. No patch to anthropics/claude-code.

| File | Verdict | What it scores |
|---|---|---|
| `vested.json` | vested | Idle charter. HOLD: FDA vested through children. |
| `hold.json` | hold | HOLD alias for idle vested. |
| `unseised.json` | unseised | Seeded #93863 path. ALARM: getcwd EPERM / TCC deny. |
| `feoffee.json` | feoffee | Product score for the chancery booth. |
| `preview-eperm.json` | preview-eperm | Path: named-config spawn vs rival-bundle walk. |
| `getcwd-eperm.json` | getcwd-eperm | shell-init getcwd EPERM; python3 Errno 1. |
| `tcc-deny.json` | tcc-deny | System Policy deny against bash/python3 PIDs. |
| `named-launch.json` | named-launch | `preview_start({name})` named launch.json. |
| `documents-demesne.json` | documents-demesne | cwd under `~/Documents/...`. |
| `bash-python-child.json` | bash-python-child | Spawned mesne tenants. |
| `desktop-fda.json` | desktop-fda | Lord bundle FDA auth_value 2. |
| `rival-bundle.json` | rival-bundle | `com.anthropic.claude-code` walks freely. |
| `letters-patent.json` | letters-patent | HOLD alias: FDA letters patent. |
| `demesne.json` | demesne-open | HOLD alias: Documents open. |
| `rival-bundle-ok.json` | rival-bundle-ok | HOLD alias: rival already walks. |
| `has-repro.json` | has-repro | Published shape. |
| `cousins.json` | cousins | Cite-only #93766. |
| `backups.json` | backups | Cite-only backups. Do not auto-pick. |
| `fixtures.json` | fixtures | Oak / parchment / oxblood / brass. |
| `walk.json` | walk | Published idle vested → preview-eperm → unseised → feoffee. |

## Cousins (cite only)

#93766 Canard (OneDrive cwd mislabel — spawn ENOENT + wrong linker message, not TCC getcwd EPERM on Documents). Do not rebuild as a separate booth.

## Backups (cite only — do NOT auto-pick or build)

#93772 #93770 #93777 #93782 #93889 #93821 #93811 #93809 #93823 #93924 #93848 #93929 #93915

Drop any file onto `projects/feoffee/index.html`. Buttons load the seeded path. The living page admits **vested** / idle charter / #93863.

Probes are diagnostic reconstructions of the issue's published facts. This booth does not run Claude.
