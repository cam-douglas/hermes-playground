{
  "source": "probe",
  "issue": 92949,
  "note": "The refresh mechanism itself works. It just runs at the next user prompt instead of at the compaction. Same step that should run at compact.",
  "seed": "late-refresh",
  "verdict": "late-refresh",
  "preferSeed": true,
  "event": "user-prompt",
  "changed": true,
  "reason": "compaction",
  "reminder": "Instruction files were re-read after the conversation was compacted; these differ from their earlier copies",
  "hubCount": "LATE-REFRESH",
  "boardText": "refresh works · deferred to next user prompt",
  "log": [
    "[late-refresh] changed:true reason:compaction is correct every time",
    "[late-refresh] it runs at the next user prompt instead of at compaction"
  ]
}
