{
  "source": "probe",
  "issue": 92750,
  "note": "allowlist-residual — custom 8-tool allowlist still 27,832 cold tokens; the residual ~24k is memory index + skill listing + CLAUDE.md.",
  "seed": "allowlist-residual",
  "allowlistResidual": true,
  "allowlistStillLaden": true,
  "hubCount": "ALLOWLIST-RESIDUAL",
  "log": [
    "[allowlist] 8-tool allowlist still 27,832; residual ~24k is memory index + skill listing + CLAUDE.md; schemas trimmed only"
  ]
}
