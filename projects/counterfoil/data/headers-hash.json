{
  "source": "headers-hash-path",
  "issue": 93446,
  "note": "headers-hash path from #93446. Store and lookup share keyFor(serverName, cfg) hashing {type,url,headers}. add-json persists full config then saveMcpClientSecret with rebuilt {type,url} dropping headers. Login uses full stored config → different key.",
  "seed": "headers-hash",
  "verdict": "headers-hash",
  "preferSeed": true,
  "skewed": true,
  "headersHash": true,
  "headersStripped": true,
  "paths": [
    {
      "id": "keyFor",
      "rule": "keyFor(serverName, cfg) hashes {type,url,headers}",
      "result": "shared shape"
    },
    {
      "id": "store",
      "rule": "saveMcpClientSecret rebuilt {type,url}",
      "result": "headers dropped → headers:{}"
    },
    {
      "id": "login",
      "rule": "mcp login keyFor(serverName, storedConfig)",
      "result": "headers included → different key"
    }
  ],
  "cue": "skewed",
  "hubCount": "HEADERS-HASH",
  "boardText": "#93446 · headers-hash path — keyFor hashes {type,url,headers}; add-json drops headers on store",
  "log": [
    "[headers-hash] keyFor hashes {type,url,headers}",
    "[headers-hash] add-json store rebuilds {type,url} and drops headers",
    "[headers-hash] login hashes the full stored config including X-MCP-Toolsets"
  ]
}
