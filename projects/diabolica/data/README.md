# Diabolica fixtures

Diagnostic JSON only. No Claude sessions. Encoded from #94040 issue facts: Worktree-isolated sessions still refuse Bash commands that never run git (2.1.270) — residual inverted burden of proof (cannot be shown not to be git / too complex to verify) after loops and quoted-argv substring git often pass. Score diabolica or admit innocent.

Idle word: **innocent**. Path word: **cannot-show-not-git**. Seeded loss: **diabolica**. Product: **diabolica**. HOLD: **innocent**. ALARM: **diabolica** / **cannot-show-not-git** / **pwd-unresolved** / **six-refusals**. Primary: [anthropics/claude-code#94040](https://github.com/anthropics/claude-code/issues/94040).

Fixtures record the published incident only. No session. No exploit payloads. No network to Anthropic. No Claude. No patch to anthropics/claude-code. Do NOT implement a fix.

| File | Verdict | What it scores |
|---|---|---|
| `innocent.json` | innocent | Idle court. HOLD: sealed writ shut; scale balances. |
| `hold.json` | hold | HOLD alias for idle innocent. |
| `diabolica.json` | diabolica | Seeded #94040 path and product. ALARM: unfinished proof. |
| `cannot-show-not-git.json` | cannot-show-not-git | Path: inverted burden of proof. |
| `quashed.json` | quashed | HOLD alias: the writ stays shut. |
| `discharged.json` | discharged | HOLD alias: six charges stay wax-shut. |
| `unindicted.json` | unindicted | HOLD alias: no bill. |
| `writ-idle.json` | writ-idle | HOLD alias: parchment court idle. |
| `pwd-unresolved.json` | pwd-unresolved | helper --raw "$PWD" refused. |
| `option-may-stand.json` | option-may-stand | sed $PWD path treated as an option. |
| `heredoc-to-helper.json` | heredoc-to-helper | git words in quoted heredoc to helper. |
| `pipeline-too-complex.json` | pipeline-too-complex | strings \| grep -c isolated refused. |
| `wrapper-find-word.json` | wrapper-find-word | time wraps python3; find is an argument. |
| `computed-program-name.json` | computed-program-name | single-quoted regex treated as a program. |
| `six-refusals.json` | six-refusals | The six published shapes. |
| `one-eighty-one.json` | one-eighty-one | 181 / 120 refusals. |
| `landing.json` | landing | Chamber / parchment / wax / flame / scale / sulfur. |
| `has-repro.json` | has-repro | Published shape. |
| `cousins.json` | cousins | Cite-only #90293 #90307 #93193. |
| `backups.json` | backups | Cite-only backups. Do not auto-pick. |
| `fixtures.json` | fixtures | Chamber / parchment / wax / flame / scale / sulfur. |
| `walk.json` | walk | Published idle innocent → cannot-show-not-git → diabolica. |

## Cousins (cite only)

#90293 — too-complex refusals including loops (2.1.243). Loops now pass. Do not conflate.

#90307 — verifier refuses safe read-only commands at scale. Do not conflate.

#93193 — Mondegreen substring "git". Different booth. Do not rebuild.

## Backups (cite only — do NOT auto-pick or build)

#94032 #94031 #94029 #93987 #93924 #93770 #93777 #94059 #94053

Drop any file onto `projects/diabolica/index.html`. Buttons load the seeded path. The innocent page admits **innocent** / idle court / #94040.

Probes are diagnostic reconstructions of the issue's published facts. This booth does not run Claude.
