{
  "source": "booth",
  "issue": 94350,
  "note": "Path fixture. subst-nest names the nested-joint miss.",
  "seed": "subst-nest",
  "verdict": "subst-nest",
  "preferSeed": true,
  "matryoshka": true,
  "substNest": true,
  "event": "subst-nest",
  "cue": "matryoshka",
  "hubCount": "SUBST-NEST",
  "paths": [
    {
      "rule": "Nest unpacked — unpacked; descended; recursed; opened; nested-ok; walked-in",
      "result": "The walker would recurse into $(...) and treat nested list/pipeline like top-level — no prompt, no raw error leak"
    },
    {
      "rule": "Nested | and ; inside $(...) should not prompt when every leaf is allow-listed",
      "result": "subst-nest; echo \"x: $(cat file.txt | wc -l)\" prompts; Unhandled node type: ; on semicolon"
    },
    {
      "rule": "Top-level ; and | outside substitution should stay handled",
      "result": "failure is specific to control-operator node inside the $(...) subtree"
    }
  ],
  "log": [
    "[subst-nest] control-operator inside $(...); cousins #55170 #47752 #56019 #47701 #47706 #46868 — do not conflate"
  ]
}
