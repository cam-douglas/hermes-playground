{
  "source": "bridge-refuse-path",
  "issue": 93485,
  "note": "bridge-refuse path from #93485. Local agent mode manufactures nlink>1 by hardlinking into the session upload cache. After Cowork sessions default to the cloud, the file bridge refuses any file with nlink > 1.",
  "seed": "bridge-refuse",
  "verdict": "bridge-refuse",
  "preferSeed": true,
  "twinlinked": true,
  "hardlink": true,
  "alias": true,
  "cloud": true,
  "bridgeRefuse": true,
  "nlink": 2,
  "paths": [
    {
      "id": "local",
      "rule": "local agent mode does not copy; it creates a hard link into local-agent-mode-sessions/.../uploads/",
      "result": "nlink rises; Claude-owned alias appears"
    },
    {
      "id": "cloud",
      "rule": "cloud file bridge refuses any file with nlink > 1",
      "result": "file is hardlinked (nlink > 1) — refused to avoid reading through a link alias"
    },
    {
      "id": "default",
      "rule": "Cowork sessions now default to the cloud; previously-fine files become unreadable with no user action",
      "result": "bridge-refuse is manufactured by local mode, then enforced by cloud mode"
    }
  ],
  "cue": "twinlinked",
  "hubCount": "BRIDGE-REFUSE",
  "boardText": "#93485 · bridge-refuse path — local mode manufactured the exact condition cloud mode is designed to reject",
  "log": [
    "[bridge-refuse] local agent mode hardlinks workspace files into the session upload cache",
    "[bridge-refuse] cloud file bridge refuses nlink > 1 — copy the file to break the link and retry",
    "[bridge-refuse] deleting the alias entries under the Claude upload cache is safe: data survives while one link remains"
  ]
}
