{
  "source": "booth",
  "issue": 93607,
  "note": "Published #93607 walk. Idle standing → start sleep → pkill -f pattern → wrapper killed → exit 144 → still-alive missing → pgrep returns wrapper → phantom/changing PID → 2.1.214 CLI-only guard → macOS ancestors OK → bracket only helps cross-call → same-call dies → wrapper-argv → petard.",
  "seed": "walk",
  "verdict": "walk",
  "preferSeed": true,
  "walk": [
    "standing",
    "start-sleep",
    "pkill-f",
    "wrapper-killed",
    "exit-144",
    "still-alive-missing",
    "pgrep-wrapper",
    "phantom-pid",
    "cli-guard-only",
    "macos-ancestors-ok",
    "bracket-partial",
    "same-call-dies",
    "wrapper-argv",
    "petard"
  ],
  "hubCount": "WALK",
  "boardText": "#93607 · published walk — standing → start sleep → pkill -f → wrapper killed → exit 144 → still-alive missing → pgrep wrapper → phantom PID → CLI-only guard → macOS ancestors OK → bracket partial → same-call dies → wrapper-argv → petard",
  "log": [
    "[walk] standing → start sleep → pkill -f → wrapper killed → exit 144 → still-alive missing → pgrep wrapper → phantom PID → CLI-only guard → macOS ancestors OK → bracket partial → same-call dies → wrapper-argv → petard"
  ]
}
