{
  "source": "probe",
  "issue": 92757,
  "note": "gui-workaround — set EDITOR to a GUI editor (gvim -f), which never touches the terminal's input path and works in every environment above.",
  "seed": "gui-workaround",
  "guiWorkaround": true,
  "hubCount": "GUI-WORKAROUND",
  "log": [
    "[gui-workaround] gvim -f never touches the terminal input path and works in every environment on the control matrix"
  ]
}
