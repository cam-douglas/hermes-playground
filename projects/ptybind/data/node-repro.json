{
  "source": "probe",
  "issue": 92757,
  "note": "node-repro — a 13-line Node script that only puts stdin in raw mode, waits for one key, leaves raw mode and spawns an editor with stdio: inherit fails exactly the same way with no Claude Code involved.",
  "seed": "node-repro",
  "nodeRepro": true,
  "hubCount": "NODE-REPRO",
  "log": [
    "[node-repro] setRawMode(true); once data → setRawMode(false); pause(); spawnSync(..., { stdio: 'inherit' }) fails in the same pane"
  ]
}
