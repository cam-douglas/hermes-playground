{
  "source": "probe",
  "issue": 92543,
  "note": "Suggested rail only, not claimed implemented: hand the script via stdin or a temp file (bash <file> / bash -s) instead of -c.",
  "seed": "stdin-bypass",
  "scenario": "stdin-bypass",
  "sheared": true,
  "fayed": false,
  "stdinBypass": true,
  "viaDashC": true,
  "windows": true,
  "version": "2.1.263",
  "platform": "windows",
  "shippedMark": "FAIL",
  "scoredMark": "FAIL",
  "isolateMark": "pass",
  "hubCount": "STDIN-BYPASS",
  "boardText": "stdin bypass rail · bash <file> / bash -s · or refuse over ~8100",
  "log": [
    "[rail] hand the script to bash via stdin or a temp file (bash <file> / bash -s) instead of as a -c argument",
    "[rail] that removes both the length cap and the backslash re-interpretation",
    "[rail] failing that, refuse assembled -c over ~8,100 chars with a clear message"
  ],
  "outputText": "stdin-bypass; suggested rail is stdin/temp file or a clear refuse over ~8100 — not claimed implemented"
}
