{
  "source": "phantom-deny-path",
  "issue": 93563,
  "note": "phantom-deny path from #93563. Refusal blames your WebFetch deny rule (WebFetch) even when no deny rule exists in any settings file.",
  "seed": "phantom-deny",
  "verdict": "phantom-deny",
  "preferSeed": true,
  "withheld": true,
  "phantomDeny": true,
  "denyRuleAbsent": true,
  "paths": [
    {
      "id": "luna",
      "rule": "Artifact read looks for native WebFetch, which Cowork has withdrawn",
      "result": "luna glass veiled — live artifact never appears for viewing"
    },
    {
      "id": "sacristy",
      "rule": "mcp__workspace__web_fetch is the vessel Cowork actually provides (1024 successful calls)",
      "result": "substitute sits unused on the sacristy shelf"
    },
    {
      "id": "ribbon",
      "rule": "refusal blames your WebFetch deny rule (WebFetch)",
      "result": "phantom DENY ribbon — user hunted ~/.claude/settings.json, settings.local.json, ~/.claude.json, managed-settings.json; no WebFetch matches"
    }
  ],
  "cue": "withheld",
  "hubCount": "PHANTOM-DENY",
  "boardText": "#93563 · phantom-deny path — false accusation of a WebFetch deny rule that does not exist",
  "log": [
    "[phantom-deny] refusal blames your WebFetch deny rule (WebFetch)",
    "[phantom-deny] no permissions block in ~/.claude/settings.json; settings.local.json absent; ~/.claude.json zero WebFetch matches",
    "[phantom-deny] managed-settings.json absent; project .claude absent; Cowork session settings.json is {}"
  ]
}
