{
  "source": "probe",
  "issue": 92452,
  "note": "Published environment and expected behaviour from the issue. Docs and prior bundle behaviour say parallel sessions are allowed. The live Dispatch path refuses a second landing on a non-git parent while the tower still clears concurrent traffic.",
  "seed": "refused",
  "scenario": "settings",
  "cleared": false,
  "refused": true,
  "dispatchOnly": true,
  "nonGitParent": true,
  "appConcurrent": true,
  "scheduledOk": true,
  "bundle258Regression": true,
  "environment": {
    "os": "macOS 26 (Darwin 25.5.0), Apple Silicon",
    "app": "1.46388.4 (regressed); last known good 1.40609.0",
    "bundle": "claude-code-vm 2.1.258 / 2.1.260 (regressed); last known good 2.1.247",
    "cwd": "~/Projects",
    "cwdKind": "normal folder (not a git repo) containing ~14 git repositories",
    "git": "Apple git 2.50.1"
  },
  "expected": "Dispatch should be able to spawn multiple sessions into the same non-repo working directory, as it did through bundle 2.1.247 / app 1.40609. If the intent is worktree isolation for git repos, a non-git parent folder should not be blocked, or at minimum the check should match the behaviour of normal session creation.",
  "docsCousinNote": "Cousin #91745 cites Desktop application → Work in parallel with sessions: multiple sessions can run in parallel and Dispatch can start sessions for you.",
  "shippedMark": "FAIL",
  "scoredMark": "FAIL",
  "isolateMark": "pass",
  "hubCount": "SETTINGS",
  "boardText": "settings · expected parallel Dispatch landings · live radio refuses the second",
  "log": [
    "[settings] last good bundle 2.1.247 / app 1.40609 · pad accepted parallel Dispatch",
    "[settings] expected: multiple sessions into the same non-repo working directory",
    "[settings] live: start_code_task refused on ~/Projects while tower clears two live",
    "[desk] already refused; advertised parallel sessions are not the live Dispatch gate"
  ],
  "outputText": "refused; settings promise parallel sessions; Dispatch radio refuses the second"
}
