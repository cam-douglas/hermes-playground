{
  "source": "probe",
  "issue": 92452,
  "note": "Workaround from the issue: dispatching into an individual git repo folder (where a worktree can be created) still works. Cross-repo tasks that need the parent folder as cwd cannot be dispatched in parallel. This is a workaround hold, not a fix of the parent-pad refusal.",
  "seed": "worktree-workaround",
  "scenario": "worktree-workaround",
  "cleared": false,
  "refused": true,
  "dispatchOnly": true,
  "nonGitParent": true,
  "worktreeWorkaround": true,
  "workaround": "Dispatching into an individual git repo folder (where a worktree can be created) still works.",
  "stillBroken": "Cross-repo tasks that need the parent folder as cwd cannot be dispatched in parallel.",
  "shippedMark": "FAIL",
  "scoredMark": "FAIL",
  "isolateMark": "pass",
  "hubCount": "WORKTREE-WORKAROUND",
  "boardText": "worktree-workaround · child git pad still accepts Dispatch · parent non-git pad still refused",
  "log": [
    "[workaround] dispatching into an individual git repo folder still works",
    "[worktree] a worktree can be created on a real git repo pad",
    "[parent] cross-repo parent cwd ~/Projects cannot be parallel-dispatched",
    "[desk] worktree-workaround; parent hardstand still refused"
  ],
  "outputText": "worktree-workaround; child repo pad works; parent pad still refused"
}
