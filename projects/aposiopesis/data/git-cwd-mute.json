{
  "source": "booth",
  "issue": 93588,
  "note": "Path fixture. git-cwd-mute names the git-root silence as the cut.",
  "seed": "git-cwd-mute",
  "verdict": "git-cwd-mute",
  "preferSeed": true,
  "furled": true,
  "gitCwdMute": true,
  "gitCwd": true,
  "neverSpawned": true,
  "event": "git-cwd-mute",
  "cue": "furled",
  "hubCount": "GIT-CWD-MUTE",
  "paths": [
    {
      "rule": "cwd is a normal git clone",
      "result": "statusLine NEVER spawned"
    },
    {
      "rule": "cwd is a git worktree of that clone",
      "result": "statusLine NEVER spawned"
    },
    {
      "rule": "cwd=$HOME is not a git repo",
      "result": "command runs and renders"
    }
  ],
  "log": [
    "[git-cwd-mute] the status standard falls silent only inside a git repo"
  ]
}
