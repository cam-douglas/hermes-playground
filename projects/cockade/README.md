# Cockade

A **milliner / hatter's cockade bench** — charcoal felt hat-blocks, crimson and navy silk ribbon reels, antique brass cockade pins, ivory brim tickets, warm walnut counters, pin cushions; Playfair Display + Nunito Sans + IBM Plex Mono — for a real Claude Code defect: **Ultracode arms with no ultracode setting in any scope; the header shows plain "with xhigh effort" while it is active**. Originally filed as a cosmetic badge bug; further checking inverted the diagnosis. Opening `/effort` shows the slider marker sitting ON ultracode ("xhigh + workflows") while the session header in the same frame reads "Fable 5 with xhigh effort". The badge was truthful; the **header** is the element mislabeling the state (ultracode reports its level as `xhigh` internally, per statusline docs). Two defects: (1) Ultracode is armed with no ultracode opt-in anywhere. (2) Header hides ultracode. With effort actually at ultracode, `/effort xhigh` is a silent no-op — no `modelSettings` entry is written.

Primary:

- [anthropics/claude-code#91033](https://github.com/anthropics/claude-code/issues/91033) (OPEN, bug, has repro, platform:windows, area:tui, area:statusline, filed 2026-08-31T16:46:22Z by kenflorian). Title: Ultracode arms with no ultracode setting in any scope; header shows plain "with xhigh effort" while it is active.

A cockade seated on ultracode with no opt-in, while the brim ticket still reads xhigh, is not a hold. Score the brim or admit **unpinned**.

Idle word: **unpinned**. Seeded state: **cocked** / #91033 — slider on ultracode, no ultracode key anywhere, header "with xhigh effort", badge truthful, `/effort xhigh` silent no-op. Never idle as cocked / armed / ultracode / cockade / rinsed / scrubbed / stripped / lye / vacant / reserved / advowson / smutch / plain / seated / bound / hallmarked / pointed / collapsed / spoiled / banked.

A **cockade** is the silk badge on a hat. The real effort badge sits on ultracode while the brim ticket still reads "xhigh effort", with no order in the book. A cocked cockade with no order in the book is not a hold. Score the brim or admit unpinned.

- **unpinned** = hold: ultracode off; header honest; slider on documented default `high`; no cockade on the hat
- **cocked** = #91033 primary — slider ultracode, no opt-in, header xhigh
- **documented-default** = hold: Fable 5 holds the documented model default (`high`)
- **mislabeled** / **header-lie** = brim ticket prints `xhigh` while ultracode is active
- **silent-noop** / **effort-xhigh** / **persist-miss** = `/effort xhigh` writes no `modelSettings` entry
- **xhigh-mask** = ultracode reports as `xhigh`; header renders that reported level
- **no-opt-in** / **settings-absent** / **env-unset** = no documented route armed ultracode
- **slider-ultracode** / **workflows-armed** = `/effort` marker on "xhigh + workflows"
- **badge-true** = footer badge ultracode is the honest element
- **fable-default** = Fable 5 has no effort setting (docs say `high`)
- **undocumented** = ultracode armed with no changelog / settings default

Verdicts: cocked, mislabeled, silent-noop, xhigh-mask, no-opt-in, slider-ultracode, header-lie, effort-xhigh, workflows-armed, settings-absent, env-unset, persist-miss, badge-true, fable-default, undocumented, unpinned, documented-default.

Hypothesis only (NON-BINDING): treat this as an undocumented / spontaneous ultracode arm (possibly a saved plain `xhigh` seated as ultracode) plus a header that prints ultracode's internal reported level (`xhigh`) so `/effort xhigh` is a silent no-op. Do not claim a root cause in Claude Code source you have not seen.

## Why not a clone

This is specifically: **UNDOCUMENTED ULTRACODE ARM + HEADER HIDES ULTRACODE AS "xhigh effort" + `/effort xhigh` SILENT NO-OP**.

NOT **Lye** ([#91020](https://github.com/anthropics/claude-code/issues/91020)) — env-scrub.
NOT **Advowson** ([#91005](https://github.com/anthropics/claude-code/issues/91005)) — Workflow name silent built-in.
NOT **Smutch** ([#90993](https://github.com/anthropics/claude-code/issues/90993)) — desktop Icon\r crawl.
NOT **Bitting** ([#90970](https://github.com/anthropics/claude-code/issues/90970)) — Slack MCP most-recent-mint exclusivity.
NOT **Puncheon** ([#90962](https://github.com/anthropics/claude-code/issues/90962)) — Write-tool BOM-less `.ps1`.
NOT **Gnomon** ([#90954](https://github.com/anthropics/claude-code/issues/90954)) — shared mtime closed transcripts.
NOT **Spoil** ([#90943](https://github.com/anthropics/claude-code/issues/90943)) — stale private `GIT_INDEX_FILE`.
NOT **Pale** ([#90683](https://github.com/anthropics/claude-code/issues/90683)) — silent-absent hooks.
NOT **Pawl** ([#90784](https://github.com/anthropics/claude-code/issues/90784)) — sticky stop.
NOT **Ambo** ([#90685](https://github.com/anthropics/claude-code/issues/90685)) — systemMessage non-render.
NOT **Chatelaine** ([#90647](https://github.com/anthropics/claude-code/issues/90647)) — mcpOAuth Keychain.
NOT **Bulla** — MSIX. NOT **Limpet** — process leak. NOT #91026 / #91028 / #91017 / #91034 / #91031.

Different UI: milliner / hatter's shop. Charcoal felt, crimson silk, navy ribbon, antique brass, ivory card, warm walnut. Playfair Display + Nunito Sans + IBM Plex Mono. NOT Lye Libre Baskerville / Source Sans 3 wet-stone/pewter/ash-green. NOT Advowson Cormorant / Karla parchment/violet. NOT Smutch Fraunces / DM Sans blotter/brass. NOT Bitting Libre Bodoni / Figtree felt-green. NOT Puncheon Cinzel / Outfit walnut/gold/oxblood.

Different verbs: score the brim, pin idle unpinned, pin seeded cocked, admit unpinned, load fixtures, reset to unpinned, admit documented-default. Not "Score the vat" / "Pin idle rinsed" / "Score the presentation" / "Pin idle vacant".

Different idle: **unpinned**.

## Live catalog path

`/cockade/` is this static milliner bench. Demo works with no secrets and no npm. Mark: `02:50 / hermes catalog #100 / #91033`.

1. Idle demo loads **unpinned** — ultracode off; header honest; slider on documented default `high`.
2. Seed **cocked** → #91033 ticket: slider on ultracode; header "Fable 5 with xhigh effort"; no ultracode key; `/effort xhigh` silent no-op.
3. Hat-block / cockade / brim-ticket board.
4. Silk-reel chips and brass pins: unpinned / cocked / documented-default.
5. Slider, brim-ticket, settings-absent, and silent-noop plaques.
6. **Score the brim** walks the ticket and lights chips on the rail.

## How to score

Open `projects/cockade/index.html` in a browser, or serve the repo root and visit `/cockade/` (Vercel rewrite → `/projects/cockade`). No build step. Optional hook:

```bash
node projects/cockade/hook/cockade.mjs projects/cockade/data/91033.json
node projects/cockade/hook/cockade.mjs projects/cockade/data/unpinned.json
node --test projects/cockade/hook/cockade.test.mjs
```

Cocked seed → cocked/alarm. Unpinned seed → unpinned/hold. Documented-default seed → documented-default / hold.

`projects/cockade/hook/cockade.mjs` classifies a brim/cockade trace and returns `{ verdict, chips[], reasons[], hold, alarm, idleWord }`. See `hook/README.md`.

Local fingerprints: `data/91033.json`, `data/cocked.json`, `data/unpinned.json`, plus one JSON per other verdict, `data/fingerprints.json`, `data/chips.json`, `data/fixtures.json`. Evidence only from issue facts. No invented session IDs.

## Native integrations

1. GitHub fetch of [anthropics/claude-code#91033](https://github.com/anthropics/claude-code/issues/91033). Unauthenticated. See `.env.example`.
2. Hat-block / cockade / brim-ticket board; silk cockade pin on the felt.
3. Pin idle unpinned / pin seeded cocked / score the brim / admit unpinned / load fixtures / reset to unpinned / admit documented-default.
4. Slider plaque (`/effort` marker).
5. Brim-ticket plaque (header vs reported `xhigh`).
6. Settings-absent plaque (no ultracode key / env unset / managed absent).
7. Silent-noop plaque (`/effort xhigh` writes nothing).

## Sources

- [anthropics/claude-code#91033](https://github.com/anthropics/claude-code/issues/91033) OPEN
- Comment by kenflorian: Fable 5 default effort is `high`; Fable 5 hold holds the model default; ultracode settings key default unset so ultracode is off; CHANGELOG has no ultracode-default entry; 2.1.243 fixed `/model` picker silently ignoring Ultracode; header "with xhigh effort" is consistent with statusline "Ultracode is not a distinct level and reports as `xhigh`".
