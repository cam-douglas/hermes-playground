# Brisure fixtures

Diagnostic JSON only. No Claude sessions. Encoded from #94396 issue facts: a forked session is never registered with the Remote Control bridge and never appears in the mobile Code tab. cold_resume never arms remoteControlAutoEligible. Score brisure or admit enrolled.

Idle word: **enrolled**. Path word: **fork-resume**. Seeded loss: **brisure**. Product: **brisure**. HOLD: **enrolled**. ALARM: **brisure** / **fork-resume** / **cadet**. Primary: [anthropics/claude-code#94396](https://github.com/anthropics/claude-code/issues/94396).

Fixtures record the published incident only. Session-census reconstructions are **synthetic example-data** labeled as such — not live session dumps. No session. No exploit payloads. No network to Anthropic. No Claude. No patch to anthropics/claude-code. Do NOT implement a fix.

| File | Verdict | What it scores |
|---|---|---|
| `enrolled.json` | enrolled | Idle college. HOLD: parent on the main roll. |
| `brisure.json` | brisure | Seeded #94396 path and product. ALARM: cadet omitted. |
| `94396.json` | brisure | Same seeded path under the issue number. |
| `fork-resume.json` | fork-resume | Path: cold_resume never arms eligibility. |
| `lineal.json` | lineal | HOLD alias: parent shield on the rack. |
| `registered.json` | registered | HOLD alias: parent registered on the roll. |
| `parent.json` | parent | HOLD alias: lineal session. |
| `rollcall.json` | rollcall | HOLD alias: parchment roll-call names the parent. |
| `cadet.json` | cadet | Younger branch never receives its brisure. |
| `omitted.json` | omitted | No error and no UI hint; simply absent. |
| `cold-resume.json` | cold-resume | Fork adopts a transcript; flag never set. |
| `first-turn.json` | first-turn | Eligibility is only assigned here. |
| `auto-eligible.json` | auto-eligible | Policy gate on remoteControlAutoEligible. |
| `user-requested.json` | user-requested | Manual Remote Control workaround. |
| `scheduled.json` | scheduled | Eligible but no bridge — expected. |
| `bridge-absent.json` | bridge-absent | Forked 5: absent eligible + absent bridge. |
| `mobile-absent.json` | mobile-absent | Parent present; fork never on the Code tab. |
| `landing.json` | landing | Herald's college / armorial roll / cadency desk. |
| `has-repro.json` | has-repro | Published shape. |
| `cousins.json` | cousins | Cite-only cousins. |
| `backups.json` | backups | Cite-only backups. Do not auto-pick. |
| `fixtures.json` | fixtures | Parent shield / cadet vacancy. |
| `walk.json` | walk | Published idle enrolled → fork-resume → brisure. |
| `closed.json` | closed | #94396 remains OPEN — cite only; not this booth. |

## Cousins (cite only)

Do NOT rebuild. Do NOT conflate.

#94400 — Cancellans resume-fork drops a server-gated tool; deferred_tools_delta / prompt-cache miss. Same family of fork words. DIFFERENT.

#94397 — Diptych brief-echo double render. DIFFERENT.

#93458 — Forksink source-fork. DIFFERENT.

#94396 is specifically: forked sessions never become Remote Control eligible, so they never appear in the mobile Code tab.

## Backups (cite only — do NOT auto-pick or build)

#94393 #94392 #86198 #94417 #94452 #94451 #94430 #94458 #93924 #93770 #93777 #94151

Drop any file onto `projects/brisure/index.html`. Buttons load the seeded path. The college admits **enrolled** / idle desk / #94396.

Probes are diagnostic reconstructions of the issue's published facts. This booth does not run Claude.
