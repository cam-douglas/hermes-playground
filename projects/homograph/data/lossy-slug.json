{
  "source": "booth",
  "issue": 93743,
  "note": "Path fixture. lossy-slug names the dash-collapse of non-ASCII path characters.",
  "seed": "lossy-slug",
  "verdict": "lossy-slug",
  "preferSeed": true,
  "collided": true,
  "lossySlug": true,
  "dashCollapse": true,
  "orphanStore": true,
  "event": "lossy-slug",
  "cue": "collided",
  "hubCount": "LOSSY-SLUG",
  "paths": [
    {
      "rule": "slug should be unique per real path (hash of absolute path, or percent-encode UTF-8)",
      "result": "non-ASCII characters collapse to a generic dash; dash count happened to match"
    },
    {
      "rule": "two different Korean folder names must not share a store",
      "result": "both encode to C--Users-<user>-Downloads--------"
    },
    {
      "rule": "storage for a deleted path should not silent-revive",
      "result": "A is gone from disk; B opens A's memory/*.md"
    }
  ],
  "log": [
    "[lossy-slug] dash-collapse of non-ASCII is the encoding path — not a #91735/#70076/#69752/#89915/#85595 rebuild"
  ]
}
