{
  "source": "probe",
  "issue": 93015,
  "note": "Session transcripts at ~/.claude/projects/<project>/<uuid>.jsonl. Only birth time (stat -f '%SB') counts real launches. find -newermt is GNU-only and matches nothing on macOS BSD find; mtime matches a resumed old session.",
  "seed": "birth-time-not-mtime",
  "verdict": "birth-time-not-mtime",
  "preferSeed": true,
  "birthTimeUsed": true,
  "mtimeMisleading": true,
  "sessionPath": "~/.claude/projects/<project>/<uuid>.jsonl",
  "birthStat": "stat -f '%SB'",
  "hubCount": "BIRTH-TIME-NOT-MTIME",
  "log": [
    "[birth-time-not-mtime] only stat -f '%SB' distinguishes a new session from a resumed one; find -newermt is GNU-only"
  ]
}
