{
  "source": "source-fork-path",
  "issue": 93458,
  "note": "source-fork path from #93458. SessionStart hook additionalContext silently dropped on rewind. Docs list fork as a SessionStart source with no stated exception.",
  "seed": "source-fork",
  "verdict": "source-fork",
  "preferSeed": true,
  "dropped": true,
  "sourceFork": true,
  "silentDrop": true,
  "noModelText": true,
  "hookRan": true,
  "exitZero": true,
  "validJson": true,
  "paths": [
    {
      "id": "street",
      "rule": "same hook, same machine, same project, same settings inject on source=startup and source=compact",
      "result": "startup/compact runoff stays lodged on the street surface"
    },
    {
      "id": "rewind",
      "rule": "desktop rewind logs [Rewind] resumeSessionAt=... + forkSession",
      "result": "SessionStart runs with source=fork one second later"
    },
    {
      "id": "sink",
      "rule": "hook exits 0, emits valid JSON additionalContext, model never receives the text",
      "result": "source-fork — runoff vanishes into the sink with no alarm"
    }
  ],
  "cue": "dropped",
  "hubCount": "SOURCE-FORK",
  "boardText": "#93458 · source-fork path — additionalContext silently dropped on rewind",
  "log": [
    "[source-fork] SessionStart hook additionalContext silently dropped when source=fork",
    "[source-fork] hook ran, exit 0, valid JSON; model never received the text",
    "[source-fork] docs say additionalContext on SessionStart is seen by Claude; fork is a documented source; no stated exception"
  ]
}
