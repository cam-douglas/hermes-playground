{
  "primary": 91422,
  "cousin": 74567,
  "cousins": [
    74567,
    76867,
    76490,
    91479
  ],
  "relatedInIssue": [
    16449,
    30519
  ],
  "contrast": "This is DONTASK + EQUAL-BREADTH BASH(CP:*)/(MV:*) REFUSE ANY OPTION TOKEN INCL BARE -- WHILE BARE FORMS AND RM -RF RUN; MATCHER ARTIFACT NOT SAFETY POLICY; AREA:BASH+PERMISSIONS. Mode: --permission-mode dontAsk. Allow rules at same breadth: Bash(cp:*), Bash(mv:*), Bash(rm:*). REFUSED: cp -f, cp -v, cp --, mv -v. RAN: bare cp, bare mv, rm -f, rm -rf. cp -f with relative paths still REFUSED (not absolute-vs-relative). Deterministic across repeated arms in one session (not a race). cp -- is POSIX end-of-options with no force semantics — refusing it proves this is not a deliberate force-flag gate. rm -rf under identical rule shape runs — a real force/destructive policy would refuse rm first. Cost: agent guidance to always pass force flags on cp/mv (avoid -i alias hang) is unfollowable; teams wrote flagless-form exemptions. Reporter alfalcon90. Claude Code 2.1.251, run E. Filed 2026-09-02. OPEN, has repro, area:bash, area:permissions.",
  "reporter": "alfalcon90",
  "filedAt": "2026-09-02T07:28:49Z",
  "updatedAt": "2026-09-02T07:29:54Z",
  "labels": [
    "bug",
    "has repro",
    "area:bash",
    "area:permissions"
  ],
  "version": "2.1.251",
  "runLabel": "run E",
  "permissionMode": "--permission-mode dontAsk",
  "allowCp": "Bash(cp:*)",
  "allowMv": "Bash(mv:*)",
  "allowRm": "Bash(rm:*)",
  "refused": [
    "cp -f",
    "cp -v",
    "cp --",
    "mv -v"
  ],
  "ran": [
    "cp",
    "mv",
    "rm -f",
    "rm -rf"
  ],
  "title": "`--permission-mode dontAsk`: `cp`/`mv` refuse any option token (including bare `--`) while the flagless form runs; `rm` is unaffected",
  "url": "https://github.com/anthropics/claude-code/issues/91422",
  "idleWord": "unbolted",
  "seededWord": "snagged",
  "mark": "21:50 / hermes catalog #122 / #91422",
  "phrase": "a bolter that catches every option token — even bare -- — while rm -rf slips the same mesh is not a safety policy; it is a snagged matcher. Score the cloth or admit the allow-rule already lied."
}
