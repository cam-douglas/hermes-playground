{
  "source": "probe",
  "issue": 92900,
  "note": "Workaround from the issue body: replace every boolean property schema with the object-form equivalent. true → { description: Any JSON value } — a bare schema object with no type keeps the identical any-value contract and passes the CLI's conversion. A lint that fails if any value in any properties map is not a JSON object prevents reintroduction.",
  "seed": "object-form-workaround",
  "intact": true,
  "culled": false,
  "relisted": true,
  "workaround": {
    "replace": true,
    "with": { "description": "Any JSON value" },
    "noType": true,
    "lint": "walk every schema node; fail if any value in any properties map is not a JSON object"
  },
  "suggestedFix": [
    "accept boolean schemas per JSON Schema (ideal: true → {}, false → { not: {} } during conversion)",
    "handle typeof node === 'boolean' before any 'properties' in node check in jsonSchemaToZodShape",
    "failing that: drop only the offending tool and emit a diagnostic naming the server, the tool, and the schema path"
  ],
  "toolsReachApi": 53,
  "ratio": "53/53",
  "hubCount": "OBJECT-FORM-WORKAROUND",
  "log": [
    "[object-form-workaround] { description: Any JSON value } (no type) passes CLI conversion",
    "[object-form-workaround] lint: fail if any properties-map value is not a JSON object"
  ],
  "outputText": "object-form-workaround; any-value object restores 53/53"
}
