{
  "source": "probe",
  "issue": 92900,
  "note": "The proven trigger is a boolean property schema in a properties map. payload: true is legal JSON Schema 2020-12 §4.3.2 meaning accept any value. false presumably triggers the same rejection path; the proven case is true.",
  "seed": "boolean-trigger",
  "intact": false,
  "culled": true,
  "relisted": false,
  "tool": {
    "name": "propose",
    "inputSchema": {
      "type": "object",
      "properties": {
        "target": { "type": "string" },
        "op": { "type": "string", "enum": ["set", "delete"] },
        "payload": true
      }
    }
  },
  "trigger": {
    "node": true,
    "path": "inputSchema.properties.payload",
    "meaning": "any value (JSON Schema 2020-12 §4.3.2)",
    "legal": true
  },
  "toolsReachApi": 0,
  "ratio": "0/53",
  "hubCount": "BOOLEAN-TRIGGER",
  "log": [
    "[boolean-trigger] propose.inputSchema.properties.payload = true — legal any-value schema",
    "[boolean-trigger] one boolean node in one tool; the converter culls the whole server"
  ],
  "outputText": "boolean-trigger; payload: true; legal any-value; whole palette culled"
}
